Done. `_inr` now emits true Indian grouping, and the boundaries are pinned against the client's actual ICU output.

**Verification** (real terminal output):

```
cd D:\Hackathon\backend; .venv\Scripts\python -m pytest tests/test_money_formatting.py -q
................                                                         [100%]
16 passed in 2.77s
```

Plus the work-item serializer tests that consume `_inr`'s output:

```
.venv\Scripts\python -m pytest tests/test_money_formatting.py tests/test_phase4.py tests/test_payment_events.py tests/test_dispute_sla.py tests/test_promise_fulfillment.py tests/test_reco_models.py -q
........................................................................ [ 69%]
...............................                                          [100%]
103 passed in 22.11s
```

**This touches `backend/db.py`, a shared module** — run the full gate yourself.

## What changed
- `backend/db.py:12290` — `_inr` rewritten. `None` → `"—"` unchanged; rounds to whole rupees; `-0.4` → `"₹0"` (no `₹-0`).
- `backend/tests/test_money_formatting.py` — new. No existing test asserted the old Western strings, so nothing needed updating; I grepped `backend/tests` for `₹\d` and the only pre-existing money assertions (`test_reco_models.py:420-424`) were *already* Indian-grouped and still pass.

**Negative handling:** sign goes *inside* the symbol — `₹-12,34,567`. Not my aesthetic preference (`-₹12,34,567` reads better), but I ran node to get ground truth and the client builds its cell as `"₹" + amount.toLocaleString("en-IN")`, which yields `₹-12,34,567`. Since you asked me not to touch the frontend, matching it was the only way to avoid trading a grouping mismatch for a sign mismatch. Documented in the docstring.

## Deliverable 3 — server/client agreement
Verified by running the values through node's en-IN and asserting the server against those literals. `AssignedQueue.tsx:169` needs no change: it renders `₹${amount.toLocaleString("en-IN")}`, and every case now matches (`1234567` → `₹12,34,567` on both sides). No frontend edit required.

One residual divergence I did **not** change, since it's rounding not grouping: for a fractional amount the server prints `₹12,34,568` while the client prints `₹12,34,567.6`. Pre-existing, and `:,.0f` rounded too.

## Sweep result — 6 sibling formatters left in place, deliberately
Grepping `backend/**.py` for `₹{…:,` found more than the brief assumed. I did **not** migrate these, because doing it properly means extracting a shared money module (there'd otherwise be a 4th copy of the grouping loop) and touching the agent-prompt path — decisively past "rewrite `_inr` in db.py", so it's your call, not mine:

| Site | Surface | Verdict |
|---|---|---|
| `agent_core/context.py:86` `_inr` | `outstanding`/`minimumDue`/`approvedAmount` fed into the **agent's prompt** | Real defect — the bot narrates Western grouping |
| `agent_core/authority/talk.py:31` `_inr` | waiver amounts quoted to the customer | Real defect |
| `agent_core/authority/enact.py:224` | `"Goodwill waiver ₹…"` label | Real defect |
| `customer_insights.py:46` `_inr` + `:104` | UI insight reasons/labels | Real defect — also note `.replace(",", ",")` on line 46 is a no-op typo |
| `agent_core/treatment/{narrate.py:122, scoring.py:489}` | internal EV narration | Real but rarely ≥6 digits |
| `db.py:3196` `_inr_compact` fallback | — | **Benign**: only reachable when `abs(value) < 1000`, so `,` never fires |
| `scripts/power_control_arm.py`, `scripts/solve_capacity.py` | offline analysis | **Leave** — not a product surface |

`agent_core/reco/talk.py:42` `written_amount` already groups correctly, but returns `""` for zero/negative, so it isn't reusable as `_inr` as-is. I also couldn't import it into `db.py` regardless: `agent_core/__init__.py` and `agent_core/reco/__init__.py` eagerly import the engine, which imports `db` — a module-level import there would be circular.

I left everything uncommitted. `db.py` already carried pre-existing uncommitted changes from your tree, so committing it would sweep in work that isn't mine; say the word if you want me to stage just the `_inr` hunk plus the new test file.
CYCLE6_EXIT=0
