OBJECTIVE (one job): Make tests/test_job_claim.py::test_two_claimers_never_take_same_job immune to the LIVE kb-index workers running on this machine, WITHOUT weakening what it asserts.

Context - diagnosed root cause (do not re-litigate, but verify my reading):
- This dev machine runs the production-style stack NATIVELY: among others, TWO instances of `python -m worker` (backend/worker.py - docstring: "KB index worker - claim queued kb_index_jobs with SKIP LOCKED"), plus uvicorn :8000, voice.bot :7860, voice.workers.insurance. All share the same Postgres (docker collections_db).
- The test parks all queued kb_index_jobs, INSERTs a synthetic queued job, then races two threads calling kb_ingest.claim_next_job on separate connections. A live worker polling in the ~millisecond window between INSERT and the race claims the synthetic job FIRST (FOR UPDATE SKIP LOCKED, oldest-first - ours is the only queued row), so both test threads legitimately see nothing -> results [None, None] -> AssertionError. Full-suite run hit this once tonight; solo re-run reproduced it deterministically enough to diagnose. Cleanup deletes the synthetic row afterwards, so the thief succeeds invisibly.
- NEVER kill/restart those worker processes - they serve the running product. Fix the TEST, not the operations.

Hard constraints:
- The assertion pair (exactly one winner, winner id == synthetic job id, exactly one loser None) MUST remain the semantic contract. No unconditional skip, no rerun-until-green loop, no sleep-and-hope.
- Acceptable shape (you may improve with justification): before racing, verify the probe job is still 'queued' AND immediately after the race determine whether a FOREIGN session claimed it (e.g. status moved to 'running'/gone with started_at set while neither thread recorded a win, or xmin evidence). Only in the proven-foreign-theft case, surface a LOUD, precisely-worded skip (pytest.skip with reason naming worker contention and what was observed) - never a silent pass. Prefer detection over prevention; if you find a clean prevention (e.g. a Postgres mechanism that blocks foreign claimers for the duration WITHOUT touching worker code and WITHOUT deadlocking the two racing connections), you may implement that instead and say why it is safe.
- Grep tests/ for sibling tests using the same park-insert-race pattern against kb_index_jobs or whatsapp_outbound job queues; apply the same protection there if the same theft is possible. Report each one you found and what you did.
- Docstring update: document WHY (shared Postgres with live native workers) so the next person does not rediscover this at 5am.

Scope: backend/tests/test_job_claim.py (+ sibling job-queue tests if implicated). NO production code changes. Nothing else.

Verify before reporting done, paste the real terminal output:
  cd D:/Hackathon/backend && .venv/Scripts/python -m pytest tests/test_job_claim.py -q
Then ALSO paste output of running it several times in a row (e.g. -q --count via a shell loop, at least 3 consecutive runs) to show stability against the live workers, and one run of the sibling tests you touched. No frontend commands needed - backend-only cycle.
In your reply: state the detection/prevention mechanism chosen, quote the exact skip wording, and confirm the original assertions are byte-for-byte intact.

Rules: Never git stash, git reset --hard, git clean, or force-push - this tree has ~270 pre-existing uncommitted files that are not yours. Never commit to main, never push, never open a PR; commit to the current branch only. Never print the contents of .env - it holds live Twilio credentials. Never kill or restart whatever is listening on ports 8000, 7860, or 8080 - and equally never kill/restart the native worker processes (python -m worker, voice.bot, voice.workers.insurance); they serve the live product. Never delete a test, skip a test UNCONDITIONALLY, add an eslint-disable, or widen an ignore list to make something pass. If it's a bug, reproduce it and write the failing test before you fix it.
