OBJECTIVE (one job): Give the Decision Intelligence (/treatment/*) backend surface its operator console. Nine finished endpoints currently have ZERO frontend callers - you will wire all nine for real.

Where: backend/main.py lines 4101-4212 defines:
  GET  /treatment/next          (main.py:4101)
  GET  /treatment/insights      (main.py:4119)
  GET  /treatment/metrics       (main.py:4129)
  GET  /treatment/model-health  (main.py:4146)
  GET  /treatment/models        (main.py:4155)
  GET  /treatment/holds         (main.py:4164)
  POST /treatment/holds         (main.py:4176)
  POST /treatment/holds/{hold_id}/release  (main.py:4188)
  GET  /treatment/cases         (main.py:4197)

Deliverables:
1. Habibi/src/api/treatment.ts - typed client + TanStack Query hooks for all NINE endpoints, using the existing apiGet/apiPost helpers from the shared lib (see how src/api/providers.ts or src/api/outbound.ts do it).
2. A /treatment route (follow the house pattern of routes/*.tsx + lazy variant where neighbours use one) with tabs/sections: Insights (the shadow-mode report: coverage, suppression mix), Model health + champion/challenger ledger, Cases, Holds (list + place-a-hold dialog calling POST /treatment/holds, and Release button calling the release endpoint).
3. Mock branch for every hook behind the existing USE_MOCK seam in src/api/config.ts - study how an existing feature splits mock/live (e.g. disputes or webhooks) and copy that structure EXACTLY, including a small realistic seed dataset consistent with an Indian collections domain.
4. Nav: add "Decision intelligence" to the sidebar group where it fits best in src/components/shell/Sidebar.tsx (groups start line 56; pick the group whose job it matches and say why in your reply).
5. States are not optional: every section renders loading, empty, error AND success. Never silently fall back to mock-looking numbers when the live call fails - show the error state.

House style rules: sentence case copy (only QA, CRM, DND keep caps); tabular numerals for aligned money/time columns; lucide-react icons; Radix primitives where neighbours use them; Tailwind 4 tokens only - no raw hex.

Scope: NEW files under Habibi/src/api/ and Habibi/src/routes/, plus Sidebar.tsx and any routeTree registration the build generates. Do NOT touch Customer-360, the workspace home screen, backend files, or anything else. If routeTree.gen.ts is auto-generated by the dev server/build, let the tooling regenerate it rather than hand-editing beyond what is required.

Verify before reporting done, and paste the real terminal output:
  cd D:/Hackathon/Habibi && npm run typecheck && npm run lint && npm run build
Backend is untouched - no pytest needed this cycle.
Then open @url:`http://localhost:8080/treatment`, look at the actual screen in BOTH light and dark theme, and describe what you saw including how loading/error states behave (use devtools to break the API if needed to see them). Never verify a UI change by reasoning about it.
In your reply list all nine endpoints and paste one grep line per endpoint proving its frontend caller now exists.

Rules: Never git stash, git reset --hard, git clean, or force-push - this tree has ~270 pre-existing uncommitted files that are not yours. Never commit to main, never push, never open a PR; commit to the current branch only. Never print the contents of .env - it holds live Twilio credentials. Never kill or restart whatever is listening on ports 8000, 7860, or 8080. Never delete a test, skip a test, add an eslint-disable, or widen an ignore list to make something pass. If it's a bug, reproduce it and write the failing test before you fix it.
