# Known-good pins from the local venv (optimization Tier 0).
# Bump deliberately after testing — do not leave floating majors in prod.
fastapi==0.139.2
uvicorn==0.51.0
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
alembic==1.18.5
openai==2.46.0
tiktoken==0.13.0
httpx==0.28.1
minio==7.2.20
python-multipart==0.0.32
twilio>=9.0,<10
redis>=5.0,<6
websockets>=12.0,<16
# Metrics exposition for /metrics. Pure Python, no transitive dependencies —
# chosen over the OpenTelemetry SDK for this pass because a Prometheus text
# endpoint is useful with `curl` alone, whereas OTLP traces are useful only
# once a collector exists. Traces remain the next step, not a replacement.
prometheus-client==0.26.0
# Error tracking. Inert unless SENTRY_DSN is set, so this costs an unconfigured
# deployment an import and nothing else.
sentry-sdk==2.66.1
# Dev / regression (auth CORS lock + readiness)
pytest==9.1.1
# Linting. ruff.toml has existed since July and ruff was never installed, so the
# per-file E402 ignores protecting the modules that must set env before
# importing db were documentation rather than configuration. Pinned because an
# unpinned linter turns somebody else's release into your red build.
ruff==0.6.9
