# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# npm is the package manager (`packageManager` in package.json). A second
# lockfile is how a 206-package divergence sat unaudited; bun.lock is not
# to be re-added.
bun.lock
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Every .env variant, not just the bare name: .env.production / .env.local hold
# the same secrets and were previously tracked-by-default.
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
