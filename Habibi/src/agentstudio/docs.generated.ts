// Generated by scripts/agentstudio-docs.mjs from agentstudio/engine/docs. Do not edit.
export const DOCS_NAV: { group: string; pages: { slug: string; title: string }[] }[] = [
 {
  "group": "Build your First Agent",
  "pages": [
   {
    "slug": "getting-started/first-agent",
    "title": "Your First Agent in 5 Minutes"
   },
   {
    "slug": "getting-started/trigger-calls-from-your-backend",
    "title": "Trigger Calls using API"
   },
   {
    "slug": "getting-started/send-call-data-with-webhooks",
    "title": "Sync Data using Webhook"
   },
   {
    "slug": "getting-started/connect-telephony",
    "title": "Connect with Telephony"
   },
   {
    "slug": "getting-started/add-tools-and-knowledge-base",
    "title": "Use External Tools, Knowledge"
   }
  ]
 },
 {
  "group": "Core Concepts",
  "pages": [
   {
    "slug": "core-concepts/how-dograh-works",
    "title": "How PayInt Voice Studio Works"
   },
   {
    "slug": "core-concepts/workflows-and-agents",
    "title": "Workflows & Agents"
   },
   {
    "slug": "core-concepts/calls-and-runs",
    "title": "Calls & Runs"
   },
   {
    "slug": "core-concepts/context-and-variables",
    "title": "Context & Variables"
   },
   {
    "slug": "core-concepts/campaigns",
    "title": "Campaigns"
   }
  ]
 },
 {
  "group": "Configurations",
  "pages": [
   {
    "slug": "configurations/inference-providers",
    "title": "Model Configurations"
   },
   {
    "slug": "configurations/llm",
    "title": "LLM"
   },
   {
    "slug": "configurations/voice",
    "title": "Voice"
   },
   {
    "slug": "configurations/transcriber",
    "title": "Transcriber"
   },
   {
    "slug": "configurations/api-keys",
    "title": "API Keys and Service Keys"
   },
   {
    "slug": "configurations/agent-uuid",
    "title": "Agent UUID"
   },
   {
    "slug": "configurations/interruption",
    "title": "Interruption Handling"
   },
   {
    "slug": "configurations/tracing",
    "title": "Tracing"
   }
  ]
 },
 {
  "group": "Voice Agent Builder",
  "pages": [
   {
    "slug": "voice-agent/introduction",
    "title": "Voice Agent Builder"
   },
   {
    "slug": "voice-agent/editing-a-workflow",
    "title": "Editing a Workflow"
   },
   {
    "slug": "voice-agent/pre-recorded-audio",
    "title": "Pre-recorded Audio"
   },
   {
    "slug": "voice-agent/template-variables",
    "title": "Template Variables"
   },
   {
    "slug": "voice-agent/pre-call-data-fetch",
    "title": "Pre-Call Data Fetch"
   },
   {
    "slug": "voice-agent/tools/introduction",
    "title": "Tools"
   },
   {
    "slug": "voice-agent/tools/call-transfer",
    "title": "Call Transfer"
   },
   {
    "slug": "voice-agent/tools/end-call",
    "title": "End Call"
   },
   {
    "slug": "voice-agent/tools/http-api",
    "title": "HTTP API"
   },
   {
    "slug": "voice-agent/tools/mcp-tool",
    "title": "MCP Tool"
   },
   {
    "slug": "voice-agent/knowledge-base",
    "title": "Knowledge Base"
   },
   {
    "slug": "voice-agent/start-call",
    "title": "Start Call"
   },
   {
    "slug": "voice-agent/end-call",
    "title": "End Call"
   },
   {
    "slug": "voice-agent/agent",
    "title": "Agent Node"
   },
   {
    "slug": "voice-agent/global",
    "title": "Global Node"
   },
   {
    "slug": "voice-agent/qa",
    "title": "QA"
   },
   {
    "slug": "voice-agent/api-trigger",
    "title": "API Trigger"
   },
   {
    "slug": "voice-agent/webhook",
    "title": "Webhook"
   },
   {
    "slug": "voice-agent/add-to-website",
    "title": "Add to Website"
   }
  ]
 },
 {
  "group": "Telephony",
  "pages": [
   {
    "slug": "integrations/telephony/overview",
    "title": "Telephony Integration"
   },
   {
    "slug": "integrations/telephony/inbound",
    "title": "Inbound Calling"
   },
   {
    "slug": "integrations/telephony/twilio",
    "title": "Twilio Integration"
   },
   {
    "slug": "integrations/telephony/vonage",
    "title": "Vonage Integration"
   },
   {
    "slug": "integrations/telephony/plivo",
    "title": "Plivo Integration"
   },
   {
    "slug": "integrations/telephony/telnyx",
    "title": "Telnyx Integration"
   },
   {
    "slug": "integrations/telephony/cloudonix",
    "title": "Cloudonix Integration"
   },
   {
    "slug": "integrations/telephony/exotel",
    "title": "Exotel Integration"
   },
   {
    "slug": "integrations/telephony/vobiz",
    "title": "Vobiz Integration"
   },
   {
    "slug": "integrations/telephony/asterisk-ari",
    "title": "Asterisk ARI Integration"
   },
   {
    "slug": "integrations/telephony/vicidial",
    "title": "VICIdial Integration"
   },
   {
    "slug": "integrations/telephony/webhooks",
    "title": "Webhooks and Callbacks"
   },
   {
    "slug": "integrations/telephony/agent-stream",
    "title": "Agent Stream"
   },
   {
    "slug": "integrations/telephony/custom",
    "title": "Custom Telephony Provider"
   }
  ]
 },
 {
  "group": "Integrations",
  "pages": [
   {
    "slug": "integrations/mcp",
    "title": "MCP Server"
   },
   {
    "slug": "integrations/tuner",
    "title": "Tuner Integration"
   },
   {
    "slug": "integrations/paygent",
    "title": "Paygent Integration"
   },
   {
    "slug": "integrations/noveum",
    "title": "Noveum Integration"
   }
  ]
 },
 {
  "group": "Guides",
  "pages": [
   {
    "slug": "developer/workflow-schema",
    "title": "Workflow Definition Schema"
   },
   {
    "slug": "developer/call-dispositions",
    "title": "Call Dispositions"
   },
   {
    "slug": "developer/webhooks",
    "title": "Webhook Payloads"
   }
  ]
 }
];
export const DOCS_PAGES: Record<string, { title: string; description: string; html: string }> = {"getting-started/first-agent":{"title":"Your First Agent in 5 Minutes","description":"Build and talk to a working voice agent in five minutes using Web Calls — no telephony setup required.","html":"<p>This is the fastest path to a working voice agent. You&#39;ll create an agent, get a generated conversation flow, and talk to it in your browser — no phone number, no telephony provider, no configuration.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Using the hosted platform, go to <a href=\"/studio/\">PayInt Voice Studio</a>. Self-hosting locally? Use <code>http://localhost:3010</code> instead, and follow <a href=\"/studio/docs/getting-started\">Getting Started</a> first to get the platform running.</p>\n</div>\n\n\n\n<h2 id=\"step-1-create-an-agent\">Step 1: Create an agent</h2>\n<p>Go to <a href=\"/studio/workflow\">PayInt Voice Studio/workflow</a> and create a new agent.</p>\n<p><img src=\"/agentstudio-docs/images/create-a-voice-agent.png\" alt=\"Create a Voice Agent\"></p>\n<p>You&#39;ll be asked for:</p>\n<ul>\n<li><strong>Direction</strong> — Inbound (agent receives calls) or Outbound (agent makes calls). Pick either; it only affects the starting prompt.</li>\n<li><strong>Use case</strong> — a description of what the agent should do, e.g. <em>&quot;Book a haircut appointment, ask for preferred date and time, confirm before ending the call.&quot;</em></li>\n</ul>\n<p>This description is sent to an LLM to generate a starting workflow. The more specific you are, the better the generated prompts and pathways will be.</p>\n<h2 id=\"step-2-land-in-the-workflow-editor\">Step 2: Land in the workflow editor</h2>\n<p>After generation, you&#39;re dropped into the <a href=\"/studio/docs/voice-agent/introduction\">Voice Agent Builder</a> with a graph already built for you — typically a <strong>Start Call</strong> node, one or more <strong>Agent</strong> nodes with prompts for your use case, and an <strong>End Call</strong> node, connected by pathways.</p>\n<p>You don&#39;t need to understand the full <a href=\"/studio/docs/voice-agent/introduction#the-graph-model\">graph model</a> yet — the generated agent works out of the box. You can come back and customize prompts once you&#39;ve heard it talk.</p>\n<h2 id=\"step-3-talk-to-it-with-a-web-call\">Step 3: Talk to it with a Web Call</h2>\n<p>In the agent editor, start a <strong>Web Call</strong>. This runs the full pipeline — speech-to-text, LLM, text-to-speech — straight from your browser microphone, exactly like a real phone call would, just without a phone number.</p>\n<p>While the call is active, you can watch:</p>\n<ul>\n<li>The <strong>live transcript</strong> as the conversation happens</li>\n<li><strong>Node transitions</strong> as the agent moves through the graph</li>\n<li>Any <strong>tool calls</strong> the agent makes</li>\n</ul>\n<p>Talk to it like a real caller would. Try to get it off-script — say something unexpected — and see how it responds.</p>\n<h2 id=\"step-4-iterate\">Step 4: Iterate</h2>\n<p>Didn&#39;t go the way you wanted? Open the node whose behavior you want to change and edit its prompt directly. Save, then start a new Web Call to hear the change immediately. No redeploy, no restart.</p>\n<p>Common first tweaks:</p>\n<ul>\n<li>Adjust tone or wording in an <strong>Agent</strong> node&#39;s prompt</li>\n<li>Add shared instructions (tone, objection handling) to the <a href=\"/studio/docs/voice-agent/global\"><strong>Global</strong></a> node</li>\n<li>Add a <a href=\"/studio/docs/voice-agent/webhook\"><strong>Webhook</strong></a> node to send call results somewhere once you&#39;re happy with the flow</li>\n</ul>\n<h2 id=\"next-steps\">Next Steps</h2>\n<p>You have a working agent that runs entirely in the browser. From here:</p>\n<ul>\n<li><strong>Take real calls</strong> — connect a <a href=\"/studio/docs/integrations/telephony/overview\">telephony provider</a> and trigger calls via <a href=\"/studio/docs/voice-agent/api-trigger\">API Trigger</a> or inbound routing.</li>\n<li><strong>Learn the graph model in depth</strong> — see <a href=\"/studio/docs/voice-agent/introduction\">Voice Agent Builder</a> for all node types and how pathways work.</li>\n<li><strong>Give it tools</strong> — let the agent call external APIs or transfer calls with <a href=\"/studio/docs/voice-agent/tools/introduction\">Tools</a>.</li>\n<li><strong>Embed it on a website</strong> — skip telephony entirely and let website visitors talk to the agent via <a href=\"/studio/docs/voice-agent/add-to-website\">Add to Website</a>.</li>\n</ul>\n"},"getting-started/trigger-calls-from-your-backend":{"title":"Trigger Calls using API","description":"Kick off a call the moment a lead lands in your CRM or spreadsheet, no manual dialing.","html":"<p>If your team is manually dialing leads one by one, or your CRM needs to kick off a call the moment a lead comes in, this is how you automate that with PayInt Voice Studio.</p>\n<p>Say you&#39;ve got leads or accounts sitting in a CRM or a Google Sheet. Each row has details like customer name, company name, amount due. The <strong>API Trigger</strong> node lets you take that row of data and turn it into a personalized call, automatically.</p>\n<h2 id=\"step-1-add-the-node\">Step 1: Add the node</h2>\n<p>Click <strong>Add node</strong> and add an <strong>API Trigger</strong> node, the topmost option in the list. This node starts the call and passes your data into the agent as <code>initial_context</code>.</p>\n<p>That&#39;s the part that matters: instead of the agent asking basic setup questions on the call, like &quot;what&#39;s your name&quot; or &quot;how much do you owe&quot;, it already knows. In this demo, the opening node greets the caller with <code>{{debtor_name}}</code>, and the next node references <code>{{client_name}}</code> and <code>{{debt_amount}}</code>. None of that is hardcoded into the agent. It all comes from the API call.</p>\n<div class=\"as-callout as-callout-tip\">\n\n\n<p>Every node has a docs link in the top right corner. If you get stuck or want to go deeper on any node type, open the docs straight from there.</p>\n</div>\n\n\n\n<h2 id=\"step-2-check-the-prerequisites\">Step 2: Check the prerequisites</h2>\n<p>Before you trigger anything:</p>\n<ol>\n<li><a href=\"/studio/docs/getting-started/connect-telephony\">Connect with Telephony</a> for outbound calls.</li>\n<li>Go to the developer portal and generate an API key. You&#39;ll need it in the request header.</li>\n</ol>\n<h2 id=\"step-3-find-your-trigger-url\">Step 3: Find your trigger URL</h2>\n<p>Open the API Trigger node. You&#39;ll see two URLs:</p>\n<ul>\n<li><strong>Test URL</strong> runs your latest draft.</li>\n<li><strong>Production URL</strong> runs your published agent.</li>\n</ul>\n<p>If you change the agent and want the production URL to reflect it, publish first, otherwise production keeps running the older version. While you&#39;re still testing, use the test URL.</p>\n<h2 id=\"step-4-send-the-request\">Step 4: Send the request</h2>\n<p>Trigger the call from Postman, or wherever your backend lives. In the body, send <code>phone_number</code> and <code>initial_context</code>:</p>\n<pre><code class=\"language-bash\">curl -X POST https://&lt;your-payint-host&gt;/api/v1/public/agent/test/{uuid} \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -H &quot;X-API-Key: dg_your_api_key&quot; \\\n  -d &#39;{\n    &quot;phone_number&quot;: &quot;+14155550100&quot;,\n    &quot;initial_context&quot;: {\n      &quot;debtor_name&quot;: &quot;Rushil&quot;,\n      &quot;client_name&quot;: &quot;Quick Credit&quot;,\n      &quot;debt_amount&quot;: &quot;3500&quot;\n    }\n  }&#39;\n</code></pre>\n<p>The body can come from your CRM, a Google Sheet, or your own backend, wherever that row of data lives.</p>\n<h2 id=\"what-you-get\">What you get</h2>\n<p>Send the request, and the call goes out immediately. Because the agent already had the context before the call started, the conversation is short and specific from the first line: <em>&quot;Hi, this is Alex from Apex Recovery Solutions. Am I speaking with Rushil? I&#39;m calling about your Quick Credit account with an outstanding balance of $3,500.&quot;</em> No setup questions, straight to the point.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<ul>\n<li><strong>Get that data back automatically</strong>: the next step is <a href=\"/studio/docs/getting-started/send-call-data-with-webhooks\">Sync Data using Webhook</a>, which takes whatever the agent learns on the call and sends it back to your system the moment it ends.</li>\n<li><strong>No phone number connected yet?</strong> See <a href=\"/studio/docs/getting-started/connect-telephony\">Connect with Telephony</a> first, outbound calls need a telephony provider attached.</li>\n<li><strong>Full endpoint reference</strong>: error codes, response shape, and choosing a specific telephony configuration are in <a href=\"/studio/docs/voice-agent/api-trigger\">API Trigger</a>.</li>\n</ul>\n"},"getting-started/send-call-data-with-webhooks":{"title":"Sync Data using Webhook","description":"Get everything PayInt Voice Studio learned during the call back into your system the moment it ends.","html":"<p>The last page covered sending data into a call with API Trigger and <code>initial_context</code>. Now the opposite side: how PayInt Voice Studio sends what it learned back out to your system once the call ends.</p>\n<p>That extracted data lives in <code>gathered_context</code>. So the full picture is: <code>initial_context</code> is what your backend sends into the call, <code>gathered_context</code> is what PayInt Voice Studio extracts from it. A <strong>Webhook</strong> node is how you get <code>gathered_context</code> back out.</p>\n<h2 id=\"step-1-add-the-node\">Step 1: Add the node</h2>\n<p>Click <strong>Add node</strong>, scroll down, and you&#39;ll find the <strong>Webhook</strong> node. Add it and open it up.</p>\n<h2 id=\"step-2-set-the-endpoint\">Step 2: Set the endpoint</h2>\n<p>First thing you&#39;ll see is the endpoint URL. Paste your backend&#39;s webhook URL here. Still testing? Use a free URL from <a href=\"https://webhook.site\">webhook.site</a>, copy your unique URL there and paste it into PayInt Voice Studio.</p>\n<p>Set the method to <strong>POST</strong>. If your backend needs auth, add a bearer token or API key right here too.</p>\n<h2 id=\"step-3-build-the-payload-template\">Step 3: Build the payload template</h2>\n<p>This is the important part, it decides exactly what JSON PayInt Voice Studio sends. Include both kinds of context: <code>initial_context</code> fields like debtor name, client name, and debt amount, and <code>gathered_context</code> fields like amount to pay, payment method, and sentiment. You can also include the final business outcome with <code>{{gathered_context.call_disposition}}</code>, such as <code>qualified</code> or <code>callback_requested</code>. If workflow-level extraction is disabled or no custom outcome is recorded, this value falls back to the reason the call ended. See <a href=\"/studio/docs/developer/call-dispositions\">Call Dispositions</a> to configure the outcomes.</p>\n<pre><code class=\"language-json\">{\n  &quot;debtor_name&quot;: &quot;{{initial_context.debtor_name}}&quot;,\n  &quot;client_name&quot;: &quot;{{initial_context.client_name}}&quot;,\n  &quot;debt_amount&quot;: &quot;{{initial_context.debt_amount}}&quot;,\n  &quot;amount_user_will_pay&quot;: &quot;{{gathered_context.amount_user_will_pay}}&quot;,\n  &quot;payment_method&quot;: &quot;{{gathered_context.payment_method}}&quot;,\n  &quot;sentiment&quot;: &quot;{{gathered_context.sentiment}}&quot;,\n  &quot;call_disposition&quot;: &quot;{{gathered_context.call_disposition}}&quot;\n}\n</code></pre>\n<p>For every variable you can reference here, see the <a href=\"/studio/docs/developer/webhooks\">Webhook Payloads developer reference</a>.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Variable names in your payload have to match your context fields exactly. If a variable cannot be resolved, its field remains in the payload with an empty string value.</p>\n</div>\n\n\n\n<h2 id=\"step-4-save-and-publish\">Step 4: Save and publish</h2>\n<p>Save the webhook node, then publish the agent.</p>\n<h2 id=\"step-5-test-it\">Step 5: Test it</h2>\n<p>Run a test call. When it ends, go back to your webhook receiver. You&#39;ll see one request with both <code>initial_context</code>, the data you sent into the call, and <code>gathered_context</code>, what PayInt Voice Studio extracted from the conversation, sitting in the same JSON payload.</p>\n<p>That&#39;s the complete loop: your backend sends <code>initial_context</code> into PayInt Voice Studio, PayInt Voice Studio runs the call and extracts <code>gathered_context</code>, and the webhook node sends the final structured data back to your backend. For production, swap the webhook.site URL for your real backend URL.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<ul>\n<li><strong>Connect a real phone number</strong>: <a href=\"/studio/docs/getting-started/connect-telephony\">Connect with Telephony</a> covers hooking up Twilio so these calls go out for real.</li>\n<li><strong>Full variable list and authentication options</strong>: see the <a href=\"/studio/docs/voice-agent/webhook\">Webhook reference</a> and the <a href=\"/studio/docs/developer/webhooks\">Webhook Payloads developer reference</a>.</li>\n</ul>\n"},"getting-started/connect-telephony":{"title":"Connect with Telephony","description":"Hook up Twilio so PayInt Voice Studio can make and receive real calls, not just browser test calls.","html":"<p>You&#39;ve covered API Trigger and Webhooks. Both of those work on real calls, which means you need a real phone number connected first. This page walks through Twilio specifically, since it&#39;s the fastest way to get a number and test with free trial credits.</p>\n<h2 id=\"step-1-create-a-twilio-account-and-buy-a-number\">Step 1: Create a Twilio account and buy a number</h2>\n<p>Sign up at Twilio. You&#39;ll land on their dashboard with free trial credits you can use for testing. Go to <strong>Phone Numbers</strong> and click <strong>Buy a number</strong>, pick any number you like.</p>\n<h2 id=\"step-2-copy-your-account-sid\">Step 2: Copy your Account SID</h2>\n<p>Back on the Twilio dashboard, copy your <strong>Account SID</strong>.</p>\n<h2 id=\"step-3-add-the-configuration-in-payint-voice-studio\">Step 3: Add the configuration in PayInt Voice Studio</h2>\n<p>Open <strong>Telephony</strong> in PayInt Voice Studio, click <strong>Add Configuration</strong>, and select <strong>Twilio</strong> as the provider. Name it something you&#39;ll recognize later, your workflow name works well. Paste the Account SID.</p>\n<p>Go back to Twilio, copy your <strong>Auth Token</strong>, and paste it in. Keep that token private.</p>\n<p>If you want this configuration to be the default for test calls and campaigns, enable that here. For a basic setup, leave answering machine detection off, then click <strong>Create</strong>.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Enable <strong>Set as default for outbound calls</strong> on your telephony configuration. If no default is set, API Trigger will return a <code>400: Telephony provider not configured for this organization</code> error even after a successful setup.</p>\n</div>\n\n\n\n<h2 id=\"step-4-add-your-phone-number\">Step 4: Add your phone number</h2>\n<p>Once the configuration exists, go to manage phone numbers and add the number you bought. Copy it from Twilio and paste it in as-is, country <strong>US</strong>. The label is optional, leave it blank if you want. Select your inbound workflow, in this demo, a debt collection agent. Keep it active, and enable default caller ID if you want test calls to always show that number.</p>\n<h2 id=\"step-5-verify-your-number\">Step 5: Verify your number</h2>\n<p>Before testing from a Twilio trial account, verify the destination number you plan to call, such as your personal phone number. This is separate from the Twilio number you purchased for use as the caller ID. In Twilio, go to the <a href=\"https://www.twilio.com/console/phone-numbers/verified\">Verified Caller IDs page</a> and add the destination number.</p>\n<h2 id=\"step-6-test-it\">Step 6: Test it</h2>\n<p>Once verified, trigger a call the same way you did in <a href=\"/studio/docs/getting-started/trigger-calls-from-your-backend\">Trigger Calls using API</a>, from Postman or your backend. PayInt Voice Studio places the outbound call through Twilio. The phone rings, the agent answers, and the connection is live.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>On a trial account, Twilio plays a default message before connecting to your agent. Press any key to skip it and go straight to the agent.</p>\n</div>\n\n\n\n<p>Inbound calls work the same way in reverse, routed through Twilio&#39;s webhook into your PayInt Voice Studio workflow.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<ul>\n<li><strong>Make your agent do more on the call</strong>: <a href=\"/studio/docs/getting-started/add-tools-and-knowledge-base\">Use External Tools, Knowledge</a> covers tools and a Knowledge Base.</li>\n<li><strong>Other providers</strong>: Vonage, Plivo, Cloudonix, and SIP-based setups are covered in <a href=\"/studio/docs/integrations/telephony/overview\">Telephony Integration</a>.</li>\n</ul>\n"},"getting-started/add-tools-and-knowledge-base":{"title":"Use External Tools, Knowledge","description":"Make your agent do something during the call, not just talk. Connect it to live data and your company documents.","html":"<p>You&#39;ve made the agent reachable through real phone calls. Now let&#39;s make it useful during the call.</p>\n<p>Your agent can hold a conversation. But can it check an order, or answer a policy question correctly? That&#39;s what this page is about.</p>\n<p>There are two ways to connect real information to your agent. A <strong>tool</strong> is for anything live, data that changes, or an action that needs to happen, like checking an order or looking up availability. A <strong>Knowledge Base</strong> is for anything written down, like your return policy or an FAQ, that doesn&#39;t change from call to call.</p>\n<p>Here&#39;s the setup used in the video: an order support agent named Maya, for a store called QuickMart. A customer can ask what&#39;s in their cart, and Maya calls an API to check it. A customer can also ask a policy question, and Maya answers from a document you uploaded.</p>\n<h2 id=\"step-1-create-the-tool\">Step 1: Create the tool</h2>\n<p>Open the node where most of the conversation happens, in this case Maya&#39;s main support node. Scroll down and click <strong>Create a Tool</strong>, then choose <strong>External HTTP API</strong>. Name it <code>get_cart_details</code>.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The description matters more than anything else here. It&#39;s what the agent reads to decide when to call the tool. Write it plainly: <em>&quot;Use this tool when the caller asks about cart details. It returns the cart&#39;s products, quantities, total, and discounted total.&quot;</em></p>\n</div>\n\n\n\n<p>For the endpoint, this demo uses <a href=\"https://dummyjson.com\">dummyjson.com</a>, a public sample API. Copy this exact URL and test it yourself, no backend required:</p>\n<pre><code>GET https://dummyjson.com/carts/1\n</code></pre>\n<p>No auth, no parameters. It&#39;s a GET request because you&#39;re fetching information, not changing anything. In your own setup, this would point at your CRM or order system instead.</p>\n<p>Save the tool.</p>\n<h2 id=\"step-2-attach-the-tool-to-the-node\">Step 2: Attach the tool to the node</h2>\n<p>Creating the tool isn&#39;t enough by itself. Go back to Maya&#39;s node and select <code>get_cart_details</code> under Tools, then save.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>A tool only becomes available once it&#39;s attached to the node that needs it. If you skip this, the tool exists but the agent will never call it.</p>\n</div>\n\n\n\n<h2 id=\"step-3-upload-a-knowledge-base-document\">Step 3: Upload a Knowledge Base document</h2>\n<p>Click <strong>Upload Document</strong>, or go straight to <strong>Knowledge Base Files</strong>. Pick the document you want the agent to read from, a return policy, a shipping FAQ, whatever your customers actually ask about.</p>\n<p>PayInt Voice Studio asks how it should retrieve from this document:</p>\n<ul>\n<li><strong>Full Document</strong> works best for small files, where the agent can see the whole thing at once. It does not require an embeddings model.</li>\n<li><strong>Chunked Search</strong> is for large documents, where the agent should only pull the relevant section. Configure an embeddings model before selecting this mode; otherwise document processing or retrieval will fail.</li>\n</ul>\n<p>A short policy document is a Full Document case. Upload it, then wait for the status to say <strong>Completed</strong>.</p>\n<h2 id=\"step-4-attach-the-document-to-the-node\">Step 4: Attach the document to the node</h2>\n<p>Same rule as the tool. Go back to Maya&#39;s node, scroll to <strong>Knowledge Base Documents</strong>, and attach the file.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Uploading isn&#39;t enough on its own. The document has to be attached to the node where the agent should use it.</p>\n</div>\n\n\n\n<h2 id=\"step-5-tell-the-agent-when-to-use-each\">Step 5: Tell the agent when to use each</h2>\n<p>In the node&#39;s prompt, spell out which mechanism handles which question. Don&#39;t leave it to guesswork:</p>\n<pre><code>If the caller asks about cart contents, order items, quantities, total, or discounts,\nuse the get_cart_details tool before answering.\n\nFor questions about returns, refunds, or cancellations,\nuse the attached Knowledge Base document.\n</code></pre>\n<p>Save the node.</p>\n<h2 id=\"step-6-test-it\">Step 6: Test it</h2>\n<p>Start a Web Call and try both:</p>\n<ul>\n<li><em>&quot;Hi Maya, what&#39;s inside my cart?&quot;</em> Watch the transcript. Maya decides the tool is relevant, calls the API, and answers from the response instead of guessing.</li>\n<li><em>&quot;What&#39;s the return policy?&quot;</em> Maya retrieves the answer from the document and responds from that context.</li>\n</ul>\n<p>That&#39;s the difference that matters: for cart details, Maya uses the tool because the data changes call to call. For the return policy, she uses the Knowledge Base because that answer lives in a document and doesn&#39;t change. Tools connect your agent to systems. Knowledge Base connects it to documents. Together, your agent starts to feel connected to your actual business, not just reciting a script.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<p>You&#39;ve now got the full loop: calls trigger automatically, data flows in and out, your number is connected, and the agent can act on live data and your own documents.</p>\n<ul>\n<li><strong>Full tool reference</strong>: parameter types, authentication, and best practices in <a href=\"/studio/docs/voice-agent/tools/http-api\">HTTP API Tools</a>.</li>\n<li><strong>Full Knowledge Base reference</strong>: chunking behavior and embedding setup in <a href=\"/studio/docs/voice-agent/knowledge-base\">Knowledge Base</a>.</li>\n</ul>\n"},"core-concepts/how-dograh-works":{"title":"How PayInt Voice Studio Works","description":"The big picture — from API call to phone conversation to transcript","html":"<p>PayInt Voice Studio is a platform for building and running voice AI agents. You define a conversation flow, connect a phone number, and PayInt Voice Studio handles the rest — transcribing the caller&#39;s speech (STT), generating intelligent responses (LLM), speaking them back in a natural voice (TTS), and returning structured results when the call ends.</p>\n<h2 id=\"the-core-loop\">The core loop</h2>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant U as Dashboard / Your App\n    participant API as PayInt Voice Studio API\n    participant STT as Transcriber (STT)\n    participant LLM as LLM Provider\n    participant TTS as Voice Synthesizer (TTS)\n    participant Tel as Telephony Provider\n    participant Cal as Caller / Contact\n\n    U-&gt;&gt;API: Trigger call (dashboard or API)\n    API-&gt;&gt;Tel: Initiate outbound call\n    Tel-&gt;&gt;Cal: Phone rings\n    Cal--&gt;&gt;Tel: Answers\n    Tel--&gt;&gt;API: Raw audio stream\n    loop Conversation\n        API-&gt;&gt;STT: Caller audio\n        STT--&gt;&gt;API: Transcribed text\n        API-&gt;&gt;LLM: Transcript + agent prompt + context\n        LLM--&gt;&gt;API: Agent response text\n        API-&gt;&gt;TTS: Response text\n        TTS--&gt;&gt;API: Synthesized audio\n        API-&gt;&gt;Tel: Audio stream\n        Tel-&gt;&gt;Cal: Agent speaks\n    end\n    API-&gt;&gt;API: Extract context, run webhooks\n    API--&gt;&gt;U: Run record (transcript, recording, gathered data)\n</code></pre>\n<h2 id=\"key-components\">Key components</h2>\n<p><strong><a href=\"/studio/docs/core-concepts/workflows-and-agents\">Workflows (Agents)</a></strong>\nThe conversation logic. A workflow is a graph of nodes (conversation steps) connected by edges (conditional transitions). You define what the agent says, when it moves on, and what data it collects.</p>\n<p><strong><a href=\"/studio/docs/core-concepts/calls-and-runs\">Runs</a></strong>\nEvery execution of a workflow creates a run. The run record holds the transcript, recording, extracted data, and cost information.</p>\n<p><strong><a href=\"/studio/docs/integrations/telephony/overview\">Telephony</a></strong>\nThe phone infrastructure. PayInt Voice Studio connects to your telephony provider (Twilio, Vonage, etc.) to place and receive calls. The audio streams between the caller and PayInt Voice Studio in real time.</p>\n<p><strong><a href=\"/studio/docs/configurations/transcriber\">Transcriber (STT)</a></strong>\nConverts the caller&#39;s speech to text in real time. PayInt Voice Studio sends the audio stream to your configured speech-to-text provider and uses the transcript to drive both the LLM and the final run record.</p>\n<p><strong><a href=\"/studio/docs/configurations/llm\">LLM Provider</a></strong>\nProcesses the transcript and the active node&#39;s prompt to generate the agent&#39;s next response. It also evaluates edge conditions to decide when to move the conversation forward.</p>\n<p><strong><a href=\"/studio/docs/configurations/voice\">Voice Synthesizer (TTS)</a></strong>\nConverts the LLM&#39;s text response to audio and streams it back to the caller. The choice of TTS provider and voice is configurable per agent.</p>\n<h2 id=\"how-it-fits-together\">How it fits together</h2>\n<p>When you trigger a call:</p>\n<ol>\n<li>PayInt Voice Studio instructs your telephony provider to dial the number</li>\n<li>When the caller answers, a real-time audio pipeline opens</li>\n<li>The caller&#39;s speech is transcribed by the STT provider</li>\n<li>The transcript is sent to the LLM with the active node&#39;s prompt and conversation history</li>\n<li>The LLM responds — the response is synthesized to audio by the TTS provider and streamed to the caller</li>\n<li>When an edge condition is met, PayInt Voice Studio transitions to the next node</li>\n<li>When an end node is reached, the call ends</li>\n<li>Post-call: context is extracted, webhooks fire, the run record is saved</li>\n</ol>\n<h2 id=\"next-steps\">Next steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/workflows-and-agents\">Workflows & Agents</a>\n\n\n<p>How the conversation graph works</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/calls-and-runs\">Calls & Runs</a>\n\n\n<p>The lifecycle of a call</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/context-and-variables\">Context & Variables</a>\n\n\n<p>How data flows through a conversation</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/campaigns\">Campaigns</a>\n\n\n<p>Running agents at scale</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"core-concepts/workflows-and-agents":{"title":"Workflows & Agents","description":"How conversation flows are defined in PayInt Voice Studio","html":"<p>In PayInt Voice Studio, what you see as an <strong>agent</strong> in the dashboard is called a <strong>workflow</strong> in the API. They are the same thing — a workflow is the underlying definition, agent is the product name for it.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Anywhere the API says <code>workflow</code>, think &quot;agent&quot;. Anywhere the API says <code>workflow_definition</code>, think &quot;the conversation logic inside your agent&quot;.</p>\n</div>\n\n\n\n<h2 id=\"the-graph-model\">The graph model</h2>\n<p>A workflow is a <strong>directed graph</strong> — a set of nodes connected by edges.</p>\n<pre><code class=\"language-mermaid\">graph LR\n    A[Start Call] --&gt;|Caller greets| B[Qualify Intent]\n    B --&gt;|Wants support| C[Support Agent]\n    B --&gt;|Wants sales| D[Sales Agent]\n    C --&gt;|Issue resolved| E[End Call]\n    D --&gt;|Demo booked| E\n</code></pre>\n<p><strong>Nodes</strong> are the steps in the conversation. Each node has a prompt that tells the LLM what to say and do at that point.</p>\n<p><strong>Edges</strong> are the transitions between nodes. Each edge has a condition — a natural language description of when to move on. The LLM evaluates whether the condition has been met based on the conversation so far.</p>\n<h2 id=\"node-types\">Node types</h2>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>startCall</code></td>\n<td>Entry point for telephony calls. The first thing the agent says when a call connects</td>\n</tr>\n<tr>\n<td><code>agentNode</code></td>\n<td>An LLM-powered conversation step. The core building block</td>\n</tr>\n<tr>\n<td><code>globalNode</code></td>\n<td>Defines instructions that apply across all agent nodes (e.g. tone, language, fallback behaviour)</td>\n</tr>\n<tr>\n<td><code>endCall</code></td>\n<td>Terminates the call</td>\n</tr>\n<tr>\n<td><code>trigger</code></td>\n<td>Entry point for API-triggered runs (non-telephony)</td>\n</tr>\n<tr>\n<td><code>webhook</code></td>\n<td>Fires an HTTP request when reached — use for CRM updates, notifications, etc.</td>\n</tr>\n<tr>\n<td><code>qa</code></td>\n<td>Runs automated quality analysis on the completed call</td>\n</tr>\n</tbody></table>\n<h2 id=\"edges-and-transitions\">Edges and transitions</h2>\n<p>An edge connects two nodes and fires when its condition is satisfied:</p>\n<p>&lt;img src=&quot;/agentstudio-docs/images/edge.png&quot; alt=&quot;Edge configuration&quot; style={{border: &quot;1px solid #d1d5db&quot;, borderRadius: &quot;8px&quot;, maxWidth: &quot;100%&quot;}} /&gt;</p>\n<p><code>transition_speech</code> is optional — if set, the agent speaks it before moving to the next node.</p>\n<h2 id=\"versioning\">Versioning</h2>\n<p>Every time you update a workflow&#39;s <code>workflow_definition</code>, PayInt Voice Studio saves a new version while keeping the history. The current version is always what runs. Old versions are retained for auditing.</p>\n<h2 id=\"creating-workflows\">Creating workflows</h2>\n<p>There are two ways to create a workflow via the API:</p>\n<ul>\n<li><strong>From a definition</strong> — provide the full node/edge graph yourself. Best for programmatic generation.</li>\n<li><strong>From a template</strong> — describe the use case in natural language and PayInt Voice Studio generates the initial graph using an LLM. Best for getting started quickly.</li>\n</ul>\n<p>See the <a href=\"/studio/docs/developer/workflow-schema\">Workflow Definition Schema</a> for the full field reference.</p>\n"},"core-concepts/calls-and-runs":{"title":"Calls & Runs","description":"The lifecycle of a call and how runs capture its results","html":"<p>Every time a workflow executes, PayInt Voice Studio creates a <strong>run</strong>. The run is the record of that execution: what was said, what data was collected, how long it took, and what it cost.</p>\n<h2 id=\"calls-vs-runs\">Calls vs runs</h2>\n<p>A <strong>call</strong> is the audio connection — whether over the phone via a telephony provider, or through the browser via Web Call.</p>\n<p>A <strong>run</strong> is the PayInt Voice Studio record of the workflow execution. Every call — outbound, inbound, web, or campaign — creates a run with the same contents: transcript, recording, gathered context, usage, and cost. Campaign runs are additionally linked to their parent campaign.</p>\n<h2 id=\"call-lifecycle\">Call lifecycle</h2>\n<p>The call lifecycle tracks the telephony connection — from the moment a number is dialed to when the line drops.</p>\n<pre><code class=\"language-mermaid\">stateDiagram-v2\n    [*] --&gt; Ringing: Number dialed\n    Ringing --&gt; Connected: Caller answers\n    Ringing --&gt; Ended: No answer / voicemail\n    Connected --&gt; Ended: Conversation completes\n    Connected --&gt; Ended: Error or disconnect\n    Ended --&gt; [*]\n</code></pre>\n<p>For inbound calls, the flow starts when the caller dials in and the telephony provider routes the call to PayInt Voice Studio.</p>\n<h2 id=\"inbound-vs-outbound\">Inbound vs outbound</h2>\n<p><strong>Outbound calls</strong> are initiated by PayInt Voice Studio — you trigger them via the API or dashboard with a phone number and agent. Used for proactive outreach, reminders, and campaigns.</p>\n<p><strong>Inbound calls</strong> are initiated by the caller — your telephony provider routes incoming calls to a PayInt Voice Studio agent via a webhook URL. Used for support lines, hotlines, and IVR replacement.</p>\n<h2 id=\"web-calls\">Web Calls</h2>\n<p>Web Call lets you talk to your agent directly from the browser — no phone number or telephony setup required. It runs the full pipeline: STT, LLM, TTS, recording, and transcript, exactly the same as a phone call.</p>\n<p>Use it to try out your agent before going live. The run view shows the live transcript as the conversation progresses, node transitions as the workflow moves through the graph, and any tool or function calls the agent makes in real time.</p>\n<h2 id=\"what-a-run-contains\">What a run contains</h2>\n<p>After a call completes, the run record includes:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>status</code></td>\n<td>Final run state</td>\n</tr>\n<tr>\n<td><code>recording_url</code></td>\n<td>Download URL for the call audio</td>\n</tr>\n<tr>\n<td><code>transcript_url</code></td>\n<td>Download URL for the conversation transcript</td>\n</tr>\n<tr>\n<td><code>gathered_context</code></td>\n<td>Structured data extracted by the agent during the call</td>\n</tr>\n<tr>\n<td><code>initial_context</code></td>\n<td>The data passed in when the call was triggered</td>\n</tr>\n<tr>\n<td><code>usage_info</code></td>\n<td>Duration in seconds, token count</td>\n</tr>\n<tr>\n<td><code>cost_info</code></td>\n<td>Cost in USD</td>\n</tr>\n</tbody></table>\n"},"core-concepts/context-and-variables":{"title":"Context & Variables","description":"How data flows into, through, and out of a conversation","html":"<p>PayInt Voice Studio has a simple data model for passing information through a call. Understanding it is key to building agents that feel personalised and to extracting useful results after a call.</p>\n<h2 id=\"the-three-context-objects\">The three context objects</h2>\n<pre><code>initial_context ──► Agent ──► gathered_context\n                       │\n                 [template variables](/voice-agent/template-variables)\n                 (used in prompts)\n</code></pre>\n<h3 id=\"initialcontext\">initial_context</h3>\n<p>Data available to the agent before the call starts — the contact&#39;s name, account details, appointment information, anything the agent should know upfront. It can be set from several places:</p>\n<ul>\n<li><strong><a href=\"/studio/docs/voice-agent/api-trigger\">API trigger</a></strong> — pass it in the request body when calling <code>POST /public/agent/{uuid}</code> or <code>POST /telephony/initiate-call</code></li>\n<li><strong><a href=\"/studio/docs/core-concepts/campaigns\">Campaign CSV</a></strong> — columns beyond <code>phone_number</code> automatically become <code>initial_context</code> fields for each contact&#39;s call</li>\n<li><strong><a href=\"/studio/docs/voice-agent/pre-call-data-fetch\">Pre-call data fetch</a></strong> — enrich the context with data from your CRM or ERP via an HTTP call as the call starts, before the agent speaks</li>\n<li><strong><a href=\"/studio/docs/voice-agent/template-variables#using-template-variables-for-testing\">Agent Settings</a></strong> — set template context variables on the agent for testing; they&#39;re included in test calls from the workflow editor and ignored on production calls</li>\n</ul>\n<h3 id=\"template-variables\">Template variables</h3>\n<p>Values from <code>initial_context</code> are available in your agent&#39;s prompt using <code>{{double_brace}}</code> syntax.</p>\n<pre><code>You are calling {{customer_name}} about their {{plan}} plan,\nwhich renews on {{renewal_date}}. Be friendly and confirm\nwhether they&#39;d like to continue.\n</code></pre>\n<p>When the call starts, PayInt Voice Studio substitutes the values before sending the prompt to the LLM — so the agent speaks naturally as if it already knows the contact.</p>\n<h3 id=\"fallback-values\">Fallback values</h3>\n<p>If a variable might be missing or empty, use a pipe (<code>|</code>) to provide a default value:</p>\n<pre><code>Hello {{customer_name | there}}, we&#39;re calling about your {{plan | current}} plan.\n</code></pre>\n<p>When <code>customer_name</code> is not set, the agent will say &quot;Hello there&quot; instead of leaving a blank. The syntax is:</p>\n<pre><code>{{variable_name | fallback_value}}\n</code></pre>\n<p>If the variable is present and non-empty, the fallback is ignored and the actual value is used.</p>\n<h3 id=\"default-variables\">Default variables</h3>\n<p>Built-in variables for current time and weekday, available in any prompt without setting up <code>initial_context</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Description</th>\n<th>Example output</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>{{current_time}}</code></td>\n<td>Current time in UTC (or inferred timezone)</td>\n<td><code>2026-04-02 14:30:45 UTC</code></td>\n</tr>\n<tr>\n<td><code>{{current_time_&lt;TIMEZONE&gt;}}</code></td>\n<td>Current time in the specified timezone</td>\n<td><code>2026-04-02 20:00:45 IST</code></td>\n</tr>\n<tr>\n<td><code>{{current_weekday}}</code></td>\n<td>Current weekday name in UTC (or inferred timezone)</td>\n<td><code>Thursday</code></td>\n</tr>\n<tr>\n<td><code>{{current_weekday_&lt;TIMEZONE&gt;}}</code></td>\n<td>Current weekday name in the specified timezone</td>\n<td><code>Thursday</code></td>\n</tr>\n</tbody></table>\n<p>Replace <code>&lt;TIMEZONE&gt;</code> with an <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\">IANA timezone name</a> such as <code>Asia/Kolkata</code>, <code>America/New_York</code>, or <code>Europe/London</code>.</p>\n<pre><code>Today is {{current_weekday}} and the current time is {{current_time_America/New_York}}.\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>When you use a timezone suffix on <strong>either</strong> <code>current_time</code> or <code>current_weekday</code>, the other variable without a suffix will automatically use the same timezone instead of UTC. For example, if your prompt contains both <code>{{current_time_Asia/Kolkata}}</code> and <code>{{current_weekday}}</code>, the weekday will also be resolved in <code>Asia/Kolkata</code>.</p>\n</div>\n\n\n\n<h3 id=\"telephony-variables\">Telephony variables</h3>\n<p>For telephony calls (inbound and outbound), PayInt Voice Studio automatically adds these variables to <code>initial_context</code>:</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Description</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>{{caller_number}}</code></td>\n<td>The phone number that initiated the call</td>\n<td><code>+14155550100</code></td>\n</tr>\n<tr>\n<td><code>{{called_number}}</code></td>\n<td>The phone number that received the call</td>\n<td><code>+18005550199</code></td>\n</tr>\n</tbody></table>\n<p>For <strong>inbound</strong> calls, <code>caller_number</code> is the customer&#39;s number and <code>called_number</code> is your PayInt Voice Studio number. For <strong>outbound</strong> calls, it&#39;s the reverse — <code>caller_number</code> is your PayInt Voice Studio number and <code>called_number</code> is the customer&#39;s number.</p>\n<pre><code>You are speaking with the caller at {{caller_number}}.\n</code></pre>\n<h3 id=\"gatheredcontext\">gathered_context</h3>\n<p>Data the agent extracts <em>during</em> the call — the opposite direction of <code>initial_context</code>. Use it to turn a conversation into structured data: what the customer wants, whether they confirmed something, a value they gave you out loud.</p>\n<h4 id=\"how-it-gets-populated\">How it gets populated</h4>\n<p>Turn on <strong>extraction</strong> on an <a href=\"/studio/docs/voice-agent/agent\">Agent</a> or <a href=\"/studio/docs/voice-agent/end-call\">End Call</a> node and define one or more variables to extract. Each variable has:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>The key it will appear under in <code>gathered_context</code></td>\n</tr>\n<tr>\n<td><code>type</code></td>\n<td><code>string</code>, <code>number</code>, or <code>boolean</code></td>\n</tr>\n<tr>\n<td><code>prompt</code></td>\n<td>A natural-language description of what to look for, e.g. <em>&quot;Did the customer confirm the appointment?&quot;</em></td>\n</tr>\n</tbody></table>\n<p>&lt;img src=&quot;/agentstudio-docs/images/extracted_variables.png&quot; alt=&quot;Extracted variables&quot; style={{border: &quot;1px solid #d1d5db&quot;, borderRadius: &quot;8px&quot;, maxWidth: &quot;100%&quot;}} /&gt;</p>\n<p>When the conversation reaches that node, the LLM reads the transcript so far and fills in each variable based on its <code>prompt</code>. If a value can&#39;t be determined from the conversation, the variable is left empty rather than guessed — leave the <code>prompt</code> specific enough that the LLM knows exactly what counts as a match.</p>\n<p>You can add extraction to more than one node. Each node&#39;s extracted variables are merged into the same <code>gathered_context</code> object as the call progresses, keyed by <code>name</code> — reuse a <code>name</code> at a later node if you want to overwrite an earlier value.</p>\n<h4 id=\"how-to-reference-it-downstream\">How to reference it downstream</h4>\n<p><code>gathered_context</code> is <strong>not available in Agent prompts</strong> — a prompt can only reference <code>initial_context</code> fields, because extraction typically happens after the conversation that would use it. To act on extracted data, send it out via a node instead:</p>\n<table>\n<thead>\n<tr>\n<th>Where</th>\n<th>Syntax</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"/studio/docs/voice-agent/webhook\">Webhook node</a> payload</td>\n<td><code>{{gathered_context.field_name}}</code></td>\n<td>Prefixed, since the payload template can also reference <code>initial_context</code></td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/developer/webhooks#payload-context-variables\">Run record</a> (API / dashboard)</td>\n<td><code>gathered_context</code> object</td>\n<td>Returned after the run completes, alongside <code>recording_url</code> and <code>transcript_url</code></td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-json\">{\n  &quot;customer&quot;: &quot;{{initial_context.customer_name}}&quot;,\n  &quot;resolution&quot;: &quot;{{gathered_context.resolution}}&quot;,\n  &quot;callback_requested&quot;: &quot;{{gathered_context.wants_callback}}&quot;\n}\n</code></pre>\n<p>See <a href=\"/studio/docs/developer/webhooks\">Webhook Payloads</a> for the full list of variables available alongside <code>gathered_context</code> in a payload template.</p>\n<h2 id=\"data-flow-example\">Data flow example</h2>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant App as Your System\n    participant Dog as PayInt Voice Studio\n    participant LLM as LLM\n\n    App-&gt;&gt;Dog: initial_context: {customer_name: &quot;Jane&quot;, plan: &quot;premium&quot;}\n    Dog-&gt;&gt;LLM: Prompt with {{customer_name}} and {{plan}} substituted\n    LLM--&gt;&gt;Dog: Conversation response\n    Note over Dog,LLM: Call progresses...\n    Dog-&gt;&gt;LLM: Extract: did the customer confirm renewal?\n    LLM--&gt;&gt;Dog: gathered_context: {renewal_confirmed: true}\n    Dog--&gt;&gt;App: Run record with gathered_context\n</code></pre>\n<h2 id=\"where-variables-are-available\">Where variables are available</h2>\n<table>\n<thead>\n<tr>\n<th>Location</th>\n<th>Variables available</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Agent node prompts</td>\n<td><code>initial_context</code> fields via <code>{{variable_name}}</code></td>\n</tr>\n<tr>\n<td>Edge conditions</td>\n<td>Evaluated against the live conversation — no explicit variable syntax needed</td>\n</tr>\n<tr>\n<td>Webhook payload templates</td>\n<td>All context objects via <code>{{initial_context.field}}</code>, <code>{{gathered_context.field}}</code> etc.</td>\n</tr>\n<tr>\n<td>Campaign CSV columns</td>\n<td>CSV columns beyond <code>phone_number</code> become <code>initial_context</code> fields automatically</td>\n</tr>\n</tbody></table>\n"},"core-concepts/campaigns":{"title":"Campaigns","description":"Running a voice agent against a list of contacts at scale","html":"<p>A campaign is how you run a workflow against many contacts automatically. Instead of triggering calls one by one via the API, you upload a list of phone numbers and PayInt Voice Studio dials them for you — respecting scheduling windows, concurrency limits, and retry rules.</p>\n<h2 id=\"how-a-campaign-works\">How a campaign works</h2>\n<pre><code class=\"language-mermaid\">flowchart LR\n    CSV[Contacts CSV] --&gt; Upload[Upload to PayInt Voice Studio]\n    Upload --&gt; Campaign[Create Campaign]\n    Campaign --&gt; Schedule[Scheduling &amp; Concurrency]\n    Schedule --&gt; Calls[Outbound Calls]\n    Calls --&gt; Runs[Run Records]\n    Runs --&gt; Results[Progress &amp; Reports]\n</code></pre>\n<ol>\n<li><strong>Upload a contacts CSV</strong> — must have a <code>phone_number</code> column; any extra columns become <code>initial_context</code> for each call</li>\n<li><strong>Create the campaign</strong> — link it to a workflow, set concurrency, time slots, and retry behaviour</li>\n<li><strong>Start it</strong> — PayInt Voice Studio begins dialing contacts up to your concurrency limit</li>\n<li><strong>Monitor progress</strong> — track processed, completed, failed, and pending counts in real time</li>\n<li><strong>Pause and resume</strong> — stop and restart at any point without losing progress</li>\n</ol>\n<h2 id=\"the-contacts-csv\">The contacts CSV</h2>\n<p>The CSV drives the campaign. Each row is one contact.</p>\n<pre><code class=\"language-csv\">phone_number,customer_name,account_id,plan,greeting_override_type,greeting_override_text\n+14155550100,Jane Smith,acc_001,premium,text,Hello {{customer_name}}\n+14155550101,Bob Jones,acc_002,basic,text,Welcome back {{customer_name}}\n</code></pre>\n<p>Columns beyond <code>phone_number</code> are automatically passed as <code>initial_context</code> to each call, making them available as template variables in your agent&#39;s prompt — so each call can feel personalised at scale.</p>\n<p>To override the Start node greeting for a row, set <code>greeting_override_type</code> to <code>text</code> and provide <code>greeting_override_text</code>. The text can use other columns as template variables. For a recorded greeting, set the type to <code>audio</code> and provide its public ID in <code>greeting_override_recording_id</code>. Leave the override columns empty to use the greeting configured in the Start node.</p>\n<h2 id=\"scheduling-and-concurrency\">Scheduling and concurrency</h2>\n<p><strong>Concurrency</strong> controls how many calls run simultaneously. It&#39;s capped by your telephony plan. Set it conservatively to start.</p>\n<p><strong>Time slots</strong> restrict when PayInt Voice Studio is allowed to dial — useful for respecting business hours or regulations:</p>\n<pre><code class=\"language-json\">{\n  &quot;timezone&quot;: &quot;America/New_York&quot;,\n  &quot;time_slots&quot;: [\n    { &quot;day&quot;: &quot;monday&quot;, &quot;start&quot;: &quot;09:00&quot;, &quot;end&quot;: &quot;17:00&quot; },\n    { &quot;day&quot;: &quot;tuesday&quot;, &quot;start&quot;: &quot;09:00&quot;, &quot;end&quot;: &quot;17:00&quot; }\n  ]\n}\n</code></pre>\n<p>If no time slots are set, PayInt Voice Studio dials continuously once the campaign is started.</p>\n<h2 id=\"retry-behaviour\">Retry behaviour</h2>\n<p>PayInt Voice Studio can automatically retry contacts who didn&#39;t answer, were busy, or went to voicemail:</p>\n<pre><code class=\"language-json\">{\n  &quot;retry_config&quot;: {\n    &quot;max_attempts&quot;: 3,\n    &quot;retry_interval_minutes&quot;: 60\n  }\n}\n</code></pre>\n<h2 id=\"circuit-breaker\">Circuit breaker</h2>\n<p>The circuit breaker automatically pauses a campaign when the call failure rate gets too high — protecting against wasted spend and telephony reputation issues caused by a misconfigured agent or a bad contact list.</p>\n<p>When enabled, PayInt Voice Studio monitors the failure rate within a rolling time window. If it exceeds the threshold, the campaign is paused automatically and must be manually resumed after the issue is investigated.</p>\n<table>\n<thead>\n<tr>\n<th>Setting</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Failure Threshold (%)</td>\n<td><code>50</code></td>\n<td>Pause when the failure rate within the window exceeds this percentage</td>\n</tr>\n<tr>\n<td>Window (seconds)</td>\n<td><code>120</code></td>\n<td>Rolling time window over which the failure rate is calculated</td>\n</tr>\n<tr>\n<td>Min Calls in Window</td>\n<td><code>5</code></td>\n<td>Minimum number of calls required before the circuit breaker can trip — prevents false positives on small samples</td>\n</tr>\n</tbody></table>\n<p>A campaign paused by the circuit breaker behaves the same as a manually paused campaign — in-flight calls complete normally, and it can be resumed once the underlying issue is resolved.</p>\n<h2 id=\"campaign-lifecycle\">Campaign lifecycle</h2>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>draft</code></td>\n<td>Created but not started</td>\n</tr>\n<tr>\n<td><code>running</code></td>\n<td>Actively dialing</td>\n</tr>\n<tr>\n<td><code>paused</code></td>\n<td>Stopped; resumes from where it left off</td>\n</tr>\n<tr>\n<td><code>completed</code></td>\n<td>All contacts processed</td>\n</tr>\n<tr>\n<td><code>failed</code></td>\n<td>Encountered a fatal error</td>\n</tr>\n</tbody></table>\n<p>You can pause and resume a campaign at any time. In-flight calls complete normally before a pause takes effect.</p>\n<h2 id=\"results\">Results</h2>\n<p>Each contact&#39;s call creates a run record with the full transcript, recording, and gathered context — same as a manually triggered call. Use the <a href=\"/studio/docs/api-reference/campaigns/runs\">Get Campaign Runs</a> endpoint to retrieve them all.</p>\n<p>See the <a href=\"/studio/docs/api-reference/campaigns\">Campaigns API reference</a> to get started.</p>\n"},"configurations/inference-providers":{"title":"Model Configurations","description":"Configure the speech-to-speech, PayInt Voice Studio-managed, or bring-your-own-key models your PayInt Voice Studio agents use.","html":"<h2 id=\"how-model-configurations-work\">How model configurations work</h2>\n<p>Model Configurations define the default AI model setup for your organization. Agents use this configuration unless you set agent-level model overrides in the agent settings.</p>\n<p>To configure models, open <strong>Models</strong> in your PayInt Voice Studio dashboard:</p>\n<ul>\n<li><strong>Hosted:</strong> <code>/studio/model-configurations</code></li>\n<li><strong>Self-hosted:</strong> <code>http://localhost:3010/model-configurations</code></li>\n<li><strong>Local development:</strong> <code>http://localhost:3000/model-configurations</code></li>\n</ul>\n<p>The Models page has three top-level sections:</p>\n<table>\n<thead>\n<tr>\n<th>Section</th>\n<th>When to use it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Speech to Speech</strong></td>\n<td>Use a realtime speech-to-speech model for the live conversation. You still configure an LLM alongside it for variable extraction and QA.</td>\n</tr>\n<tr>\n<td><strong>PayInt Voice Studio</strong></td>\n<td>Use PayInt Voice Studio-managed LLM, voice, and transcriber models behind one PayInt Voice Studio Service Key.</td>\n</tr>\n<tr>\n<td><strong>BYOK</strong></td>\n<td>Bring your own provider keys and configure LLM, Voice, Transcriber, and Embedding models separately.</td>\n</tr>\n</tbody></table>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Model settings are organization-scoped. If no agent-level override is set, every agent in the organization uses the saved global configuration.</p>\n</div>\n\n\n\n<h2 id=\"speech-to-speech\">Speech to Speech</h2>\n<p>Use <strong>Speech to Speech</strong> when you want a realtime model to handle the live spoken conversation directly. In this mode, the realtime model handles speech input and speech output, so you do not configure separate Voice and Transcriber services.</p>\n<p><img src=\"/agentstudio-docs/images/model-configuration-speech-to-speech.png\" alt=\"Speech to Speech model configuration\"></p>\n<p>The Speech to Speech section has nested tabs:</p>\n<table>\n<thead>\n<tr>\n<th>Tab</th>\n<th>What to configure</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Realtime Model</strong></td>\n<td>The speech-to-speech provider, model, voice, and API key.</td>\n</tr>\n<tr>\n<td><strong>LLM</strong></td>\n<td>A standard LLM used for non-realtime tasks such as variable extraction and QA analysis.</td>\n</tr>\n<tr>\n<td><strong>Embedding</strong></td>\n<td>An embedding model used by features that need embeddings, such as retrieval from knowledge base content.</td>\n</tr>\n</tbody></table>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>An LLM is still required when you use Speech to Speech. The realtime model handles the live voice conversation, but PayInt Voice Studio uses the LLM for analysis tasks that happen outside the live audio stream.</p>\n</div>\n\n\n\n<h2 id=\"payint-voice-studio\">PayInt Voice Studio</h2>\n<p>Use <strong>PayInt Voice Studio</strong> when you want PayInt Voice Studio to manage the model providers for you. This path uses one PayInt Voice Studio Service Key for PayInt Voice Studio-managed models instead of separate provider keys for LLM, Voice, and Transcriber.</p>\n<p><img src=\"/agentstudio-docs/images/model-configuration-dograh.png\" alt=\"PayInt Voice Studio model configuration\"></p>\n<p>Configure:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>What it controls</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Voice</strong></td>\n<td>The PayInt Voice Studio-managed voice to use.</td>\n</tr>\n<tr>\n<td><strong>Speed</strong></td>\n<td>The voice playback speed.</td>\n</tr>\n<tr>\n<td><strong>Language</strong></td>\n<td>The language behavior, including multilingual auto-detect when available.</td>\n</tr>\n<tr>\n<td><strong>API Key</strong></td>\n<td>Your PayInt Voice Studio Service Key. Create Service Keys from <strong>Developers</strong>.</td>\n</tr>\n</tbody></table>\n<p>For details on creating and using Service Keys, see <a href=\"/studio/docs/configurations/api-keys#service-keys\">API Keys and Service Keys</a>.</p>\n<h2 id=\"byok\">BYOK</h2>\n<p>Use <strong>BYOK</strong> when you want to bring your own provider accounts and API keys. This gives you separate control over each model category.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>When you use BYOK or external model providers, PayInt Voice Studio sends only the data required for the selected service to operate. Depending on the provider and service type, this may include prompts, conversation history, transcripts, audio, generated text, tool/function definitions, tool inputs or results, and request metadata.</p>\n<p>Provider data handling varies. Review each provider&#39;s data processing, retention, model training, and regional hosting policies before using sensitive data.</p>\n</div>\n\n\n\n<p><img src=\"/agentstudio-docs/images/model-configuration-byok.png\" alt=\"BYOK model configuration\"></p>\n<p>The BYOK section has nested tabs:</p>\n<table>\n<thead>\n<tr>\n<th>Tab</th>\n<th>What to configure</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>LLM</strong></td>\n<td>The chat or reasoning model provider, model, optional base URL, and API key.</td>\n</tr>\n<tr>\n<td><strong>Voice</strong></td>\n<td>The text-to-speech provider, voice, model, speed, optional base URL, and API key.</td>\n</tr>\n<tr>\n<td><strong>Transcriber</strong></td>\n<td>The speech-to-text provider, model, language, and API key.</td>\n</tr>\n<tr>\n<td><strong>Embedding</strong></td>\n<td>The embedding provider, model, and API key.</td>\n</tr>\n</tbody></table>\n<p>Provider-specific fields appear only when they apply. For example, OpenAI-compatible LLM providers can expose a <strong>Base URL</strong> field, ElevenLabs voices can expose a voice ID, and transcribers can expose language options.</p>\n<h2 id=\"agent-level-model-overrides\">Agent-level model overrides</h2>\n<p>You can override the organization model configuration for an individual agent. This is useful when different agents need different models, voices, transcribers, or providers.</p>\n<p>To configure an override:</p>\n<ol>\n<li>Open the agent.</li>\n<li>Go to <strong>Settings</strong>.</li>\n<li>Open <strong>Model Overrides</strong>.</li>\n<li>Enable the override for the service you want to customize.</li>\n<li>Configure the provider, model, and keys for that service.</li>\n<li>Save the agent settings.</li>\n</ol>\n<p>Agent-level overrides are selective. For example, you can override only the Voice service for one agent while it continues to use the organization-level LLM and Transcriber configuration.</p>\n"},"configurations/llm":{"title":"LLM","description":"Voice Agents use LLM (Large Language Models), which are trained to understand the conversational context, and respond to users.","html":"<p>PayInt Voice Studio platform supports OpenAI, Google AI Studio, Google Vertex AI, Azure OpenAI, AWS Bedrock, Groq, OpenRouter, Hugging Face, MiniMax, Sarvam, and PayInt Voice Studio-managed LLMs. There are some models provided by default for you to choose from the drop down. </p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>LLM providers receive the data needed to generate responses, such as prompts, conversation history, model settings, and any configured tool/function definitions or tool call context. Review the provider&#39;s data processing, retention, model training, and regional hosting policies before using sensitive data.</p>\n</div>\n\n\n\n<p>For locally deployed or self-hosted LLMs, PayInt Voice Studio also supports OpenAI-compatible endpoints such as Ollama and vLLM.</p>\n<p><img src=\"/agentstudio-docs/images/models_dropdown.png\" alt=\"Select Models from DropDown\"></p>\n<p>If you don&#39;t find a model in the drop down, you can always add a model manually.</p>\n<p><img src=\"/agentstudio-docs/images/add_model_manually.png\" alt=\"Select Models from DropDown\"></p>\n"},"configurations/voice":{"title":"Voice","description":"Voice Agents use TTS (Text to Speech), which generates audio that LLMs generate during the course of a conversation. This is the audio that the end user having the conversation listens to.","html":"<p>PayInt Voice Studio platform supports ElevenLabs, OpenAI, Google, Azure Speech, Deepgram, Cartesia, Smallest AI, MiniMax, Sarvam, Rime, Inworld, Camb.ai, xAI, LMNT, Speechify, and PayInt Voice Studio TTS engines. There are some voices from the providers that we ship by default. You can refer to the providers API documentation to select a voice ID that&#39;s most relevant for your language requirement.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Voice providers receive the data needed to synthesize speech, such as generated text, selected voice, model settings, and request metadata. Review the provider&#39;s data processing, retention, model training, and regional hosting policies before using sensitive data.</p>\n</div>\n\n\n\n<p>For locally deployed or self-hosted TTS models, PayInt Voice Studio also supports Speaches, an OpenAI API-compatible server for speech generation.</p>\n<p>If you don&#39;t find your favourite voice, you can always add the voice ID manually.</p>\n<p><img src=\"/agentstudio-docs/images/add_tts_manually.png\" alt=\"Add Voice Manually\"></p>\n"},"configurations/transcriber":{"title":"Transcriber","description":"Voice Agents use STT (Speech to Text), to transcribe what the user speaks. This transcribed speech as text goes into an LLM to generate the response that gets played out to the user.","html":"<p>PayInt Voice Studio platform supports Deepgram, OpenAI, Google, Azure Speech, AssemblyAI, Speechmatics, Cartesia, Gladia, Sarvam, Smallest AI, Hugging Face, ElevenLabs, and PayInt Voice Studio transcribers. You can take a look at the providers documentation of which language to select for your language requirements. </p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Transcriber providers receive the data needed to transcribe speech, such as call audio, language settings, keyterms, and request metadata. Review the provider&#39;s data processing, retention, model training, and regional hosting policies before using sensitive data.</p>\n</div>\n\n\n\n<p>For locally deployed or self-hosted STT models, PayInt Voice Studio also supports Speaches, an OpenAI API-compatible server for streaming transcription.</p>\n<p>Example: Deepgram has their language support documentation at <a href=\"https://developers.deepgram.com/docs/models-languages-overview#nova-3\">https://developers.deepgram.com/docs/models-languages-overview#nova-3</a></p>\n"},"configurations/api-keys":{"title":"API Keys and Service Keys","description":"You can create API Keys to trigger PayInt Voice Studio Voice Agents and Service Keys to use with Inference Providers","html":"<p>The option to create the Keys are in /studio/api-keys if you are using hosted version, or <a href=\"http://localhost:3010/api-keys\">http://localhost:3010/api-keys</a> if you are using the self hosted version.</p>\n<h3 id=\"api-keys\">API Keys</h3>\n<p>API keys can be used to trigger a voice agent from an external system, like n8n or programatically from your other workflows. In order to generate that, you can go to <code>/api-keys</code> and create a new key. </p>\n<p><img src=\"/agentstudio-docs/images/api-keys.png\" alt=\"Create a new API Key\"></p>\n<p>Please note that you must copy and keep the API key secretly, since this is the only time that you would be able to copy it. If you lose it, you can always delete that, if its not being used anywhere, and create a new API key. </p>\n<h3 id=\"service-keys\">Service Keys</h3>\n<p>Service Keys are the keys which you generate to be used in <a href=\"/studio/docs/configurations/inference-providers\">Model Configurations</a>. In order to generate that, you can go to <code>/api-keys</code> and create a new key. </p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>You can use a Service Key created in PayInt Voice Studio Cloud (<code>/studio/api-keys</code>) in your self-hosted PayInt Voice Studio deployment. Create the Service Key from your PayInt Voice Studio Cloud account, then paste it into <strong>Model Configurations</strong> in your self-hosted instance to use PayInt Voice Studio-managed inference providers. You can purchase PayInt Voice Studio credits in PayInt Voice Studio Cloud; billing happens on the Cloud account that owns the Service Key.</p>\n</div>\n\n\n\n<p><img src=\"/agentstudio-docs/images/service-keys.png\" alt=\"Create a new Service Key\"></p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Service Keys are scoped to the PayInt Voice Studio Cloud account that created them. You cannot use a Service Key from one cloud-hosted account in another cloud-hosted account; create a new Service Key from the account where you want to use it.</p>\n</div>\n\n\n"},"configurations/agent-uuid":{"title":"Agent UUID","description":"Find and use your workflow's stable Agent UUID","html":"<p>The <strong>Agent UUID</strong> is the workflow&#39;s stable identifier. In API routes and payloads you may also see it called <code>workflow_uuid</code>.</p>\n<p>Unlike numeric workflow IDs, the Agent UUID is portable across environments and does not change when you publish new versions of the same workflow.</p>\n<h2 id=\"where-to-find-it\">Where to find it</h2>\n<p>You can copy the Agent UUID from two places in the dashboard.</p>\n<h3 id=\"from-the-workflow-editor\">From the workflow editor</h3>\n<ol>\n<li>Open your agent in the workflow editor</li>\n<li>Click the <strong>⋮</strong> menu in the top-right of the header</li>\n<li>Click <strong>Copy Agent UUID</strong></li>\n</ol>\n<h3 id=\"from-the-agent39s-settings-page\">From the agent&#39;s Settings page</h3>\n<ol>\n<li>Open the agent and go to <strong>Settings</strong></li>\n<li>Scroll to the <strong>Agent UUID</strong> section</li>\n<li>Click the UUID code block, or use <strong>Copy UUID</strong></li>\n</ol>\n<h2 id=\"where-you-use-it\">Where you use it</h2>\n<p>Use the Agent UUID for workflow-level routes such as:</p>\n<ul>\n<li><code>POST /api/v1/public/agent/workflow/{workflow_uuid}</code></li>\n<li><code>POST /api/v1/public/agent/test/workflow/{workflow_uuid}</code></li>\n<li><code>wss://&lt;your-host&gt;/api/v1/agent-stream/{provider}/{agent_uuid}</code></li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Do not confuse the Agent UUID with an API Trigger node UUID (<code>trigger_path</code>).</p>\n<ul>\n<li>Use the Agent UUID for workflow-level routes like <code>/public/agent/workflow/{workflow_uuid}</code></li>\n<li>Use the API Trigger node UUID for trigger routes like <code>/public/agent/{uuid}</code></li>\n<li>The API Trigger node path can be edited if you want something more descriptive</li>\n<li>The Agent UUID is generated by PayInt Voice Studio and cannot be changed</li>\n</ul>\n<p>See <a href=\"/studio/docs/voice-agent/api-trigger\">API Trigger</a> for trigger-node URLs.</p>\n</div>\n\n\n"},"configurations/interruption":{"title":"Interruption Handling","description":"Control whether users can interrupt the bot while it is speaking by configuring the Allow Interruption toggle on each node.","html":"<h2 id=\"overview\">Overview</h2>\n<p>Interruption handling controls whether the user can &quot;barge in&quot; and interrupt the bot while it is speaking. This is configured <strong>per node</strong> in the workflow editor, giving you fine-grained control over conversation flow.</p>\n<p><img src=\"/agentstudio-docs/images/allow-interruption.png\" alt=\"Allow Interruption Toggle\"></p>\n<h2 id=\"what-is-vad\">What is VAD?</h2>\n<p>VAD (Voice Activity Detection) is what makes barge-in possible — it&#39;s the model that listens to the incoming audio stream and decides whether the caller is currently speaking. PayInt Voice Studio&#39;s pipeline runs <a href=\"https://github.com/snakers4/silero-vad\">Silero VAD</a> by default to detect the start and end of user speech in real time. Some realtime providers (such as OpenAI Realtime, Azure Realtime, and Grok Voice Agent) supply their own VAD signals, so local Silero VAD is skipped for those calls.</p>\n<p>When <strong>Allow Interruption</strong> is enabled on a node, VAD is what triggers the interrupt: the moment it detects the caller has started talking, the bot&#39;s speech is cut off and the pipeline starts processing the new input. When interruption is disabled, VAD output for the user&#39;s mic is ignored until the bot finishes speaking.</p>\n<p>VAD also feeds turn-taking more generally — it&#39;s part of how PayInt Voice Studio decides a caller has finished a turn and the agent should respond, independent of whether interruption is enabled.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><strong>Valid sample rates</strong>: PayInt Voice Studio&#39;s pipeline runs VAD at either <strong>8000 Hz</strong> or <strong>16000 Hz</strong> — no other value is accepted. The overall pipeline sample rate is capped at 16 kHz to satisfy this. This only matters if you&#39;re integrating a <a href=\"/studio/docs/integrations/telephony/custom#audio-format-considerations\">custom telephony provider</a>; telephony providers built into PayInt Voice Studio (Twilio, Vonage, Plivo, etc.) already negotiate a supported rate for you.</p>\n</div>\n\n\n\n<h2 id=\"how-it-works\">How It Works</h2>\n<p>Each node in your workflow has an <strong>Allow Interruption</strong> toggle:</p>\n<ul>\n<li><strong>Disabled (default)</strong> — The bot finishes its entire response before accepting user input. The user&#39;s microphone is temporarily muted while the bot speaks.</li>\n<li><strong>Enabled</strong> — The bot stops speaking as soon as the user starts talking, and immediately processes their input. This creates a natural, conversational experience.</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>When interruption is disabled and the user tries to speak during bot speech, a one-time warning appears in the live transcript indicating that interruption is disabled for that step.</p>\n</div>\n\n\n\n<h2 id=\"when-to-disable-interruption\">When to Disable Interruption</h2>\n<p>Disabling interruption is useful when the bot needs to deliver a complete message without being cut off:</p>\n<ul>\n<li><strong>Legal disclaimers</strong> — Ensure the full disclaimer is spoken before proceeding.</li>\n<li><strong>Critical instructions</strong> — Step-by-step directions that lose meaning if partially heard.</li>\n<li><strong>Greeting or introduction</strong> — Let the bot finish its opening before the user responds.</li>\n<li><strong>Confirmation summaries</strong> — Read back important details (appointment times, order totals) in full.</li>\n</ul>\n<h2 id=\"when-to-enable-interruption\">When to Enable Interruption</h2>\n<p>Keep interruption enabled for interactive conversation stages:</p>\n<ul>\n<li><strong>Q&amp;A or objection handling</strong> — Let the user jump in naturally.</li>\n<li><strong>Open-ended discussion</strong> — Feels more human when either party can interject.</li>\n<li><strong>Long responses</strong> — Allow the user to redirect if the bot goes off track.</li>\n</ul>\n<h2 id=\"configuring-interruption\">Configuring Interruption</h2>\n<ol>\n<li>Open your workflow in the <strong>Voice Agent Builder</strong>.</li>\n<li>Select the node you want to configure.</li>\n<li>Toggle <strong>Allow Interruption</strong> on or off in the node settings panel.</li>\n<li>Save your workflow.</li>\n</ol>\n<p>You can set different interruption behavior for each node. For example, disable interruption on your Start Node greeting but enable it on all subsequent Agent Nodes.</p>\n<h2 id=\"what-the-user-experiences\">What the User Experiences</h2>\n<table>\n<thead>\n<tr>\n<th>Interruption</th>\n<th>Bot Speaking</th>\n<th>User Speaks</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Enabled</td>\n<td>Yes</td>\n<td>Yes</td>\n<td>Bot stops, processes user input</td>\n</tr>\n<tr>\n<td>Disabled</td>\n<td>Yes</td>\n<td>Yes</td>\n<td>Bot continues, user input is ignored until bot finishes</td>\n</tr>\n<tr>\n<td>Either</td>\n<td>No</td>\n<td>Yes</td>\n<td>User input is processed normally</td>\n</tr>\n</tbody></table>\n<p>When interruption is disabled, the platform mutes the user&#39;s audio input while the bot is speaking. Once the bot finishes, the microphone is automatically unmuted and the user can respond normally.</p>\n"},"configurations/tracing":{"title":"Tracing","description":"Trace your agent runs to debug issues, understand LLM behavior, and refine your prompts.","html":"<h2 id=\"overview\">Overview</h2>\n<p>Tracing gives you full visibility into what happened during a test call or live call. It&#39;s one of the most powerful features for making your voice agents production-ready.</p>\n<p>With traces, you can see:</p>\n<ul>\n<li>The exact conversation that occurred</li>\n<li>What prompts were sent to the LLM</li>\n<li>Which tools the LLM had access to (and which it called)</li>\n<li>Speech-to-text transcriptions of user input and Transcription errors</li>\n<li>Tool call requests and responses</li>\n</ul>\n<h2 id=\"accessing-traces\">Accessing Traces</h2>\n<p>After a call ends, you&#39;ll see a call summary screen. Click the <strong>Traces</strong> tab to view the detailed trace log for that call.</p>\n<p><img src=\"/agentstudio-docs/images/accessing-traces.png\" alt=\"Accessing Traces\"></p>\n<h2 id=\"understanding-trace-sections\">Understanding Trace Sections</h2>\n<p>Traces are organized by event type. The two most important sections to focus on are:</p>\n<h3 id=\"stt-speech-to-text\">STT (Speech-to-Text)</h3>\n<p>These entries capture the transcription of what the user said during the call. Each STT entry shows the text conversion of the user&#39;s speech at that point in the conversation.</p>\n<p><strong>Example:</strong></p>\n<p>STT: &quot;Yeah, I would want to check what your operating hours are.&quot;</p>\n<h3 id=\"llm-language-model-calls\">LLM (Language Model Calls)</h3>\n<p>These entries show every call made to the LLM. Each LLM trace includes:</p>\n<ul>\n<li><strong>Prompt</strong> - The full prompt sent to the LLM (combination of global prompt + node-specific prompt)</li>\n<li><strong>Conversation history</strong> - The entire conversation up to that point  </li>\n<li><strong>Available tools</strong> - All tools the LLM can access for that node</li>\n<li><strong>Response</strong> - What the LLM generated</li>\n</ul>\n<p><img src=\"/agentstudio-docs/images/llm-response.png\" alt=\"LLM Response\"></p>\n<p><img src=\"/agentstudio-docs/images/conversation-history.png\" alt=\"Conversation History\"></p>\n<h2 id=\"tools-in-traces\">Tools in Traces</h2>\n<p>Each LLM call shows the tools available to the agent at that moment. These fall into three categories:</p>\n<h3 id=\"system-tools-default\">System Tools (Default)</h3>\n<p>Every node includes these by default:</p>\n<ul>\n<li><strong>Safe Calculator</strong> — Helps LLMs perform math accurately</li>\n<li><strong>Get Current Time</strong> — Retrieves current time</li>\n<li><strong>Convert Time</strong> — Converts time to a specific timezone</li>\n</ul>\n<h3 id=\"pathway-tools\">Pathway Tools</h3>\n<p>These correspond to the pathways (node transitions) you&#39;ve configured. For example, if your node has pathways to &quot;End Call&quot; and &quot;Move to Summary,&quot; you&#39;ll see those as available tools.</p>\n<p>The tool descriptions shown in traces match the descriptions you set in your pathway configuration.</p>\n<h3 id=\"custom-tools\">Custom Tools</h3>\n<p>Any external tools you&#39;ve attached to the node (e.g. any custom tools you created for API endpoints for booking, order submission, etc.) will appear here. See the <a href=\"/studio/docs/voice-agent/tools/http-api\">custom tools documentation</a>.</p>\n<h2 id=\"tool-calls-in-traces\">Tool Calls in Traces</h2>\n<p>When the LLM decides to call a tool, you&#39;ll see:</p>\n<ol>\n<li><strong>Tool call request</strong> — The tool name and parameters sent</li>\n<li><strong>Tool response</strong> — The result returned (e.g., <code>{&quot;status&quot;: &quot;ok&quot;}</code>)</li>\n</ol>\n<p>The tool call and response also appear in subsequent conversation history, so the LLM knows the outcome.</p>\n<p><strong>Example flow:</strong></p>\n<p><strong>User:</strong> &quot;Can you book me a table for tomorrow at 7pm?&quot;</p>\n<p><strong>LLM calls:</strong> book_table(date: &quot;tomorrow&quot;, time: &quot;7pm&quot;, party_size: 2)</p>\n<p><strong>Tool response:</strong> <code>{&quot;status&quot;: &quot;done&quot;, &quot;confirmation&quot;: &quot;Table booked&quot;}</code></p>\n<p><strong>Agent:</strong> &quot;I&#39;ve booked your table for tomorrow at 7pm.&quot;</p>\n<h2 id=\"how-prompts-are-combined\">How Prompts Are Combined</h2>\n<p>The prompt sent to the LLM is a combination of:</p>\n<ol>\n<li><strong>Global Prompt</strong> — Defined in your Global Node settings</li>\n<li><strong>Node Prompt</strong> — Defined in the specific node being executed</li>\n</ol>\n<p>If you disable the Global Node for a particular node, only the node-specific prompt will be used.</p>\n<h2 id=\"key-concept-conversation-history\">Key Concept: Conversation History</h2>\n<p>Every time an LLM call is made, the <strong>entire conversation history up to that point</strong> is passed to the LLM. This means:</p>\n<ul>\n<li>The LLM always has full context</li>\n<li>You can see exactly what context the LLM had when it made a decision</li>\n<li>Useful for debugging unexpected responses</li>\n</ul>\n<h2 id=\"setting-up-langfuse-tracing\">Setting Up Langfuse Tracing</h2>\n<p><a href=\"https://langfuse.com/docs\">Langfuse</a> is an open-source LLM observability platform — it stores and visualizes traces (prompts, responses, tool calls) so you can debug and iterate on LLM-powered applications outside of PayInt Voice Studio&#39;s own trace viewer.</p>\n<p>We provide seamless integration with Langfuse for tracing if you want to use your own account. This enables you to use the <a href=\"https://langfuse.com/docs/prompt-management/features/playground\">playground feature of Langfuse</a>. This works on both managed and self-hosted PayInt Voice Studio deployments.</p>\n<p><strong>Setup steps:</strong></p>\n<ol>\n<li>Sign up at <a href=\"https://langfuse.com\">Langfuse</a> and create API credentials</li>\n<li>In the PayInt Voice Studio UI, go to <strong>Platform Settings</strong> (<code>/settings</code>) and enter your Langfuse host, public key, and secret key</li>\n<li>Click <strong>Save</strong></li>\n</ol>\n<p>Once enabled, traces will be available for every completed call in PayInt Voice Studio.</p>\n<h3 id=\"trace-visibility\">Trace visibility</h3>\n<p>Traces sent to your own Langfuse project are private by default — opening a trace link requires access to that project. <strong>Make traces publicly viewable</strong> in <strong>Platform Settings</strong> turns that off for your project only.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>A public trace is readable by anyone holding its URL, with no Langfuse login, and it exposes the full call transcript, prompts, and tool payloads. Only enable it if you intend to share trace links outside your Langfuse project.</p>\n</div>\n\n\n\n<p><code>LANGFUSE_TRACES_PUBLIC</code> is the equivalent switch for the Langfuse project configured by environment variables. It has no effect on organizations that have connected their own Langfuse project — those follow the setting above.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>For self-hosted deployments, you can also configure Langfuse via <a href=\"/studio/docs/developer/environment-variables#tracing-langfuse\">environment variables</a> (<code>LANGFUSE_SECRET_KEY</code>, <code>LANGFUSE_PUBLIC_KEY</code>, <code>LANGFUSE_HOST</code>, <code>LANGFUSE_PROJECT_ID</code>) as a default for all organizations. Tracing activates automatically once credentials are available — no separate enable flag is required. Per-organization credentials set in the UI take precedence over environment variables.</p>\n</div>\n\n\n\n<h2 id=\"quick-reference\">Quick Reference</h2>\n<table>\n<thead>\n<tr>\n<th>Trace Type</th>\n<th>What It Shows</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>STT</td>\n<td>User speech transcribed to text</td>\n</tr>\n<tr>\n<td>LLM</td>\n<td>Prompt + conversation + tools + response</td>\n</tr>\n<tr>\n<td>Tool Call</td>\n<td>Tool invocation and response</td>\n</tr>\n</tbody></table>\n<h2 id=\"tips-for-using-traces\">Tips for Using Traces</h2>\n<ul>\n<li><strong>Debug unexpected responses</strong> — Check the LLM trace to see what prompt and context the model received</li>\n<li><strong>Verify tool calls</strong> — Confirm tools are being triggered with correct parameters</li>\n<li><strong>Refine prompts</strong> — Use traces to see how your prompt instructions affect LLM behavior</li>\n<li><strong>Check transcription accuracy</strong> — Review STT entries if the agent misunderstands users</li>\n</ul>\n"},"voice-agent/introduction":{"title":"Voice Agent Builder","description":"PayInt Voice Studio's voice agent builder lets you design a conversation as a graph of nodes and pathways. Learn the graph model, the available node types, and where to start.","html":"<p><img src=\"/agentstudio-docs/images/create-a-voice-agent.png\" alt=\"Create a Voice Agent\"></p>\n<p>The Voice Agent Builder is a visual, graph-based editor for designing what your voice agent says and does during a call. Instead of writing one giant prompt, you break the conversation into <strong>nodes</strong> (stages of the conversation) connected by <strong>pathways</strong> (the routes the LLM can take between stages, based on how the conversation is going).</p>\n<p>You can create a new agent from the <a href=\"/studio/workflow\">dashboard</a>. Describe whether you need an &quot;Inbound&quot; or &quot;Outbound&quot; agent and your use case — this is sent to an LLM to generate a starting workflow with default prompts and pathways, which you can then edit.</p>\n<h2 id=\"the-graph-model\">The graph model</h2>\n<p>A workflow is a directed graph:</p>\n<ul>\n<li><strong>Nodes</strong> represent a stage of the conversation or an action (e.g. greet the caller, collect an address, transfer the call).</li>\n<li><strong>Pathways (edges)</strong> connect nodes and represent the routes the LLM can take depending on what the caller says. The LLM decides at runtime which pathway to follow.</li>\n</ul>\n<p>A workflow should have only one <strong>Start Call</strong> node and typically ends at an <strong>End Call</strong> node, with <strong>Agent</strong> nodes and other node types in between. See <a href=\"/studio/docs/voice-agent/editing-a-workflow\">Editing a Workflow</a> for how nodes, pathways, and node-level toggles fit together.</p>\n<h2 id=\"node-types\">Node types</h2>\n<table>\n<thead>\n<tr>\n<th>Node</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"/studio/docs/voice-agent/start-call\"><strong>Start Call</strong></a></td>\n<td>Starts the call and configures the agent&#39;s greeting. Should have only one per workflow.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/agent\"><strong>Agent</strong></a></td>\n<td>Holds the prompt that drives conversation at a given stage; connects to other nodes via pathways.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/global\"><strong>Global</strong></a></td>\n<td>Common instructions (tone, objection handling) prepended to every node with &quot;Add Global Prompt&quot; enabled.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/qa\"><strong>QA</strong></a></td>\n<td>Runs automated post-call quality analysis against criteria you define.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/api-trigger\"><strong>API Trigger</strong></a></td>\n<td>Exposes an endpoint so external systems (n8n, Zapier, your backend) can start outbound calls.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/end-call\"><strong>End Call</strong></a></td>\n<td>Configures the agent&#39;s final message before the call is terminated. A workflow can have multiple End Call nodes, each with a distinct Prompt.</td>\n</tr>\n<tr>\n<td><a href=\"/studio/docs/voice-agent/webhook\"><strong>Webhook</strong></a></td>\n<td>Sends call results to an external system (CRM, Zapier, n8n) when a run ends.</td>\n</tr>\n</tbody></table>\n<p>Beyond nodes, you can extend an agent with:</p>\n<ul>\n<li><a href=\"/studio/docs/voice-agent/tools/introduction\"><strong>Tools</strong></a> — let the LLM call external APIs, transfer calls, or invoke MCP servers mid-conversation.</li>\n<li><a href=\"/studio/docs/voice-agent/knowledge-base\"><strong>Knowledge Base</strong></a> — attach documents the agent can reference during a call.</li>\n<li><a href=\"/studio/docs/voice-agent/pre-call-data-fetch\"><strong>Pre-Call Data Fetch</strong></a> — enrich context with an HTTP call before the agent starts speaking.</li>\n<li><a href=\"/studio/docs/voice-agent/pre-recorded-audio\"><strong>Pre-recorded Audio</strong></a> — mix in real recordings alongside LLM-generated speech.</li>\n<li><a href=\"/studio/docs/voice-agent/template-variables\"><strong>Template Variables</strong></a> — reference <code>initial_context</code> and <code>gathered_context</code> values in prompts and payloads.</li>\n</ul>\n<h2 id=\"where-to-start\">Where to start</h2>\n<ol>\n<li><strong>New to PayInt Voice Studio?</strong> Follow <a href=\"/studio/docs/voice-agent/editing-a-workflow\">Editing a Workflow</a> to learn the fundamentals of nodes and pathways.</li>\n<li><strong>Want to test immediately?</strong> Use a <a href=\"/studio/docs/core-concepts/calls-and-runs#web-calls\">Web Call</a> from the agent editor — no telephony setup required.</li>\n<li><strong>Ready to go live?</strong> Configure a <a href=\"/studio/docs/integrations/telephony/overview\">telephony provider</a> and add an <a href=\"/studio/docs/voice-agent/api-trigger\">API Trigger</a> or set up inbound routing.</li>\n<li><strong>Need the call to hit your systems?</strong> Add a <a href=\"/studio/docs/voice-agent/webhook\">Webhook</a> node to sync results to your CRM or automation tool.</li>\n<li><strong>Want to embed the agent on a website?</strong> See <a href=\"/studio/docs/voice-agent/add-to-website\">Add to Website</a>.</li>\n</ol>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li><a href=\"/studio/docs/core-concepts/workflows-and-agents\">Core Concepts: Workflows and Agents</a></li>\n<li><a href=\"/studio/docs/core-concepts/context-and-variables\">Core Concepts: Context &amp; Variables</a></li>\n<li><a href=\"/studio/docs/voice-agent/editing-a-workflow\">Editing a Workflow</a></li>\n</ul>\n"},"voice-agent/editing-a-workflow":{"title":"Editing a Workflow","description":"Learn the fundamentals of building workflows with nodes, pathways, and conversation flow in the voice agent builder.","html":"<h2 id=\"overview\">Overview</h2>\n<p>A workflow is a graph of connected nodes representing conversation stages, with pathways defining agent movement between stages.</p>\n<h2 id=\"global-node\">Global Node</h2>\n<p>Contains instructions applicable across the entire call:</p>\n<ul>\n<li>Company context</li>\n<li>Overall agent goal</li>\n<li>Tone and personality</li>\n<li>Common objection handling</li>\n</ul>\n<h2 id=\"node-level-toggles\">Node-Level Toggles</h2>\n<h3 id=\"add-global-prompt\">Add Global Prompt</h3>\n<ul>\n<li>Recommended to inject <a href=\"/studio/docs/voice-agent/global\">Global Node</a> instructions into most nodes</li>\n</ul>\n<h3 id=\"allow-interruption\">Allow Interruption</h3>\n<ul>\n<li><strong>Default &quot;On&quot;</strong>: Bot stops and responds to user interruption</li>\n<li><strong>&quot;Off&quot;</strong>: Bot continues speaking, ignoring user input</li>\n</ul>\n<h3 id=\"detect-voicemail\">Detect Voicemail</h3>\n<ul>\n<li>Available only in Start Node</li>\n<li>Automatically disconnects if voicemail detected</li>\n</ul>\n<h2 id=\"variable-extraction\">Variable Extraction</h2>\n<p>Supported in Agent and End Call Nodes:</p>\n<ul>\n<li>Extract structured variables</li>\n<li>Define name, type, and description</li>\n<li>Examples: customer name, budget, interest</li>\n</ul>\n<h2 id=\"webhook-node\"><a href=\"/studio/docs/voice-agent/webhook\">Webhook Node</a></h2>\n<ul>\n<li>Executed at call end</li>\n<li>Sends payload to specific endpoint</li>\n<li>Use for:<ul>\n<li>Storing call data</li>\n<li>Triggering automation</li>\n<li>Syncing with CRMs</li>\n</ul>\n</li>\n</ul>\n<h2 id=\"editing-workflows\">Editing Workflows</h2>\n<ul>\n<li>Selectively delete nodes/pathways</li>\n<li>Completely reset workflow</li>\n</ul>\n<h2 id=\"pathways-and-tool-calls\">Pathways and Tool Calls</h2>\n<ul>\n<li>Define agent node movement</li>\n<li>Use <a href=\"/studio/docs/voice-agent/tools/http-api\">tool calls</a> to transition between nodes</li>\n<li><strong>&quot;tool&quot;</strong> is a reserved keyword for function calls and actions</li>\n</ul>\n"},"voice-agent/pre-recorded-audio":{"title":"Pre-recorded Audio","description":"Build hybrid voice agents that combine pre-recorded audio with dynamic text generation for lower latency, reduced TTS costs, and natural-sounding conversations.","html":"<p>Custom recordings allow you to build <strong>hybrid voice agents</strong> that use your own pre-recorded audio for key parts of the conversation, while falling back to LLM-generated speech (via a cloned voice) for dynamic responses. This gives you the best of both worlds — the emotional depth of real human speech and the flexibility of AI-generated dialogue.</p>\n<h2 id=\"why-use-custom-recordings\">Why use custom recordings?</h2>\n<ul>\n<li><strong>Reduced TTS cost</strong> — Pre-recorded audio is played directly, so you are not charged for TTS synthesis on those segments.</li>\n<li><strong>Emotional variance</strong> — Real recordings carry natural intonation and emotion that TTS cannot fully replicate.</li>\n<li><strong>Lower latency</strong> — Playing a pre-recorded clip is faster than synthesizing text at runtime.</li>\n</ul>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A TTS provider that supports <strong>voice cloning</strong> (e.g., Cartesia, ElevenLabs, or Deepgram).</li>\n<li>An API key for your chosen TTS provider, configured in <a href=\"/studio/docs/configurations/voice\">Voice settings</a>.</li>\n</ul>\n<h2 id=\"step-1-clone-your-voice\">Step 1: Clone your voice</h2>\n<p>Clone your voice with your TTS provider so that dynamically generated speech sounds similar to your recordings. For example, with Cartesia:</p>\n<ol>\n<li>Go to Cartesia and navigate to <strong>Instant Clone</strong>.</li>\n<li>Record a short audio clip (up to 10 seconds) of your voice.</li>\n<li>Give the clone a name and select your language.</li>\n<li>Copy the <strong>Voice ID</strong> — you will need it in the next step.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>You can use any TTS provider that supports voice cloning. The steps will vary by provider, but the key output is always a <strong>Voice ID</strong> tied to your cloned voice.</p>\n</div>\n\n\n\n<h2 id=\"step-2-configure-the-cloned-voice-in-payint-voice-studio\">Step 2: Configure the cloned voice in PayInt Voice Studio</h2>\n<ol>\n<li>Go to your agent&#39;s <strong>Model Configuration</strong> in the PayInt Voice Studio dashboard.</li>\n<li>Under voice settings, select <strong>Add Voice ID manually</strong>.</li>\n<li>Paste the Voice ID from your cloned voice.</li>\n<li>Make sure the <strong>provider</strong> matches where you cloned your voice (e.g., Cartesia).</li>\n<li>Enter the provider&#39;s API key if you haven&#39;t already.</li>\n<li>Save the configuration.</li>\n</ol>\n<h2 id=\"step-3-upload-recordings\">Step 3: Upload recordings</h2>\n<p>Navigate to the <strong>Recordings</strong> page in the PayInt Voice Studio dashboard. Recordings are shared across all agents in your organization. You can either upload pre-recorded audio files or record directly in the browser.</p>\n<p>For each recording:</p>\n<ol>\n<li>Click <strong>Upload Recording</strong>.</li>\n<li>Choose an audio file or click <strong>Record</strong> to record in the browser.</li>\n<li>Verify the transcription is correct — edit it if needed.</li>\n<li>Click <strong>Upload</strong>.</li>\n</ol>\n<p>You can rename a recording&#39;s ID at any time by clicking the edit icon next to it in the recordings list.</p>\n<h2 id=\"step-4-build-the-workflow\">Step 4: Build the workflow</h2>\n<p>Open your agent&#39;s workflow and write the conversation flow in natural language. To insert a recording, type <strong><code>@</code></strong> in the prompt editor — this will show a list of all available recordings in your organization.</p>\n<p>For any user question that falls outside your recordings, the agent automatically generates a dynamic response using the LLM, which is then synthesized using your cloned voice via TTS.</p>\n<h2 id=\"tips-for-best-results\">Tips for best results</h2>\n<ul>\n<li><strong>Record in a quiet environment</strong> to improve audio quality and consistency with the cloned voice.</li>\n<li><strong>Use pro cloning services</strong> (when available) and provide more sample audio for a higher-quality voice clone.</li>\n<li><strong>Keep recordings concise</strong> — short, focused clips work best for specific conversation moments.</li>\n<li><strong>Review call recordings</strong> after testing to identify where the transition between pre-recorded and dynamic audio can be improved.</li>\n</ul>\n"},"voice-agent/template-variables":{"title":"Template Variables","description":"You can use Template Variables in your prompts for your Agent nodes, or when constructing the payload for the Webhook Node","html":"<h3 id=\"template-rendering\">Template Rendering</h3>\n<p>You reference template variables with <code>{{double_brace}}</code> syntax. The data comes from <a href=\"/studio/docs/core-concepts/context-and-variables#initial_context\"><code>initial_context</code></a> — set via the <a href=\"/studio/docs/voice-agent/api-trigger\">API Trigger</a>, a <a href=\"/studio/docs/core-concepts/campaigns\">campaign</a> sheet, or a <a href=\"/studio/docs/voice-agent/pre-call-data-fetch\">Pre-Call Data Fetch</a> that enriches the context when the call starts — and, in Webhook payloads only, from <a href=\"/studio/docs/core-concepts/context-and-variables#gathered_context\"><code>gathered_context</code></a> (variables extracted during the call).</p>\n<p><strong>The syntax depends on where you use it:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Where</th>\n<th><code>initial_context</code></th>\n<th><code>gathered_context</code></th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Agent node prompts</td>\n<td><code>{{field_name}}</code> (referenced directly)</td>\n<td>Not available</td>\n</tr>\n<tr>\n<td>Webhook Node payloads</td>\n<td><code>{{initial_context.field_name}}</code></td>\n<td><code>{{gathered_context.field_name}}</code></td>\n</tr>\n</tbody></table>\n<h4 id=\"agent-node-prompts\">Agent node prompts</h4>\n<p>In an Agent node prompt, reference each <code>initial_context</code> field <strong>directly by name</strong>. Nested values are supported with dot notation.</p>\n<p>Example: if the initial context is</p>\n<pre><code class=\"language-json\">{\n    &quot;initial_context&quot;: {\n        &quot;user&quot;: {\n            &quot;name&quot;: &quot;John&quot;\n        }\n    }\n}\n</code></pre>\n<p>write your prompt to access the user&#39;s name as below:</p>\n<p>Prompt: <code>You are Alice, who is talking to {{user.name}}.</code></p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Variables extracted during the call (<code>gathered_context</code>) are <strong>not</strong> available in Agent prompts — a prompt can only reference <code>initial_context</code> fields. To act on extracted data, send it to a <a href=\"/studio/docs/voice-agent/webhook\">Webhook Node</a>.</p>\n</div>\n\n\n\n<h4 id=\"webhook-node-payloads\">Webhook Node payloads</h4>\n<p>When constructing a <a href=\"/studio/docs/voice-agent/webhook\">Webhook Node</a> payload, the context objects are nested under their names, so reference them with the <code>initial_context.</code> and <code>gathered_context.</code> prefixes:</p>\n<p>Payload value: <code>{{initial_context.user.name}}</code> or <code>{{gathered_context.call_disposition}}</code></p>\n<h3 id=\"using-template-variables-for-testing\">Using Template Variables for Testing</h3>\n<p>Template variables defined in your workflow <strong>Settings &gt; Context Variables</strong> are included in test calls (both web and phone) made from the workflow editor. This is useful for simulating data that would normally come from telephony or an API trigger.</p>\n<p>&lt;img src=&quot;/agentstudio-docs/images/template-variables.png&quot; alt=&quot;Template Variables panel in workflow Settings, showing a customer_name variable and fields to add new key/value pairs&quot; style={{border: &quot;1px solid #d1d5db&quot;, borderRadius: &quot;8px&quot;, maxWidth: &quot;100%&quot;}} /&gt;</p>\n<p>For example, you can set <code>caller_number</code> and <code>called_number</code> as context variables to test <a href=\"/studio/docs/voice-agent/pre-call-data-fetch#testing-with-test-calls\">Pre-Call Data Fetch</a> without needing a real inbound call.</p>\n<p>Browser voice tests are treated as inbound calls by default. To simulate an outbound call with web, set the <code>direction</code> template-context variable to <code>outbound</code>.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>These context variables are only used during test calls from the workflow editor. On production inbound calls and campaign outbound calls, the actual telephony data is used and these values are ignored.</p>\n</div>\n\n\n\n<h3 id=\"nodes\">Nodes</h3>\n<p>PayInt Voice Studio Voice Agents are composed of various nodes. These nodes can provide instructions to the voice agent, help you setup a <a href=\"/studio/docs/voice-agent/api-trigger\">trigger</a> where you can trigger the voice agent to call someone, or help you setup a <a href=\"/studio/docs/voice-agent/webhook\">webhook</a>, where you can update the results of the call in your CRM or trigger a downstream workflow in n8n. In the next steps, we will be documenting the nodes that you can use in building the voice agent.</p>\n"},"voice-agent/pre-call-data-fetch":{"title":"Pre-Call Data Fetch","description":"Fetch customer data from your CRM or ERP before the call starts, so your voice agent can greet callers by name and reference their account details.","html":"<p>Pre-Call Data Fetch allows you to enrich the call context with external data before the voice agent starts speaking. Configure it on the <a href=\"/studio/docs/voice-agent/start-call\"><strong>Start Call</strong></a> node for every call, inbound calls only, or outbound calls only. While the response is loading, the caller hears a ring-back tone. Once the data arrives, it is merged into the call&#39;s <a href=\"/studio/docs/core-concepts/context-and-variables#initial_context\">initial context</a> and becomes available as template variables in your prompts and greetings.</p>\n<h2 id=\"how-it-works\">How It Works</h2>\n<ol>\n<li>A call arrives (inbound) or is initiated (outbound).</li>\n<li>PayInt Voice Studio sends a <strong>POST</strong> request to your configured endpoint with a standardized payload.</li>\n<li>The caller hears a ring-back tone while waiting for the response.</li>\n<li>Your API responds with a JSON object containing an <code>initial_context</code> object.</li>\n<li>The variables are merged into the call&#39;s initial context.</li>\n<li>The voice agent starts with full access to the fetched data via <code>{{variable_name}}</code> syntax.</li>\n</ol>\n<h2 id=\"configuration\">Configuration</h2>\n<p>Open the <a href=\"/studio/docs/voice-agent/start-call\"><strong>Start Call</strong></a> node editor and expand <strong>Advanced Settings</strong>. Choose a <strong>Pre-Call Data Fetch</strong> mode and configure the endpoint when the mode is not disabled:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Pre-Call Data Fetch</strong></td>\n<td><strong>Disabled</strong>, <strong>Always</strong>, <strong>Inbound calls only</strong>, or <strong>Outbound calls only</strong>. Calls without an inbound or outbound direction run the fetch only in <strong>Always</strong> mode.</td>\n</tr>\n<tr>\n<td><strong>Endpoint URL</strong></td>\n<td>The URL PayInt Voice Studio will send the POST request to.</td>\n</tr>\n<tr>\n<td><strong>Authentication</strong></td>\n<td>Optional credential for authenticating the request. Supports API key, bearer token, basic auth, and custom header.</td>\n</tr>\n</tbody></table>\n<h2 id=\"request-format\">Request Format</h2>\n<p>PayInt Voice Studio sends a <code>POST</code> request with the following JSON payload:</p>\n<pre><code class=\"language-json\">{\n  &quot;event&quot;: &quot;call_inbound&quot;,\n  &quot;call_inbound&quot;: {\n    &quot;agent_id&quot;: 123,\n    &quot;from_number&quot;: &quot;+12137771234&quot;,\n    &quot;to_number&quot;: &quot;+12137771235&quot;\n  }\n}\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>event</code></td>\n<td>Always <code>&quot;call_inbound&quot;</code>.</td>\n</tr>\n<tr>\n<td><code>call_inbound.agent_id</code></td>\n<td>The workflow (agent) ID.</td>\n</tr>\n<tr>\n<td><code>call_inbound.from_number</code></td>\n<td>The caller&#39;s phone number (<code>caller_number</code> from initial context).</td>\n</tr>\n<tr>\n<td><code>call_inbound.to_number</code></td>\n<td>The called phone number (<code>called_number</code> from initial context).</td>\n</tr>\n</tbody></table>\n<p>The <code>Content-Type</code> header is set to <code>application/json</code>. If you configured a credential, the corresponding authentication header is included.</p>\n<h2 id=\"expected-response-format\">Expected Response Format</h2>\n<p>Your API should return a <strong>JSON object</strong> with a <code>2xx</code> status code. The variables to inject into the call context should be placed inside the <code>initial_context</code> key:</p>\n<pre><code class=\"language-json\">{\n  &quot;call_inbound&quot;: {\n    &quot;initial_context&quot;: {\n      &quot;customer_name&quot;: &quot;Jane Doe&quot;,\n      &quot;account_status&quot;: &quot;active&quot;,\n      &quot;loyalty_tier&quot;: &quot;gold&quot;,\n      &quot;open_tickets&quot;: 2\n    }\n  }\n}\n</code></pre>\n<p>You can also place <code>initial_context</code> at the top level:</p>\n<pre><code class=\"language-json\">{\n  &quot;initial_context&quot;: {\n    &quot;customer_name&quot;: &quot;Jane Doe&quot;,\n    &quot;account_status&quot;: &quot;active&quot;\n  }\n}\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The legacy <code>dynamic_variables</code> key is still accepted as a drop-in alias for <code>initial_context</code>, so existing integrations keep working without any changes. Use <code>initial_context</code> for new integrations. If a response contains both keys, <code>initial_context</code> takes precedence.</p>\n</div>\n\n\n\n<p>After the response is received, you can reference these values anywhere template variables are supported:</p>\n<ul>\n<li><strong>Greeting</strong>: <code>Hello {{customer_name}}, thank you for calling!</code></li>\n<li><strong>Prompt</strong>: <code>The customer is a {{loyalty_tier}} member with {{open_tickets}} open support tickets.</code></li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If the response is not a valid JSON object, does not contain <code>initial_context</code> (or the legacy <code>dynamic_variables</code>), or the request fails or times out, the call proceeds normally without the additional context. The pre-call fetch never blocks or fails a call.</p>\n</div>\n\n\n\n<h2 id=\"nested-variables\">Nested Variables</h2>\n<p>If your <code>initial_context</code> contains nested objects, you can access them using dot notation:</p>\n<pre><code class=\"language-json\">{\n  &quot;call_inbound&quot;: {\n    &quot;initial_context&quot;: {\n      &quot;customer&quot;: {\n        &quot;name&quot;: &quot;Jane Doe&quot;,\n        &quot;address&quot;: {\n          &quot;city&quot;: &quot;Los Angeles&quot;\n        }\n      }\n    }\n  }\n}\n</code></pre>\n<p>Access in prompts as <code>{{customer.name}}</code> and <code>{{customer.address.city}}</code>.</p>\n<h2 id=\"timeout\">Timeout</h2>\n<p>The request has a <strong>10-second timeout</strong>. If your API does not respond within this window, the call proceeds without the fetched data. Design your endpoint to respond as quickly as possible to minimize the ring-back tone duration.</p>\n<h2 id=\"testing-with-test-calls\">Testing with Test Calls</h2>\n<p>When a real phone call comes in, the <code>caller_number</code> and <code>called_number</code> context variables are automatically set by the telephony provider and included in the pre-call data fetch request as <code>from_number</code> and <code>to_number</code>. However, when you make a test call — either a <strong>web call</strong> (WebRTC) or a <strong>phone test call</strong> from the workflow editor — these variables are not available by default.</p>\n<p>To simulate telephony data during testing:</p>\n<ol>\n<li>Open your workflow and go to <strong>Settings</strong>.</li>\n<li>Under <strong>Context Variables</strong>, add the following variables:<ul>\n<li><code>caller_number</code> — set to a phone number you want to simulate as the caller (e.g., <code>+12137771234</code>).</li>\n<li><code>called_number</code> — set to the number that would be dialed (e.g., <code>+12137771235</code>).</li>\n</ul>\n</li>\n<li>Save the settings.</li>\n</ol>\n<p>Now when you make a test call (web or phone), these values will be sent in the pre-call data fetch request to your endpoint, allowing you to test the full flow as if a real inbound call were coming in.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>These context variables are only used during test calls from the workflow editor. On production inbound calls and campaign outbound calls, the actual telephony data is used and these values are ignored.</p>\n</div>\n\n\n\n<h2 id=\"example-integration\">Example Integration</h2>\n<p>A simple Node.js endpoint that looks up a customer by phone number:</p>\n<pre><code class=\"language-javascript\">app.post(&quot;/dograh/pre-call&quot;, async (req, res) =&gt; {\n  const { call_inbound } = req.body;\n\n  const customer = await db.customers.findOne({\n    phone: call_inbound.from_number,\n  });\n\n  if (!customer) {\n    return res.json({});\n  }\n\n  res.json({\n    call_inbound: {\n      initial_context: {\n        customer_name: customer.name,\n        account_status: customer.status,\n        loyalty_tier: customer.tier,\n      },\n    },\n  });\n});\n</code></pre>\n"},"voice-agent/tools/introduction":{"title":"Tools","description":"Extend your voice agent's capabilities by giving it tools to perform actions during live conversations.","html":"<p>Tools let your AI agent take actions during a conversation — transfer calls, end calls, call external APIs, or invoke remote MCP servers — based on the context of the conversation and your prompt instructions.</p>\n<p>When a tool is attached to a workflow node, the LLM decides <strong>when</strong> to invoke it and <strong>what parameters</strong> to pass, based on the user&#39;s spoken intent and your node-level instructions.</p>\n<h2 id=\"tool-types\">Tool Types</h2>\n<p>PayInt Voice Studio provides two categories of tools:</p>\n<h3 id=\"built-in-tools\">Built-in Tools</h3>\n<p>Pre-configured tools that handle common telephony operations out of the box:</p>\n<ul>\n<li><a href=\"/studio/docs/voice-agent/tools/call-transfer\"><strong>Call Transfer</strong></a> — Transfer the active call to a phone number or SIP endpoint</li>\n<li><a href=\"/studio/docs/voice-agent/tools/end-call\"><strong>End Call</strong></a> — Terminate the call when the conversation is complete</li>\n</ul>\n<h3 id=\"custom-tools\">Custom Tools</h3>\n<p>Tools you define to integrate with any external system:</p>\n<ul>\n<li><a href=\"/studio/docs/voice-agent/tools/http-api\"><strong>HTTP API</strong></a> — Call any REST API endpoint during a conversation (e.g., CRM updates, data lookups, triggering automations)</li>\n<li><a href=\"/studio/docs/voice-agent/tools/mcp-tool\"><strong>MCP Tool</strong></a> — Connect an external MCP server and expose its remote tools to the LLM during a conversation</li>\n</ul>\n<h2 id=\"how-tools-work\">How Tools Work</h2>\n<ol>\n<li>You <strong>define</strong> a tool with a name, description, and parameters</li>\n<li>You <strong>attach</strong> the tool to one or more workflow nodes</li>\n<li>During a call, the LLM reads your node prompt, the tool description, and the caller&#39;s intent to decide whether to invoke the tool</li>\n<li>The tool executes and returns a result that the agent can use to continue the conversation</li>\n</ol>\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li><strong>Attach only relevant tools to each node</strong> — fewer tools means more reliable invocations</li>\n<li><strong>Write clear tool descriptions</strong> — the LLM uses these to decide when to call the tool</li>\n<li><strong>Guide the LLM in your node prompt</strong> — explicitly describe when a tool should be used</li>\n<li><strong>Test tool behavior</strong> — verify your agent invokes tools at the right moments using web or phone calls</li>\n</ul>\n"},"voice-agent/tools/call-transfer":{"title":"Call Transfer","description":"Transfer live telephony calls using static destinations, ordered context mappings, or dynamic resolution.","html":"<p>The Call Transfer tool lets your AI agent transfer an active telephony call to a phone number or SIP endpoint. You can configure a fixed destination, select one with ordered context rules, use a template from call context, or resolve the destination at transfer time by calling your HTTP endpoint.</p>\n<p>Use this tool when the caller should speak to a human operator, department, escalation queue, support queue, or another phone system.</p>\n<h2 id=\"supported-providers\">Supported providers</h2>\n<p>Call transfer is available for telephony calls using Twilio, Telnyx, or Asterisk ARI providers.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Web calls do not support call transfer.</p>\n</div>\n\n\n\n<h2 id=\"how-call-transfer-works\">How call transfer works</h2>\n<p>When the LLM decides a transfer is needed, it calls the Call Transfer tool. PayInt Voice Studio resolves the destination, validates the transfer setup, and then starts the provider transfer.</p>\n<p>For a successful transfer:</p>\n<ol>\n<li>The agent calls the Call Transfer tool.</li>\n<li>PayInt Voice Studio resolves the destination from static configuration, ordered context mappings, or a dynamic HTTP resolver.</li>\n<li>PayInt Voice Studio optionally plays a pre-transfer message.</li>\n<li>PayInt Voice Studio starts dialing the destination through the telephony provider.</li>\n<li>The caller hears hold music while the destination leg is connecting.</li>\n<li>When the destination answers, PayInt Voice Studio connects the caller and ends the AI agent&#39;s involvement.</li>\n</ol>\n<p>The transfer is a blind transfer. PayInt Voice Studio does not currently pass conversation context to the receiving human as part of the transfer.</p>\n<h2 id=\"destination-source\">Destination source</h2>\n<p>You must choose one destination source for each Call Transfer tool.</p>\n<h3 id=\"static-template\">Static / Template</h3>\n<p>Use this when the transfer destination is already known before the transfer happens.</p>\n<p>Static destinations can be:</p>\n<ul>\n<li>A fixed E.164 phone number, such as <code>+1234567890</code></li>\n<li>A SIP endpoint, such as <code>PJSIP/sales-queue</code></li>\n<li>A template value from context, such as <code>{{initial_context.transfer_destination}}</code></li>\n</ul>\n<p>This is the simplest option for fixed support lines, fixed departments, or cases where the destination is provided before the call starts.</p>\n<h3 id=\"context-mapping\">Context Mapping</h3>\n<p>Use this when a finite set of context values should select the transfer destination. You can add multiple rules and order them. PayInt Voice Studio evaluates the rules from top to bottom and uses the first route whose value matches, ignoring case and surrounding whitespace.</p>\n<p>For each rule, enter a context field and one or more value-to-destination mappings. An unprefixed field such as <code>department</code> reads <code>gathered_context.department</code> first and falls back to <code>initial_context.department</code> when the gathered value is absent. You can use <code>gathered_context.department</code> or <code>initial_context.department</code> when you want to read only one context.</p>\n<p>Mapped and fallback destinations can be:</p>\n<ul>\n<li>A VICIdial in-group, such as <code>sales</code></li>\n<li>A SIP endpoint, such as <code>PJSIP/1001</code></li>\n<li>An E.164 PSTN number, such as <code>+1234567890</code></li>\n<li>A context template, such as <code>{{initial_context.transfer_destination}}</code></li>\n</ul>\n<p>PayInt Voice Studio performs final variable extraction before evaluating context mappings. If no rule matches, it uses the optional fallback destination. If there is no fallback, or a selected destination template resolves to an empty value, the transfer fails before dialing and the agent can continue the call.</p>\n<h3 id=\"dynamic-http-resolver\">Dynamic HTTP Resolver</h3>\n<p>Use this when the destination must be decided during the conversation.</p>\n<p>PayInt Voice Studio sends a <code>POST</code> request to your resolver endpoint before starting the transfer. Your endpoint returns the destination and may return a custom message to play before dialing.</p>\n<p>Common use cases:</p>\n<ul>\n<li>Route by region, timezone, or language</li>\n<li>Look up the right account team in your customer system</li>\n<li>Route by customer tier or account status</li>\n<li>Route by issue type, language, or department</li>\n<li>Use business logic from your backend instead of hardcoding numbers in PayInt Voice Studio</li>\n</ul>\n<h2 id=\"dynamic-resolver-request\">Dynamic resolver request</h2>\n<p>The resolver request body is a flat JSON object.</p>\n<p>PayInt Voice Studio builds it from:</p>\n<ul>\n<li><strong>LLM parameters</strong>: values the agent extracts from the conversation when it calls the transfer tool</li>\n<li><strong>Preset parameters</strong>: values PayInt Voice Studio injects from fixed values or templates such as <code>{{initial_context.account_id}}</code> or <code>{{gathered_context.billing_issue_type}}</code></li>\n</ul>\n<p>If both define the same key, preset parameters take precedence.</p>\n<p>Example request body:</p>\n<pre><code class=\"language-json\">{\n  &quot;account_id&quot;: &quot;acct_123&quot;,\n  &quot;plan&quot;: &quot;enterprise&quot;,\n  &quot;billing_issue_type&quot;: &quot;invoice_dispute&quot;\n}\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>PayInt Voice Studio does not send the full conversation transcript to the resolver. Add the specific values you need as LLM parameters or preset parameters.</p>\n</div>\n\n\n\n<h2 id=\"dynamic-resolver-response\">Dynamic resolver response</h2>\n<p>Your resolver must return a JSON object with <code>transfer_context.destination</code>.</p>\n<p><code>transfer_context.custom_message</code> is optional.</p>\n<pre><code class=\"language-json\">{\n  &quot;transfer_context&quot;: {\n    &quot;destination&quot;: &quot;+1234567890&quot;,\n    &quot;custom_message&quot;: &quot;Please wait while I connect you with our enterprise billing team.&quot;\n  }\n}\n</code></pre>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Follow this response shape exactly. PayInt Voice Studio reads specific keys from the resolver response. For dynamic transfer resolution, an invalid response does not fall back silently: the transfer fails before dialing.</p>\n</div>\n\n\n\n<p>Field behavior:</p>\n<ul>\n<li><code>destination</code>: Required. E.164 phone number or SIP endpoint.</li>\n<li><code>custom_message</code>: Optional. If returned, this message is played before the provider transfer starts.</li>\n</ul>\n<p>If the resolver fails, times out, returns invalid JSON, or omits <code>transfer_context.destination</code>, the transfer fails gracefully and the agent can continue handling the caller.</p>\n<h2 id=\"dynamic-resolver-configuration\">Dynamic resolver configuration</h2>\n<p>When using a dynamic resolver, configure:</p>\n<ul>\n<li><strong>Resolver URL</strong>: HTTPS or HTTP endpoint PayInt Voice Studio calls with <code>POST</code></li>\n<li><strong>Resolver Timeout</strong>: How long PayInt Voice Studio waits for the resolver response</li>\n<li><strong>Resolver Wait Message</strong>: Optional message spoken while PayInt Voice Studio waits, such as &quot;One moment while I find the right team.&quot;</li>\n<li><strong>LLM Parameters</strong>: Values the agent should extract from the conversation, such as <code>billing_issue_type</code> or <code>requested_department</code></li>\n<li><strong>Preset Parameters</strong>: Values injected by PayInt Voice Studio from context or fixed values</li>\n<li><strong>Custom Headers</strong>: Static headers sent to your resolver endpoint</li>\n<li><strong>Credential</strong>: Optional saved credential used to authenticate the resolver request</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The dynamic resolver uses <code>POST</code> only. If you need custom backend behavior, implement it behind your resolver endpoint and return the expected <code>transfer_context</code> shape.</p>\n</div>\n\n\n\n<h2 id=\"pre-transfer-messages\">Pre-transfer messages</h2>\n<p>Call Transfer supports a common pre-transfer message setting for static, context-mapped, and dynamic destinations.</p>\n<p>For static transfers:</p>\n<ol>\n<li>PayInt Voice Studio resolves the static or template destination.</li>\n<li>PayInt Voice Studio plays the configured pre-transfer message, if any.</li>\n<li>PayInt Voice Studio starts the provider transfer.</li>\n</ol>\n<p>For dynamic transfers:</p>\n<ol>\n<li>PayInt Voice Studio may play the resolver wait message while calling your resolver.</li>\n<li>If the resolver returns <code>custom_message</code>, PayInt Voice Studio plays that message.</li>\n<li>If the resolver does not return <code>custom_message</code>, PayInt Voice Studio uses the configured pre-transfer message.</li>\n<li>PayInt Voice Studio starts the provider transfer.</li>\n</ol>\n<p>Resolver <code>custom_message</code> overrides the configured pre-transfer message for that transfer attempt.</p>\n<h2 id=\"transfer-timeout\">Transfer timeout</h2>\n<p>Transfer timeout is the maximum time PayInt Voice Studio waits for the destination to answer after the provider transfer starts.</p>\n<p>This timeout applies to static, context-mapped, and dynamic transfers.</p>\n<p>The resolver timeout is separate. It controls only how long PayInt Voice Studio waits for your HTTP resolver before dialing starts.</p>\n<h2 id=\"destination-formats\">Destination formats</h2>\n<h3 id=\"twilio-and-telnyx\">Twilio and Telnyx</h3>\n<p>Use E.164 phone numbers:</p>\n<pre><code class=\"language-text\">+1234567890\n</code></pre>\n<p>The number must be reachable from your telephony provider.</p>\n<h3 id=\"asterisk-ari\">Asterisk ARI</h3>\n<p>Use SIP endpoints:</p>\n<pre><code class=\"language-text\">PJSIP/sales-queue\nSIP/1001\n</code></pre>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Asterisk ARI transfers only work with SIP endpoints configured on your Asterisk server. External phone numbers require PSTN trunk configuration in your Asterisk setup.</p>\n</div>\n\n\n\n<h2 id=\"example-route-enterprise-billing-calls\">Example: route enterprise billing calls</h2>\n<p>Use this setup when the agent identifies a billing issue and your backend decides whether to route the caller to standard billing, enterprise billing, or collections.</p>\n<p>Configure LLM parameters:</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;name&quot;: &quot;billing_issue_type&quot;,\n    &quot;type&quot;: &quot;string&quot;,\n    &quot;description&quot;: &quot;The billing issue the caller needs help with, such as invoice_dispute, payment_failed, refund_request, or plan_change.&quot;,\n    &quot;required&quot;: true\n  },\n  {\n    &quot;name&quot;: &quot;requested_department&quot;,\n    &quot;type&quot;: &quot;string&quot;,\n    &quot;description&quot;: &quot;The department the caller is asking for, if they explicitly mention one.&quot;,\n    &quot;required&quot;: false\n  }\n]\n</code></pre>\n<p>Configure preset parameters:</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;name&quot;: &quot;account_id&quot;,\n    &quot;type&quot;: &quot;string&quot;,\n    &quot;value_template&quot;: &quot;{{initial_context.account_id}}&quot;,\n    &quot;required&quot;: true\n  },\n  {\n    &quot;name&quot;: &quot;plan&quot;,\n    &quot;type&quot;: &quot;string&quot;,\n    &quot;value_template&quot;: &quot;{{initial_context.plan}}&quot;,\n    &quot;required&quot;: true\n  }\n]\n</code></pre>\n<p>Your resolver receives:</p>\n<pre><code class=\"language-json\">{\n  &quot;billing_issue_type&quot;: &quot;invoice_dispute&quot;,\n  &quot;requested_department&quot;: &quot;billing&quot;,\n  &quot;account_id&quot;: &quot;acct_123&quot;,\n  &quot;plan&quot;: &quot;enterprise&quot;\n}\n</code></pre>\n<p>Your resolver returns:</p>\n<pre><code class=\"language-json\">{\n  &quot;transfer_context&quot;: {\n    &quot;destination&quot;: &quot;+18005550199&quot;,\n    &quot;custom_message&quot;: &quot;Please wait while I connect you with our enterprise billing team.&quot;\n  }\n}\n</code></pre>\n<h2 id=\"best-practices\">Best practices</h2>\n<ul>\n<li>Keep resolver latency low. Aim for 2-3 seconds.</li>\n<li>Use a resolver wait message if the resolver may take noticeable time.</li>\n<li>Send only the parameters your resolver needs.</li>\n<li>Prefer preset parameters for values already available in <code>initial_context</code> or <code>gathered_context</code>.</li>\n<li>Put the most specific context-mapping rules first and configure a fallback only when it is safe for every unmatched value.</li>\n<li>Make LLM parameter descriptions explicit so the agent knows what to extract.</li>\n<li>Test both successful transfer and resolver failure paths before going live.</li>\n<li>Keep destination validation and routing logic in your backend when routing depends on account data or business rules.</li>\n</ul>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<h3 id=\"the-transfer-tool-is-not-called\">The transfer tool is not called</h3>\n<p>Check that the tool is attached to the correct node and that the node prompt clearly tells the agent when to transfer.</p>\n<h3 id=\"static-transfer-fails-with-no-destination\">Static transfer fails with no destination</h3>\n<p>Static mode requires a configured destination. Use a fixed destination or a template that resolves to a non-empty value.</p>\n<h3 id=\"dynamic-transfer-fails-before-dialing\">Dynamic transfer fails before dialing</h3>\n<p>Check the resolver response. It must include:</p>\n<pre><code class=\"language-json\">{\n  &quot;transfer_context&quot;: {\n    &quot;destination&quot;: &quot;+1234567890&quot;\n  }\n}\n</code></pre>\n<p>Also verify the resolver URL, authentication headers, credential, and timeout.</p>\n<h3 id=\"resolver-receives-missing-parameters\">Resolver receives missing parameters</h3>\n<p>Confirm the parameter is configured as either:</p>\n<ul>\n<li>an LLM parameter, if the agent should extract it from the conversation</li>\n<li>a preset parameter, if PayInt Voice Studio should inject it from context</li>\n</ul>\n<p>For known context values, prefer preset parameters like <code>{{initial_context.account_id}}</code> or <code>{{gathered_context.billing_issue_type}}</code>.</p>\n<h3 id=\"destination-does-not-connect\">Destination does not connect</h3>\n<p>Verify that the phone number or SIP endpoint is valid and reachable from the configured telephony provider.</p>\n"},"voice-agent/tools/end-call":{"title":"End Call","description":"Allow your AI agent to terminate calls when conversations reach a natural conclusion or specific conditions are met.","html":"<p>The End Call tool enables your AI agent to terminate calls programmatically when conversations reach a natural conclusion, specific conditions are met, or when instructed by the caller.</p>\n<h2 id=\"how-it-works\">How It Works</h2>\n<p>The End Call tool provides a clean way to terminate conversations:</p>\n<ol>\n<li><strong>Agent Decision</strong>: Your AI agent determines the call should end based on conversation context</li>\n<li><strong>Optional Message</strong>: Agent can provide a closing message before ending the call</li>\n<li><strong>Clean Termination</strong>: Call is terminated gracefully through the telephony provider</li>\n<li><strong>Call Disposition</strong>: Call disposition is updated in the system</li>\n</ol>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"basic-settings\">Basic Settings</h3>\n<ul>\n<li><strong>End Message</strong>: Optional closing message played before termination</li>\n<li><strong>Reason</strong>: Optional reason code for call termination tracking</li>\n</ul>\n<h2 id=\"agent-usage\">Agent Usage</h2>\n<p>Your AI agent can end calls during conversations:</p>\n<pre><code>end_call(message=&quot;Thank you for calling. Have a great day!&quot;, reason=&quot;conversation_complete&quot;)\n</code></pre>\n<p>Common scenarios:</p>\n<ul>\n<li>Conversation has reached natural conclusion</li>\n<li>User explicitly requests to end the call</li>\n<li>Agent has completed all requested tasks</li>\n<li>Call timeout or error conditions</li>\n</ul>\n"},"voice-agent/tools/http-api":{"title":"HTTP API","description":"Create REST API tools that your AI agent can invoke during conversations to integrate with external systems.","html":"<p>HTTP API tools allow you to attach extrernal REST API calls directly to workflow nodes, enabling your voice agents to call any internal or external system during live conversations based on LLM judgment and your prompts. This works similar to function calling on any agentic platform and is 100% open source and fully customizable.</p>\n<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<h2 id=\"what-is-an-http-api-tool\">What is an HTTP API Tool?</h2>\n<p>An HTTP API Tool is a REST API definition that the LLM can invoke at runtime.</p>\n<p><strong>Typical use cases:</strong></p>\n<ul>\n<li>Call your own backend endpoints</li>\n<li>Trigger n8n automations</li>\n<li>Sync data with a CRM</li>\n<li>Fetch data from external APIs (weather, pricing, availability, etc.)</li>\n<li>Write/update/read data using Rest API</li>\n</ul>\n<p><strong>The LLM decides:</strong></p>\n<ul>\n<li>which tool to call</li>\n<li>when to call it</li>\n<li>what parameters to send</li>\n</ul>\n<p><strong>All based on:</strong></p>\n<ul>\n<li>Your prompts: node-level instructions in simple english (or any language)</li>\n<li>Tool name</li>\n<li>Tool description</li>\n<li>Parameter definitions</li>\n</ul>\n<br/>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h2 id=\"defining-an-http-api-tool\">Defining an HTTP API Tool</h2>\n<h3 id=\"1-tool-name\">1. Tool Name</h3>\n<ul>\n<li><p>Must be clear and action-oriented.</p>\n</li>\n<li><p><strong>Examples:</strong> <code>capture_lead_interest</code>, <code>fetch_weather</code>, &#39;create_crm_contact&#39;, etc</p>\n</li>\n</ul>\n<h3 id=\"2-tool-description\">2. Tool Description</h3>\n<ul>\n<li>Extremely important</li>\n<li>This is how the LLM decides <strong>when</strong> to use the tool. </li>\n<li>Write it in plain, explicit English.</li>\n</ul>\n<p><strong>Bad:</strong>\n&quot;API to capture data&quot;</p>\n<p><strong>Good:</strong>\n&quot;This tool is to capture interest. Use this tool when the user clearly expresses interest in the product or wants to be contacted&quot;</p>\n<p><img src=\"/agentstudio-docs/images/tool%20description.png\" alt=\"Tool Description Example\"></p>\n<h3 id=\"3-endpoint-configuration\">3. Endpoint Configuration</h3>\n<ul>\n<li>Full URL (<strong>must include <code>http://</code> or <code>https://</code></strong>)</li>\n<li>Supports <strong>REST</strong> methods</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n<p>Common mistake: forgetting https:// in the URL.</p>\n</div>\n\n\n\n<br/>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h3 id=\"4-authentication-amp-headers\">4. Authentication &amp; Headers</h3>\n<ul>\n<li>Add custom authentication</li>\n<li>Add custom headers</li>\n<li>Works with internal services and third-party APIs</li>\n</ul>\n<br/>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h3 id=\"5-parameters\">5. Parameters</h3>\n<p>Each parameter must have:</p>\n<ul>\n<li>Name</li>\n<li>Type</li>\n<li>Description</li>\n<li>Required/Optional flag</li>\n</ul>\n<p><strong>Parameter descriptions matter more than types.</strong></p>\n<p>Guidelines:</p>\n<ul>\n<li>Start with string parameters when possible</li>\n<li>Be explicit in what the value represents</li>\n<li>Mark only truly mandatory fields as required</li>\n</ul>\n<p>Example:</p>\n<ul>\n<li>interest (string):\n&quot;Set to true if the user clearly shows intent to buy or wants follow-up. Otherwise false.&quot;</li>\n</ul>\n<p><img src=\"/agentstudio-docs/images/tool%20params.png\" alt=\"Parameter Example\"></p>\n<h3 id=\"send-the-workflow-run-id\">Send the workflow run ID</h3>\n<p>Every call and text chat has a reserved <code>initial_context.workflow_run_id</code> that external context cannot override. To send it, add a <strong>Preset Parameter</strong>:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Name</td>\n<td><code>workflow_run_id</code></td>\n</tr>\n<tr>\n<td>Type</td>\n<td><code>number</code></td>\n</tr>\n<tr>\n<td>Value template</td>\n<td><code>{{initial_context.workflow_run_id}}</code></td>\n</tr>\n<tr>\n<td>Required</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>The preset is sent in the JSON body for POST/PUT/PATCH or query parameters for GET/DELETE. With a custom body template, include <code>{{workflow_run_id}}</code> where needed. For UI tool tests, enter a sample numeric value.</p>\n<br/>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h2 id=\"attaching-tools-to-workflow-nodes\">Attaching Tools to Workflow Nodes</h2>\n<ul>\n<li>You can attach <strong>multiple tools to a single node</strong></li>\n<li>All the tools that you have created will be available for selection in the node</li>\n<li>Tools are only callable when attached to that node</li>\n<li>The LLM will choose which one to call</li>\n</ul>\n<p>Inside the node, guide the LLM using <strong>simple English instructions</strong>.</p>\n<p>Example:</p>\n<p>&quot;If the user shows interest in speaking to sales or wants a callback, immediately call the capture_lead_interest tool and set interest to true.&quot;\nThis instruction is often the deciding factor for correct tool usage.</p>\n<p>This instruction is often the <strong>deciding factor</strong> for correct tool usage.</p>\n<p><img src=\"/agentstudio-docs/images/tool%20attachment.png\" alt=\"Parameter Example\">\n<br/></p>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h2 id=\"tool-invocation-logic-how-the-llm-thinks\">Tool Invocation Logic (How the LLM Thinks)</h2>\n<p>The LLM considers:</p>\n<ul>\n<li>User&#39;s spoken intent</li>\n<li>Node prompt instructions</li>\n<li>Tool name and description</li>\n<li>Parameter descriptions</li>\n</ul>\n<p>If these align clearly, the tool is called automatically.</p>\n<p>Poor naming or vague descriptions lead to:</p>\n<ul>\n<li>Missed tool calls</li>\n<li>Wrong parameters</li>\n<li>Hallucinated values</li>\n</ul>\n<br/>\n<div style={{ borderBottom: '2px solid #e5e5e5', margin: '20px 0' }}></div>\n\n<h2 id=\"key-best-practices\">Key Best Practices</h2>\n<ul>\n<li>Name tools clearly</li>\n<li>Write detailed, action-based descriptions</li>\n<li>Keep parameters simple at first</li>\n<li>Always include http/https in URLs</li>\n<li>Use plain English in node instructions</li>\n<li>Attach only relevant tools to each node</li>\n</ul>\n<p><strong>Well-defined tools + clear prompts = reliable, production-grade voice agents.</strong></p>\n"},"voice-agent/tools/mcp-tool":{"title":"MCP Tool","description":"Connect an external MCP server to your voice agent so the LLM can call remote tools during a live conversation.","html":"<div class=\"as-callout as-callout-note\">\n\n\n<p>This page is about using an external MCP server as a <strong>tool inside a voice agent</strong>. If you want Claude, Cursor, or another coding agent to control PayInt Voice Studio itself over MCP, see <a href=\"/studio/docs/integrations/mcp\">PayInt Voice Studio MCP Server</a>.</p>\n</div>\n\n\n\n<p>MCP tools let a PayInt Voice Studio voice agent call tools exposed by a remote <a href=\"https://modelcontextprotocol.io/\">Model Context Protocol</a> server during a live conversation. PayInt Voice Studio discovers the remote tool catalog, turns those tools into LLM-callable functions, and forwards invocations to the MCP server over authenticated Streamable HTTP.</p>\n<h2 id=\"what-to-configure\">What to configure</h2>\n<p>An MCP tool in PayInt Voice Studio has four important pieces:</p>\n<ul>\n<li><strong>Name</strong>: the server label shown in PayInt Voice Studio</li>\n<li><strong>Description</strong>: tells the LLM when this MCP server is relevant</li>\n<li><strong>URL</strong>: the remote MCP server endpoint (<code>http://</code> or <code>https://</code>)</li>\n<li><strong>Credential</strong>: the auth PayInt Voice Studio should send when connecting to that server</li>\n</ul>\n<p>You can also set a <strong>tool filter</strong> to allow only specific remote MCP tools to be exposed.</p>\n<h2 id=\"authentication\">Authentication</h2>\n<p>Most hosted MCP servers expect:</p>\n<pre><code class=\"language-http\">Authorization: Bearer &lt;token&gt;\n</code></pre>\n<p>So before creating the MCP tool, create a credential in PayInt Voice Studio with:</p>\n<ul>\n<li><strong>Credential Type</strong>: <code>Bearer Token</code></li>\n<li><strong>Token</strong>: the access token issued by your MCP server</li>\n</ul>\n<p>Then select that credential on the MCP tool.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If the remote MCP server documentation says to use Bearer auth, choose <strong>Bearer Token</strong> in the credential dialog. PayInt Voice Studio will translate that into the exact <code>Authorization: Bearer &lt;token&gt;</code> header on the MCP connection.</p>\n</div>\n\n\n\n<p>PayInt Voice Studio also supports other credential types, but <strong>Bearer Token</strong> is the default thing to try for third-party MCP servers unless their docs say otherwise.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<p>The runtime path is:</p>\n<ol>\n<li>When you save or refresh the MCP tool, PayInt Voice Studio opens a short-lived authenticated MCP session and fetches the remote tool catalog.</li>\n<li>PayInt Voice Studio stores that catalog as the tool&#39;s discovered tools so the UI can show you which remote functions exist.</li>\n<li>When a call starts, PayInt Voice Studio opens one live MCP session per attached MCP server and reuses your selected credential for that session.</li>\n<li>For each node, PayInt Voice Studio exposes only the MCP tools allowed by the server-level filter and the node-level selection.</li>\n<li>PayInt Voice Studio namespaces those remote tools into ordinary LLM function definitions so they can safely coexist with HTTP API tools, call transfer, end call, and other tools.</li>\n<li>During the conversation, the LLM sees only the tool name, description, and argument schema. It does <strong>not</strong> see the secret.</li>\n<li>When the LLM calls one of those tools, PayInt Voice Studio forwards the invocation to the MCP server over the active authenticated session, receives the result, and feeds that result back into the agent turn.</li>\n</ol>\n<p>In short: <strong>PayInt Voice Studio handles discovery, authentication, session management, tool registration, and result plumbing; the LLM only decides when to call the tool and with which arguments.</strong></p>\n<h2 id=\"creating-an-mcp-tool\">Creating an MCP tool</h2>\n<ol>\n<li>Go to <strong>Tools</strong> and create a new tool.</li>\n<li>Choose <strong>MCP Server</strong>.</li>\n<li>Enter a clear name and a description that explains when the LLM should use this server.</li>\n<li>Paste the MCP server URL.</li>\n<li>Select the credential. In most cases this should be a <strong>Bearer Token</strong> credential.</li>\n<li>Save the tool and confirm that PayInt Voice Studio discovered the remote tools.</li>\n</ol>\n<p>If the server exposes many tools, use filtering to keep only the ones your agent actually needs.</p>\n<h2 id=\"attaching-it-to-a-node\">Attaching it to a node</h2>\n<p>After the MCP tool is created:</p>\n<ol>\n<li>Open the workflow node where the tool should be available.</li>\n<li>Add the MCP tool from the node&#39;s tool list.</li>\n<li>Select only the remote MCP functions that should be callable on that node.</li>\n<li>In the node prompt, tell the LLM exactly when to use those functions.</li>\n</ol>\n<p>The tighter the node-level selection and prompt guidance, the more reliable MCP tool usage becomes.</p>\n<h2 id=\"best-practices\">Best practices</h2>\n<ul>\n<li>Use one MCP server per logical integration when possible.</li>\n<li>Keep the tool description explicit about <strong>when</strong> the LLM should use that server.</li>\n<li>Expose only the minimum remote functions needed for each node.</li>\n<li>Prefer a <strong>Bearer Token</strong> credential unless the MCP server specifies another auth scheme.</li>\n<li>Test discovery first, then test a real phone/web call to confirm the LLM invokes the right MCP function with the right arguments.</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If the remote MCP server is temporarily unavailable, PayInt Voice Studio degrades gracefully and the call can continue without those MCP tools rather than crashing the entire conversation.</p>\n</div>\n\n\n"},"voice-agent/knowledge-base":{"title":"Knowledge Base","description":"Upload documents that your voice agent can reference during live conversations to provide accurate, context-aware responses.","html":"<p>The Knowledge Base lets you upload documents that your voice agents can reference during conversations. Instead of encoding all information into prompts, you can provide source documents and let the agent retrieve relevant content on the fly.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>An embedding API key (configured in <strong>AI Models Configuration → Embedding</strong>) is required for <strong>Chunked Search</strong> mode. <strong>Full Document</strong> mode does not require embeddings.</p>\n</div>\n\n\n\n<p><img src=\"/agentstudio-docs/images/embedding-configurations.png\" alt=\"Embedding Configuration\"></p>\n<h2 id=\"how-it-works\">How It Works</h2>\n<ol>\n<li>You <strong>upload</strong> a document (PDF, DOCX, TXT, or JSON) to the Knowledge Base</li>\n<li>You <strong>choose a retrieval mode</strong> — Full Document or Chunked Search</li>\n<li>PayInt Voice Studio <strong>processes</strong> the document based on the selected mode</li>\n<li>You <strong>attach</strong> the document to one or more workflow nodes</li>\n<li>During a call, the agent <strong>retrieves</strong> information from the document and uses it to generate accurate responses</li>\n</ol>\n<h2 id=\"supported-file-types\">Supported File Types</h2>\n<table>\n<thead>\n<tr>\n<th>Format</th>\n<th>Extension</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>PDF</td>\n<td><code>.pdf</code></td>\n</tr>\n<tr>\n<td>Word</td>\n<td><code>.docx</code>, <code>.doc</code></td>\n</tr>\n<tr>\n<td>Text</td>\n<td><code>.txt</code></td>\n</tr>\n<tr>\n<td>JSON</td>\n<td><code>.json</code></td>\n</tr>\n</tbody></table>\n<p>Maximum file size: <strong>5 MB</strong></p>\n<h2 id=\"uploading-documents\">Uploading Documents</h2>\n<ol>\n<li>Go to <strong>Knowledge Base Files</strong> in the dashboard</li>\n<li>Click <strong>Upload New</strong> or drag and drop a file</li>\n<li>Choose a <strong>retrieval mode</strong> (see below)</li>\n<li>Click <strong>Upload &amp; Process</strong> — the document will be processed according to the selected mode</li>\n</ol>\n<h3 id=\"retrieval-modes\">Retrieval Modes</h3>\n<p>When uploading a document, you choose how the agent retrieves information from it:</p>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><strong class=\"as-card-title\">Full Document</strong>\n\n\n<p>The entire document text is provided to the agent whenever it queries the knowledge base. No chunking or embedding is performed.</p>\n<p><strong>Best for:</strong> menus, price lists, FAQs, short reference sheets, and any document where the agent needs access to all the information at once.</p>\n<div class=\"as-callout as-callout-tip\">\n\n<p>Full Document mode does not require an embedding API key.</p>\n</div>\n\n\n\n\n</div>\n\n\n\n\n<div class=\"as-card\"><strong class=\"as-card-title\">Chunked Search</strong>\n\n\n<p>The document is split into smaller chunks and indexed with vector embeddings. When the agent queries the knowledge base, only the most relevant chunks are returned.</p>\n<p><strong>Best for:</strong> large documents like policies, manuals, or contracts where only a portion is relevant to any given question.</p>\n</div>\n\n\n\n\n</div>\n\n\n\n<h2 id=\"attaching-documents-to-nodes\">Attaching Documents to Nodes</h2>\n<p>Once a document is processed, you can attach it to any <strong>Start Call</strong> or <strong>Agent</strong> node in your workflow:</p>\n<ol>\n<li>Open the node edit dialog</li>\n<li>Scroll to the <strong>Knowledge Base Documents</strong> section</li>\n<li>Select one or more documents for the agent to reference</li>\n</ol>\n<p>The agent will only search documents attached to the current node, so attach only the documents relevant to that conversation step.</p>\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li><strong>Use Full Document for small reference files</strong> — menus, price lists, and FAQs work best when the agent sees the entire document</li>\n<li><strong>Use Chunked Search for large documents</strong> — policies, manuals, and contracts are too large to fit in context, so chunked retrieval finds the relevant sections</li>\n<li><strong>Keep documents focused</strong> — a single topic per document produces better retrieval results than a large multi-topic file</li>\n<li><strong>Use clear, structured content</strong> — headings, lists, and short paragraphs help the chunking process</li>\n<li><strong>Attach selectively</strong> — only attach documents relevant to a specific node rather than attaching everything everywhere</li>\n<li><strong>Keep documents up to date</strong> — re-upload when source information changes to avoid stale answers</li>\n</ul>\n"},"voice-agent/start-call":{"title":"Start Call","description":"You can use Start Call node to Start the call and configure how Agent greets the user when the conversation starts.","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>You should have only one Start Call node per Voice Agent.</p>\n</div>\n\n\n\n<p>Start Call is the entry point for every conversation. It fires the moment a call connects and controls the first thing your agent says. Every workflow must have exactly one.</p>\n<h2 id=\"when-it-runs\">When it runs</h2>\n<p>Start Call executes before any Agent Node. No other node can precede it in the call flow.</p>\n<p>After Start Call completes, the conversation moves to the next node - typically your first Agent Node. If the caller hangs up immediately or reaches a wrong-number condition, Start Call can route directly to End Call instead.</p>\n<h2 id=\"fields\">Fields</h2>\n<p><strong>Name</strong></p>\n<p>Short identifier shown in the canvas and call logs. Has no runtime effect on what the agent says.</p>\n<p><strong>Greeting Type</strong></p>\n<p>Whether the greeting is spoken via TTS from text or played from a pre-recorded audio file. Defaults to &quot;Text (TTS)&quot;.</p>\n<p><strong>Greeting Text</strong></p>\n<p>Optional fixed line spoken via TTS the moment the call connects, before the AI&#39;s conversational turn begins. Supports <code>{{template_variables}}</code>. Leave empty to skip the greeting and let the Prompt handle the opening.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">Hi {{initial_context.debtor_name}}, this is Alex from Apex Recovery Solutions.\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>See <a href=\"/studio/docs/voice-agent/pre-recorded-audio\">Pre-recorded Audio</a> to use a recording file instead of TTS text.</p>\n</div>\n\n\n\n<p><strong>Prompt</strong> (required)</p>\n<p>System instructions for the AI&#39;s opening conversational turn. Runs after Greeting Text (if set). Supports <code>{{initial_context.field_name}}</code> and variables from Pre-Call Data Fetch.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">After greeting the caller, confirm you are speaking with\n{{initial_context.debtor_name}}. If they confirm, move to Debt Disclosure.\nIf they say wrong number, move to End Call.\n</code></pre>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>If a variable referenced in Greeting Text or Prompt is missing from the API request payload, the agent speaks the raw placeholder out loud - for example, &quot;Hi <code>{{initial_context.debtor_name}}</code>&quot; - instead of the caller&#39;s name. Test with a complete payload before publishing.</p>\n</div>\n\n\n\n<p><strong>Allow Interruption</strong></p>\n<p>When on, the caller can interrupt the agent mid-utterance. Off by default on Start Call.</p>\n<p><strong>Add Global Prompt</strong></p>\n<p>When on and a Global Node exists, the Global Prompt is prepended to this node&#39;s Prompt at runtime. On by default.</p>\n<p><strong>Delayed Start</strong></p>\n<p>When on, the agent waits before speaking after the call connects. Useful for outbound calls where the called party needs a moment to settle. Duration can be set between 0.1 and 10 seconds.</p>\n<p><strong>Enable Variable Extraction</strong></p>\n<p>When on, an LLM extraction pass runs after this node&#39;s turn to capture structured data from the conversation into <code>gathered_context</code>.</p>\n<p><strong>Tools</strong></p>\n<p>Functions the agent can call during the opening turn. Add via the &quot;HTTP &amp; Tools&quot; tab (HTTP API and built-in tools) or the &quot;MCP&quot; tab (MCP server tools).</p>\n<p><strong>Knowledge Base Documents</strong></p>\n<p>Documents the agent can reference during this step. Attach via &quot;Manage Documents.&quot;</p>\n<p><strong>Pre-Call Data Fetch</strong></p>\n<p>Choose whether PayInt Voice Studio makes a POST request before <strong>all</strong>, <strong>inbound</strong>, or <strong>outbound</strong> calls, or leave the fetch <strong>disabled</strong>. PayInt Voice Studio merges the JSON response into the call context as template variables before rendering the greeting and Start-node prompt.</p>\n<h2 id=\"outgoing-edges\">Outgoing edges</h2>\n<p>Start Call can have multiple outgoing edges, each with a user-defined label and a condition written in plain language. The LLM reads the conversation and picks the edge whose condition best matches.</p>\n<p>Each edge needs two things:</p>\n<ul>\n<li><strong>Label:</strong> a short name shown on the canvas (you define this)</li>\n<li><strong>Condition:</strong> the instruction that tells the LLM when to take this edge</li>\n</ul>\n<p>A typical Start Call has two edges:</p>\n<pre><code class=\"language-text\">Label: &quot;caller confirmed&quot;\nCondition: &quot;The caller confirmed they are the right person and are ready to continue.&quot;\n\nLabel: &quot;wrong number&quot;\nCondition: &quot;The caller says this is the wrong number, asks not to be called, or refuses to confirm their identity.&quot;\n</code></pre>\n<p>You can add more edges if the opening turn needs to branch further.</p>\n<h2 id=\"common-mistakes\">Common mistakes</h2>\n<p><strong>Leaving Greeting Text empty with no Prompt.</strong> If both Greeting Text and Prompt are empty, the agent says nothing at the start of the call. The caller hears silence.</p>\n<p><strong>Confusing Node Name with the agent&#39;s spoken name.</strong> Node Name is a canvas label for your own reference. The name the agent introduces itself as comes from the Greeting Text or Prompt content, or the persona you define in the <a href=\"/studio/docs/voice-agent/global\">Global Node</a>.</p>\n<p><strong>Adding a second Start Call node.</strong> Each workflow has one entry point. A second Start Call node will throw an error.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/agent\">Agent Node</a>\n\n\n<p>Build the core conversation logic that follows the greeting.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/global\">Global Node</a>\n\n\n<p>Set persona and tone rules that apply across all nodes.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/context-and-variables\">Context and Variables</a>\n\n\n<p>Learn how <code>initial_context</code> variables flow through a call.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/end-call\">End Call Node</a>\n\n\n<p>Configure the agent&#39;s final message before the call drops.</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"voice-agent/end-call":{"title":"End Call","description":"You can use End Call node to configure how the Agent says its final message right before the call is terminated","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<p>End Call is the terminal node in a workflow. It fires right before PayInt Voice Studio terminates the connection, giving your agent one last chance to speak. Every path through your workflow must eventually reach an End Call node.</p>\n<h2 id=\"when-it-runs\">When it runs</h2>\n<p>End Call executes after all conversation logic is complete. The agent delivers its closing message, then the call drops.</p>\n<p>You can have multiple End Call nodes in one workflow, each reached by a different edge condition. For example, one for a successful outcome and one for a wrong-number path. Each can have its own distinct Prompt.</p>\n<h2 id=\"fields\">Fields</h2>\n<p><strong>Name</strong></p>\n<p>Short identifier shown in the canvas and call logs. Use names that describe the ending context, for example &quot;Successful close&quot; or &quot;Polite decline&quot;.</p>\n<p><strong>Prompt</strong></p>\n<p>System instructions for what the agent says before the call ends. You can make this conditional by describing different outcomes in the prompt.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">If the caller committed to a payment, confirm the arrangement: the\nexact amount and expected processing time. If no commitment was made,\nthank them for their time and let them know they can call back to\ndiscuss options. Always close professionally.\n</code></pre>\n<p>The Prompt cannot reference <code>gathered_context</code> - variables extracted by this or earlier nodes are not available in any node&#39;s Prompt. To act on extracted data, send it out via a <a href=\"/studio/docs/voice-agent/webhook\">Webhook node</a> instead.</p>\n<p><strong>Add Global Prompt</strong></p>\n<p>When on and a Global Node exists, the Global Prompt is prepended to this node&#39;s Prompt at runtime. On by default.</p>\n<p><strong>Enable Variable Extraction</strong></p>\n<p>When on, an LLM extraction pass runs after this node&#39;s turn to capture structured data from the conversation into <code>gathered_context</code>.</p>\n<h2 id=\"common-mistakes\">Common mistakes</h2>\n<p><strong>Leaving the Prompt empty.</strong> An empty Prompt means the agent says nothing before the line drops. The caller experiences an abrupt silence.</p>\n<p><strong>Routing all paths into one End Call when outcomes need different closing lines.</strong> Multiple End Call nodes are supported. If a wrong-number path and a successful-commitment path need different closing lines, use two End Call nodes with distinct Prompts rather than cramming both into one.</p>\n<p><strong>Agent Nodes that never route to End Call.</strong> Any node without a downstream path to End Call leaves calls in an unresolvable state.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Check every Agent Node for a path to End Call before publishing. Nodes with no downstream connection to End Call will drop the call without a closing message.</p>\n</div>\n\n\n\n<h2 id=\"next-steps\">Next Steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/qa\">QA Node</a>\n\n\n<p>Automatically evaluate calls after they end.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/webhook\">Webhook Node</a>\n\n\n<p>Sync call outcomes to your CRM or external systems.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/agent\">Agent Node</a>\n\n\n<p>Configure the conversation logic that routes into End Call.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/context-and-variables\">Context and Variables</a>\n\n\n<p>Learn where <code>gathered_context</code> can and cannot be referenced.</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"voice-agent/agent":{"title":"Agent Node","description":"Agent node contains the prompts that drives the conversation with the Voice Agent","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<p>Agent Node is the core building block of every PayInt Voice Studio workflow. It holds the LLM prompt that drives the conversation, any tools the agent can call, and knowledge base documents to reference. You can have multiple Agent Nodes in a single workflow, each handling a distinct phase of the conversation.</p>\n<p>The Edges connected with Agent Nodes are pathways that the LLMs can take depending on how the conversation has been going so far.</p>\n<h2 id=\"what-goes-in-an-agent-node\">What goes in an Agent Node</h2>\n<p><strong>Prompt</strong></p>\n<p>The instructions for how the agent should behave at this step. Be explicit about sequence: if the agent should confirm identity before discussing payment, say so in the prompt. Vague prompts produce inconsistent behavior.</p>\n<p><strong>Tools</strong></p>\n<p>External functions the agent can call during this step - for example, looking up account balances or updating a CRM record. Add tools via the &quot;HTTP &amp; Tools&quot; or &quot;MCP&quot; tabs in the node settings panel.</p>\n<p><strong>Allow Interruption</strong></p>\n<p>When on, the caller can interrupt the agent mid-utterance.</p>\n<p><strong>Add Global Prompt</strong></p>\n<p>When on and a Global Node exists, the Global Prompt is prepended to this node&#39;s Prompt at runtime. On by default.</p>\n<p><strong>Knowledge Base Documents</strong></p>\n<p>Files the agent can reference when answering questions - product guides, FAQs, policies. Attach them via the &quot;Manage Documents&quot; button in the node settings panel.</p>\n<h2 id=\"configuring-edges\">Configuring edges</h2>\n<p>Each outgoing edge has a condition written in plain language. The LLM evaluates the conversation and picks the edge whose condition best matches.</p>\n<p>Write edge conditions as specifically as possible:</p>\n<table>\n<thead>\n<tr>\n<th>Vague (avoid)</th>\n<th>Specific (use)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Debtor responds&quot;</td>\n<td>&quot;Debtor confirms their identity and is ready to discuss the account&quot;</td>\n</tr>\n<tr>\n<td>&quot;Call ends&quot;</td>\n<td>&quot;Debtor commits to a specific payment option and states the amount&quot;</td>\n</tr>\n<tr>\n<td>&quot;Problem&quot;</td>\n<td>&quot;Debtor disputes owing the debt or requests validation documentation&quot;</td>\n</tr>\n</tbody></table>\n<div class=\"as-callout as-callout-tip\">\n\n\n<p>If the agent keeps routing to the wrong edge, make the condition more specific. Conditions like &quot;user responds&quot; or &quot;conversation ends&quot; are too broad and will route unpredictably.</p>\n</div>\n\n\n\n<h2 id=\"extracting-variables\">Extracting variables</h2>\n<p>Agent Node can extract structured data from the conversation after the node completes. Enable this in the node settings under &quot;Enable Variable Extraction.&quot;</p>\n<p>Once enabled, configure two things:</p>\n<ul>\n<li><strong>Extraction Prompt:</strong> overall instructions telling the LLM how to approach the extraction.</li>\n<li><strong>Variables to Extract:</strong> a list of variables, each with a name, a data type (<code>string</code>, <code>number</code>, <code>boolean</code>), and a per-variable hint describing what to capture.</li>\n</ul>\n<p>For example:</p>\n<pre><code class=\"language-text\">Extraction Prompt:\nExtract the payment outcome from this debt collection call.\n\nVariables:\n- amount_user_will_pay (string): Amount the user said they will pay.\n- payment_method (string): How they said they will pay, e.g. card, bank transfer.\n- sentiment (string): User tone: cooperative, hesitant, angry, refused.\n</code></pre>\n<p>Each extracted value is stored in <code>gathered_context</code>. It is not available in any node&#39;s Prompt - reference it in <a href=\"/studio/docs/voice-agent/webhook\">Webhook node</a> payloads as <code>{{gathered_context.variable_name}}</code>, or read it from the run record after the call completes.</p>\n<h2 id=\"using-multiple-agent-nodes\">Using multiple Agent Nodes</h2>\n<p>Break complex conversations into phases. Each Agent Node handles one part of the flow. A debt collection workflow, for example, might chain three Agent Nodes: identity verification, offer presentation, and objection handling.</p>\n<p>Nodes pass context forward automatically, but not into prompts. Variables extracted in one node are stored in <code>gathered_context</code> and merged with variables from later nodes as the call progresses. <code>gathered_context</code> is not available in any node&#39;s Prompt - it can only be referenced in <a href=\"/studio/docs/voice-agent/webhook\">Webhook node</a> payloads and in the run record after the call completes. If a later Agent Node needs to react to something extracted earlier, that logic has to live in the prompt&#39;s own instructions, not in a <code>{{gathered_context...}}</code> reference.</p>\n<h2 id=\"common-mistakes\">Common mistakes</h2>\n<p><strong>No outgoing edges.</strong> Every Agent Node needs at least one edge. A dead-end node has no exit path and will trap the call.</p>\n<p><strong>Putting global instructions in the Agent prompt.</strong> Rules that apply across the entire workflow - persona, tone, compliance guardrails - belong in the <a href=\"/studio/docs/voice-agent/global\">Global Node</a>, not repeated in every Agent Node.</p>\n<p><strong>Vague edge conditions.</strong> The LLM picks the edge that best matches the conversation state. Overly broad conditions cause unpredictable routing between nodes.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/end-call\">End Call Node</a>\n\n\n<p>Terminate the conversation with a closing message.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/global\">Global Node</a>\n\n\n<p>Write shared instructions that apply to every node.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/context-and-variables\">Context and Variables</a>\n\n\n<p>Understand how <code>gathered_context</code> is built and where it can be referenced.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/qa\">QA Node</a>\n\n\n<p>Evaluate agent performance automatically after each call.</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"voice-agent/global":{"title":"Global Node","description":"Global Node contains the common prompt that is prepended to the prompt of every node, in which `Add Global Prompt` is turned on.","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>You should have only one Global node per Voice Agent.</p>\n</div>\n\n\n\n<p><img src=\"/agentstudio-docs/images/global-node.png\" alt=\"Add Global Prompt Selector\"></p>\n<p>Global Node is a shared prompt layer for your entire workflow. Write your agent&#39;s persona, tone guidelines, and compliance rules once in the <strong>Global Prompt</strong> field. Any Agent Node or Start Call node with &quot;Add Global Prompt&quot; turned on will receive this content prepended to its own prompt before each LLM call.</p>\n<h2 id=\"what-goes-in-the-global-node\">What goes in the Global Node</h2>\n<p>Use Global Node for instructions that apply across every step of the conversation:</p>\n<ul>\n<li><strong>Persona:</strong> Who is the agent? What company do they represent?</li>\n<li><strong>Tone rules:</strong> How formal or warm should they be?</li>\n<li><strong>Compliance guardrails:</strong> What must the agent never say?</li>\n<li><strong>Objection handling:</strong> How should the agent respond to pushback?</li>\n</ul>\n<p>Avoid putting step-specific logic here. Instructions like &quot;offer the settlement option after the debtor confirms their identity&quot; belong in the Agent Node prompt, not in Global.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<p>Global Node does not run as a separate step in the call. It has no edges and cannot be connected to other nodes. The Global Prompt is silently prepended to each node&#39;s prompt where &quot;Add Global Prompt&quot; is enabled.</p>\n<p>The LLM sees one combined prompt: the global content first, followed by the node-specific instructions. The caller never notices the seam.</p>\n<h2 id=\"enabling-it-on-an-agent-node\">Enabling it on an Agent Node</h2>\n<p>Each Agent Node and Start Call node has an &quot;Add Global Prompt&quot; toggle in its settings panel. Nodes with the toggle off receive no global content for that step.</p>\n<div class=\"as-callout as-callout-tip\">\n\n\n<p>Turn the toggle off selectively when a specific node needs different behavior. An escalation node, for example, may need a more formal tone that should not inherit your default persona.</p>\n</div>\n\n\n\n<h2 id=\"common-mistakes\">Common mistakes</h2>\n<p><strong>Putting node-specific logic in the Global Node.</strong> Global is for rules that always apply, not for conversation-specific decisions. Sequence and outcome logic belong in each Agent Node prompt.</p>\n<p><strong>Leaving the Global Node prompt empty with toggles on.</strong> A blank Global Node prepended to every prompt does nothing harmful, but it is also pointless. Either fill it in or leave the toggle off.</p>\n<p><strong>Adding a second Global Node.</strong> Each workflow supports exactly one. Adding a second throws an error.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/start-call\">Start Call Node</a>\n\n\n<p>Configure how your agent opens the conversation.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/agent\">Agent Node</a>\n\n\n<p>Build the core conversation logic that inherits your global prompt.</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"voice-agent/qa":{"title":"QA","description":"QA node allows you to run post-call quality analysis on your Voice Agent runs","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<p>The QA node runs an LLM quality review on the call transcript after each conversation ends. Use it to score agent performance, flag compliance issues, and surface patterns across calls, without listening to recordings manually.</p>\n<h2 id=\"when-it-runs\">When it runs</h2>\n<p>QA Node runs post-call only. It is not part of the live call flow - it reads the completed transcript after the conversation ends. It has no edges and cannot connect to any other node in the workflow.</p>\n<p>This means QA does not affect call behavior. It only measures it.</p>\n<h2 id=\"where-it-sits-on-the-canvas\">Where it sits on the canvas</h2>\n<p>Add QA Node anywhere on the canvas. It does not need to connect to other nodes - most builders place it off to the side to make clear it is a standalone evaluator. The node runs automatically for every eligible call once the workflow is published.</p>\n<h2 id=\"configuring-the-system-prompt\">Configuring the System Prompt</h2>\n<p>The core configuration is a single <strong>System Prompt</strong>: instructions to the reviewer LLM that describe how to evaluate each call. The LLM reads the transcript and returns a structured JSON result.</p>\n<p>The default prompt already covers common quality dimensions out of the box:</p>\n<ul>\n<li><strong>Tags</strong>: named issues detected in the transcript. The default tag set includes <code>DEAD_AIR</code>, <code>USER_FRUSTRATED</code>, <code>ASSISTANT_IN_LOOP</code>, <code>ASSISTANT_REPLY_IMPROPER</code>, <code>USER_NOT_UNDERSTANDING</code>, <code>HEARING_ISSUES</code>, <code>UNCLEAR_CONVERSATION</code>, <code>USER_REQUESTING_FEATURE</code>, <code>ASSISTANT_LACKS_EMPATHY</code>, and <code>USER_DETECTS_AI</code></li>\n<li><strong>Call quality score</strong>: 1-10 rating</li>\n<li><strong>Overall sentiment</strong>: positive, neutral, or negative</li>\n<li><strong>Summary</strong>: 1-2 sentence description of the call segment</li>\n</ul>\n<p>You can replace the default System Prompt with your own instructions when you need domain-specific evaluation. Write the prompt as instructions to the QA reviewer LLM, and specify the JSON structure you want it to return.</p>\n<p>For compliance use cases, for example:</p>\n<pre><code class=\"language-text\">You are a compliance reviewer for a debt collection workflow.\n\nReview the transcript and return a JSON object with the following fields:\n- &quot;identity_verified&quot;: true or false — did the agent confirm the caller&#39;s identity before discussing the account?\n- &quot;options_presented&quot;: true or false — did the agent offer all three payment options before accepting a decision?\n- &quot;tone_score&quot;: 1-5 — how calm and professional was the agent throughout?\n- &quot;summary&quot;: one sentence describing the call outcome.\n</code></pre>\n<div class=\"as-callout as-callout-tip\">\n\n\n<p>Be specific about what counts as a pass or a valid score. Include the exact behaviors you want the LLM to look for. Vague instructions produce inconsistent results across calls.</p>\n</div>\n\n\n\n<h2 id=\"additional-settings\">Additional settings</h2>\n<table>\n<thead>\n<tr>\n<th>Setting</th>\n<th>Default</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Enabled</td>\n<td>On</td>\n<td>Toggle to disable QA without removing the node</td>\n</tr>\n<tr>\n<td>Use Workflow&#39;s LLM</td>\n<td>Off</td>\n<td>When on, uses the same LLM configured for the workflow instead of the default QA model</td>\n</tr>\n<tr>\n<td>Minimum Call Duration (seconds)</td>\n<td>15</td>\n<td>Calls shorter than this are skipped</td>\n</tr>\n<tr>\n<td>Include Voicemail Calls</td>\n<td>Off</td>\n<td>When off, voicemail calls are skipped</td>\n</tr>\n<tr>\n<td>Sample Rate (%)</td>\n<td>100</td>\n<td>Run QA on a percentage of calls rather than all of them</td>\n</tr>\n</tbody></table>\n<h2 id=\"viewing-qa-results\">Viewing QA Results</h2>\n<p>After a call completes, the QA analysis runs automatically. You can view the results on the <strong>Run Detail</strong> page for each individual run.</p>\n<p>Open a run and scroll to the <strong>QA Analysis</strong> section. The result shows the full JSON output your QA System Prompt requested - tags, scores, sentiment, and summary for the default prompt, or whatever structure you defined.</p>\n<h2 id=\"common-mistakes\">Common mistakes</h2>\n<p><strong>Expecting QA to affect the live call.</strong> QA Node is read-only at runtime. It evaluates what happened; it does not change what happens.</p>\n<p><strong>Connecting QA Node to other nodes.</strong> QA has no edges. Drawing a connection from QA to another node is not possible - edges will snap back when you try.</p>\n<p><strong>Writing a vague System Prompt.</strong> &quot;Was the agent good?&quot; produces unhelpful scores. Define specific, observable behaviors and the exact JSON fields you want back.</p>\n<p><strong>Not specifying output format.</strong> The LLM follows your instructions, but if you do not specify a return format, the output may be inconsistent across calls. Always describe the exact JSON structure in the System Prompt.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/agent\">Agent Node</a>\n\n\n<p>Configure the conversation logic that QA evaluates.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/webhook\">Webhook Node</a>\n\n\n<p>Send call outcomes to external systems after the call ends.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/core-concepts/context-and-variables\">Context and Variables</a>\n\n\n<p>Learn how <code>gathered_context</code> is built during the call.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/voice-agent/end-call\">End Call Node</a>\n\n\n<p>Configure the final message before the call drops.</p>\n</div>\n\n\n\n\n</div>\n\n\n"},"voice-agent/api-trigger":{"title":"API Trigger","description":"Trigger outbound calls from external systems like n8n, Zapier, or your own backend","html":"<p>The API Trigger node lets you initiate outbound calls to your voice agent programmatically. When you add an API Trigger node to your workflow, PayInt Voice Studio generates a unique endpoint URL that external systems can call to start a conversation.</p>\n<p>This is useful when you want to trigger calls from your own backend, a CRM, or workflow tools like n8n and Zapier.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A configured <a href=\"/studio/docs/integrations/telephony/overview\">telephony provider</a> — outbound calls will fail without one</li>\n<li>An <a href=\"/studio/docs/configurations/api-keys\">API key</a> for authentication</li>\n</ul>\n<h2 id=\"finding-your-trigger-url\">Finding your trigger URL</h2>\n<p>When you add an API Trigger node to your workflow, PayInt Voice Studio assigns it a unique UUID. The trigger node exposes two URLs that share this UUID:</p>\n<ul>\n<li>A <strong>production URL</strong> for the published workflow</li>\n<li>A <strong>test URL</strong> for the latest draft</li>\n</ul>\n<p>You can copy either URL from the trigger node&#39;s settings dialog.</p>\n<pre><code>POST https://your-dograh-instance/api/v1/public/agent/{uuid}        # Production\nPOST https://your-dograh-instance/api/v1/public/agent/test/{uuid}   # Test\n</code></pre>\n<p>If you are using the hosted version, replace <code>your-dograh-instance</code> with <code>api.PayInt Voice Studio</code>.</p>\n<h3 id=\"test-vs-production\">Test vs production</h3>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>URL</th>\n<th>Runs</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Production</td>\n<td><code>/api/v1/public/agent/{uuid}</code></td>\n<td>The published version of the agent.</td>\n</tr>\n<tr>\n<td>Test</td>\n<td><code>/api/v1/public/agent/test/{uuid}</code></td>\n<td>The latest draft. Falls back to the published version if no draft exists.</td>\n</tr>\n</tbody></table>\n<p>Use the test URL while iterating on changes so production traffic continues to hit the published version.</p>\n<p>The production URL only executes a <strong>published</strong> workflow. If you update the workflow but do not publish it, the production trigger will continue to run the older published version.</p>\n<p>Once you publish your draft, both URLs run the same definition.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>A common pitfall is editing the workflow, saving the draft, and then calling the production trigger URL expecting the new behavior. That will not work until you publish the workflow. Use the test URL to verify draft changes before publishing.</p>\n</div>\n\n\n\n<p>The request body, headers, and response shape are identical for both URLs.</p>\n<h2 id=\"making-a-request\">Making a request</h2>\n<p>Authenticate by passing your API key in the <code>X-API-Key</code> header. The request body requires a <code>phone_number</code> and accepts optional <code>initial_context</code>, <code>telephony_configuration_id</code>, and <code>from_phone_number_id</code> fields.</p>\n<pre><code class=\"language-bash\">curl -X POST https://your-dograh-instance/api/v1/public/agent/{uuid} \\\n-H &quot;Content-Type: application/json&quot; \\\n-H &quot;X-API-Key: dg_your_api_key&quot; \\\n-d &#39;{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;initial_context&quot;: {\n    &quot;customer_name&quot;: &quot;Jane&quot;,\n    &quot;appointment_date&quot;: &quot;March 15&quot;\n  }\n}&#39;\n</code></pre>\n<pre><code class=\"language-bash\">curl -X POST https://your-dograh-instance/api/v1/public/agent/test/{uuid} \\\n-H &quot;Content-Type: application/json&quot; \\\n-H &quot;X-API-Key: dg_your_api_key&quot; \\\n-d &#39;{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;initial_context&quot;: {\n    &quot;customer_name&quot;: &quot;Jane&quot;,\n    &quot;appointment_date&quot;: &quot;March 15&quot;\n  }\n}&#39;\n</code></pre>\n<p>Use the test URL when you want to verify draft changes before publishing.</p>\n<h3 id=\"response\">Response</h3>\n<p>A successful request returns a <code>workflow_run_id</code> that you can use to <a href=\"/studio/docs/api-reference/runs/get-run\">retrieve run details</a>, recordings, and transcripts.</p>\n<pre><code class=\"language-json\">{\n  &quot;status&quot;: &quot;initiated&quot;,\n  &quot;workflow_run_id&quot;: 12345,\n  &quot;workflow_run_name&quot;: &quot;WR-API-7823&quot;\n}\n</code></pre>\n<h3 id=\"error-responses\">Error responses</h3>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>400</code></td>\n<td>Telephony provider not configured, or call failed to initiate</td>\n</tr>\n<tr>\n<td><code>401</code></td>\n<td>Missing or invalid API key</td>\n</tr>\n<tr>\n<td><code>403</code></td>\n<td>API key does not have access to this agent</td>\n</tr>\n<tr>\n<td><code>404</code></td>\n<td>Trigger not found or not active</td>\n</tr>\n</tbody></table>\n<h2 id=\"initial-context\">Initial context</h2>\n<p><code>initial_context</code> is a JSON object containing any information you want your voice agent to access during the call. You can reference these values in your prompts using <a href=\"/studio/docs/voice-agent/template-variables\">template variables</a> — values enclosed in <code>{{</code> and <code>}}</code>.</p>\n<p>For example, if your request includes:</p>\n<pre><code class=\"language-json\">{\n    &quot;phone_number&quot;: &quot;+14155550100&quot;,\n    &quot;initial_context&quot;: {\n        &quot;user&quot;: {\n            &quot;name&quot;: &quot;John&quot;\n        }\n    }\n}\n</code></pre>\n<p>You can reference the user&#39;s name in your agent prompt as <code>{{user.name}}</code> — in Agent prompts, <code>initial_context</code> fields are referenced directly by name (not prefixed with <code>initial_context.</code>). See <a href=\"/studio/docs/voice-agent/template-variables\">template variables</a> for the exact syntax in prompts versus webhook payloads.</p>\n<p>See <a href=\"/studio/docs/core-concepts/context-and-variables\">Context &amp; Variables</a> for more on how data flows through a call.</p>\n<h3 id=\"override-the-greeting-for-one-call\">Override the greeting for one call</h3>\n<p>Pass <code>greeting_override</code> inside <code>initial_context</code> to replace the Start Call node&#39;s saved greeting for one API-triggered call. A valid override takes precedence over the saved greeting. If you omit it, PayInt Voice Studio uses the saved greeting; if neither exists, the Start node follows its normal no-greeting behavior.</p>\n<p>For a text greeting:</p>\n<pre><code class=\"language-json\">{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;initial_context&quot;: {\n    &quot;greeting_override&quot;: {\n      &quot;type&quot;: &quot;text&quot;,\n      &quot;text&quot;: &quot;Hi, please confirm account {{account_id}}.&quot;\n    }\n  }\n}\n</code></pre>\n<p>For a pre-recorded greeting, pass the recording&#39;s string ID from the Recordings page:</p>\n<pre><code class=\"language-json\">{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;initial_context&quot;: {\n    &quot;greeting_override&quot;: {\n      &quot;type&quot;: &quot;audio&quot;,\n      &quot;recording_id&quot;: &quot;callback-welcome&quot;\n    }\n  }\n}\n</code></pre>\n<p>PayInt Voice Studio renders a text override after Pre-Call Data Fetch completes, so the override can reference variables returned by the fetch. The override follows the same playback restrictions as a saved greeting. In particular, static text greetings are not spoken by realtime speech-to-speech models; use an audio override for those configurations.</p>\n<p>Pre-Call Data Fetch can also return <code>greeting_override</code>. Because fetched context is applied after trigger context, a valid fetched override replaces one supplied by the trigger.</p>\n<h2 id=\"choosing-a-telephony-configuration-and-caller-id\">Choosing a telephony configuration and caller ID</h2>\n<p>By default, calls are placed through your organization&#39;s default outbound <a href=\"/studio/docs/integrations/telephony/overview\">telephony configuration</a>. To route a specific call through a different configuration — for example, to dial out from a regional number — pass <code>telephony_configuration_id</code> in the request body.</p>\n<pre><code class=\"language-json\">{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;telephony_configuration_id&quot;: 42\n}\n</code></pre>\n<p>The id is shown on each row in <strong>Telephony configurations</strong> (<code>/studio/telephony-configurations</code> for hosted or <code>http://localhost:3010/telephony-configurations</code> for local). The configuration must belong to the same organization as the API Trigger; otherwise the request returns <code>404</code>.</p>\n<p>To use a specific caller ID from that configuration, also pass <code>from_phone_number_id</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;phone_number&quot;: &quot;+14155550100&quot;,\n  &quot;telephony_configuration_id&quot;: 42,\n  &quot;from_phone_number_id&quot;: 84\n}\n</code></pre>\n<p><code>from_phone_number_id</code> is the ID of a phone number configured under the selected telephony configuration. The number must be active and belong to that configuration; otherwise the request returns <code>400</code>. You can retrieve configured numbers with <a href=\"/studio/docs/api-reference/telephony-configs/phone-numbers/list\">List phone numbers</a>.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>For full endpoint details including all parameters and response fields, see the <a href=\"/studio/docs/api-reference/runs/trigger\">API reference</a>.</p>\n</div>\n\n\n"},"voice-agent/webhook":{"title":"Webhook","description":"Webhook node allow you to sync the result of Voice Agent runs to your external systems, like CRM or to other workflow orchestrator like Zapier or n8n.","html":"<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>You can have multiple Webhook Nodes for a single Voice Agent, if you want to sync the result of the call at multiple places.</p>\n</div>\n\n\n\n<h3 id=\"creating-the-webhook-payload\">Creating the Webhook Payload</h3>\n<p>The payload can contain a valid JSON, and you can reference context variables while constructing it using double-brace syntax — for example <code>{{workflow_run_id}}</code>, <a href=\"/studio/docs/core-concepts/context-and-variables#initial_context\"><code>{{initial_context.*}}</code></a>, and <a href=\"/studio/docs/core-concepts/context-and-variables#gathered_context\"><code>{{gathered_context.*}}</code></a>, including the <a href=\"/studio/docs/developer/call-dispositions\">final call outcome</a> <code>{{gathered_context.call_disposition}}</code>.</p>\n<p>For the full list of available variables, authentication options, and receiver examples, see the <a href=\"/studio/docs/developer/webhooks\">Webhook Payloads developer reference</a>.</p>\n<p>An example of the payload is given below</p>\n<pre><code class=\"language-json\">{\n  &quot;call_id&quot;: &quot;{{workflow_run_id}}&quot;,\n  &quot;first_name&quot;: &quot;{{initial_context.first_name}}&quot;,\n  &quot;rsvp&quot;: &quot;{{gathered_context.rsvp}}&quot;,\n  &quot;disposition&quot;: &quot;{{gathered_context.call_disposition}}&quot;,\n  &quot;duration&quot;: &quot;{{cost_info.call_duration_seconds}}&quot;,\n  &quot;recording_url&quot;: &quot;{{recording_url}}&quot;,\n  &quot;transcript_url&quot;: &quot;{{transcript_url}}&quot;\n}\n</code></pre>\n"},"voice-agent/add-to-website":{"title":"Add to Website","description":"Add your PayInt Voice Studio agent to any website so visitors can talk to it by voice or chat with it by text.","html":"<h3 id=\"how-to-add-it\">How to add it</h3>\n<p>Add your agent to any website using the Configure Widget dialog in your agent&#39;s settings.</p>\n<p>Step 1: Open the agent settings by clicking the gear icon in the top-right of the agent editor.</p>\n<p><img src=\"/agentstudio-docs/images/open-settings.png\" alt=\"Open agent settings\"></p>\n<p>Step 2: Scroll to the <strong>Add to Website</strong> section and click <strong>Configure Widget</strong>.</p>\n<p><img src=\"/agentstudio-docs/images/add-to-website.png\" alt=\"Go to Add to Website\"></p>\n<p>Step 3: Enable embedding, add your website&#39;s domain to <strong>Allowed Domains</strong>, choose a <strong>Widget Type</strong> (Voice or Chat) and an embed mode (<strong>Floating Widget</strong>, <strong>Inline Component</strong>, or <strong>Headless (Bring Your Own UI)</strong>), customize the button (position, color, text) if applicable, and click <strong>Save Configurations</strong>.</p>\n<p><img src=\"/agentstudio-docs/images/save-configurations.png\" alt=\"Save configurations\"></p>\n<p>Step 4: Copy the generated embed code and paste it into your web page to test your agent.</p>\n<p><img src=\"/agentstudio-docs/images/copy-deployment-code.png\" alt=\"Copy deployment code\"></p>\n<h2 id=\"widget-types\">Widget types</h2>\n<p>Each embed widget is either a voice widget or a chat widget — pick the type in the Configure Widget dialog. Both types support all three embed modes.</p>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>How visitors interact</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Voice</strong></td>\n<td>Visitors talk to your agent over a live audio call (WebRTC, microphone required).</td>\n</tr>\n<tr>\n<td><strong>Chat</strong></td>\n<td>Visitors type messages in a chat panel and the agent replies as text. No microphone.</td>\n</tr>\n</tbody></table>\n<p>How chat conversations behave:</p>\n<ul>\n<li>The conversation starts when the visitor opens the chat (clicks the chat button) — the agent greets them first. Page loads alone never start a conversation.</li>\n<li>A chat session lasts up to <strong>1 hour</strong>. When it expires, the visitor is offered a <strong>Start new chat</strong> button, which begins a fresh conversation.</li>\n<li>Reloading the page starts a fresh conversation on the next open — chat history isn&#39;t carried across page loads.</li>\n<li>Each conversation counts once toward the embed token&#39;s usage limit, same as one voice call.</li>\n<li>Chat conversations appear in your agent&#39;s call history with a full transcript.</li>\n</ul>\n<h2 id=\"embed-modes\">Embed modes</h2>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>What it renders</th>\n<th>When to use</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Floating Widget</strong></td>\n<td>A pill-shaped CTA button anchored to a corner of the page. For chat widgets it toggles a chat panel.</td>\n<td>You want a turn-key experience that doesn&#39;t disturb your existing layout.</td>\n</tr>\n<tr>\n<td><strong>Inline Component</strong></td>\n<td>A panel rendered inside a <code>&lt;div id=&quot;dograh-inline-container&quot;&gt;</code> that you place in your page.</td>\n<td>You want the agent embedded in a specific section (landing-page hero, support tab, etc.).</td>\n</tr>\n<tr>\n<td><strong>Headless</strong></td>\n<td>No UI. Only the audio/chat pipeline plus a JavaScript API on <code>window.DograhWidget</code>.</td>\n<td>You want full control over the UI — your own buttons, design system, framework state, animations.</td>\n</tr>\n</tbody></table>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>These apply to all three modes:</p>\n<ul>\n<li><strong>Voice widgets:</strong> serve your page over <strong>HTTPS</strong> or from <code>http://localhost</code>. Browsers refuse microphone access on plain HTTP origins or <code>file://</code>. Chat widgets have no microphone requirement, though HTTPS is still recommended.</li>\n<li>If you set <strong>Allowed Domains</strong> in the dashboard, include your test origin (e.g. <code>localhost</code>) — otherwise the widget&#39;s requests are rejected. Leave the list empty to allow all domains.</li>\n<li>The embed snippet you copy from the dashboard is a single <code>&lt;script&gt;</code> tag that loads <code>dograh-widget.js</code> <strong>asynchronously</strong>. The widget auto-initializes once it loads and exposes <code>window.DograhWidget</code>. Code that registers callbacks must wait for the widget to be available.</li>\n</ul>\n<h2 id=\"pass-context-to-the-agent\">Pass context to the agent</h2>\n<p>Your page usually knows something about the visitor — their name, plan, cart value, the article they were reading. Pass it along and your agent can use it from the first word.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Context Key names cannot contain dots, whitespace, pipes, or braces because those\ncharacters have structural meaning in template expressions. Invalid entries are\ndropped without preventing the conversation from starting.</p>\n</div>\n\n\n\n\n<p>The snippet you copy from the dashboard carries a <code>data-dograh-context</code> attribute — a JSON object of details about the visitor. The snippet is a small bootstrap function: <code>js</code> is the widget <code>&lt;script&gt;</code> element it creates, and the context is attached to that element before it is added to the page. The relevant part of the generated snippet looks like this (keep the generated <code>js.src</code> value, which contains your embed token):</p>\n<pre><code class=\"language-html\">&lt;script&gt;\n  (function(d, s, id) {\n    var js, fjs = d.getElementsByTagName(s)[0];\n    if (d.getElementById(id)) return;\n    js = d.createElement(s);\n    js.id = id;\n    js.src = &#39;&lt;dashboard-generated widget URL&gt;&#39;;\n    js.setAttribute(&#39;data-dograh-context&#39;, JSON.stringify({\n      page_url: window.location.href,\n      today: new Date().toISOString().slice(0, 10)\n    }));\n    js.async = true;\n    fjs.parentNode.insertBefore(js, fjs);\n  }(document, &#39;script&#39;, &#39;dograh-widget&#39;));\n&lt;/script&gt;\n</code></pre>\n<p>Because it&#39;s built in JavaScript at page load, you can put anything your page knows in it — a logged-in customer&#39;s name, their plan, cart contents. Replace the object inside <code>JSON.stringify(...)</code> in the generated snippet, for example:</p>\n<pre><code class=\"language-js\">{\n  customer_name: currentUser.firstName,\n  plan: currentUser.plan,\n  cart: { items: cart.length, total: cart.total }\n}\n</code></pre>\n<p>Each key is then available in any node prompt as <code>{{initial_context.&lt;name&gt;}}</code>:</p>\n<pre><code class=\"language-text\">Greet {{initial_context.customer_name | there}} and mention their {{initial_context.plan}} plan.\n</code></pre>\n<p>Values can be strings, numbers, booleans, or nested objects. This works for voice and chat widgets alike, and the values are recorded on the conversation so you can see what the agent was given.</p>\n<h3 id=\"update-context-after-the-page-loads\">Update context after the page loads</h3>\n<p>The attribute is fixed at page load, which doesn&#39;t fit a single-page app — the visitor logs in, changes route, or fills a cart long after the snippet ran. For that, call <code>setContext()</code>:</p>\n<pre><code class=\"language-js\">window.DograhWidget.setContext({\n  customer_name: user.firstName,\n  plan: user.plan\n});\n</code></pre>\n<p>Each call merges into the context already collected, so you can add details as they arrive and re-send a name to correct it. <code>getContext()</code> returns the current set.</p>\n<p>Context is read when a conversation starts, so <code>setContext()</code> applies to the <strong>next</strong> conversation — calling it mid-call or mid-chat doesn&#39;t change the one in progress (the widget logs a console warning if you do). For chat widgets, &quot;next&quot; includes the fresh conversation started by <strong>Start new chat</strong> after a session expires.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The widget script loads asynchronously, so <code>window.DograhWidget</code> may not exist yet when your app&#39;s code first runs. Call <code>setContext()</code> from an event that fires after load — a <code>window.load</code> listener, or a user action like clicking your own &quot;Chat with us&quot; button. See <a href=\"#lifecycle-callbacks-all-modes\">Lifecycle callbacks</a> for the same timing rule.</p>\n</div>\n\n\n\n<p>Use whichever fits: <code>data-dograh-context</code> for what the page knows at render, <code>setContext()</code> for what it learns later. They merge, and <code>setContext()</code> wins on a repeated name.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Context comes from the page, so a visitor can both read it and change it before it reaches your agent. Never pass secrets, and don&#39;t let it gate what the agent will do or disclose — treat <code>plan: &quot;pro&quot;</code> as a hint for phrasing, not proof of entitlement. For data the agent must trust, pass an opaque id like <code>customer_id</code> and let PayInt Voice Studio fetch the real details from your API with <a href=\"/studio/docs/voice-agent/pre-call-data-fetch\">Pre-Call Data Fetch</a>.</p>\n</div>\n\n\n\n<p>Limits, applied per conversation: up to 50 variables, 64 characters per name, 2000 characters per value, and 8 KB in total. Anything past a limit is dropped and the conversation still starts. The names <code>provider</code> and <code>runtime_configuration</code> are reserved and ignored.</p>\n<h2 id=\"floating-widget\">Floating Widget</h2>\n<p><img src=\"/agentstudio-docs/images/floating-widget-example.png\" alt=\"Floating widget shown in the corner of a host page\"></p>\n<p>Renders a pill-shaped button anchored to a corner of the page.</p>\n<ul>\n<li><strong>Voice:</strong> clicking the button (microphone icon + text) starts a call; clicking again ends it. The button auto-updates its label and color across the call lifecycle: configured text → &quot;Connecting…&quot; → &quot;End Call&quot; → &quot;Retry&quot; on failure.</li>\n<li><strong>Chat:</strong> clicking the button (chat icon + text) opens a chat panel anchored to the same corner; the agent greets the visitor and the conversation happens in the panel. Clicking the button (or the panel&#39;s ×) closes the panel without ending the conversation — reopening shows the same transcript.</li>\n</ul>\n<p>Configure <strong>Button Text</strong>, <strong>Button Color</strong>, and <strong>Position</strong> (top/bottom + left/right) from the dashboard.</p>\n<p>Every other word a visitor sees is editable too, under a collapsible section in the same dialog, so you can run the widget in your site&#39;s language: <strong>Chat Panel Text</strong> for chat widgets (end chat button, conversation-ended message, start-new-chat and retry buttons, message placeholder, and the send/close screen-reader labels), or <strong>Voice Call Text</strong> for voice widgets. Leave a field blank to keep the English default.</p>\n<p>The host page writes no JavaScript — pasting the embed snippet is the entire integration. If you want to subscribe to call lifecycle events (e.g. analytics), see <a href=\"#lifecycle-callbacks-all-modes\">Lifecycle callbacks</a> below</p>\n<h2 id=\"inline-component\">Inline Component</h2>\n<p><img src=\"/agentstudio-docs/images/inline-widget-example.png\" alt=\"Inline widget rendered inside a page section\"></p>\n<p>Renders a panel inside a <code>&lt;div&gt;</code> you place in your page.</p>\n<ul>\n<li><strong>Voice:</strong> a status panel (status icon + status text + CTA button). Status changes update the panel in place.</li>\n<li><strong>Chat:</strong> a call-to-action screen first; clicking the button replaces it with a chat panel that fills the container. No extra JavaScript is needed.</li>\n</ul>\n<p>Configure <strong>Button Text</strong>, <strong>Button Color</strong>, and <strong>Call to Action Text</strong> from the dashboard, plus the <strong>Chat Panel Text</strong> / <strong>Voice Call Text</strong> section described under <a href=\"#floating-widget\">Floating Widget</a>. Inline voice widgets show the most copy of any mode — a heading and subtext for each of ready, connecting, connected, ended, failed, and lost — so that section carries the full set here.</p>\n<h3 id=\"plain-html\">Plain HTML</h3>\n<p>Place a container <code>&lt;div&gt;</code> where you want the widget to render. The widget auto-attaches to it.</p>\n<pre><code class=\"language-html\">&lt;!-- Paste the dograh embed snippet from the dashboard somewhere on the page --&gt;\n&lt;div id=&quot;dograh-inline-container&quot;&gt;&lt;/div&gt;\n</code></pre>\n<h3 id=\"react\">React</h3>\n<p>Because React mounts after the widget script may have already loaded, integrate via <code>initInline</code> on first mount and <code>refresh</code> on remount. Poll for <code>window.DograhWidget</code> to handle the async script load.</p>\n<pre><code class=\"language-tsx\">import { useEffect } from &#39;react&#39;;\n\ndeclare global {\n  interface Window {\n    DograhWidget?: {\n      initInline: (options: { container: HTMLElement }) =&gt; void;\n      refresh: () =&gt; void;\n      getState: () =&gt; { isInitialized: boolean };\n    };\n  }\n}\n\nexport function Assistant() {\n  useEffect(() =&gt; {\n    let retries = 0;\n    const tryInit = () =&gt; {\n      const container = document.getElementById(&#39;dograh-inline-container&#39;);\n      if (window.DograhWidget &amp;&amp; container) {\n        const { isInitialized } = window.DograhWidget.getState();\n        if (isInitialized) window.DograhWidget.refresh();\n        else window.DograhWidget.initInline({ container });\n      } else if (retries++ &lt; 50) {\n        setTimeout(tryInit, 100);\n      }\n    };\n    tryInit();\n  }, []);\n\n  return &lt;div id=&quot;dograh-inline-container&quot; /&gt;;\n}\n</code></pre>\n<h2 id=\"headless-mode\">Headless Mode</h2>\n<p><img src=\"/agentstudio-docs/images/headless-widget-example.png\" alt=\"Headless widget driven by host-page UI\"></p>\n<p>In Headless mode the widget injects no UI of its own. You render whatever buttons, banners, or chat interfaces you want, and drive the agent through the JavaScript API.</p>\n<h3 id=\"javascript-api-voice-widgets\">JavaScript API (voice widgets)</h3>\n<table>\n<thead>\n<tr>\n<th>Method / Callback</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>window.DograhWidget.start()</code></td>\n<td>Begin a voice call. Must be called from inside a user-gesture handler (e.g. <code>click</code>) so the browser grants microphone access.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.end()</code></td>\n<td>End the active call.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onCallStart(cb)</code></td>\n<td>Fires when <code>start()</code> is invoked (status <code>connecting</code>). No payload.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onCallConnected(cb)</code></td>\n<td>Fires when the WebRTC connection is established. Payload: <code>{ agentId, workflowRunId, token }</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onCallDisconnected(cb)</code></td>\n<td>Fires only if the call had connected, when teardown runs. Payload: <code>{ agentId, workflowRunId, token, durationSeconds }</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onCallEnd(cb)</code></td>\n<td>Fires whenever the call session is torn down (including failed-to-connect attempts). No payload.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onStatusChange(cb)</code></td>\n<td>Fires on every status change. Callback receives <code>(status, text, subtext)</code>. Status values: <code>idle</code>, <code>connecting</code>, <code>connected</code>, <code>failed</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onError(cb)</code></td>\n<td>Fires on errors (mic permission denied, server error, etc.). Callback receives an <code>Error</code> object.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.setContext(vars)</code></td>\n<td>Merge visitor context for the next call — see <a href=\"#pass-context-to-the-agent\">Pass context to the agent</a>. Works in every embed mode, not just headless.</td>\n</tr>\n</tbody></table>\n<p>All <code>on*</code> setters are single-listener — calling the same one again replaces the previous handler.</p>\n<h3 id=\"javascript-api-chat-widgets\">JavaScript API (chat widgets)</h3>\n<table>\n<thead>\n<tr>\n<th>Method / Callback</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>window.DograhWidget.startChat()</code></td>\n<td>Start a conversation. The agent&#39;s greeting arrives via <code>onMessage</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.sendMessage(text)</code></td>\n<td>Send a visitor message. Returns a Promise that resolves with the updated transcript (array of turns), or <code>null</code> if the message couldn&#39;t be delivered.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.getMessages()</code></td>\n<td>Current transcript as an array of turns: <code>{ id, status, user_message, assistant_message }</code>, each message being <code>{ text, created_at }</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onMessage(cb)</code></td>\n<td>Fires once per new agent reply. Callback receives <code>(text, turn)</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onChatStateChange(cb)</code></td>\n<td>Fires on every chat state change. States: <code>idle</code>, <code>starting</code>, <code>ready</code>, <code>waiting</code> (agent is replying), <code>ended</code>, <code>expired</code>, <code>error</code>.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.onError(cb)</code></td>\n<td>Fires on errors. Callback receives an <code>Error</code> object.</td>\n</tr>\n<tr>\n<td><code>window.DograhWidget.setContext(vars)</code></td>\n<td>Merge visitor context for the next conversation — see <a href=\"#pass-context-to-the-agent\">Pass context to the agent</a>. Works in every embed mode, not just headless.</td>\n</tr>\n</tbody></table>\n<p>In chat mode <code>start()</code> aliases <code>startChat()</code> and <code>end()</code> is a no-op teardown (chat sessions need none), so generic snippets keep working. Sends are serialized — <code>sendMessage</code> while a reply is pending (<code>waiting</code>) resolves to <code>null</code>.</p>\n<pre><code class=\"language-html\">&lt;button id=&quot;open-chat&quot;&gt;Chat with us&lt;/button&gt;\n&lt;div id=&quot;transcript&quot;&gt;&lt;/div&gt;\n&lt;input id=&quot;chat-input&quot; /&gt;&lt;button id=&quot;send-btn&quot;&gt;Send&lt;/button&gt;\n\n&lt;script&gt;\n  window.addEventListener(&#39;load&#39;, () =&gt; {\n    window.DograhWidget.onMessage((text) =&gt; {\n      const p = document.createElement(&#39;p&#39;);\n      p.textContent = &#39;Agent: &#39; + text;\n      document.getElementById(&#39;transcript&#39;).appendChild(p);\n    });\n\n    document.getElementById(&#39;open-chat&#39;).addEventListener(&#39;click&#39;, () =&gt; {\n      window.DograhWidget.startChat();\n    });\n\n    document.getElementById(&#39;send-btn&#39;).addEventListener(&#39;click&#39;, async () =&gt; {\n      const input = document.getElementById(&#39;chat-input&#39;);\n      const p = document.createElement(&#39;p&#39;);\n      p.textContent = &#39;You: &#39; + input.value;\n      document.getElementById(&#39;transcript&#39;).appendChild(p);\n      await window.DograhWidget.sendMessage(input.value);\n      input.value = &#39;&#39;;\n    });\n  });\n&lt;/script&gt;\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><strong>About timing.</strong> The widget script loads asynchronously, so <code>window.DograhWidget</code> may not exist at the moment your inline <code>&lt;script&gt;</code> first runs. The examples below assume <code>window.DograhWidget</code> is already available when registration runs. To guarantee that:</p>\n<ul>\n<li><strong>Vanilla JS:</strong> wrap your registration code in <code>window.addEventListener(&#39;load&#39;, () =&gt; { /* register here */ })</code>.</li>\n<li><strong>React:</strong> inside <code>useEffect</code>, register immediately if <code>document.readyState === &#39;complete&#39;</code>, otherwise add a one-time <code>window.load</code> listener that registers on fire.</li>\n<li><strong>Click handlers</strong> that call <code>start()</code> / <code>end()</code> don&#39;t need a guard — by the time a user clicks, the widget has long since loaded.</li>\n</ul>\n</div>\n\n\n\n<h3 id=\"vanilla-js\">Vanilla JS</h3>\n<pre><code class=\"language-html\">&lt;button id=&quot;talk-btn&quot;&gt;Talk to AI&lt;/button&gt;\n\n&lt;script&gt;\n  let callStatus = &#39;idle&#39;;\n  const btn = document.getElementById(&#39;talk-btn&#39;);\n\n  function render() {\n    btn.textContent =\n      callStatus === &#39;connected&#39; ? &#39;End Call&#39;\n      : callStatus === &#39;connecting&#39; ? &#39;Connecting…&#39;\n      : callStatus === &#39;failed&#39; ? &#39;Retry&#39;\n      : &#39;Talk to AI&#39;;\n  }\n\n  window.DograhWidget.onStatusChange((status) =&gt; {\n    callStatus = status;\n    render();\n  });\n\n  window.DograhWidget.onError((err) =&gt; {\n    console.error(&#39;PayInt Voice Studio error:&#39;, err.message);\n  });\n\n  btn.addEventListener(&#39;click&#39;, () =&gt; {\n    if (callStatus === &#39;connected&#39; || callStatus === &#39;connecting&#39;) {\n      window.DograhWidget.end();\n    } else {\n      window.DograhWidget.start();\n    }\n  });\n&lt;/script&gt;\n</code></pre>\n<h3 id=\"react-typescript\">React + TypeScript</h3>\n<pre><code class=\"language-tsx\">import { useEffect, useState } from &#39;react&#39;;\n\ntype CallStatus = &#39;idle&#39; | &#39;connecting&#39; | &#39;connected&#39; | &#39;failed&#39;;\n\ndeclare global {\n  interface Window {\n    DograhWidget: {\n      start: () =&gt; void;\n      end: () =&gt; void;\n      onStatusChange: (cb: (status: CallStatus, text?: string, subtext?: string) =&gt; void) =&gt; void;\n      onError: (cb: (err: Error) =&gt; void) =&gt; void;\n    };\n  }\n}\n\nexport function TalkButton() {\n  const [status, setStatus] = useState&lt;CallStatus&gt;(&#39;idle&#39;);\n\n  useEffect(() =&gt; {\n    window.DograhWidget.onStatusChange((s) =&gt; setStatus(s));\n    window.DograhWidget.onError((err) =&gt; console.error(&#39;PayInt Voice Studio error:&#39;, err.message));\n  }, []);\n\n  const isLive = status === &#39;connected&#39; || status === &#39;connecting&#39;;\n  const label = { idle: &#39;Talk to AI&#39;, connecting: &#39;Connecting…&#39;, connected: &#39;End Call&#39;, failed: &#39;Retry&#39; }[status];\n\n  return (\n    &lt;button onClick={() =&gt; (isLive ? window.DograhWidget.end() : window.DograhWidget.start())}&gt;\n      {label}\n    &lt;/button&gt;\n  );\n}\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><code>start()</code> must run inside a real user-gesture handler (<code>click</code>, <code>touchend</code>, etc.). Browsers refuse to grant microphone access to scripts that request it outside of one — calling <code>start()</code> from a <code>setTimeout</code> or on page load will fail with a permission error.</p>\n</div>\n\n\n\n<h2 id=\"lifecycle-callbacks-all-modes\">Lifecycle callbacks (all modes)</h2>\n<p>The <code>on*</code> callbacks in the <a href=\"#javascript-api-voice-widgets\">Headless JavaScript API</a> work in <strong>all three embed modes</strong>, not just Headless. Use them for analytics or to trigger UI in the host page even when the widget is rendering its own UI (Floating or Inline). The call callbacks (<code>onCall*</code>) fire for voice widgets; for chat widgets use <code>onMessage</code> and <code>onChatStateChange</code> the same way.</p>\n<pre><code class=\"language-js\">window.DograhWidget.onCallConnected(({ agentId, workflowRunId }) =&gt; {\n  analytics.track(&#39;voice_call_started&#39;, { agentId, workflowRunId });\n});\n\nwindow.DograhWidget.onCallDisconnected(({ workflowRunId, durationSeconds }) =&gt; {\n  analytics.track(&#39;voice_call_ended&#39;, { workflowRunId, durationSeconds });\n});\n</code></pre>\n<p><code>onCallConnected</code> and <code>onCallDisconnected</code> only fire when the call actually establishes a media connection — failed-to-connect attempts (e.g. denied mic, network failure) don&#39;t trigger them, so analytics stay clean.</p>\n"},"integrations/telephony/overview":{"title":"Telephony Integration","description":"Connect voice agents with telephony providers for inbound and outbound calls","html":"<h2 id=\"overview\">Overview</h2>\n<p>PayInt Voice Studio AI&#39;s telephony integration system provides a unified interface for connecting with various telephony providers. The same configuration powers both outbound calls (initiated from PayInt Voice Studio) and inbound calls (received on a phone number you own).</p>\n<p>PayInt Voice Studio does not sell phone numbers or minutes. The phone service is always\nyours — there are two ways to bring it.</p>\n<h2 id=\"two-ways-to-connect\">Two ways to connect</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><strong class=\"as-card-title\">Use a telephony provider</strong>\n\n\n<p>Open an account with Twilio, Plivo, Telnyx, Vonage or Vobiz, paste the\ncredentials into PayInt Voice Studio, and call using their numbers. <strong>Start here if you\ndon&#39;t already run phone infrastructure.</strong> Pick your provider from the list\nbelow.</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/dograh-sip\">Bring your own SIP</a>\n\n\n<p>Already have a SIP trunk or a PBX? Point it at PayInt Voice Studio and keep your existing\ncarrier and numbers. Every organization is provisioned with a\n<strong>PayInt Voice Studio Cloudonix SIP</strong> connection for exactly this; self-hosted\ndeployments can use <a href=\"/studio/docs/integrations/telephony/asterisk-ari\">Asterisk ARI</a>\ninstead.</p>\n</div>\n\n\n\n\n</div>\n\n\n\n<h2 id=\"supported-providers\">Supported Providers</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/twilio\">Twilio</a>\n\n\n<p>Industry-leading cloud communications platform with global reach</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/vonage\">Vonage</a>\n\n\n<p>High-quality voice with 16kHz audio and excellent international coverage</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/plivo\">Plivo</a>\n\n\n<p>Cloud communications platform with programmable voice and global PSTN reach</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/cloudonix\">Cloudonix</a>\n\n\n<p>SIP-based telephony with flexible trunk configuration</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/exotel\">Exotel</a>\n\n\n<p>India-focused cloud telephony with Connect Voice AI streaming</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/vobiz\">Vobiz</a>\n\n\n<p>Cloud-based telephony with global reach and competitive pricing</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/asterisk-ari\">Asterisk ARI</a>\n\n\n<p>Connect to your own Asterisk PBX via the Asterisk REST Interface</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/custom\">Custom Provider</a>\n\n\n<p>Build your own telephony provider integration</p>\n</div>\n\n\n\n\n</div>\n\n\n\n<h2 id=\"configuration\">Configuration</h2>\n<p>All telephony providers are configured from a single page in PayInt Voice Studio:</p>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select your provider</li>\n<li>Enter your credentials and save</li>\n<li>Open the new configuration and add at least one <strong>phone number</strong></li>\n<li>(Optional) Assign an <strong>Inbound workflow</strong> to a phone number to enable inbound calling</li>\n</ol>\n<p>A single org can hold multiple configurations (for example, separate Twilio sub-accounts) and multiple phone numbers per configuration. Mark one configuration as the <strong>default outbound</strong> to use it for test calls and campaigns by default.</p>\n<h2 id=\"common-features\">Common Features</h2>\n<p>The telephony integration in PayInt Voice Studio AI supports:</p>\n<ul>\n<li><strong>Outbound Calls</strong>: Initiate calls to any phone number from a workflow or campaign</li>\n<li><strong>Inbound Calls</strong>: Route incoming calls to the right voice agent — see the <a href=\"/studio/docs/integrations/telephony/inbound\">Inbound Calling guide</a></li>\n<li><strong>Call Transfer</strong>: Transfer an in-progress call to a human or another number (provider-dependent)</li>\n<li><strong>Call Status Tracking</strong>: Monitor call lifecycle events (initiated, ringing, answered, completed, failed)</li>\n<li><strong>WebSocket Audio Streaming</strong>: Real-time, bidirectional audio between caller and agent</li>\n<li><strong>Webhook Signature Verification</strong>: Inbound webhooks are verified against the matched configuration&#39;s credentials</li>\n</ul>\n<h2 id=\"inbound-calling\">Inbound Calling</h2>\n<p>Inbound calls use a <strong>single org-wide webhook URL</strong>:</p>\n<pre><code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run\n</code></pre>\n<p>PayInt Voice Studio resolves the org from the webhook&#39;s account credentials and the agent from the called number&#39;s <strong>Inbound workflow</strong> assignment. See <a href=\"/studio/docs/integrations/telephony/inbound\">Inbound Calling</a> for the full setup.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Calls not connecting</summary>\n\n\n<ul>\n<li>Verify credentials are correctly configured</li>\n<li>Check phone number format (<a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> — the international standard: a <code>+</code>, then country code, then the number, no spaces or dashes, e.g. <code>+1234567890</code>)</li>\n<li>Ensure webhook URLs are publicly accessible</li>\n<li>Review provider-specific error logs</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Audio quality issues</summary>\n\n\n<ul>\n<li>Check network bandwidth and latency</li>\n<li>Verify audio codec compatibility</li>\n<li>Review WebSocket connection stability</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Webhook signature validation failing</summary>\n\n\n<ul>\n<li>Confirm auth tokens / API secrets match between provider and PayInt Voice Studio configuration</li>\n<li>Verify webhook URL matches exactly (including the <code>/inbound/run</code> path)</li>\n<li>Check for proxy or load balancer modifications</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"next-steps\">Next Steps</h2>\n<ul>\n<li><a href=\"/studio/docs/integrations/telephony/twilio\">Set up your first telephony provider</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/inbound\">Configure inbound calling</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/custom\">Build a custom provider integration</a></li>\n</ul>\n"},"integrations/telephony/inbound":{"title":"Inbound Calling","description":"Configure inbound calling for Twilio, Vonage, Plivo, Telnyx, Cloudonix, Exotel, Vobiz, and Asterisk ARI providers in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>PayInt Voice Studio AI supports inbound calling across all supported telephony providers. When someone calls your configured phone number, your voice agent automatically answers and handles the interaction. Inbound and outbound calls share the same telephony configuration.</p>\n<h2 id=\"supported-providers-for-inbound-calls\">Supported Providers for Inbound Calls</h2>\n<div class=\"as-cards\">\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/twilio\">Twilio</a>\n\n\n<p>Industry-leading platform with robust inbound call handling</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/vonage\">Vonage</a>\n\n\n<p>Application-level inbound routing with high-quality 16kHz audio</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/plivo\">Plivo</a>\n\n\n<p>Application-level inbound routing on Plivo&#39;s global voice cloud</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/telnyx\">Telnyx</a>\n\n\n<p>Application-level inbound via Telnyx Call Control API</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/cloudonix\">Cloudonix</a>\n\n\n<p>SIP-based inbound calling with flexible trunk configuration</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/exotel\">Exotel</a>\n\n\n<p>Connect Voice AI inbound with bidirectional media streaming</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/vobiz\">Vobiz</a>\n\n\n<p>Cloud-based telephony with global reach and competitive pricing</p>\n</div>\n\n\n\n\n<div class=\"as-card\"><a class=\"as-card-title\" href=\"/studio/docs/integrations/telephony/asterisk-ari\">Asterisk ARI</a>\n\n\n<p>Connect to your own Asterisk PBX via the Asterisk REST Interface</p>\n</div>\n\n\n\n\n</div>\n\n\n\n<h2 id=\"how-inbound-calling-works\">How Inbound Calling Works</h2>\n<p>PayInt Voice Studio exposes a <strong>single inbound webhook URL</strong> for the whole org:</p>\n<pre><code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run\n</code></pre>\n<p>When a call arrives:</p>\n<ol>\n<li><strong>Call Received</strong>: Your telephony provider receives the incoming call</li>\n<li><strong>Webhook Triggered</strong>: The provider sends a webhook to <code>/inbound/run</code></li>\n<li><strong>Org &amp; Agent Resolved</strong>: PayInt Voice Studio identifies the org from the webhook&#39;s account credentials, then looks up which agent should answer based on the called number&#39;s <strong>Inbound workflow</strong> assignment in your telephony configuration</li>\n<li><strong>Audio Streaming</strong>: Real-time audio streams between caller and agent via WebSocket</li>\n<li><strong>Conversation Handled</strong>: Your voice agent manages the entire conversation</li>\n</ol>\n<p>You don&#39;t construct per-workflow URLs anymore — the routing is done by the called number&#39;s inbound workflow assignment in PayInt Voice Studio.</p>\n<h2 id=\"configuration-in-payint-voice-studio-ai\">Configuration in PayInt Voice Studio AI</h2>\n<p>Setting up inbound calling takes three steps:</p>\n<h3 id=\"step-1-configure-telephony-provider\">Step 1: Configure Telephony Provider</h3>\n<p>The telephony configuration for inbound calling is <strong>identical</strong> to outbound calling. Add (or open) a configuration at <strong>/telephony-configurations</strong> following your provider&#39;s documentation:</p>\n<ul>\n<li><a href=\"/studio/docs/integrations/telephony/twilio#configuration\">Twilio Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/vonage#configuration\">Vonage Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/plivo#configuration\">Plivo Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/telnyx#configuration\">Telnyx Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/cloudonix#configuration\">Cloudonix Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/exotel#configuration\">Exotel Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/vobiz#configuration\">Vobiz Configuration</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/asterisk-ari#part-2-create-the-configuration-in-dograh\">Asterisk ARI Configuration</a></li>\n</ul>\n<h3 id=\"step-2-assign-an-inbound-workflow-to-the-phone-number\">Step 2: Assign an Inbound Workflow to the Phone Number</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<p>This is what tells PayInt Voice Studio which agent to run when a call comes in for that number.</p>\n<h3 id=\"step-3-point-your-provider-at-inboundrun\">Step 3: Point Your Provider at <code>/inbound/run</code></h3>\n<p>Each provider needs its inbound webhook (Twilio Voice URL / Vonage Answer URL / etc.) set to the global PayInt Voice Studio endpoint:</p>\n<pre><code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If you&#39;re self-hosting PayInt Voice Studio, replace <code>api.PayInt Voice Studio</code> with your own domain.</p>\n</div>\n\n\n\n<p>The exact place to set this varies by provider — follow the provider-specific instructions:</p>\n<ul>\n<li><a href=\"/studio/docs/integrations/telephony/twilio#inbound-calling-setup\">Twilio Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/vonage#inbound-calling-setup\">Vonage Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/plivo#inbound-calling-setup\">Plivo Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/telnyx#inbound-calling-setup\">Telnyx Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/cloudonix#inbound-calling-setup\">Cloudonix Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/exotel#inbound-calling-setup\">Exotel Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/vobiz#inbound-calling-setup\">Vobiz Inbound Setup</a></li>\n<li><a href=\"/studio/docs/integrations/telephony/asterisk-ari#inbound-calling\">Asterisk ARI Inbound Setup</a></li>\n</ul>\n<h2 id=\"testing-inbound-calls\">Testing Inbound Calls</h2>\n<p>After completing both the PayInt Voice Studio AI configuration and provider-specific setup:</p>\n<ol>\n<li><strong>Test Call</strong>: Call your configured phone number from another phone</li>\n<li><strong>Verify Connection</strong>: Ensure your voice agent answers and responds appropriately</li>\n<li><strong>Check Logs</strong>: Monitor call logs in both PayInt Voice Studio AI and your provider&#39;s dashboard</li>\n<li><strong>Audio Quality</strong>: Verify clear two-way audio communication</li>\n</ol>\n<h2 id=\"troubleshooting-common-issues\">Troubleshooting Common Issues</h2>\n<details class=\"as-accordion\"><summary>Calls go to voicemail instead of agent</summary>\n\n\n<ul>\n<li>Verify webhook URLs are correctly configured at your provider</li>\n<li>Check that webhook endpoints are publicly accessible</li>\n<li>Ensure phone numbers are properly linked to your application</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Agent doesn't respond to incoming calls</summary>\n\n\n<ul>\n<li>Confirm your voice agent workflow is properly configured</li>\n<li>Check webhook signature validation is working</li>\n<li>Verify WebSocket connections are established successfully</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Provider mismatch error</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Configuration error: This phone number is configured for a different telephony provider&quot;</li>\n<li><strong>Solution</strong>: Check your dashboard settings and update your webhook URL configuration to match the correct provider</li>\n<li>Ensure the phone number is configured for the same provider in both PayInt Voice Studio AI and your telephony console</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Workflow not found error</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Workflow not found&quot;</li>\n<li><strong>Solution</strong>: Open the called number in <strong>/telephony-configurations</strong> and confirm an <strong>Inbound workflow</strong> is assigned. Without an assignment, PayInt Voice Studio has no agent to route the call to.</li>\n<li>Verify the assigned workflow still exists in your dashboard</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Account validation failed</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Authentication error: Account credentials do not match&quot;</li>\n<li><strong>Solution</strong>: Verify your account SID/credentials configuration in PayInt Voice Studio AI matches your telephony provider settings</li>\n<li>Re-enter your provider credentials in the telephony configuration page</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Phone number not configured</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Phone number not configured: This number is not set up for inbound calls&quot;</li>\n<li><strong>Solution</strong>: Add this phone number under <strong>Phone numbers</strong> in your telephony configuration at <strong>/telephony-configurations</strong></li>\n<li>Ensure the phone number is also linked to your provider account</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Signature validation failed</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Security error: Webhook signature validation failed&quot;</li>\n<li><strong>Solution</strong>: Verify your auth token configuration in PayInt Voice Studio AI matches your provider settings</li>\n<li>Ensure webhook requests are coming from your telephony provider&#39;s verified IP ranges</li>\n<li>Check that your webhook URL is accessible and responding correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Quota exceeded error</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Service temporarily unavailable: Your account has exceeded usage limits&quot;</li>\n<li><strong>Solution</strong>: Contact your administrator or upgrade your plan to continue receiving calls</li>\n<li>Check your provider account billing and usage limits</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>General authentication failed</summary>\n\n\n<ul>\n<li><strong>Error</strong>: &quot;Authentication failed&quot;</li>\n<li><strong>Solution</strong>: Check your webhook URL configuration and ensure your telephony provider settings match your dashboard configuration</li>\n<li>Verify all credentials are entered correctly and haven&#39;t expired</li>\n<li>Test the webhook URL is publicly accessible from your provider&#39;s servers</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Poor audio quality on inbound calls</summary>\n\n\n<ul>\n<li>Check network bandwidth and latency from provider to PayInt Voice Studio AI</li>\n<li>Verify audio pipeline configuration matches provider requirements</li>\n<li>Review WebSocket connection stability</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li><strong>Single Configuration</strong>: Use the same telephony configuration for both inbound and outbound calls</li>\n<li><strong>Testing</strong>: Always test inbound calling after configuration changes</li>\n<li><strong>Monitoring</strong>: Monitor both PayInt Voice Studio AI logs and provider dashboards for call analytics</li>\n<li><strong>Security</strong>: Ensure webhook signature verification is enabled for security</li>\n</ul>\n<h2 id=\"next-steps\">Next Steps</h2>\n<ol>\n<li>Choose your telephony provider and complete the basic configuration</li>\n<li>Follow the provider-specific setup instructions</li>\n<li>Assign an inbound workflow to each phone number that should accept calls</li>\n<li>Test your inbound calling setup</li>\n</ol>\n"},"integrations/telephony/twilio":{"title":"Twilio Integration","description":"Configure Twilio for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Twilio is a cloud communications platform that enables voice calling, messaging, and video capabilities. PayInt Voice Studio AI&#39;s Twilio integration provides seamless connectivity for your voice agents.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Twilio integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://www.twilio.com/try-twilio\">Twilio account</a></li>\n<li>Account SID and Auth Token from your Twilio Console</li>\n<li>At least one Twilio phone number</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-get-twilio-credentials\">Step 1: Get Twilio Credentials</h3>\n<ol>\n<li>Log in to your <a href=\"https://console.twilio.com/\">Twilio Console</a></li>\n<li>Find your <strong>Account SID</strong> and <strong>Auth Token</strong> on the dashboard</li>\n<li>Navigate to <strong>Phone Numbers</strong> → <strong>Manage</strong> → <strong>Active Numbers</strong></li>\n<li>Copy your phone number(s)</li>\n</ol>\n<h3 id=\"step-2-configure-in-payint-voice-studio-ai\">Step 2: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Twilio</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>Account SID</li>\n<li>Auth Token</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (with country code, in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format — <code>+</code> then country code then number, no spaces or dashes, e.g. <code>+1234567890</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<h3 id=\"step-3-test-your-configuration\">Step 3: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Inbound routing is driven by the phone number itself — there is a <strong>single webhook URL</strong> for the entire org, and PayInt Voice Studio resolves which agent to run from the called number&#39;s assigned inbound workflow. <strong>When you save an inbound workflow on a phone number, PayInt Voice Studio automatically pushes the webhook URL to that number&#39;s <code>VoiceUrl</code> in your Twilio account</strong> (provided the credentials are correct and the number belongs to that account).</p>\n<h3 id=\"step-1-assign-an-inbound-workflow-to-the-phone-number\">Step 1: Assign an Inbound Workflow to the Phone Number</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Twilio configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-2-verify-the-webhook-url-in-twilio-console\">Step 2: Verify the Webhook URL in Twilio Console</h3>\n<ol>\n<li>Go to <strong>Phone Numbers</strong> → <strong>Manage</strong> → <strong>Active Numbers</strong> in Twilio Console</li>\n<li>Click the phone number you assigned an inbound workflow to in Step 1</li>\n<li>In the <strong>Voice Configuration</strong> section, confirm:<ul>\n<li><strong>Webhook</strong> is set to: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>HTTP Method</strong> is <code>POST</code></li>\n</ul>\n</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushed this URL automatically when you saved the inbound workflow\n   in Step 1. If the field is empty, shows a different URL, or PayInt Voice Studio\n   surfaced a sync warning, the auto-push failed — most often because the\n   credentials are incorrect or the number isn&#39;t owned by this Twilio\n   account. Paste the URL into the field yourself, set the method to\n   <code>POST</code>, and click <strong>Save Configuration</strong>. On self-hosted PayInt Voice Studio,\n   replace <code>api.PayInt Voice Studio</code> with your backend domain.</p>\n</div>\n\n\n\n<p>   <img\n     className=\"block dark:hidden\"\n     src=\"/agentstudio-docs/images/twilio-inbound-config.png\"\n     alt=\"Twilio phone number voice configuration showing webhook URL setup for inbound calls\"\n   />\n   <img\n     className=\"hidden dark:block\"\n     src=\"/agentstudio-docs/images/twilio-inbound-config.png\"\n     alt=\"Twilio phone number voice configuration showing webhook URL setup for inbound calls\"\n   /></p>\n<h3 id=\"step-3-verify-setup\">Step 3: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify any firewalls allow Twilio&#39;s IP ranges</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured Twilio phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Twilio Console</li>\n</ol>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<p>Ensure phone numbers include country code in E.164 format: <code>+1234567890</code></p>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify Account SID and Auth Token are correct - Check for extra spaces in\ncredentials - Ensure credentials haven&#39;t been regenerated in Twilio Console</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Webhook signature validation failing</summary>\n\n\n<ul>\n<li>Confirm your Auth Token matches exactly - Verify the webhook URL matches\nwhat Twilio sends - Check if you&#39;re behind a proxy that modifies requests</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established - Check firewall rules for\nWebSocket traffic - Ensure audio pipeline is configured correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls go to voicemail</summary>\n\n\n<ul>\n<li>Verify the Twilio number&#39;s webhook is set to\n<code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code> - Ensure the webhook\nURL is publicly accessible from the internet - Confirm the called number\nexists in your PayInt Voice Studio telephony configuration and has an <strong>Inbound\nworkflow</strong> assigned - Verify PayInt Voice Studio AI instance is running and responding</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in\n/telephony-configurations - Check webhook signature validation is working\n(Auth Token in PayInt Voice Studio matches Twilio Console) - Verify WebSocket connection\nestablishes successfully - Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Test your configuration with a single call before running campaigns</li>\n<li>Monitor Twilio Console for usage and billing</li>\n</ul>\n"},"integrations/telephony/vonage":{"title":"Vonage Integration","description":"Configure Vonage (Nexmo) for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Vonage (formerly Nexmo) is a cloud communications platform that provides global voice, messaging, and video capabilities. PayInt Voice Studio AI&#39;s Vonage integration enables high-quality voice interactions with your agents using Vonage&#39;s robust infrastructure.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Vonage integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://www.vonage.com/communications-apis/\">Vonage account</a></li>\n<li>Vonage Application with Voice capability enabled</li>\n<li>Application ID and Private Key from your Vonage Dashboard</li>\n<li>API Key and API Secret from your Vonage Dashboard</li>\n<li>Signature Secret from your Vonage Dashboard</li>\n<li>At least one Vonage phone number linked to the application</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-create-vonage-application\">Step 1: Create Vonage Application</h3>\n<ol>\n<li>Log in to your <a href=\"https://dashboard.nexmo.com/\">Vonage Dashboard</a></li>\n<li>Navigate to <strong>Applications</strong> → <strong>Create a new application</strong></li>\n<li>Enable <strong>Voice</strong> capability</li>\n<li>Generate a private key (save this securely - you&#39;ll need it)</li>\n<li>Note your <strong>Application ID</strong></li>\n</ol>\n<h3 id=\"step-2-get-api-credentials\">Step 2: Get API Credentials</h3>\n<ol>\n<li>Find your <strong>API Key</strong> and <strong>API Secret</strong> in the dashboard under <strong>API Settings</strong></li>\n<li>Copy your <strong>Signature Secret</strong> from the same Vonage account. PayInt Voice Studio uses it\nto verify <a href=\"https://developer.vonage.com/en/getting-started/concepts/webhooks\">Vonage signed webhooks</a>.</li>\n<li>Navigate to <strong>Numbers</strong> → <strong>Your Numbers</strong></li>\n<li>Copy your phone number(s)</li>\n<li>Link each inbound number to your Voice application</li>\n</ol>\n<h3 id=\"step-3-configure-in-payint-voice-studio-ai\">Step 3: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Vonage</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>Application ID</li>\n<li>Private Key (entire key including BEGIN/END lines)</li>\n<li>API Key</li>\n<li>API Secret</li>\n<li>Signature Secret</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (without <code>+</code> prefix, e.g. <code>14155551234</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<h3 id=\"step-4-test-your-configuration\">Step 4: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Vonage routes inbound Voice API calls through a Voice application. The application owns the Answer URL and Event URL, and the phone number must be linked to that application. PayInt Voice Studio routes the call to the right agent based on the called number&#39;s inbound workflow assignment inside PayInt Voice Studio.</p>\n<p>When you save an inbound workflow on a phone number, PayInt Voice Studio updates the configured Vonage application&#39;s Voice webhooks and enables signed callbacks, provided the API Key, API Secret, and Application ID are correct and a Signature Secret is configured.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Linking the phone number to the Voice application is required. If the number\nis not linked, Vonage will not call PayInt Voice Studio&#39;s Answer URL, and you may hear a\nbusy or disconnected tone without seeing any PayInt Voice Studio application logs.</p>\n</div>\n\n\n\n<h3 id=\"step-1-link-phone-numbers-to-your-vonage-application\">Step 1: Link Phone Numbers to Your Vonage Application</h3>\n<ol>\n<li>Open the <a href=\"https://dashboard.nexmo.com/\">Vonage Dashboard</a></li>\n<li>Go to <strong>Numbers</strong> → <strong>Your Numbers</strong></li>\n<li>Open each number you want to use for inbound calls</li>\n<li>Set the number&#39;s Voice application to the same Vonage Application whose ID you configured in PayInt Voice Studio</li>\n</ol>\n<p>Vonage&#39;s Numbers API describes this as the number&#39;s <code>app_id</code>: the application that handles inbound traffic to that number. See the <a href=\"https://developer.vonage.com/en/api/numbers\">Numbers API reference</a>.</p>\n<h3 id=\"step-2-assign-an-inbound-workflow-to-the-phone-number-in-payint-voice-studio\">Step 2: Assign an Inbound Workflow to the Phone Number in PayInt Voice Studio</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Vonage configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-3-verify-the-answer-url-on-the-vonage-application\">Step 3: Verify the Answer URL on the Vonage Application</h3>\n<ol>\n<li>Open your Vonage Application in the <a href=\"https://dashboard.nexmo.com/\">Vonage Dashboard</a></li>\n<li>Under <strong>Capabilities</strong> → <strong>Voice</strong>, confirm:<ul>\n<li><strong>Answer URL</strong> is set to: <code>https://&lt;your-backend-domain&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>HTTP Method</strong> is <code>POST</code></li>\n<li><strong>Event URL</strong> is set to: <code>https://&lt;your-backend-domain&gt;/api/v1/telephony/vonage/events</code></li>\n<li><strong>Event Method</strong> is <code>POST</code></li>\n<li><strong>Signed callbacks</strong> are enabled</li>\n</ul>\n</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushes these settings automatically when you save the inbound\n   workflow in Step 2. If the fields are empty, show a different URL, or\n   PayInt Voice Studio surfaced a sync warning on save, check the API Key, API Secret,\n   Application ID, and Signature Secret in PayInt Voice Studio, then save the inbound\n   workflow again. On self-hosted PayInt Voice Studio, the backend domain must be publicly\n   reachable by Vonage.</p>\n</div>\n\n\n\n<h3 id=\"step-4-verify-setup\">Step 4: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify your public backend URL is reachable from the internet</li>\n<li>Use the <a href=\"https://dashboard.nexmo.com/logs\">Vonage logs</a> or Voice Inspector to confirm Vonage is sending the Answer webhook to PayInt Voice Studio</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured Vonage phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Vonage Dashboard</li>\n</ol>\n<h2 id=\"audio-quality-optimization\">Audio Quality Optimization</h2>\n<p>Vonage uses higher quality audio (16kHz) which provides:</p>\n<ul>\n<li>Clearer voice reproduction</li>\n<li>Better speech recognition accuracy</li>\n<li>More natural-sounding TTS output</li>\n<li>Reduced transcription errors</li>\n</ul>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Voice application capabilities error</summary>\n\n\n<ul>\n<li>Ensure &quot;Voice&quot; is enabled in your Vonage application</li>\n<li>Verify the application ID matches your configuration</li>\n<li>Check that your phone numbers are linked to the application</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>JWT authentication failed</summary>\n\n\n<ul>\n<li>Verify your private key is complete (including BEGIN/END lines)</li>\n<li>Check the Application ID is correct</li>\n<li>Ensure the private key hasn&#39;t been regenerated in Vonage Dashboard</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Signed webhook validation failed</summary>\n\n\n<ul>\n<li>Verify the Signature Secret in PayInt Voice Studio matches the Vonage account&#39;s signature secret</li>\n<li>Ensure signed callbacks are enabled on the Vonage Voice application</li>\n<li>Check that the webhook request includes an <code>Authorization</code> header</li>\n<li>Confirm the application belongs to the same API Key saved in PayInt Voice Studio</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<ul>\n<li>Remove the &#39;+&#39; prefix for Vonage (use <code>14155551234</code> not <code>+14155551234</code>)</li>\n<li>Ensure numbers are in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format (country code + number, no spaces or dashes) without the &#39;+&#39;</li>\n<li>Verify numbers are active in your Vonage account</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established</li>\n<li>Check audio pipeline is configured for 16kHz PCM</li>\n<li>Monitor WebSocket for binary audio frames</li>\n<li>Review <a href=\"/studio/docs/configurations/interruption#what-is-vad\">VAD (Voice Activity Detection)</a> behavior — this is what detects when the caller starts and stops speaking</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Calls disconnecting early</summary>\n\n\n<ul>\n<li>Check WebSocket heartbeat/ping-pong frames</li>\n<li>Verify no timeout in load balancer/proxy</li>\n<li>Monitor for audio pipeline errors</li>\n<li>Review max call duration settings</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls not reaching voice agent</summary>\n\n\n<ul>\n<li>Verify the Vonage application&#39;s Answer URL is set to <code>https://&lt;your-backend-domain&gt;/api/v1/telephony/inbound/run</code></li>\n<li>Ensure the Answer URL is publicly accessible</li>\n<li>Confirm the called number is linked to the correct Vonage application</li>\n<li>Confirm the called number exists in your PayInt Voice Studio telephony configuration and has an <strong>Inbound workflow</strong> assigned</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound call gives busy tone and PayInt Voice Studio shows no logs</summary>\n\n\n<ul>\n<li>Confirm the Vonage number is linked to the Voice application configured in PayInt Voice Studio</li>\n<li>Confirm the Voice application has the PayInt Voice Studio Answer URL set with method <code>POST</code></li>\n<li>Confirm your backend domain is public; Vonage cannot call <code>localhost</code></li>\n<li>Check Vonage logs for Answer URL delivery errors before debugging PayInt Voice Studio</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in /telephony-configurations</li>\n<li>Verify API Key matches the one stored in your PayInt Voice Studio telephony configuration (used to identify the org from the inbound webhook)</li>\n<li>Verify WebSocket connection establishes successfully</li>\n<li>Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li><strong>Security</strong>: Private keys are stored securely in the database</li>\n<li><strong>Signed callbacks</strong>: Keep Vonage signed callbacks enabled and keep the Signature Secret in PayInt Voice Studio up to date</li>\n<li><strong>Testing</strong>: Use Vonage Voice Inspector for debugging call issues</li>\n<li><strong>Numbers</strong>: Configure multiple numbers for redundancy</li>\n<li><strong>Monitoring</strong>: Set up alerts in Vonage Dashboard for failures</li>\n<li><strong>Cost Management</strong>: Monitor usage to control costs</li>\n</ul>\n<h2 id=\"cost-considerations\">Cost Considerations</h2>\n<p>Vonage pricing includes:</p>\n<ul>\n<li>Per-minute charges for calls</li>\n<li>Phone number rental fees</li>\n<li>Optional features (recording, transcription)</li>\n</ul>\n<p>Check <a href=\"https://www.vonage.com/communications-apis/voice/pricing/\">Vonage pricing</a> for current rates.</p>\n<h2 id=\"next-steps\">Next Steps</h2>\n<ul>\n<li>Test your Vonage integration with a simple workflow</li>\n<li>Review <a href=\"/studio/docs/configurations/interruption\">VAD and interruption behavior</a> for your workflow&#39;s nodes</li>\n<li>Set up monitoring and alerts</li>\n<li>Explore advanced features like call recording</li>\n</ul>\n"},"integrations/telephony/plivo":{"title":"Plivo Integration","description":"Configure Plivo for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Plivo is a cloud communications platform that provides global voice and messaging APIs. PayInt Voice Studio AI&#39;s Plivo integration uses Plivo&#39;s XML and WebSocket streaming to power your voice agents.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Plivo integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://www.plivo.com/\">Plivo account</a></li>\n<li>Auth ID and Auth Token from your Plivo Console</li>\n<li>A Plivo <strong>Application</strong> with Voice capability (optional — leave the field blank in PayInt Voice Studio and we&#39;ll auto-create one for you on save, with the <code>answer_url</code> pre-set)</li>\n<li>At least one Plivo phone number</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-get-plivo-credentials\">Step 1: Get Plivo Credentials</h3>\n<ol>\n<li>Log in to your <a href=\"https://console.plivo.com/\">Plivo Console</a></li>\n<li>Find your <strong>Auth ID</strong> and <strong>Auth Token</strong> on the dashboard</li>\n<li>(Optional) Navigate to <strong>Voice</strong> → <strong>Applications</strong> and create (or open) the application you&#39;ll use with PayInt Voice Studio, then copy its <strong>Application ID</strong> (a UUID). Skip this if you want PayInt Voice Studio to auto-create the application for you on save.</li>\n<li>Navigate to <strong>Phone Numbers</strong> → <strong>Your Numbers</strong> and copy the numbers you plan to use</li>\n</ol>\n<h3 id=\"step-2-configure-in-payint-voice-studio-ai\">Step 2: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Plivo</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>Auth ID</li>\n<li>Auth Token</li>\n<li>Application ID — <em>optional</em>. Leave blank and PayInt Voice Studio will auto-create a Plivo Application on save (with the <code>answer_url</code> already configured) and store its Application ID on this configuration.</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (with country code, in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format — <code>+</code> then country code then number, no spaces or dashes, e.g. <code>+1234567890</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   If PayInt Voice Studio auto-created the Plivo Application for you, you still need to\n   link your Plivo numbers to that application in the Plivo Console under\n   <strong>Phone Numbers</strong> → <strong>Your Numbers</strong>. The auto-created application is\n   named <code>dograh-&lt;random&gt;</code> — its Application ID is shown on the saved\n   configuration.</p>\n</div>\n\n\n\n<h3 id=\"step-3-test-your-configuration\">Step 3: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Plivo numbers don&#39;t carry an <code>answer_url</code> directly — the URL lives on a Plivo <strong>Application</strong>, and each number is linked to one application. <strong>When you save an inbound workflow on a phone number, PayInt Voice Studio automatically pushes the webhook URL to your Plivo Application&#39;s <code>answer_url</code></strong> (provided the credentials are correct). You don&#39;t need to set the webhook by hand. If PayInt Voice Studio auto-created the application during configuration save, the <code>answer_url</code> is already set and this push is a no-op.</p>\n<h3 id=\"step-1-link-the-phone-number-to-your-plivo-application\">Step 1: Link the Phone Number to Your Plivo Application</h3>\n<ol>\n<li>Go to <strong>Phone Numbers</strong> → <strong>Your Numbers</strong> in the <a href=\"https://console.plivo.com/\">Plivo Console</a></li>\n<li>Open the number you want to use for inbound calls</li>\n<li>Set its <strong>Application</strong> to the same application whose ID you configured in PayInt Voice Studio</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-2-assign-an-inbound-workflow-to-the-phone-number-in-payint-voice-studio\">Step 2: Assign an Inbound Workflow to the Phone Number in PayInt Voice Studio</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Plivo configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-3-verify-the-answer-url-on-the-plivo-application\">Step 3: Verify the Answer URL on the Plivo Application</h3>\n<ol>\n<li>Open your Plivo Application in the <a href=\"https://console.plivo.com/\">Plivo Console</a> under <strong>Voice</strong> → <strong>Applications</strong></li>\n<li>Confirm:<ul>\n<li><strong>Answer URL</strong> is set to: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>Answer Method</strong> is <code>POST</code></li>\n</ul>\n</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushed this URL automatically when you saved the inbound workflow\n   in Step 2. The same URL is shared across every number linked to that\n   application — PayInt Voice Studio routes each inbound call to the right agent based\n   on the called number&#39;s inbound workflow assignment. If the field is\n   empty, shows a different URL, or PayInt Voice Studio surfaced a sync warning on\n   save, the auto-push failed — most often because the Auth ID/Token or\n   Application ID in PayInt Voice Studio is incorrect. Paste the URL into the field\n   yourself, set the method to <code>POST</code>, and save the application. On\n   self-hosted PayInt Voice Studio, replace <code>api.PayInt Voice Studio</code> with your backend domain.</p>\n</div>\n\n\n\n<h3 id=\"step-4-verify-setup\">Step 4: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify any firewalls allow Plivo&#39;s IP ranges</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured Plivo phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Plivo Console</li>\n</ol>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<p>Ensure phone numbers include country code in E.164 format: <code>+1234567890</code></p>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify Auth ID and Auth Token are correct - Check for extra spaces in\ncredentials - Ensure credentials haven&#39;t been regenerated in Plivo Console</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Webhook signature validation failing</summary>\n\n\n<ul>\n<li>Confirm your Auth Token matches exactly - Verify the webhook URL matches\nwhat Plivo sends - Check if you&#39;re behind a proxy that modifies requests</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established - Check firewall rules for\nWebSocket traffic - Ensure audio pipeline is configured correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls go to voicemail or aren't answered</summary>\n\n\n<ul>\n<li>Verify the phone number is linked to the same Plivo Application whose\nID you configured in PayInt Voice Studio - Confirm the called number exists in your\nPayInt Voice Studio telephony configuration and has an <strong>Inbound workflow</strong> assigned</li>\n<li>After assigning the inbound workflow, confirm PayInt Voice Studio successfully\nupdated the application&#39;s <code>answer_url</code> (no warning shown on save) -\nVerify PayInt Voice Studio AI instance is running and responding</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in\n/telephony-configurations - Check webhook signature validation is working\n(Auth Token in PayInt Voice Studio matches Plivo Console) - Verify WebSocket connection\nestablishes successfully - Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Test your configuration with a single call before running campaigns</li>\n<li>Monitor Plivo Console for usage and billing</li>\n<li>Use a dedicated Plivo Application for PayInt Voice Studio so the shared <code>answer_url</code> doesn&#39;t conflict with other systems</li>\n</ul>\n"},"integrations/telephony/telnyx":{"title":"Telnyx Integration","description":"Configure Telnyx for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Telnyx is a cloud communications platform that provides programmable voice via its Call Control API. PayInt Voice Studio AI&#39;s Telnyx integration uses Call Control plus WebSocket media streaming to power your voice agents.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Telnyx integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://telnyx.com/\">Telnyx account</a></li>\n<li>An <strong>API Key</strong> from the Telnyx Mission Control Portal</li>\n<li>A <strong>Call Control Application</strong> (optional — leave the field blank in PayInt Voice Studio and we&#39;ll auto-create one for you on save, with the inbound webhook URL pre-set)</li>\n<li>At least one Telnyx phone number assigned to that Call Control Application</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-get-telnyx-credentials\">Step 1: Get Telnyx Credentials</h3>\n<ol>\n<li>Log in to the <a href=\"https://portal.telnyx.com/\">Telnyx Mission Control Portal</a></li>\n<li>Navigate to <strong>API Keys</strong> and create (or copy) an API Key</li>\n<li>(Optional) Navigate to <strong>Call Control</strong> → <strong>Applications</strong> and create (or open) the application you&#39;ll use with PayInt Voice Studio, then copy its <strong>Connection ID</strong> (Call Control App ID). Skip this if you want PayInt Voice Studio to auto-create the Call Control Application for you on save.</li>\n<li>Navigate to <strong>Numbers</strong> → <strong>My Numbers</strong> and assign your phone numbers to the Call Control Application you&#39;ll use with PayInt Voice Studio (if you&#39;re letting PayInt Voice Studio auto-create the application, do this after saving the configuration in Step 2)</li>\n</ol>\n<h3 id=\"step-2-configure-in-payint-voice-studio-ai\">Step 2: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Telnyx</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>API Key</li>\n<li>Call Control App ID (Connection ID) — <em>optional</em>. Leave blank and PayInt Voice Studio will auto-create a Call Control Application on save (with the inbound webhook URL already configured) and store its Connection ID on this configuration.</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (with country code, in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format — <code>+</code> then country code then number, no spaces or dashes, e.g. <code>+1234567890</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   If PayInt Voice Studio auto-created the Call Control Application for you, you still\n   need to assign your Telnyx numbers to that application in the Telnyx\n   Portal under <strong>Numbers</strong> → <strong>My Numbers</strong>. The auto-created application\n   is named <code>dograh-&lt;random&gt;</code> — its Connection ID is shown on the saved\n   configuration.</p>\n</div>\n\n\n\n<h3 id=\"step-3-test-your-configuration\">Step 3: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Telnyx delivers inbound webhooks at the <strong>Call Control Application</strong> level — the webhook URL is configured once on the application, and applies to every number assigned to it. <strong>When you save an inbound workflow on a phone number, PayInt Voice Studio automatically pushes the webhook URL to your Call Control Application&#39;s <code>webhook_event_url</code></strong> (provided the credentials are correct). If PayInt Voice Studio auto-created the application during configuration save, the webhook URL is already set and this step is a no-op.</p>\n<h3 id=\"step-1-assign-an-inbound-workflow-to-the-phone-number\">Step 1: Assign an Inbound Workflow to the Phone Number</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Telnyx configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-2-verify-the-webhook-url-on-the-call-control-application\">Step 2: Verify the Webhook URL on the Call Control Application</h3>\n<ol>\n<li>Go to <strong>Call Control</strong> → <strong>Applications</strong> in the Telnyx Portal</li>\n<li>Open the application whose Connection ID you configured in PayInt Voice Studio</li>\n<li>In <strong>Webhook Settings</strong>, confirm:<ul>\n<li><strong>Webhook URL</strong> is set to: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>HTTP Method</strong> is <code>POST</code></li>\n</ul>\n</li>\n<li>Make sure the phone numbers you want to use for inbound are assigned to this application</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushed this URL automatically when you saved the inbound workflow\n   in Step 1. The same URL is shared across every number on the Call\n   Control Application — PayInt Voice Studio matches the inbound call to the right\n   agent using the called number&#39;s inbound workflow assignment. If the\n   field is empty, shows a different URL, or PayInt Voice Studio surfaced a sync\n   warning on save, the auto-push failed — most often because the API\n   Key or Connection ID in PayInt Voice Studio is incorrect. Paste the URL into the\n   field yourself, set the method to <code>POST</code>, and save. On self-hosted\n   PayInt Voice Studio, replace <code>api.PayInt Voice Studio</code> with your backend domain.</p>\n</div>\n\n\n\n<h3 id=\"step-3-verify-setup\">Step 3: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify any firewalls allow Telnyx&#39;s IP ranges</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured Telnyx phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Telnyx Portal</li>\n</ol>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<p>Ensure phone numbers include country code in E.164 format: <code>+1234567890</code></p>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify the API Key is correct and active - Check for extra spaces in the\nkey - Ensure the key hasn&#39;t been revoked in the Telnyx Portal</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Webhook signature validation failing</summary>\n\n\n<ul>\n<li>Telnyx signs webhooks with Ed25519 - confirm the public key on the\napplication hasn&#39;t changed - Verify the webhook URL matches what Telnyx\nsends - Check if you&#39;re behind a proxy that modifies request bodies</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established - Check firewall rules for\nWebSocket traffic - Ensure audio pipeline is configured correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls aren't answered</summary>\n\n\n<ul>\n<li>Verify the Call Control Application&#39;s webhook URL is set to\n<code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code> - Ensure the webhook\nURL is publicly accessible from the internet - Confirm the called number\nis assigned to the same Call Control Application whose Connection ID is\nconfigured in PayInt Voice Studio - Confirm the called number exists in your PayInt Voice Studio\ntelephony configuration and has an <strong>Inbound workflow</strong> assigned - Verify\nPayInt Voice Studio AI instance is running and responding</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in\n/telephony-configurations - Verify the API Key matches the one stored in\nyour PayInt Voice Studio telephony configuration - Verify WebSocket connection\nestablishes successfully - Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Test your configuration with a single call before running campaigns</li>\n<li>Monitor the Telnyx Portal for usage and billing</li>\n<li>Use a dedicated Call Control Application for PayInt Voice Studio so the shared webhook URL doesn&#39;t conflict with other systems</li>\n</ul>\n"},"integrations/telephony/cloudonix":{"title":"Cloudonix Integration","description":"Configure Cloudonix for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Cloudonix is a cloud communications platform that enables PayInt Voice Studio voice agents to connect to SIP communications\nplatform, either on-premise or in the cloud. In addition, Cloudonix exposes a full set of voice APIs, that provide \na full suite of call-control capabilities and more.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><strong>Already have a configuration named &quot;PayInt Voice Studio Cloudonix SIP&quot;?</strong> That one is\nprovisioned for you — PayInt Voice Studio owns the Cloudonix domain, so it needs no\nCloudonix account or credentials of your own. It does need you to connect\nyour SIP carrier and add your phone numbers. Follow\n<a href=\"/studio/docs/integrations/telephony/dograh-sip\">PayInt Voice Studio Managed SIP</a> instead of this page.</p>\n<p>This page covers bringing <strong>your own</strong> Cloudonix account as a separate\nconfiguration.</p>\n</div>\n\n\n\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Cloudonix integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://cockpit.cloudonix.io/onboarding?affiliate=DOGRAH\">Cloudonix account</a></li>\n<li>A Cloudonix domain UUID (or the domain name)</li>\n<li>A Cloudonix domain API Key (Bearer Token)</li>\n<li>A Cloudonix <strong>Voice Application</strong> on that domain (optional — leave the field blank in PayInt Voice Studio and we&#39;ll auto-create one for you on save, with the application <code>url</code> pre-set)</li>\n<li>A Cloudonix outbound voice trunk service provider connection</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"video-tutorial\">Video Tutorial</h2>\n<p>Watch this step-by-step guide to set up Cloudonix with PayInt Voice Studio AI:</p>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-get-cloudonix-credentials\">Step 1: Get Cloudonix Credentials</h3>\n<ol>\n<li>Log in to your <a href=\"https://cockpit.cloudonix.io/onboarding?affiliate=DOGRAH\">Cloudonix Console</a></li>\n<li>Find your <strong>Domain ID</strong> (UUID or domain name) and <strong>Bearer Token</strong> (Domain API Key) on the dashboard</li>\n<li>(Optional) Navigate to your domain&#39;s <strong>Applications</strong> and create (or open) the application you&#39;ll use with PayInt Voice Studio, then copy its <strong>Application Name</strong>. Skip this if you want PayInt Voice Studio to auto-create the Voice Application for you on save.</li>\n</ol>\n<h3 id=\"step-2-configure-in-payint-voice-studio-ai\">Step 2: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Watch the Cloudonix setup video tutorial above for detailed guidance</li>\n<li>Select <strong>Cloudonix</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>Bearer Token</li>\n<li>Domain ID</li>\n<li>Application Name — <em>optional</em>. Leave blank and PayInt Voice Studio will auto-create a Voice Application on save (with the application <code>url</code> and CXML runtime already configured) and store its name on this configuration.</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (with country code, in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format — <code>+</code> then country code then number, no spaces or dashes, e.g. <code>+1234567890</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   If PayInt Voice Studio auto-created the Voice Application for you, you still need to\n   bind your DNIDs to that application in the Cloudonix cockpit (see\n   <a href=\"#step-2-create-the-voice-application-and-link-dnids\">Step 2 of Inbound Calling Setup</a>).\n   The auto-created application is named <code>dograh-&lt;random&gt;</code> — its name is\n   shown on the saved configuration.</p>\n</div>\n\n\n\n<h3 id=\"step-3-test-your-configuration\">Step 3: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Cloudonix routes inbound calls per <strong>Voice Application</strong> — the webhook URL is set once on the application, and applies to every DNID bound to it. <strong>When you save an inbound workflow on a phone number, PayInt Voice Studio automatically pushes the webhook URL to your Voice Application&#39;s <code>url</code></strong> (provided the credentials are correct), so you don&#39;t need to set the webhook by hand. If PayInt Voice Studio auto-created the application during configuration save, the <code>url</code> is already set and this push is a no-op — you only need to bind your DNIDs to the auto-created application.</p>\n<h3 id=\"step-1-set-up-the-inbound-trunk\">Step 1: Set Up the Inbound Trunk</h3>\n<ol>\n<li><p>Log in to your <a href=\"https://cockpit.cloudonix.io\">Cloudonix Console</a></p>\n</li>\n<li><p>Navigate to <strong>Trunks</strong> → <strong>Create Inbound Trunk</strong> and configure your inbound trunk with your voice service provider</p>\n<p><img\n  className=\"block dark:hidden\"\n  src=\"/agentstudio-docs/images/cloudonix-inbound-config-1.png\"\n  alt=\"Cloudonix console showing domain selection\"\n/>\n<img\n  className=\"hidden dark:block\"\n  src=\"/agentstudio-docs/images/cloudonix-inbound-config-1.png\"\n  alt=\"Cloudonix console showing domain selection\"\n/></p>\n</li>\n</ol>\n<h3 id=\"step-2-create-the-voice-application-and-link-dnids\">Step 2: Create the Voice Application and Link DNIDs</h3>\n<ol>\n<li>In the Cloudonix cockpit, select your domain and navigate to <strong>Applications</strong></li>\n<li>Create a new application (or open the existing one whose name you configured in PayInt Voice Studio) with these settings:<ul>\n<li><strong>Application Resource Type</strong>: <code>Remote Application Resource</code></li>\n<li><strong>Application Runtime</strong>: <code>Cloudonix (CXML)</code></li>\n<li><strong>Application URL</strong>: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>HTTP Method</strong>: <code>POST</code></li>\n</ul>\n</li>\n<li>Under <strong>DNID Numbers</strong>, add each phone number (DNID) you want to route through this application</li>\n<li>Save</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   The Application URL is what PayInt Voice Studio&#39;s auto-push updates in Step 3 — you\n   can leave it blank during creation and let the auto-push fill it in,\n   or paste the URL above so the application is usable immediately.\n   Either works. Self-hosted PayInt Voice Studio deployments use their backend domain\n   in place of <code>api.PayInt Voice Studio</code>.</p>\n</div>\n\n\n\n<p>   <img\n     className=\"block dark:hidden\"\n     src=\"/agentstudio-docs/images/cloudonix-inbound-config-application.png\"\n     alt=\"Cloudonix application form showing Remote Application Resource type, Cloudonix CXML runtime, the PayInt Voice Studio inbound URL, POST method, and a DNID number entry\"\n   />\n   <img\n     className=\"hidden dark:block\"\n     src=\"/agentstudio-docs/images/cloudonix-inbound-config-application.png\"\n     alt=\"Cloudonix application form showing Remote Application Resource type, Cloudonix CXML runtime, the PayInt Voice Studio inbound URL, POST method, and a DNID number entry\"\n   /></p>\n<h3 id=\"step-3-assign-an-inbound-workflow-to-the-phone-number-in-payint-voice-studio\">Step 3: Assign an Inbound Workflow to the Phone Number in PayInt Voice Studio</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Cloudonix configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-4-verify-the-url-on-the-voice-application\">Step 4: Verify the URL on the Voice Application</h3>\n<ol>\n<li>Open your Cloudonix cockpit and navigate to your domain&#39;s <strong>Applications</strong></li>\n<li>Open the application whose name you configured in PayInt Voice Studio</li>\n<li>Confirm:<ul>\n<li><strong>URL</strong> is set to: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>Method</strong> is <code>POST</code></li>\n</ul>\n</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushed this URL automatically when you saved the inbound workflow\n   in Step 3. The same URL is shared across every DNID bound to that\n   application — PayInt Voice Studio routes each inbound call to the right agent based\n   on the called number&#39;s inbound workflow assignment. If the field is\n   empty, shows a different URL, or PayInt Voice Studio surfaced a sync warning on\n   save, the auto-push failed — most often because the Bearer Token,\n   Domain ID, or Application Name in PayInt Voice Studio is incorrect. Paste the URL\n   into the field yourself, set the method to <code>POST</code>, and save. On\n   self-hosted PayInt Voice Studio, replace <code>api.PayInt Voice Studio</code> with your backend domain.</p>\n</div>\n\n\n\n<p>   <img\n     className=\"block dark:hidden\"\n     src=\"/agentstudio-docs/images/cloudonix-inbound-config-2.png\"\n     alt=\"Cloudonix application showing webhook URL configuration\"\n   />\n   <img\n     className=\"hidden dark:block\"\n     src=\"/agentstudio-docs/images/cloudonix-inbound-config-2.png\"\n     alt=\"Cloudonix application showing webhook URL configuration\"\n   /></p>\n<h3 id=\"step-5-verify-setup\">Step 5: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify any firewalls allow Cloudonix&#39;s IP ranges</li>\n<li>Confirm your inbound trunk is active and receiving calls</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Cloudonix Console</li>\n<li>Confirm clear two-way audio communication</li>\n</ol>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<p>Ensure phone numbers include country code in E.164 format: <code>+1234567890</code></p>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify Domain ID, Bearer Token, and Application Name are correct</li>\n<li>Check for extra spaces in credentials</li>\n<li>Ensure credentials haven&#39;t been disabled or deleted in Cloudonix Console</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established</li>\n<li>Check firewall rules for WebSocket traffic</li>\n<li>Ensure audio pipeline is configured correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls not reaching voice agent</summary>\n\n\n<ul>\n<li>Verify the DNID is bound to the same Voice Application whose name you\nconfigured in PayInt Voice Studio - Confirm the called number exists in your PayInt Voice Studio\ntelephony configuration and has an <strong>Inbound workflow</strong> assigned -\nAfter assigning the inbound workflow, confirm PayInt Voice Studio successfully updated\nthe application&#39;s <code>url</code> (no warning shown on save) - Verify your inbound\ntrunk is active and receiving calls</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in\n/telephony-configurations - Verify the Bearer Token in PayInt Voice Studio matches the\none in your Cloudonix Console - Verify WebSocket connection establishes\nsuccessfully - Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Test your configuration with a single call before running campaigns</li>\n<li>Monitor Cloudonix Sessions for usage</li>\n<li>Use a dedicated Voice Application for PayInt Voice Studio so the shared <code>url</code> doesn&#39;t conflict with other systems</li>\n</ul>\n"},"integrations/telephony/exotel":{"title":"Exotel Integration","description":"Configure Exotel for voice communication in PayInt Voice Studio AI","html":"<h1 id=\"exotel-integration\">Exotel Integration</h1>\n<p>Exotel places and receives calls with bidirectional AgentStream media streaming into PayInt Voice Studio agents.</p>\n<h2 id=\"walkthrough-video\">Walkthrough video</h2>\n<p>The walkthrough covers, in order:</p>\n<ol>\n<li>ExoPhone provisioning</li>\n<li>PayInt Voice Studio telephony configuration</li>\n<li>Outbound call from a workflow</li>\n<li>Inbound call landing on a workflow</li>\n</ol>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>An <a href=\"https://my.exotel.com/\">Exotel</a> account with AgentStream / Connect Voice AI enabled</li>\n<li><strong>KYC verification completed</strong> — mandatory for all Indian accounts post-signup before ExoPhones can carry live voice traffic. Follow <a href=\"https://docs.exotel.com/business-phone-system/kyc-verification-indian-accounts\">KYC Verification — Indian Accounts</a> (typically &lt; 30 minutes online).</li>\n<li>Account SID, API Key, and API Token (Dashboard -&gt; Settings -&gt; API Settings)</li>\n<li>A PayInt Voice Studio instance reachable over HTTPS and WSS</li>\n<li>At least one ExoPhone assigned to your account (see provisioning below — don&#39;t assume one already exists)</li>\n</ul>\n<h2 id=\"supported-exotel-front-doors\">Supported Exotel front-doors</h2>\n<p>Per <a href=\"https://docs.exotel.com/exotel-agentstream/agentstream-what-to-use-when\">AgentStream: what to use when</a>, Exotel exposes five entry points to AgentStream (Connect Voice AI, Connect + Flow, ExoML outbound, Flow-Based inbound, ExoML inbound). PayInt Voice Studio&#39;s Exotel provider supports <strong>exactly two</strong> of these today:</p>\n<ul>\n<li><strong>Outbound: Connect Voice AI API</strong> — <code>POST /v1/Accounts/{sid}/Calls/connect.json</code> with <code>StreamUrl</code> in the API body. No App Bazaar flow involved.</li>\n<li><strong>Inbound: Flow-Based via Voicebot Applet</strong> — ExoPhone → App Bazaar flow → Voicebot Applet with an HTTPS dynamic-URL pointing at PayInt Voice Studio.</li>\n</ul>\n<p>Together these cover the &quot;answer → bot&quot; use case without requiring a gRPC control plane.</p>\n<h3 id=\"not-currently-supported\">Not currently supported</h3>\n<ul>\n<li><strong>Connect Voice AI with Flow API</strong> (outbound + App Bazaar journey). PayInt Voice Studio&#39;s <code>initiate_call</code> has no path for dialing with <code>Url=&lt;app_bazaar_flow_url&gt;</code>, and <code>transfer_call</code> raises <code>NotImplementedError</code>, so the Connect-applet agent-handoff pattern isn&#39;t wired. Operators who need IVR/greeting/DTMF/agent-handoff journeys can trigger Connect + Flow calls directly against Exotel&#39;s API from outside PayInt Voice Studio; the flow&#39;s Voicebot Applet can still terminate into PayInt Voice Studio&#39;s inbound dynamic-URL endpoint.</li>\n<li><strong>ExoML / Programmable Voice APIs</strong> (gRPC leg events + leg actions), for both outbound and inbound. Would require a gRPC endpoint on PayInt Voice Studio&#39;s side and code-owned per-leg control, which is not implemented today.</li>\n</ul>\n<h2 id=\"exophone-provisioning-and-activation\">ExoPhone provisioning and activation</h2>\n<p>Provision ExoPhones before configuring PayInt Voice Studio. All calls use HTTP Basic Auth with your API Key / API Token.</p>\n<ol>\n<li><strong>List available numbers</strong><ul>\n<li><code>GET /v2_beta/Accounts/{account_sid}/AvailablePhoneNumbers/{country}/{type}</code></li>\n<li><code>{type}</code> is one of <code>Landline</code>, <code>Mobile</code>, or <code>TollFree</code>.</li>\n<li>Optional filters: <code>IncomingSMS</code>, <code>InRegion</code>, <code>Contains</code>.</li>\n<li>Response fields include <code>phone_number</code>, <code>capabilities</code>, <code>country</code>, <code>region</code>, <code>rental_price</code>.</li>\n<li>Docs: <a href=\"https://developer.exotel.com/docs/exophones/api-reference/available-numbers\">Available numbers</a></li>\n</ul>\n</li>\n<li><strong>Purchase a number</strong><ul>\n<li><code>POST /v2_beta/Accounts/{account_sid}/IncomingPhoneNumbers</code></li>\n<li>Request body: <code>PhoneNumber</code> (required); <code>VoiceUrl</code>, <code>SMSUrl</code>, <code>FriendlyName</code> (all optional).</li>\n<li>Response fields include <code>sid</code>, <code>phone_number</code>, <code>friendly_name</code>, <code>capabilities</code>, <code>country</code>, <code>rental_price</code>, <code>currency</code>.</li>\n<li>Docs: <a href=\"https://developer.exotel.com/docs/exophones/api-reference/purchase-number\">Purchase number</a></li>\n</ul>\n</li>\n<li><strong>Verify a purchased number</strong><ul>\n<li><code>GET /v2_beta/Accounts/{account_sid}/IncomingPhoneNumbers/{exophone_sid}</code></li>\n<li>Returns <code>sid</code>, <code>phone_number</code>, <code>friendly_name</code>, <code>capabilities</code>, <code>country</code>, <code>region</code>, <code>voice_url</code>, <code>sms_url</code>.</li>\n<li>Docs: <a href=\"https://developer.exotel.com/docs/exophones/api-reference/number-details\">Number details</a></li>\n</ul>\n</li>\n<li><strong>Assign the number to a Flow</strong><ul>\n<li>In the Exotel dashboard, attach the ExoPhone&#39;s Voice URL to an App Bazaar flow (see <a href=\"#inbound\">Inbound</a> below for the Voicebot Applet wiring).</li>\n<li><strong>Required before the number can carry inbound traffic</strong>, and part of the setup the reviewer&#39;s checklist expects.</li>\n</ul>\n</li>\n</ol>\n<blockquote>\n<p><strong>KYC (mandatory for Indian accounts):</strong> Complete KYC in the Exotel dashboard before expecting purchased numbers to become fully active for voice traffic. A purchased ExoPhone can appear in the API but silently fail on real calls until KYC clears. See <a href=\"https://docs.exotel.com/business-phone-system/kyc-verification-indian-accounts\">KYC Verification — Indian Accounts</a> for the three-step flow (PAN confirmation → business details → authorised signatory) and the &quot;help me verify&quot; escape hatch if automated checks fail.</p>\n</blockquote>\n<h2 id=\"payint-voice-studio-telephony-configuration\">PayInt Voice Studio telephony configuration</h2>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Exotel</strong></li>\n<li>Enter:<ul>\n<li><strong>Account SID</strong></li>\n<li><strong>API Key</strong> / <strong>API Token</strong> (HTTP Basic auth for Exotel APIs)</li>\n<li><strong>API Base URL</strong> — default <code>https://api.in.exotel.com</code> (India); use <code>https://api.exotel.com</code> for other regions</li>\n</ul>\n</li>\n<li>Save, then add your ExoPhone under <strong>Phone numbers</strong> in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format (e.g. <code>+9180XXXXXXXX</code>)</li>\n<li>Set an <strong>Inbound workflow</strong> on that phone number in PayInt Voice Studio.</li>\n</ol>\n<h3 id=\"programmatic-configuration\">Programmatic configuration</h3>\n<p>When wiring Exotel via API or environment rather than the UI, the provider config accepts:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>account_sid</code></td>\n<td>Exotel Account SID</td>\n</tr>\n<tr>\n<td><code>api_key</code></td>\n<td>Exotel API Key (HTTP Basic username)</td>\n</tr>\n<tr>\n<td><code>api_token</code></td>\n<td>Exotel API Token (HTTP Basic password)</td>\n</tr>\n<tr>\n<td><code>api_base_url</code></td>\n<td><code>https://api.in.exotel.com</code> (default) or <code>https://api.exotel.com</code>; any other origin is rejected</td>\n</tr>\n<tr>\n<td><code>from_numbers</code></td>\n<td>List of ExoPhones (E.164) available as <code>CallerId</code></td>\n</tr>\n<tr>\n<td><code>default_from_number</code></td>\n<td>Default <code>CallerId</code> when a workflow does not specify one</td>\n</tr>\n</tbody></table>\n<h2 id=\"outbound\">Outbound</h2>\n<p>PayInt Voice Studio dials using Exotel Connect Voice AI (the &quot;simplest outbound&quot; pattern — bot is the whole experience, no App Bazaar flow):</p>\n<p><code>POST {api_base}/v1/Accounts/{AccountSid}/Calls/connect.json</code></p>\n<p>Request fields:</p>\n<ul>\n<li><code>From</code> — the recipient (callee)</li>\n<li><code>CallerId</code> — the ExoPhone</li>\n<li><code>StreamUrl</code> — PayInt Voice Studio&#39;s tokenized WSS endpoint (fixed per call, minted by <code>ws_auth.build_media_ws_url</code>)</li>\n<li><code>StreamType=bidirectional</code></li>\n<li><code>StatusCallback</code> and <code>StatusCallbackEvents[]=terminal</code> — <strong>only attached when a <code>workflow_run_id</code> is present</strong></li>\n</ul>\n<p>For Indian ExoPhones, PayInt Voice Studio&#39;s <code>_exotel_dial_number()</code> helper maps the stored E.164 <code>CallerId</code> to Exotel&#39;s 0-prefixed national form (Exotel&#39;s <a href=\"https://docs.exotel.com/exotel-agentstream/connect-voice-ai-api\">Connect Voice AI example</a> uses <code>CallerId=0XXXXXXXXXX</code>). Example: <code>+917314852338</code> → <code>07314852338</code>. The destination <code>From</code> stays in E.164 per the same example (<code>From=+91XXXXXXXXXX</code>). Non-India numbers pass through unchanged.</p>\n<p>Test from a workflow <strong>Call</strong> button. Confirm two-way audio and that hangup marks the run completed.</p>\n<p>Per-call <em>dynamic</em> StreamUrl or dashboard-flow-driven outbound would require Connect + Flow or ExoML respectively — see <a href=\"#supported-exotel-front-doors\">Supported Exotel front-doors</a> for why those aren&#39;t implemented here.</p>\n<h2 id=\"inbound\">Inbound</h2>\n<p>PayInt Voice Studio implements <strong>Flow-Based Inbound via the Voicebot Applet&#39;s dynamic-URL contract</strong>. Exotel&#39;s <a href=\"https://developer.exotel.com/docs/agentstream/stream-voicebot-applet\">Stream and Voicebot Applet reference</a> explicitly documents the applet&#39;s URL parameter as &quot;a WebSocket URL … <strong>or an HTTPS endpoint that dynamically returns the WSS URL</strong>&quot; — that is the contract PayInt Voice Studio implements.</p>\n<h3 id=\"wiring\">Wiring</h3>\n<ol>\n<li>In Exotel, open or create an <strong>App Bazaar flow</strong>.</li>\n<li>Drop a <strong>Voicebot Applet</strong> into the flow.</li>\n<li>Configure the applet:<ul>\n<li><strong>URL</strong> = <code>https://&lt;dograh-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>Authentication</strong> = HTTP Basic; username = your Exotel API Key, password = your Exotel API Token (PayInt Voice Studio verifies this in <code>verify_inbound_signature</code>).</li>\n<li><strong>Sample Rate</strong> = <code>8000</code>. PayInt Voice Studio&#39;s Exotel transport is fixed at 8 kHz today; do not append <code>?sample-rate=…</code> — the applet will negotiate a rate the transport doesn&#39;t decode and audio breaks.</li>\n</ul>\n</li>\n<li>Assign the ExoPhone&#39;s Voice URL to this flow in Exotel.</li>\n<li>In PayInt Voice Studio, assign the matching <strong>inbound workflow</strong> to the number.</li>\n</ol>\n<h3 id=\"response-contract\">Response contract</h3>\n<p>On invocation, PayInt Voice Studio validates auth and route mapping, creates a workflow run, and returns JSON:</p>\n<pre><code class=\"language-json\">{ &quot;url&quot;: &quot;wss://&lt;dograh-host&gt;/api/v1/telephony/ws/...&quot; }\n</code></pre>\n<p>Exotel&#39;s Voicebot Applet then opens the bidirectional WebSocket to that URL.</p>\n<h3 id=\"voicebot-applet-operational-limits\">Voicebot Applet operational limits</h3>\n<p>Drawn from Exotel&#39;s <a href=\"https://developer.exotel.com/docs/agentstream/stream-voicebot-applet\">applet documentation</a>:</p>\n<ul>\n<li><strong>Audio format</strong>: raw/slin — 16-bit PCM little-endian, mono, base64-encoded.</li>\n<li><strong>Sample rate</strong>: <code>8000</code> (PSTN). Exotel&#39;s applet also supports <code>16000</code> and <code>24000</code> via <code>?sample-rate=…</code>, but PayInt Voice Studio&#39;s Exotel transport is hard-coded to 8 kHz today, so leave the sample rate at the default.</li>\n<li><strong>Chunk size</strong>: min 3.2 KB (100 ms of audio), max 100 KB, must be a multiple of 320 bytes. Smaller chunks risk audio distortion; larger cause timeouts.</li>\n<li><strong>Custom parameters</strong>: up to 3 key-value pairs appended to the URL (<code>?param1=value1&amp;param2=value2</code>, 256 chars total).</li>\n</ul>\n<h2 id=\"status-callbacks\">Status callbacks</h2>\n<p>Outbound registers a per-run status callback URL:</p>\n<p><code>/api/v1/telephony/exotel/status-callback/{workflow_run_id}?exotel_auth=...</code></p>\n<p>The <code>exotel_auth</code> token is an HMAC minted from your API token so Exotel&#39;s POST does not need an <code>Authorization</code> header. The handler also rejects <code>CallSid</code> values that do not match the run&#39;s stored call id.</p>\n<p>Terminal events update the workflow run status.</p>\n<blockquote>\n<p><strong>Inbound status callbacks:</strong> the inbound dynamic-URL path currently returns the WSS URL without registering a per-run <code>StatusCallback</code> on the Exotel side. If you rely on Exotel-driven terminal events to close inbound runs, be aware that this is a known open item on the PR.</p>\n</blockquote>\n<h2 id=\"phone-ownership-validation\">Phone ownership validation</h2>\n<p>Exotel does not publish a purpose-built &quot;verify ownership&quot; endpoint. The closest documented mechanism is <a href=\"https://developer.exotel.com/docs/exophones/api-reference/number-details\">Number Details</a> — but it assumes the number is already on the account and only returns its configuration; it does not confirm ownership by itself.</p>\n<p>PayInt Voice Studio therefore validates ownership as a <strong>list + membership check</strong>:</p>\n<ol>\n<li>Normalize the configured number and generate match-key variants (E.164, national, with/without leading <code>+</code>). For Indian numbers stored as <code>0</code>-prefixed national forms, an <code>IN</code> country hint is applied.</li>\n<li>Query <a href=\"https://developer.exotel.com/docs/exophones/api-reference/list-numbers\"><code>GET /v2_beta/Accounts/{account_sid}/IncomingPhoneNumbers</code></a> and iterate the returned ExoPhones.</li>\n<li>Set-intersect the wanted variants against each entry&#39;s <code>phone_number</code> / <code>PhoneNumber</code> / <code>FriendlyName</code> (also India-normalized).</li>\n<li>Raise <code>ProviderPhoneNumberLookupError</code> if no variant matches, with a message pointing the operator back to the Exotel dashboard.</li>\n</ol>\n<p>This is the documented workaround given Exotel&#39;s public API surface — there is no dedicated ownership-check endpoint to switch to.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<ul>\n<li><strong>Connect fails</strong> — verify Account SID, API key/token, base URL region (<code>api.in.exotel.com</code> or <code>api.exotel.com</code> only), and AgentStream entitlement.</li>\n<li><strong>No inbound agent</strong> — confirm the ExoPhone has an inbound workflow in PayInt Voice Studio and the Exotel Voicebot Applet points to <code>/api/v1/telephony/inbound/run</code>.</li>\n<li><strong>Auth failed on inbound</strong> — ensure the Voicebot Applet is configured with HTTP Basic (username = API Key, password = API Token) matching the credentials stored in PayInt Voice Studio.</li>\n<li><strong><code>ProviderPhoneNumberLookupError</code> at config save</strong> — the ExoPhone is not on the Exotel account, the wrong <code>api_base_url</code> region is selected, or the number was purchased but has not cleared <a href=\"https://docs.exotel.com/business-phone-system/kyc-verification-indian-accounts\">KYC</a> yet.</li>\n<li><strong>One-way audio</strong> — confirm public WSS reachability and firewall/IP rules for Exotel egress; also check the sample rate on the applet matches what your bot pipeline expects.</li>\n<li><strong>Choppy or dropped audio</strong> — the outgoing chunk size may be out of range; keep chunks ≥ 3.2 KB and ≤ 100 KB, in multiples of 320 bytes. See Exotel&#39;s <a href=\"https://docs.exotel.com/exotel-agentstream/agentstream-wss-errors-and-handling\">WSS errors and handling</a>.</li>\n<li><strong>Status callback 401</strong> — ensure PayInt Voice Studio minted the StatusCallback URL (includes <code>exotel_auth</code>) and that the query string is preserved by any intervening proxy.</li>\n</ul>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://docs.exotel.com/business-phone-system/kyc-verification-indian-accounts\">KYC Verification — Indian Accounts</a> (mandatory post-signup step for Indian accounts)</li>\n<li><a href=\"https://developer.exotel.com/docs/exophones/api-reference/available-numbers\">ExoPhone — Available numbers</a></li>\n<li><a href=\"https://developer.exotel.com/docs/exophones/api-reference/purchase-number\">ExoPhone — Purchase number</a></li>\n<li><a href=\"https://developer.exotel.com/docs/exophones/api-reference/number-details\">ExoPhone — Number details</a></li>\n<li><a href=\"https://docs.exotel.com/exotel-agentstream/agentstream-what-to-use-when\">AgentStream — What to use when</a> (context for why PayInt Voice Studio uses these two front-doors and not ExoML)</li>\n<li><a href=\"https://docs.exotel.com/exotel-agentstream/overview-and-quickstart\">AgentStream — Overview &amp; Quickstart</a></li>\n<li><a href=\"https://docs.exotel.com/exotel-agentstream/agentstream-wss-errors-and-handling\">AgentStream — WSS errors and handling</a></li>\n<li><a href=\"https://developer.exotel.com/docs/agentstream/stream-voicebot-applet\">Working with the Stream and Voicebot Applet</a></li>\n<li><a href=\"https://docs.exotel.com/exotel-agentstream/connect-voice-ai-api\">Connect Voice AI API</a></li>\n<li><a href=\"https://github.com/exotel/Agent-Stream\">Exotel Agent-Stream reference implementation</a></li>\n<li><a href=\"https://github.com/exotel/Agent-Stream-echobot\">Exotel Agent-Stream echobot</a></li>\n</ul>\n"},"integrations/telephony/vobiz":{"title":"Vobiz Integration","description":"Configure Vobiz for voice communication in PayInt Voice Studio AI","html":"<h2 id=\"overview\">Overview</h2>\n<p>Vobiz is a cloud-based telephony service provider that offers global reach with competitive pricing. PayInt Voice Studio AI&#39;s Vobiz integration provides reliable voice connectivity for your conversational AI agents.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up Vobiz integration, you&#39;ll need:</p>\n<ul>\n<li>A <a href=\"https://vobiz.ai\">Vobiz account</a></li>\n<li>Auth ID and Auth Token from your Vobiz dashboard</li>\n<li>A Vobiz <strong>Application</strong> (optional — leave the field blank in PayInt Voice Studio and we&#39;ll auto-create one for you on save, with the <code>answer_url</code> pre-set)</li>\n<li>At least one Vobiz phone number</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<h2 id=\"configuration\">Configuration</h2>\n<h3 id=\"step-1-get-vobiz-credentials\">Step 1: Get Vobiz Credentials</h3>\n<ol>\n<li>Log in to your Vobiz dashboard</li>\n<li>Find your <strong>Auth ID</strong> and generate an <strong>Auth Token</strong> for API access</li>\n<li>(Optional) Navigate to the <strong>Applications</strong> section and create (or open) the application you&#39;ll use with PayInt Voice Studio, then copy its <strong>Application ID</strong>. Skip this if you want PayInt Voice Studio to auto-create the application for you on save.</li>\n<li>Navigate to <strong>Phone Numbers</strong> and copy the numbers you plan to use</li>\n</ol>\n<h3 id=\"step-2-configure-in-payint-voice-studio-ai\">Step 2: Configure in PayInt Voice Studio AI</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Vobiz</strong> as your provider</li>\n<li>Enter your credentials:<ul>\n<li>Auth ID</li>\n<li>Auth Token</li>\n<li>Application ID — <em>optional</em>. Leave blank and PayInt Voice Studio will auto-create a Vobiz Application on save (with the <code>answer_url</code> already configured) and store its Application ID on this configuration.</li>\n</ul>\n</li>\n<li>Click <strong>Save Configuration</strong></li>\n<li>Open the configuration you just created and add at least one <strong>phone number</strong> (with country code, in <a href=\"https://en.wikipedia.org/wiki/E.164\">E.164</a> format — <code>+</code> then country code then number, no spaces or dashes, e.g. <code>+1234567890</code>). The default caller ID is used for outbound calls.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   If PayInt Voice Studio auto-created the Vobiz Application for you, you still need to\n   attach your Vobiz numbers to that application in the Vobiz Console under\n   <strong>Applications</strong>. The auto-created application is named\n   <code>dograh-&lt;random&gt;</code> — its Application ID is shown on the saved\n   configuration.</p>\n</div>\n\n\n\n<h3 id=\"step-3-test-your-configuration\">Step 3: Test Your Configuration</h3>\n<ol>\n<li>Create a test workflow</li>\n<li>Click &quot;Call&quot; to verify connection</li>\n<li>Check call logs for successful connection</li>\n</ol>\n<h2 id=\"inbound-calling-setup\">Inbound Calling Setup</h2>\n<p>Vobiz numbers don&#39;t carry an <code>answer_url</code> directly — the URL lives on a Vobiz <strong>Application</strong>, and each number is linked to one application. <strong>When you save an inbound workflow on a phone number, PayInt Voice Studio automatically pushes the webhook URL to your Vobiz Application&#39;s <code>answer_url</code></strong> (provided the credentials are correct). If PayInt Voice Studio auto-created the application during configuration save, the <code>answer_url</code> is already set and this push is a no-op.</p>\n<h3 id=\"step-1-link-the-phone-number-to-your-vobiz-application\">Step 1: Link the Phone Number to Your Vobiz Application</h3>\n<ol>\n<li><p>Log in to your Vobiz Console and open the <strong>Applications</strong> section</p>\n</li>\n<li><p>Edit the application whose ID you configured in PayInt Voice Studio</p>\n</li>\n<li><p>Attach the phone number you want to use for inbound calls</p>\n</li>\n<li><p>Save</p>\n<p><img\n  className=\"block dark:hidden\"\n  src=\"/agentstudio-docs/images/vobiz-inbound-config-3.png\"\n  alt=\"Vobiz application showing phone number attachment interface\"\n/>\n<img\n  className=\"hidden dark:block\"\n  src=\"/agentstudio-docs/images/vobiz-inbound-config-3.png\"\n  alt=\"Vobiz application showing phone number attachment interface\"\n/></p>\n</li>\n</ol>\n<h3 id=\"step-2-assign-an-inbound-workflow-to-the-phone-number-in-payint-voice-studio\">Step 2: Assign an Inbound Workflow to the Phone Number in PayInt Voice Studio</h3>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Vobiz configuration</li>\n<li>In the <strong>Phone numbers</strong> section, edit the number that should receive inbound calls</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<h3 id=\"step-3-verify-the-answer-url-on-the-vobiz-application\">Step 3: Verify the Answer URL on the Vobiz Application</h3>\n<ol>\n<li>Open your Vobiz Console and navigate to <strong>Applications</strong></li>\n<li>Open the application whose ID you configured in PayInt Voice Studio</li>\n<li>Confirm:<ul>\n<li><strong>Answer URL</strong> is set to: <code>https://&lt;your-payint-host&gt;/api/v1/telephony/inbound/run</code></li>\n<li><strong>Answer Method</strong> is <code>POST</code></li>\n</ul>\n</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>   PayInt Voice Studio pushed this URL automatically when you saved the inbound workflow\n   in Step 2. The same URL is shared across every number linked to that\n   application — PayInt Voice Studio routes each inbound call to the right agent based\n   on the called number&#39;s inbound workflow assignment. If the field is\n   empty, shows a different URL, or PayInt Voice Studio surfaced a sync warning on\n   save, the auto-push failed — most often because the Auth ID/Token or\n   Application ID in PayInt Voice Studio is incorrect. Paste the URL into the field\n   yourself, set the method to <code>POST</code>, and save. On self-hosted PayInt Voice Studio,\n   replace <code>api.PayInt Voice Studio</code> with your backend domain.</p>\n</div>\n\n\n\n<h3 id=\"step-4-verify-setup\">Step 4: Verify Setup</h3>\n<ul>\n<li>Ensure your PayInt Voice Studio AI instance is publicly accessible</li>\n<li>Verify any firewalls allow Vobiz&#39;s webhook requests</li>\n</ul>\n<h3 id=\"test-inbound-calling\">Test Inbound Calling</h3>\n<ol>\n<li>Call your configured Vobiz phone number from another phone</li>\n<li>Verify your PayInt Voice Studio AI voice agent answers and responds</li>\n<li>Check call logs in both PayInt Voice Studio AI dashboard and Vobiz dashboard</li>\n</ol>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Invalid phone number error</summary>\n\n\n<p>Ensure phone numbers include country code in E.164 format: <code>+1234567890</code></p>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify Auth ID and Auth Token are correct</li>\n<li>Check for extra spaces in credentials</li>\n<li>Ensure credentials haven&#39;t been regenerated in Vobiz dashboard</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Webhook requests failing</summary>\n\n\n<ul>\n<li>Confirm your Auth Token matches exactly</li>\n<li>Verify the webhook URL is accessible from Vobiz servers</li>\n<li>Check if you&#39;re behind a proxy that modifies requests</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio on calls</summary>\n\n\n<ul>\n<li>Verify WebSocket connection is established</li>\n<li>Check firewall rules for WebSocket traffic</li>\n<li>Ensure audio pipeline is configured correctly</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls go to voicemail or aren't answered</summary>\n\n\n<ul>\n<li>Verify the phone number is linked to the same Vobiz Application whose\nID you configured in PayInt Voice Studio - Confirm the called number exists in your\nPayInt Voice Studio telephony configuration and has an <strong>Inbound workflow</strong> assigned</li>\n<li>After assigning the inbound workflow, confirm PayInt Voice Studio successfully\nupdated the application&#39;s <code>answer_url</code> (no warning shown on save) -\nVerify PayInt Voice Studio AI instance is running and responding</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Voice agent doesn't respond to inbound calls</summary>\n\n\n<ul>\n<li>Confirm the phone number has an <strong>Inbound workflow</strong> assigned in\n/telephony-configurations - Check webhook signature validation is working\n(Auth Token in PayInt Voice Studio matches Vobiz dashboard) - Verify WebSocket connection\nestablishes successfully - Review call logs for error messages</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Test your configuration with a single call before running campaigns</li>\n<li>Monitor Vobiz dashboard for usage and billing</li>\n<li>Keep your Auth Token secure and rotate it periodically</li>\n<li>Use a dedicated Vobiz Application for PayInt Voice Studio so the shared <code>answer_url</code> doesn&#39;t conflict with other systems</li>\n</ul>\n"},"integrations/telephony/asterisk-ari":{"title":"Asterisk ARI Integration","description":"Connect PayInt Voice Studio AI to your Asterisk PBX using the Asterisk REST Interface (ARI)","html":"<h2 id=\"overview\">Overview</h2>\n<p>Asterisk ARI (Asterisk REST Interface) allows you to connect PayInt Voice Studio AI voice agents to your existing Asterisk PBX. ARI provides a WebSocket-based event model for controlling calls via Stasis applications, giving PayInt Voice Studio full control over call flow and audio streaming.</p>\n<p>This guide focuses on the PayInt Voice Studio-specific configuration. For general Asterisk installation and administration, refer to the <a href=\"https://docs.asterisk.org/\">official Asterisk documentation</a>.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before setting up the ARI integration, ensure you have:</p>\n<ul>\n<li>A running Asterisk instance with <code>chan_websocket</code> and <code>res_websocket_client</code> modules available. Known-working setups: (a) Asterisk 22+, (b) Asterisk 20 LTS with these modules included</li>\n<li>ARI module enabled in Asterisk</li>\n<li><code>chan_websocket</code> (WebSocket channel driver) and <code>res_websocket_client</code> (loads <code>websocket_client.conf</code>) enabled in your Asterisk build. Verify with <code>asterisk -rx &quot;module show like chan_websocket&quot;</code> and <code>asterisk -rx &quot;module show like res_websocket_client&quot;</code> — both should report <strong>Running</strong>.</li>\n<li>Network connectivity between your PayInt Voice Studio instance and Asterisk</li>\n<li>PayInt Voice Studio AI instance running and accessible</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If you compiled Asterisk from source, ensure both <code>chan_websocket</code> and <code>res_websocket_client</code> are included during the build. These modules are required for external media streaming between Asterisk and PayInt Voice Studio. Refer to the <a href=\"https://docs.asterisk.org/\">Asterisk build system documentation</a> for details on enabling modules.</p>\n</div>\n\n\n\n<h2 id=\"how-setup-fits-together\">How setup fits together</h2>\n<p>Setup crosses between the two systems, so the order matters:</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Where</th>\n<th>What</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>1</strong></td>\n<td>Asterisk</td>\n<td>Create the ARI user, enable the HTTP server, and add the external-media WebSocket client — the values PayInt Voice Studio asks for</td>\n</tr>\n<tr>\n<td><strong>2</strong></td>\n<td>PayInt Voice Studio</td>\n<td>Enter those values and save. PayInt Voice Studio generates a <strong>Stasis App Name</strong> unique to this configuration</td>\n</tr>\n<tr>\n<td><strong>3</strong></td>\n<td>Asterisk</td>\n<td>Put that generated name in your dialplan as <code>Stasis(&lt;name&gt;)</code> and reload</td>\n</tr>\n<tr>\n<td><strong>4</strong></td>\n<td>PayInt Voice Studio</td>\n<td>Register the extensions that should reach an agent, then place a test call</td>\n</tr>\n</tbody></table>\n<p>The dialplan comes last because the Stasis application name does not exist until the configuration is saved. Everything else can be set up in advance.</p>\n<h2 id=\"part-1-asterisk-connection-settings\">Part 1: Asterisk connection settings</h2>\n<p>These are minimal examples focused on the PayInt Voice Studio integration -- refer to the <a href=\"https://docs.asterisk.org/\">Asterisk documentation</a> for full configuration details.</p>\n<h3 id=\"enable-ari-ariconf\">Enable ARI (<code>ari.conf</code>)</h3>\n<p>Create an ARI user that PayInt Voice Studio will use to authenticate:</p>\n<pre><code class=\"language-ini\">[general]\nenabled = yes\n\n[dograh]\ntype = user\nread_only = no\npassword = your_secure_password\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The username (section name, e.g., <code>dograh</code>) and password here are the <strong>ARI Username</strong> and <strong>App Password</strong> you enter in PayInt Voice Studio. This name authenticates to Asterisk — it is <em>not</em> the Stasis application name, which PayInt Voice Studio generates for you in <a href=\"#part-2-create-the-configuration-in-dograh\">Part 2</a>.</p>\n</div>\n\n\n\n<h3 id=\"enable-the-http-server-httpconf\">Enable the HTTP Server (<code>http.conf</code>)</h3>\n<p>ARI requires the Asterisk HTTP server to be enabled:</p>\n<pre><code class=\"language-ini\">[general]\nenabled = yes\nbindaddr = 0.0.0.0\nbindport = 8088\n</code></pre>\n<h3 id=\"configure-external-media-streaming-websocketclientconf\">Configure External Media Streaming (<code>websocket_client.conf</code>)</h3>\n<p>PayInt Voice Studio uses Asterisk&#39;s external media streaming to send and receive audio over WebSocket. Configure a WebSocket client connection that points to your PayInt Voice Studio instance:</p>\n<h4 id=\"payint-voice-studio-cloud\">PayInt Voice Studio Cloud</h4>\n<pre><code class=\"language-ini\">[dograh]\ntype = websocket_client\nuri = wss://api.PayInt Voice Studio/api/v1/telephony/ws/ari\nprotocols = media\ntls_enabled = yes\nca_list_file = /etc/ssl/certs/ca-certificates.crt\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><code>tls_enabled = yes</code> is required even though the URI scheme is <code>wss://</code> — without it Asterisk will not negotiate TLS and the connection will fail. The ARI credentials (<strong>ARI Username</strong> and <strong>App Password</strong>) must match what you configure in the PayInt Voice Studio dashboard under Telephony Settings.</p>\n</div>\n\n\n\n\n\n\n\n<h4 id=\"self-hosted\">Self-hosted</h4>\n<pre><code class=\"language-ini\">[dograh]\ntype = websocket_client\nuri = ws://your-dograh-host:port/api/v1/telephony/ws/ari\nprotocols = media\n</code></pre>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Self-hosted deployments on an internal network may use an unencrypted WebSocket (<code>ws://</code>). If your PayInt Voice Studio instance is exposed over HTTPS, use <code>wss://</code> and the corresponding hostname instead.</p>\n</div>\n\n\n\n\n\n\n\n\n\n\n\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The section name (e.g., <code>dograh</code>) is the <strong>WebSocket Client Name</strong> you&#39;ll enter in the PayInt Voice Studio telephony configuration. This name tells Asterisk which WebSocket connection to use for external media streaming during calls.</p>\n</div>\n\n\n\n\n\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Configure the <code>uri</code> as a base URL only, without a query string. During each call, PayInt Voice Studio asks Asterisk to create an <code>externalMedia</code> channel and Asterisk appends <code>workflow_id</code>, <code>organization_id</code>, and <code>workflow_run_id</code> through the <code>v()</code> transport data for that call. Opening <code>/api/v1/telephony/ws/ari</code> directly in a browser or with <code>wscat</code> can return HTTP 403 because those routing parameters are missing; that is expected and does not indicate a <code>websocket_client.conf</code> misconfiguration.</p>\n</div>\n\n\n\n\n\n<div class=\"as-callout as-callout-note\">\n\n\n<p>PayInt Voice Studio&#39;s external media channel uses <strong>G.711 μ-law (<code>ulaw</code>)</strong>. Make sure any PJSIP endpoint or SIP trunk that places or receives calls through PayInt Voice Studio allows <code>ulaw</code> (e.g. <code>allow=ulaw</code> in the endpoint config).</p>\n</div>\n\n\n\n<p>Refer to the <a href=\"https://docs.asterisk.org/\">Asterisk WebSocket documentation</a> for additional <code>websocket_client.conf</code> options and TLS configuration.</p>\n<h3 id=\"apply-the-configuration-changes\">Apply the configuration changes</h3>\n<p>Reload the affected Asterisk modules from the Asterisk CLI (<code>asterisk -rvvv</code>):</p>\n<pre><code class=\"language-bash\">module reload res_ari.so                   # picks up ari.conf changes\nmodule reload res_websocket_client.so      # picks up websocket_client.conf changes\n</code></pre>\n<p>Changes to <code>http.conf</code> require a full Asterisk reload (<code>core reload</code>) or a service restart. <code>core reload</code> also covers both commands above if you would rather reload everything at once.</p>\n<h2 id=\"part-2-create-the-configuration-in-payint-voice-studio\">Part 2: Create the configuration in PayInt Voice Studio</h2>\n<h3 id=\"step-1-navigate-to-telephony-settings\">Step 1: Navigate to Telephony Settings</h3>\n<ol>\n<li>Navigate to <strong>/telephony-configurations</strong> and click <strong>Add configuration</strong></li>\n<li>Select <strong>Asterisk ARI</strong> as your provider</li>\n</ol>\n<h3 id=\"step-2-enter-your-ari-credentials\">Step 2: Enter Your ARI Credentials</h3>\n<p>Configure the following fields:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>ARI Endpoint URL</strong></td>\n<td>HTTP base URL of your Asterisk ARI server</td>\n<td><code>http://asterisk.example.com:8088</code></td>\n</tr>\n<tr>\n<td><strong>ARI Username</strong></td>\n<td>The ARI username configured in <code>ari.conf</code></td>\n<td><code>dograh</code></td>\n</tr>\n<tr>\n<td><strong>App Password</strong></td>\n<td>The ARI password configured in <code>ari.conf</code></td>\n<td><code>your_secure_password</code></td>\n</tr>\n<tr>\n<td><strong>WebSocket Client Name</strong></td>\n<td>The connection name from <code>websocket_client.conf</code></td>\n<td><code>dograh</code></td>\n</tr>\n<tr>\n<td><strong>From Extensions</strong></td>\n<td>Optional SIP extensions or trunk numbers for outbound calls</td>\n<td><code>PJSIP/6001</code> or <code>6001</code></td>\n</tr>\n<tr>\n<td><strong>Dial String Template</strong></td>\n<td>How a dialed number reaches your trunk. See <a href=\"#outbound-calling\">Outbound Calling</a></td>\n<td><code>PJSIP/{number}@my-trunk</code></td>\n</tr>\n</tbody></table>\n<p><strong>Stasis App Name</strong> is not something you enter — PayInt Voice Studio generates it when you save and displays it on the configuration for you to copy.</p>\n<h3 id=\"step-3-save-and-copy-the-stasis-app-name\">Step 3: Save and copy the Stasis App Name</h3>\n<p>Click <strong>Save Configuration</strong>. PayInt Voice Studio assigns a <strong>Stasis App Name</strong> to this configuration — something like <code>dograh_a1b2c3d4e5f6</code> — and shows it on the configuration page. Copy it; you need it for <a href=\"#part-3-route-calls-into-the-stasis-application\">Part 3</a>.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The Stasis App Name is generated, not chosen. Asterisk gives a Stasis application to whichever ARI connection registered for it most recently and stops delivering events to the previous one, with no error on either side. If two configurations named the same application on one Asterisk, one would silently stop receiving calls while the other received calls that were not its own. Generating the name is what prevents that.</p>\n</div>\n\n\n\n<h2 id=\"part-3-route-calls-into-the-stasis-application\">Part 3: Route calls into the Stasis application</h2>\n<h3 id=\"configure-the-stasis-dialplan-extensionsconf\">Configure the Stasis Dialplan (<code>extensions.conf</code>)</h3>\n<p>Route incoming calls into the Stasis application PayInt Voice Studio generated for your configuration:</p>\n<pre><code class=\"language-ini\">[from-external]\nexten =&gt; _X.,1,NoOp(Incoming call to ${EXTEN})\n same =&gt; n,Stasis(dograh_a1b2c3d4e5f6)\n same =&gt; n,Hangup()\n</code></pre>\n<p>Replace <code>dograh_a1b2c3d4e5f6</code> with the <strong>Stasis App Name</strong> from your configuration, then reload the dialplan:</p>\n<pre><code class=\"language-bash\">dialplan reload\n</code></pre>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Until the dialplan names the generated application, calls reach Asterisk but never arrive at PayInt Voice Studio. This is the most common reason a correctly configured ARI integration receives no calls.</p>\n</div>\n\n\n\n<h2 id=\"part-4-add-extensions-and-test\">Part 4: Add extensions and test</h2>\n<ol>\n<li>Add each SIP extension that should be reachable as a <strong>phone number</strong> (e.g. <code>8000</code>). For inbound, you&#39;ll assign a workflow to each extension separately — see <a href=\"#inbound-calling\">Inbound Calling</a> below.</li>\n<li>Create a test workflow and initiate a test call to verify the connection.</li>\n</ol>\n<h2 id=\"outbound-calling\">Outbound Calling</h2>\n<p>Asterisk dials a channel technology and a route, not a number. PayInt Voice Studio builds that dial string from the <strong>Dial String Template</strong> on your configuration, replacing <code>{number}</code> with the number being called.</p>\n<p>The default is <code>PJSIP/{number}</code>, which dials a PJSIP endpoint <em>named after the number</em>. That is right when your endpoints are the extensions you dial, and wrong on an install whose endpoints are trunks — there, originating <code>PJSIP/+966500000000</code> fails with <code>Allocation failed</code>, because no endpoint by that name exists.</p>\n<p>Pick the template that matches how your dialplan places outbound calls:</p>\n<table>\n<thead>\n<tr>\n<th>Your Asterisk</th>\n<th>Template</th>\n<th>Dials</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Endpoints named after extensions</td>\n<td><code>PJSIP/{number}</code></td>\n<td><code>PJSIP/1001</code></td>\n</tr>\n<tr>\n<td>One outbound trunk</td>\n<td><code>PJSIP/{number}@my-trunk</code></td>\n<td><code>PJSIP/+966500000000@my-trunk</code></td>\n</tr>\n<tr>\n<td>FreePBX, or any dialplan with outbound routes</td>\n<td><code>Local/{number}@from-internal</code></td>\n<td><code>Local/+966500000000@from-internal</code></td>\n</tr>\n</tbody></table>\n<p>Routing through a <code>Local</code> channel is usually the right choice on FreePBX: the call enters your dialplan, and your own outbound routes handle number translation and trunk selection exactly as they do for a deskphone.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>A destination that already names a channel technology — <code>PJSIP/1001@my-trunk</code>, <code>Local/1001@from-internal</code>, <code>SIP/1001</code> — is dialed exactly as written, whatever the template says. That is what lets one campaign row or one test call address a specific trunk without changing the configuration.</p>\n</div>\n\n\n\n<h3 id=\"campaign-contacts\">Campaign contacts</h3>\n<p>A campaign&#39;s <code>phone_number</code> column is a destination for this Asterisk, not necessarily a phone number. Extensions (<code>1001</code>), SIP URIs (<code>sip:1001@pbx.local</code>) and dial strings (<code>PJSIP/1001@my-trunk</code>) are all accepted alongside E.164 numbers, and each row goes through the same template above.</p>\n<p>Two rules still apply: a destination cannot contain spaces, commas or ampersands, because each of those means something else inside a dial string; and destinations must be unique within a campaign.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Other providers are carriers and reject anything that is not a routable number, so their campaign uploads still require E.164 (<code>+</code> and country code). The rule follows the configuration the campaign dials with.</p>\n</div>\n\n\n\n<h2 id=\"inbound-calling\">Inbound Calling</h2>\n<p>Unlike other telephony providers that use HTTP webhooks for inbound calls, ARI delivers inbound calls as <strong>StasisStart events on the ARI WebSocket</strong>. PayInt Voice Studio automatically detects these events and activates the workflow assigned to the called extension.</p>\n<h3 id=\"how-it-works\">How It Works</h3>\n<ol>\n<li>An external call arrives at Asterisk and the dialplan routes it into your configuration&#39;s Stasis application</li>\n<li>Asterisk fires a StasisStart event over the ARI WebSocket with the channel in <code>Ring</code> state and the dialed extension in the dialplan context</li>\n<li>PayInt Voice Studio looks up the called extension in your telephony configuration&#39;s phone numbers, finds the assigned workflow, validates quota, and creates a workflow run</li>\n<li>The call is answered, bridged to an external media channel, and your voice agent workflow begins</li>\n</ol>\n<p>Workflow assignment is <strong>per extension</strong>, so different extensions on the same Asterisk can route to different agents.</p>\n<h3 id=\"setting-up-inbound-calls\">Setting Up Inbound Calls</h3>\n<p><strong>Step 1: Configure the Asterisk dialplan</strong></p>\n<p>Ensure your dialplan routes the extensions you care about into the Stasis application you configured in <a href=\"#part-3-route-calls-into-the-stasis-application\">Part 3</a>. Either route a specific extension:</p>\n<pre><code class=\"language-ini\">[from-external]\nexten =&gt; 8000,1,NoOp(Incoming call to 8000)\n same =&gt; n,Stasis(dograh_a1b2c3d4e5f6)\n same =&gt; n,Hangup()\n</code></pre>\n<p>…or use a pattern that catches every extension you&#39;ll register in PayInt Voice Studio:</p>\n<pre><code class=\"language-ini\">[from-external]\nexten =&gt; _X.,1,NoOp(Incoming call to ${EXTEN})\n same =&gt; n,Stasis(dograh_a1b2c3d4e5f6)\n same =&gt; n,Hangup()\n</code></pre>\n<p>Replace <code>dograh_a1b2c3d4e5f6</code> with the <strong>Stasis App Name</strong> shown on your PayInt Voice Studio configuration.</p>\n<p><strong>Step 2: Add the extension as a phone number in PayInt Voice Studio</strong></p>\n<ol>\n<li>Go to <strong>/telephony-configurations</strong> and open your Asterisk ARI configuration</li>\n<li>In the <strong>Phone numbers</strong> section, add a phone number whose address is the SIP extension (e.g. <code>8000</code>)</li>\n<li>Set its <strong>Inbound workflow</strong> to the agent that should answer</li>\n<li>Save</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p> Adding the extension in PayInt Voice Studio doesn&#39;t change Asterisk&#39;s dialplan — that&#39;s\n what Step 1 is for. The PayInt Voice Studio entry tells the StasisStart handler which\n workflow to run when a call to that extension reaches the Stasis app.</p>\n</div>\n\n\n\n<p>Repeat Step 2 for each extension that should reach a voice agent.</p>\n<p><strong>Step 3: Test an inbound call</strong></p>\n<p>Place a call to one of the extensions you configured. You should see the assigned workflow activate and the voice agent respond.</p>\n<h3 id=\"inbound-call-context\">Inbound Call Context</h3>\n<p>When an inbound call activates a workflow, the following context is available to your workflow:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>caller_number</code></td>\n<td>The caller&#39;s phone number or extension</td>\n</tr>\n<tr>\n<td><code>called_number</code></td>\n<td>The dialed number or extension</td>\n</tr>\n<tr>\n<td><code>direction</code></td>\n<td>Always <code>inbound</code></td>\n</tr>\n<tr>\n<td><code>call_id</code></td>\n<td>The Asterisk channel ID</td>\n</tr>\n<tr>\n<td><code>provider</code></td>\n<td>Always <code>ari</code></td>\n</tr>\n</tbody></table>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Cannot connect to ARI endpoint</summary>\n\n\n<ul>\n<li>Verify the ARI endpoint URL is correct and reachable from your PayInt Voice Studio instance</li>\n<li>Check that the Asterisk HTTP server is running (<code>http.conf</code> has <code>enabled = yes</code>)</li>\n<li>Ensure firewall rules allow traffic on the ARI port (default: 8088)</li>\n<li>Confirm the ARI module is loaded: run <code>module show like res_ari</code> in the Asterisk CLI</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Authentication failed</summary>\n\n\n<ul>\n<li>Verify the ARI Username matches the ARI user section name in <code>ari.conf</code></li>\n<li>Check the App Password matches the password in <code>ari.conf</code></li>\n<li>Ensure there are no extra spaces in the credentials</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>No audio during calls</summary>\n\n\n<ul>\n<li>Verify <code>chan_websocket</code> is loaded: run <code>module show like chan_websocket</code> in the Asterisk CLI</li>\n<li>Check that <code>websocket_client.conf</code> is correctly configured with the right PayInt Voice Studio URI</li>\n<li>Ensure the WebSocket Client Name in PayInt Voice Studio matches the section name in <code>websocket_client.conf</code></li>\n<li>Verify network connectivity and firewall rules allow WebSocket traffic between Asterisk and PayInt Voice Studio</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Calls not reaching PayInt Voice Studio</summary>\n\n\n<ul>\n<li>Ensure the dialplan routes calls to <code>Stasis(...)</code> using the <strong>Stasis App Name</strong> from your PayInt Voice Studio configuration, not the ARI username</li>\n<li>Confirm you ran <code>dialplan reload</code> after editing <code>extensions.conf</code></li>\n<li>If two PayInt Voice Studio configurations point at this Asterisk, check that they do not name the same Stasis application — the one that connected most recently takes the application over and the other stops receiving calls entirely</li>\n<li>Check Asterisk CLI for errors: <code>asterisk -rvvv</code></li>\n<li>Confirm the ARI WebSocket connection is active</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Inbound calls are immediately hung up</summary>\n\n\n<ul>\n<li>Verify the called extension is added as a phone number under your ARI\nconfiguration in /telephony-configurations and has an <strong>Inbound workflow</strong>\nassigned</li>\n<li>Confirm the workflow exists and belongs to the same organization as the\nARI config</li>\n<li>Check that your organization has available quota</li>\n<li>Review PayInt Voice Studio logs for warnings like &quot;no matching phone number registered\nfor config&quot; or &quot;has no inbound_workflow_id assigned&quot;</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>WebSocket client connection issues</summary>\n\n\n<ul>\n<li>Check the URI in <code>websocket_client.conf</code> points to the correct PayInt Voice Studio host and port</li>\n<li>Verify the PayInt Voice Studio instance is running and accepting WebSocket connections</li>\n<li>If using TLS, ensure certificates are correctly configured on both sides</li>\n</ul>\n</details>\n\n\n\n\n\n\n<h2 id=\"best-practices\">Best Practices</h2>\n<ul>\n<li>Keep your Asterisk instance on the same network or a low-latency connection to PayInt Voice Studio for optimal audio quality</li>\n<li>Use strong passwords for ARI authentication</li>\n<li>Restrict ARI access to known IP addresses using firewall rules</li>\n<li>Monitor Asterisk logs alongside PayInt Voice Studio logs when debugging call issues</li>\n<li>Keep Asterisk updated to the latest stable version for security and compatibility</li>\n</ul>\n<h2 id=\"further-reading\">Further Reading</h2>\n<ul>\n<li><a href=\"https://docs.asterisk.org/\">Asterisk Documentation</a> -- official reference for all Asterisk configuration</li>\n<li><a href=\"https://docs.asterisk.org/Configuration/Interfaces/Asterisk-REST-Interface-ARI/\">ARI Documentation</a> -- detailed ARI configuration and API reference</li>\n</ul>\n"},"integrations/telephony/vicidial":{"title":"VICIdial Integration","description":"Connect PayInt Voice Studio to VICIdial through Asterisk ARI for call control, in-group transfers, and lead updates","html":"<h2 id=\"overview\">Overview</h2>\n<p>Use this integration when VICIdial owns the customer call and connects PayInt Voice Studio\nto it through Asterisk. PayInt Voice Studio uses Asterisk ARI for audio and VICIdial APIs for\noperations that must affect the original customer leg:</p>\n<ul>\n<li>Hang up the customer call when the agent ends the conversation</li>\n<li>Transfer the customer to a VICIdial in-group</li>\n<li>Update selected VICIdial lead fields from gathered workflow context</li>\n</ul>\n<p>VICIdial credentials and lead-field controls are an advanced organization\nfeature. Context Mapping in Transfer Call tools is generally available and can\nalso route calls for telephony providers that are not connected to VICIdial.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before configuring VICIdial, ensure that:</p>\n<ul>\n<li>Asterisk ARI is connected to PayInt Voice Studio as described in the\n<a href=\"/studio/docs/integrations/telephony/asterisk-ari\">Asterisk ARI integration</a></li>\n<li>VICIdial sends the customer call to the Asterisk dialplan that enters the\nPayInt Voice Studio Stasis application</li>\n<li>You have VICIdial agent API credentials with permission to hang up and\nperform in-group transfers</li>\n<li>If workflows will update leads, you also have non-agent API credentials with\npermission to call <code>update_lead</code></li>\n</ul>\n<h2 id=\"enable-the-organization-feature\">Enable the organization feature</h2>\n<ol>\n<li>Open <strong>Platform Settings</strong> by going to /studio/settings</li>\n<li>Enable <strong>External PBX integrations</strong>.</li>\n</ol>\n<p>This switch reveals VICIdial credentials in Asterisk telephony configurations\nand lead capture/update settings in workflow configurations. Disabling it hides\nthose VICIdial-specific controls without deleting their saved values; Context\nMapping remains available in Transfer Call tools.</p>\n<h2 id=\"configure-vicidial-credentials\">Configure VICIdial credentials</h2>\n<ol>\n<li>Open <strong>Telephony Configurations</strong>.</li>\n<li>Create or edit an <strong>Asterisk ARI</strong> configuration.</li>\n<li>In <strong>External PBX</strong>, set <strong>External PBX Type</strong> to <strong>VICIdial</strong>.</li>\n<li>Configure the agent API:<ul>\n<li><strong>Agent API URL</strong>: normally ends in <code>/agc/api.php</code></li>\n<li><strong>Agent API User</strong></li>\n<li><strong>Agent API Password</strong></li>\n</ul>\n</li>\n<li>To update leads, configure the non-agent API:<ul>\n<li><strong>Non-Agent API URL</strong>: normally ends in <code>/vicidial/non_agent_api.php</code></li>\n<li><strong>Non-Agent API User</strong></li>\n<li><strong>Non-Agent API Password</strong></li>\n</ul>\n</li>\n<li>Save the configuration.</li>\n</ol>\n<p>The non-agent API is optional when no lead updates are required. If it is used,\nits URL, user, and password must all be present. Credentials are stored with the\ntelephony configuration and are returned masked by the PayInt Voice Studio API.</p>\n<h2 id=\"pass-vicidial-call-identity-to-asterisk\">Pass VICIdial call identity to Asterisk</h2>\n<p>PayInt Voice Studio needs VICIdial&#39;s call identity to control the original customer leg.\nConfigure VICIdial or the connecting dialplan to preserve these SIP headers on\nthe call that enters the PayInt Voice Studio Stasis application:</p>\n<table>\n<thead>\n<tr>\n<th>Header</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>X-VICIDIAL-callerid</code></td>\n<td>Call-control identifier used by the agent API</td>\n</tr>\n<tr>\n<td><code>X-VICIDIAL-user</code></td>\n<td>Remote-agent user used by the agent API</td>\n</tr>\n<tr>\n<td><code>X-VICIDIAL-lead_id</code></td>\n<td>Lead identifier used by optional lead updates</td>\n</tr>\n<tr>\n<td><code>X-VICIDIAL-campaign_id</code></td>\n<td><strong>(Optional)</strong> Campaign context retained with the call</td>\n</tr>\n<tr>\n<td><code>X-VICIDIAL-ingroup_id</code></td>\n<td><strong>(Optional)</strong> Original in-group available to transfer fallback routing</td>\n</tr>\n</tbody></table>\n<p>Hangup and transfer require the call-control identifier and remote-agent user.\nLead updates additionally require the lead ID. The source in-group header is\nonly required when a transfer mapping uses the <code>source</code> fallback.</p>\n<h3 id=\"pass-lead-data-in-additional-headers\">Pass lead data in additional headers</h3>\n<p>Configure VICIdial or the dialplan to attach lead fields as headers, for example\n<code>X-VICIDIAL-first_name</code>, <code>X-VICIDIAL-city</code>, or <code>X-VICIDIAL-vendor_lead_code</code>.\nHeaders with empty values are ignored.</p>\n<p>Each lead field is then read individually during call setup, so PayInt Voice Studio captures\nonly the fields you list:</p>\n<ol>\n<li>Open the workflow&#39;s <strong>General Settings</strong>.</li>\n<li>Under <strong>External PBX Field Updates</strong>, find <strong>Lead Fields To Capture</strong>.</li>\n<li>Add one entry per field, named <strong>without</strong> the <code>X-VICIDIAL-</code> prefix — enter\n<code>first_name</code> to capture <code>X-VICIDIAL-first_name</code>.</li>\n</ol>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Every field you add costs one extra request to Asterisk while the call is being\nset up, which delays the agent&#39;s first response. List only the fields your\nprompts actually use.</p>\n</div>\n\n\n\n<h4 id=\"discover-which-fields-your-pbx-sends\">Discover which fields your PBX sends</h4>\n<p>Each inbound call logs the available field names automatically:</p>\n<pre><code class=\"language-text\">[ARI org=7] Available vicidial lead fields on channel 1712...: [&#39;callerid&#39;,\n&#39;city&#39;, &#39;first_name&#39;, &#39;lead_id&#39;, &#39;user&#39;] — add the ones you need under the\nworkflow&#39;s Lead Fields To Capture setting\n</code></pre>\n<p>Listing the names costs a single request no matter how many headers are present,\nbecause it reads names only — never values.</p>\n<p>Captured fields are available to workflow prompts through template variables\nnamed after the header suffix:</p>\n<pre><code class=\"language-text\">Hello {{external_pbx_call.lead.first_name | there}}, I see you&#39;re calling\nfrom {{external_pbx_call.lead.city}}.\n</code></pre>\n<h2 id=\"configure-in-group-transfers\">Configure in-group transfers</h2>\n<p>In-group mappings select a VICIdial destination from information gathered by\nthe agent during a call.</p>\n<ol>\n<li>Open or create a <strong>Transfer Call</strong> tool.</li>\n<li>Select <strong>Context Mapping</strong> as the destination source.</li>\n<li>In <strong>Rule 1</strong>, enter a context path, such as <code>qualified</code>,\n<code>gathered_context.qualified</code>, or <code>initial_context.qualified</code>. An unprefixed\npath checks gathered context first and then initial context.</li>\n<li>Add one route for each expected value and its destination in-group ID.</li>\n<li>Optionally select <strong>Add routing rule</strong> to check another context path when the\nrule above it does not match, and use the arrows to order the rules.</li>\n<li>Optionally configure a fallback in-group. Enter <code>source</code> to return the\ncaller to the original in-group captured from the SIP headers.</li>\n<li>Save the tool and attach it to the workflow.</li>\n</ol>\n<p>Rules are evaluated top to bottom. The first rule whose context value matches\none of its routes wins, so put the most specific routing first; the fallback\napplies only after every rule misses. The match is case-insensitive. PayInt Voice Studio\nperforms final variable extraction before resolving the mapping. If no rule or\nfallback matches, the transfer fails and the agent can recover instead of\nsilently hanging up the caller.</p>\n<h2 id=\"configure-lead-field-mappings\">Configure lead field mappings</h2>\n<p>Lead field mappings copy selected values from gathered context into the\nVICIdial lead before transfer or hangup.</p>\n<ol>\n<li>Open the workflow&#39;s <strong>Configurations</strong> dialog.</li>\n<li>In <strong>External PBX Field Updates</strong>, add a mapping.</li>\n<li>Enter the gathered-context path and the destination VICIdial lead field.</li>\n<li>Add any additional mappings and save the workflow configuration.</li>\n</ol>\n<p>For example, map <code>extracted_variables.customer_state</code> to <code>state</code>. A context\npath can address a direct gathered-context value or a nested value. Mappings\nwhose source value is absent are skipped.</p>\n<p>The VICIdial adapter ignores mappings that target the API control parameters\n<code>source</code>, <code>user</code>, <code>pass</code>, <code>function</code>, and <code>lead_id</code>. The non-agent API\nconfiguration is required for mappings to be applied. A lead-update failure\ndoes not prevent PayInt Voice Studio from attempting the requested transfer or hangup.</p>\n<h2 id=\"configure-disposition-mapping\">Configure disposition mapping</h2>\n<p>PayInt Voice Studio can record a completed call&#39;s outcome on the VICIdial lead&#39;s <code>status</code>\ncolumn, so a call PayInt Voice Studio handled appears in the same reports as one an agent\nhandled. First,\n<a href=\"/studio/docs/developer/call-dispositions#configure-outcomes-for-a-workflow\">configure the business outcomes for each workflow</a>.\nYou can then map those raw outcomes to the status codes used by your VICIdial\ndeployment:</p>\n<ol>\n<li>Open <strong>Platform Settings</strong> and enable <strong>Disposition mapping</strong> under\nPreferences.</li>\n<li>Select <strong>Configure mapping</strong>.</li>\n<li>Choose the VICIdial status code you want recorded for each PayInt Voice Studio\ndisposition. A disposition left as itself is sent unchanged.</li>\n<li>Select <strong>Add disposition</strong> for any custom workflow outcome or end-call tool\nreason that is not already listed, then save your preferences.</li>\n</ol>\n<p>Use the status codes already configured in your VICIdial deployment. PayInt Voice Studio\ndoes not prescribe the mapping, and VICIdial exposes no API for discovering\nthose codes automatically. Consult your own VICIdial status configuration or\nreport when choosing each target value.</p>\n<p>The mapped code is written to <code>gathered_context.mapped_call_disposition</code>, which\nis what VICIdial write-back, the disposition run filter, and reports read. To\nsend it in a webhook, reference that field explicitly. The automatically added\ntop-level webhook field contains the raw <code>gathered_context.call_disposition</code>.\nSee <a href=\"/studio/docs/developer/call-dispositions#understand-the-stored-fields\">Call Dispositions</a>\nfor the complete data model.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Map your do-not-call outcomes to <code>DNC</code> to have PayInt Voice Studio add the customer&#39;s\nnumber to VICIdial&#39;s <code>SYSTEM_INTERNAL</code> do-not-call list. A lead status stops\nthe current list being redialed; only the DNC entry travels with the number.</p>\n</div>\n\n\n\n<p>A disposition that does not contain one to six letters, numbers, underscores,\nor hyphens is not written at all. VICIdial keeps whatever it recorded for the\ncall, usually <code>RAXFER</code>. An unmapped disposition that already fits this format is\nwritten unchanged. PayInt Voice Studio never truncates a longer outcome into a\nplausible-looking but incorrect status.</p>\n<h2 id=\"runtime-behavior\">Runtime behavior</h2>\n<p>When the workflow transfers the call, PayInt Voice Studio resolves the configured in-group,\napplies available lead updates, asks VICIdial to transfer the customer leg, and\nthen closes only the local Asterisk media leg.</p>\n<p>When the conversation ends without a transfer, PayInt Voice Studio persists final gathered\ncontext, applies available lead updates, asks VICIdial to hang up the customer\nleg, and then closes the local Asterisk leg.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<ul>\n<li><strong>VICIdial settings are not visible:</strong> enable <strong>External PBX integrations</strong>\nin Platform Settings.</li>\n<li><strong>The local media leg ends but the customer remains connected:</strong> confirm that\n<code>X-VICIDIAL-callerid</code> and <code>X-VICIDIAL-user</code> reach Asterisk and that the agent\nAPI credentials can control the call.</li>\n<li><strong>A transfer cannot resolve a destination:</strong> inspect the workflow&#39;s final\ngathered context, the configured context path, and the mapping values.</li>\n<li><strong>The <code>source</code> fallback fails:</strong> ensure <code>X-VICIDIAL-ingroup_id</code> is present on\nthe inbound call.</li>\n<li><strong>Lead fields are unchanged:</strong> confirm <code>X-VICIDIAL-lead_id</code>, the non-agent API\ncredentials, and the destination field names.</li>\n<li><strong>Leads keep VICIdial&#39;s own status:</strong> the call&#39;s disposition has no entry\nunder <strong>Disposition mapping</strong>, or its mapped code is longer than six\ncharacters. Both are reported in the call logs, naming the disposition to\nmap.</li>\n</ul>\n"},"integrations/telephony/webhooks":{"title":"Webhooks and Callbacks","description":"How PayInt Voice Studio AI handles telephony webhooks and audio streaming","html":"<h2 id=\"overview\">Overview</h2>\n<p>PayInt Voice Studio AI uses webhooks to communicate with telephony providers for call events and audio streaming. For outbound calls, PayInt Voice Studio builds these URLs and hands them to the provider when it dials — you never configure them by hand. For inbound calls, you point your provider at a single dispatcher URL; see <a href=\"/studio/docs/integrations/telephony/inbound\">Inbound Calls</a>.</p>\n<p>Every webhook path lives under <code>/api/v1/telephony</code>.</p>\n<h2 id=\"webhook-types\">Webhook Types</h2>\n<h3 id=\"1-answer-webhook\">1. Answer Webhook</h3>\n<p>When an outbound call connects, the provider requests instructions. The path is provider-specific, and PayInt Voice Studio appends the routing parameters as a query string:</p>\n<pre><code>?workflow_id={workflow_id}&amp;workflow_run_id={workflow_run_id}&amp;organization_id={organization_id}\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Provider</th>\n<th>Method</th>\n<th>Path</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Twilio, Cloudonix</td>\n<td><code>POST</code></td>\n<td><code>/twiml</code></td>\n</tr>\n<tr>\n<td>Plivo</td>\n<td><code>POST</code></td>\n<td><code>/plivo-xml</code></td>\n</tr>\n<tr>\n<td>Vobiz</td>\n<td><code>POST</code></td>\n<td><code>/vobiz-xml</code></td>\n</tr>\n<tr>\n<td>Vonage</td>\n<td><code>GET</code></td>\n<td><code>/ncco</code></td>\n</tr>\n</tbody></table>\n<p>Telnyx, Asterisk ARI, and Exotel outbound have no answer webhook. Telnyx is call-control style — PayInt Voice Studio POSTs the stream and event URLs to Telnyx&#39;s API instead of returning markup. ARI streams over a WebSocket only. Exotel Connect Voice AI attaches <code>StreamUrl</code> at dial time.</p>\n<h4 id=\"twilio-twiml\">Twilio (TwiML)</h4>\n<pre><code class=\"language-xml\">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;\n&lt;Response&gt;\n    &lt;Connect&gt;\n        &lt;Stream url=&quot;wss://your-domain/api/v1/telephony/ws/123/11/789&quot; /&gt;\n    &lt;/Connect&gt;\n&lt;/Response&gt;\n</code></pre>\n<h4 id=\"vonage-ncco\">Vonage (NCCO)</h4>\n<pre><code class=\"language-json\">[\n  {\n    &quot;action&quot;: &quot;connect&quot;,\n    &quot;endpoint&quot;: [{\n      &quot;type&quot;: &quot;websocket&quot;,\n      &quot;uri&quot;: &quot;wss://your-domain/api/v1/telephony/ws/123/11/789&quot;,\n      &quot;content-type&quot;: &quot;audio/l16;rate=16000&quot;\n    }]\n  }\n]\n</code></pre>\n<p>Here <code>123</code> is the workflow, <code>11</code> the organization, and <code>789</code> the workflow run.</p>\n<h3 id=\"2-status-callbacks\">2. Status Callbacks</h3>\n<p>Receive call lifecycle events. Each is keyed on the workflow run:</p>\n<table>\n<thead>\n<tr>\n<th>Provider</th>\n<th>Path</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Twilio</td>\n<td><code>/twilio/status-callback/{workflow_run_id}</code></td>\n</tr>\n<tr>\n<td>Plivo</td>\n<td><code>/plivo/hangup-callback/{workflow_run_id}</code>, <code>/plivo/ring-callback/{workflow_run_id}</code></td>\n</tr>\n<tr>\n<td>Vobiz</td>\n<td><code>/vobiz/hangup-callback/{workflow_run_id}</code>, <code>/vobiz/ring-callback/{workflow_run_id}</code></td>\n</tr>\n<tr>\n<td>Vonage</td>\n<td><code>/vonage/events/{workflow_run_id}</code></td>\n</tr>\n<tr>\n<td>Telnyx</td>\n<td><code>/telnyx/events/{workflow_run_id}</code></td>\n</tr>\n<tr>\n<td>Cloudonix</td>\n<td><code>/cloudonix/status-callback/{workflow_run_id}</code>, <code>/cloudonix/cdr</code></td>\n</tr>\n<tr>\n<td>Exotel</td>\n<td><code>/exotel/status-callback/{workflow_run_id}</code></td>\n</tr>\n</tbody></table>\n<p>Providers report their own vocabulary; PayInt Voice Studio normalizes it into a common set of states:</p>\n<ul>\n<li><code>initiated</code> - Call request received</li>\n<li><code>ringing</code> - Call is ringing</li>\n<li><code>in-progress</code> - Call is connected and streaming</li>\n<li><code>answered</code> - Call was answered</li>\n<li><code>completed</code> - Call ended normally</li>\n<li><code>busy</code> - Line was busy</li>\n<li><code>no-answer</code> - Call not answered</li>\n<li><code>canceled</code> - Call was canceled before connecting</li>\n<li><code>failed</code> - Call failed</li>\n<li><code>error</code> - Provider reported an error</li>\n</ul>\n<p>A status PayInt Voice Studio does not recognize is passed through unchanged rather than dropped.</p>\n<h3 id=\"3-websocket-audio-stream\">3. WebSocket Audio Stream</h3>\n<p>Real-time audio streaming for voice interaction.</p>\n<p><strong>Endpoint</strong>: <code>/api/v1/telephony/ws/{workflow_id}/{organization_id}/{workflow_run_id}</code></p>\n<p>When <a href=\"/studio/docs/developer/environment-variables#telephony\"><code>TELEPHONY_WS_TOKEN_SECRET</code></a> is set, the URL PayInt Voice Studio hands the carrier gains a fourth segment holding the HMAC signature: <code>/api/v1/telephony/ws/{workflow_id}/{organization_id}/{workflow_run_id}/{token}</code>. It is a path segment rather than a query parameter because carriers do not reliably forward query strings — Twilio strips them from `` entirely.</p>\n<p>Asterisk ARI instead connects to <code>/api/v1/telephony/ws/ari</code> and passes the same three values as query parameters, plus <code>token</code> when a secret is configured — see <a href=\"/studio/docs/integrations/telephony/asterisk-ari\">Asterisk ARI</a>.</p>\n<p>The <code>organization_id</code> segment is the tenant that owns the workflow. PayInt Voice Studio scopes every workflow and workflow-run lookup by it, so a run belonging to one organization can never be served under another&#39;s id.</p>\n<p><strong>Audio Formats</strong>:</p>\n<ul>\n<li><strong>Twilio / Plivo / Vobiz</strong>: 8kHz μ-law (MULAW), Base64-encoded in JSON messages</li>\n<li><strong>Vonage</strong>: 16kHz Linear PCM, Binary frames</li>\n<li><strong>Asterisk ARI</strong>: 8kHz Linear PCM via externalMedia</li>\n</ul>\n<h2 id=\"how-it-works\">How It Works</h2>\n<p>PayInt Voice Studio AI automatically:</p>\n<ol>\n<li>Constructs webhook URLs based on your deployment</li>\n<li>Passes them to the telephony provider when initiating calls</li>\n<li>Verifies webhook signatures for security:<ul>\n<li><strong>Twilio</strong>: HMAC-SHA1 signature validation</li>\n<li><strong>Plivo / Vobiz</strong>: HMAC-SHA256 signature validation</li>\n<li><strong>Vonage</strong>: JWT token verification</li>\n</ul>\n</li>\n<li>Processes status updates to track call lifecycle</li>\n<li>Manages WebSocket connections for audio streaming</li>\n<li>Handles provider-specific audio formats and protocols</li>\n</ol>\n<p>Signature verification is implemented per provider in <code>verify_inbound_signature</code>. If you are adding a provider, see <a href=\"/studio/docs/integrations/telephony/custom\">Custom Telephony Provider</a>.</p>\n<h2 id=\"local-development\">Local Development</h2>\n<p>For local development, use the built-in Cloudflare tunnel:</p>\n<pre><code class=\"language-yaml\"># docker-compose.yml includes:\ncloudflared:\n  image: cloudflare/cloudflared:latest\n  command: tunnel --no-autoupdate --url http://api:8000\n</code></pre>\n<p>The tunnel URL is automatically detected and used for webhooks.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<details class=\"as-accordion\"><summary>Webhook URL not accessible</summary>\n\n\n<ul>\n<li>Verify your domain/tunnel URL is publicly accessible</li>\n<li>Check firewall rules allow incoming HTTPS traffic</li>\n<li>Test with <code>curl</code> from external network</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Signature verification failures</summary>\n\n\n<ul>\n<li>Providers sign the full URL, query string included — a proxy that rewrites or reorders query parameters will invalidate the signature</li>\n<li>Confirm the backend&#39;s public URL matches what the provider was given, including scheme and port</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>WebSocket connection dropping</summary>\n\n\n<ul>\n<li>Check WebSocket upgrade headers are preserved</li>\n<li>Verify no timeout on load balancer/proxy</li>\n<li>Monitor for memory/CPU constraints</li>\n</ul>\n</details>\n\n\n\n\n\n<details class=\"as-accordion\"><summary>Status callbacks not received</summary>\n\n\n<ul>\n<li>Verify workflow_run_id is included in URL</li>\n<li>Check provider console for webhook errors</li>\n<li>Review webhook retry logs</li>\n</ul>\n</details>\n\n\n\n\n\n"},"integrations/telephony/agent-stream":{"title":"Agent Stream","description":"Stream audio to a PayInt Voice Studio agent over a WebSocket","html":"<h2 id=\"overview\">Overview</h2>\n<p>Agent Stream is a WebSocket endpoint that lets a telephony provider point its media stream at a single URL and drive a PayInt Voice Studio agent run. The agent UUID in the URL selects the agent, and the provider path segment selects the streaming protocol. Provider-specific identifiers come from the stream protocol itself. For Cloudonix, PayInt Voice Studio reads the domain from the <code>start.accountSid</code> field, then uses the matching stored telephony configuration to validate the session and issue provider API calls (hangup, transfer) during the call.</p>\n<p>This is useful when:</p>\n<ul>\n<li>You&#39;re integrating PayInt Voice Studio into a SIP gateway or in-house dialer that already speaks a supported provider&#39;s streaming protocol</li>\n<li>You want one stable endpoint per agent rather than wiring up an inbound webhook per phone number</li>\n</ul>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Agent Stream currently supports the <strong>Cloudonix</strong> provider only. Other providers\nreturn <code>NotImplementedError</code> until a per-provider implementation lands. If you\nneed Twilio, Plivo, Telnyx, Vonage, ARI, or another provider, please open a\nrequest on <a href=\"https://github.com/dograh-hq/dograh/discussions\">GitHub Discussions</a>\nwith your use case.</p>\n</div>\n\n\n\n<h2 id=\"endpoint\">Endpoint</h2>\n<pre><code>wss://api.PayInt Voice Studio/api/v1/agent-stream/{provider}/{agent_uuid}\n</code></pre>\n<p><code>{provider}</code> is the registered provider name, currently <code>cloudonix</code>. <code>{agent_uuid}</code> is the agent&#39;s stable UUID (see <a href=\"#get-the-agent-uuid\">Get the Agent UUID</a> below). On self-hosted deployments, replace <code>api.PayInt Voice Studio</code> with your backend host.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A PayInt Voice Studio agent (workflow) — published or in draft is fine</li>\n<li>A Cloudonix telephony configuration in your PayInt Voice Studio organization whose <code>domain_id</code> matches the <code>accountSid</code> Cloudonix sends in the stream <code>start</code> message. PayInt Voice Studio uses the bearer token from this configuration to validate the call session and to issue provider API calls (hangup, transfer).</li>\n</ul>\n<h2 id=\"get-the-agent-uuid\">Get the Agent UUID</h2>\n<p>The Agent UUID is the workflow&#39;s stable identifier — it doesn&#39;t change when versions are published.</p>\n<p>To find and copy it in the UI, see <a href=\"/studio/docs/configurations/agent-uuid\">Agent UUID</a>.</p>\n<h2 id=\"connect-to-the-websocket\">Connect to the WebSocket</h2>\n<h3 id=\"path-parameters\">Path parameters</h3>\n<table>\n<thead>\n<tr>\n<th>Param</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>provider</code></td>\n<td>Yes</td>\n<td>Provider name. Currently only <code>cloudonix</code> is supported.</td>\n</tr>\n<tr>\n<td><code>agent_uuid</code></td>\n<td>Yes</td>\n<td>Stable UUID of the PayInt Voice Studio agent to run.</td>\n</tr>\n</tbody></table>\n<h3 id=\"cloudonix-example\">Cloudonix example</h3>\n<pre><code>wss://api.PayInt Voice Studio/api/v1/agent-stream/cloudonix/{agent_uuid}\n</code></pre>\n<p>Use this URL inside the CXML `` your Cloudonix Voice Application returns when the call needs to be bridged to the PayInt Voice Studio agent:</p>\n<pre><code class=\"language-xml\">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;\n&lt;Response&gt;\n  &lt;Connect&gt;\n    &lt;Stream url=&quot;wss://api.PayInt Voice Studio/api/v1/agent-stream/cloudonix/{agent_uuid}&quot;/&gt;\n  &lt;/Connect&gt;\n  &lt;Pause length=&quot;40&quot;/&gt;\n&lt;/Response&gt;\n</code></pre>\n<p>The first two messages on the socket should be Cloudonix&#39;s standard <code>connected</code> and <code>start</code> events (Twilio-compatible framing). PayInt Voice Studio extracts <code>streamSid</code>, <code>callSid</code>, <code>session</code>, <code>accountSid</code>, <code>from</code>, <code>to</code>, <code>context</code>, <code>tracks</code>, and <code>mediaFormat</code> from the <code>start</code> event payload. It validates the session against Cloudonix using the bearer token from the stored telephony configuration matched by <code>accountSid</code>, then begins streaming audio.</p>\n<h2 id=\"workflow-run-lifecycle\">Workflow run lifecycle</h2>\n<p>When the WebSocket is accepted, PayInt Voice Studio:</p>\n<ol>\n<li>Looks up the workflow by <code>agent_uuid</code></li>\n<li>Runs a quota check against the workflow&#39;s owning user</li>\n<li>Creates a new <code>WorkflowRun</code> (<code>call_type=inbound</code>, <code>mode=cloudonix</code>, name <code>WR-AGS-XXXXXXXX</code>)</li>\n<li>Transitions the run to <code>running</code></li>\n<li>Reads and validates the Cloudonix <code>start</code> message, then stamps the <code>from</code>/<code>to</code> numbers on <code>initial_context</code>, stores <code>session</code> as <code>call_id</code> on <code>gathered_context</code>, and records <code>accountSid</code> under the run&#39;s <code>inbound_webhook</code> log</li>\n<li>Starts the agent pipeline</li>\n</ol>\n<p>The run is visible under the agent&#39;s <strong>Runs</strong> tab as soon as it&#39;s minted, just like an inbound or outbound call.</p>\n<h2 id=\"close-codes\">Close codes</h2>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Reason</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>1008</code></td>\n<td>Routing failure — unknown provider, workflow not found, or quota exceeded</td>\n</tr>\n<tr>\n<td><code>1011</code></td>\n<td>Server-side failure or unsupported provider for Agent Stream</td>\n</tr>\n<tr>\n<td><code>4400</code></td>\n<td>Provider-level handshake error — for cloudonix, missing <code>start.accountSid</code>, no matching telephony configuration, missing bearer token on the configuration, malformed <code>connected</code>/<code>start</code> events, or session validation failed against Cloudonix</td>\n</tr>\n</tbody></table>\n<h2 id=\"security-notes\">Security notes</h2>\n<ul>\n<li>Treat the URL as a secret — the agent UUID itself authorizes the connection. Store and transmit it only over TLS, and avoid logging the raw URL in places where access is broader than your operations team.</li>\n<li>No bearer tokens or provider secrets are passed in the URL. Provider credentials live in the stored telephony configuration (matched by <code>start.accountSid</code> for Cloudonix) and are used server-side by PayInt Voice Studio to validate the session and issue provider API calls.</li>\n</ul>\n"},"integrations/telephony/custom":{"title":"Custom Telephony Provider","description":"Build your own telephony provider integration for PayInt Voice Studio AI","html":"<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>This guide assumes familiarity with two libraries the provider interface is built on:</p>\n<ul>\n<li><strong><a href=\"https://docs.pipecat.ai\">Pipecat</a></strong> — the open-source real-time voice/multimodal pipeline framework PayInt Voice Studio&#39;s call pipeline is built on. Its <a href=\"https://docs.pipecat.ai/server/services/transport/fastapi-websocket\"><code>FastAPIWebsocketTransport</code></a> is what streams audio between the telephony provider and PayInt Voice Studio&#39;s pipeline — you&#39;ll be constructing one of these in your transport factory. If you haven&#39;t built a Pipecat transport or frame serializer before, skim their <a href=\"https://docs.pipecat.ai/server/base-classes/transport\">transports guide</a> first.</li>\n<li><strong><a href=\"https://docs.pydantic.dev\">Pydantic</a></strong> — used for the request/response schemas that define a provider&#39;s stored credentials (<code>config.py</code> below). If you&#39;re new to Pydantic, its <a href=\"https://docs.pydantic.dev/latest/concepts/models/\">models documentation</a> covers everything used here (<code>BaseModel</code>, <code>Field</code>, <code>Literal</code> discriminators).</li>\n</ul>\n<p>You don&#39;t need deep expertise in either — enough to read a <code>BaseModel</code> and a Pipecat transport class is sufficient to follow this guide.</p>\n<h2 id=\"overview\">Overview</h2>\n<p>A telephony provider is implemented as a <strong>self-registering package</strong> under <code>api/services/telephony/providers/&lt;name&gt;/</code>. The package contributes everything PayInt Voice Studio needs to wire the provider in — the provider class, transport factory, audio config, request/response schemas, optional HTTP routes, and the form metadata used to render its configuration UI — through a single <code>ProviderSpec</code> registered at import time.</p>\n<p>Adding a new provider should not require touching the factory, the audio config, the API routes module, the run-pipeline module, or the frontend. The only edits outside the provider folder are:</p>\n<ol>\n<li>One import line in <code>api/services/telephony/providers/__init__.py</code></li>\n<li>One import line in <code>api/schemas/telephony_config.py</code> to add the request/response classes to the <code>TelephonyConfigRequest</code> discriminated union</li>\n</ol>\n<h2 id=\"provider-package-layout\">Provider Package Layout</h2>\n<pre><code>api/services/telephony/providers/your_provider/\n├── __init__.py        # Builds and registers ProviderSpec\n├── config.py          # Pydantic Request/Response schemas\n├── provider.py        # TelephonyProvider subclass\n├── transport.py       # Pipecat WebSocket transport factory\n├── serializers.py     # Frame serializer (usually re-exports from pipecat)\n├── routes.py          # (optional) HTTP webhook/callback handlers\n└── strategies.py      # (optional) Transfer/hangup strategies\n</code></pre>\n<p>Three files are required (<code>__init__.py</code>, <code>config.py</code>, <code>provider.py</code>, <code>transport.py</code>). The rest are optional and are discovered automatically when present:</p>\n<ul>\n<li><strong><code>routes.py</code></strong> — if the module exists and exports <code>router: APIRouter</code>, the routes module is imported lazily and mounted under <code>/api/v1/telephony</code> by <code>api.routes.telephony</code> via <code>importlib</code>. Providers that only stream over WebSocket (e.g. ARI) can omit it.</li>\n<li><strong><code>strategies.py</code></strong> — used by transports that need provider-specific call transfer/hangup logic in the frame serializer (e.g. Twilio Conference transfers).</li>\n<li><strong><code>serializers.py</code></strong> — typically a re-export from pipecat. Keep the file even when it&#39;s a one-line re-export so transport code imports from <code>.serializers</code>, giving you an obvious place to drop a custom subclass later.</li>\n</ul>\n<h2 id=\"the-telephonyprovider-interface\">The <code>TelephonyProvider</code> Interface</h2>\n<p>Subclass <code>TelephonyProvider</code> in <code>provider.py</code>:</p>\n<pre><code class=\"language-python\">from api.services.telephony.base import (\n    CallInitiationResult,\n    NormalizedInboundData,\n    ProviderSyncResult,\n    TelephonyProvider,\n)\n\n\nclass YourProvider(TelephonyProvider):\n    PROVIDER_NAME = &quot;your_provider&quot;\n    WEBHOOK_ENDPOINT = &quot;your-provider-xml&quot;  # path under /api/v1/telephony\n\n    def __init__(self, config: dict):\n        self.api_key = config.get(&quot;api_key&quot;)\n        self.from_numbers = config.get(&quot;from_numbers&quot;, [])\n\n    # ---------- outbound ----------\n    async def initiate_call(self, to_number, webhook_url, workflow_run_id=None,\n                            from_number=None, **kwargs) -&gt; CallInitiationResult: ...\n    async def get_call_status(self, call_id) -&gt; dict: ...\n    async def get_call_cost(self, call_id) -&gt; dict: ...\n    async def get_available_phone_numbers(self) -&gt; list[str]: ...\n    def validate_config(self) -&gt; bool: ...\n\n    # ---------- webhooks ----------\n    async def verify_webhook_signature(self, url, params, signature) -&gt; bool: ...\n    async def get_webhook_response(self, workflow_id, organization_id, workflow_run_id) -&gt; str: ...\n    def parse_status_callback(self, data: dict) -&gt; dict: ...\n\n    # ---------- websocket ----------\n    async def handle_websocket(self, websocket, workflow_id, organization_id, workflow_run_id): ...\n\n    # ---------- inbound ----------\n    @classmethod\n    def can_handle_webhook(cls, webhook_data, headers) -&gt; bool: ...\n\n    @staticmethod\n    def parse_inbound_webhook(webhook_data) -&gt; NormalizedInboundData: ...\n\n    @staticmethod\n    def validate_account_id(config_data, webhook_account_id) -&gt; bool: ...\n\n    def normalize_phone_number(self, phone_number: str) -&gt; str: ...\n\n    async def verify_inbound_signature(self, url, webhook_data, headers, body=&quot;&quot;) -&gt; bool: ...\n\n    async def start_inbound_stream(self, *, websocket_url, workflow_run_id,\n                                   normalized_data, backend_endpoint): ...\n\n    @staticmethod\n    def generate_error_response(error_type, message) -&gt; tuple: ...\n\n    # ---------- transfers ----------\n    async def transfer_call(self, destination, transfer_id, conference_name,\n                            timeout=30, **kwargs) -&gt; dict: ...\n    def supports_transfers(self) -&gt; bool: ...\n\n    # ---------- optional ----------\n    async def configure_inbound(self, address, webhook_url) -&gt; ProviderSyncResult:\n        # Default returns ok=True — implement only if your provider supports\n        # programmatic webhook configuration (e.g. binding a number to a URL\n        # via API). Used to point inbound numbers at /api/v1/telephony/inbound/run.\n        return ProviderSyncResult(ok=True)\n</code></pre>\n<p>See <code>api/services/telephony/base.py</code> for the full docstrings on each method.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p><code>get_webhook_response</code> and <code>handle_websocket</code> receive an <code>organization_id</code>, not a\nuser id. It is the tenant that every workflow and workflow-run lookup must be\nscoped by — pass it straight through to <code>run_pipeline_telephony</code>. The workflow\nowner is deliberately not handed to providers; read it off the workflow row if you\nneed it for attribution.</p>\n</div>\n\n\n\n<h2 id=\"implementation-guide\">Implementation Guide</h2>\n<h3 id=\"1-configuration-schemas\">1. Configuration schemas</h3>\n<p>Define Pydantic models for the credential payload. The <code>provider</code> <code>Literal</code> discriminator is what makes the schemas dispatch correctly through the registry&#39;s discriminated union.</p>\n<pre><code class=\"language-python\"># providers/your_provider/config.py\nfrom typing import List, Literal\nfrom pydantic import BaseModel, Field\n\n\nclass YourProviderConfigurationRequest(BaseModel):\n    provider: Literal[&quot;your_provider&quot;] = Field(default=&quot;your_provider&quot;)\n    api_key: str = Field(..., description=&quot;Your Provider API key&quot;)\n    api_secret: str = Field(..., description=&quot;Your Provider API secret&quot;)\n    from_numbers: List[str] = Field(default_factory=list)\n\n\nclass YourProviderConfigurationResponse(BaseModel):\n    provider: Literal[&quot;your_provider&quot;] = Field(default=&quot;your_provider&quot;)\n    api_key: str        # masked when returned\n    api_secret: str     # masked when returned\n    from_numbers: List[str]\n</code></pre>\n<h3 id=\"2-transport-factory\">2. Transport factory</h3>\n<p>Build the Pipecat <code>FastAPIWebsocketTransport</code> for accepted WebSockets. Always load credentials through <code>load_credentials_for_transport</code> so the right config row is picked when the workflow run carries a <code>telephony_configuration_id</code> (multi-config orgs).</p>\n<pre><code class=\"language-python\"># providers/your_provider/transport.py\nfrom fastapi import WebSocket\nfrom api.services.pipecat.audio_config import AudioConfig\nfrom api.services.pipecat.audio_mixer import build_audio_out_mixer\nfrom api.services.telephony.factory import load_credentials_for_transport\nfrom pipecat.transports.websocket.fastapi import (\n    FastAPIWebsocketParams,\n    FastAPIWebsocketTransport,\n)\nfrom .serializers import YourProviderFrameSerializer\n\n\nasync def create_transport(\n    websocket: WebSocket,\n    workflow_run_id: int,\n    audio_config: AudioConfig,\n    organization_id: int,\n    *,\n    vad_config: dict | None = None,\n    ambient_noise_config: dict | None = None,\n    telephony_configuration_id: int | None = None,\n    # provider-specific kwargs (forwarded by run_pipeline_telephony as **transport_kwargs)\n    stream_id: str,\n    call_id: str,\n):\n    config = await load_credentials_for_transport(\n        organization_id, telephony_configuration_id,\n        expected_provider=&quot;your_provider&quot;,\n    )\n\n    serializer = YourProviderFrameSerializer(\n        stream_id=stream_id,\n        call_id=call_id,\n        api_key=config[&quot;api_key&quot;],\n    )\n    mixer = await build_audio_out_mixer(\n        audio_config.transport_out_sample_rate, ambient_noise_config\n    )\n\n    return FastAPIWebsocketTransport(\n        websocket=websocket,\n        params=FastAPIWebsocketParams(\n            audio_in_enabled=True,\n            audio_out_enabled=True,\n            audio_in_sample_rate=audio_config.transport_in_sample_rate,\n            audio_out_sample_rate=audio_config.transport_out_sample_rate,\n            audio_out_mixer=mixer,\n            serializer=serializer,\n        ),\n    )\n</code></pre>\n<h3 id=\"3-routes-optional\">3. Routes (optional)</h3>\n<p>If your provider POSTs webhooks to PayInt Voice Studio (answer URL, status callbacks, hangup callbacks), expose them through a module-level <code>router</code>. The routes are auto-mounted under <code>/api/v1/telephony</code>.</p>\n<pre><code class=\"language-python\"># providers/your_provider/routes.py\nfrom fastapi import APIRouter, Request\nfrom api.services.telephony.status_processor import (\n    StatusCallbackRequest,\n    _process_status_update,\n)\n\nrouter = APIRouter()\n\n\n@router.post(&quot;/your-provider/status-callback/{workflow_run_id}&quot;)\nasync def status_callback(workflow_run_id: int, request: Request):\n    ...\n</code></pre>\n<p>Routes are loaded lazily via <code>importlib</code> from <code>api.routes.telephony._mount_provider_routers</code>, so your route module can freely import other backend services without creating import cycles at provider-class load time.</p>\n<h3 id=\"4-register-the-providerspec\">4. Register the <code>ProviderSpec</code></h3>\n<p>The package&#39;s <code>__init__.py</code> is where everything comes together:</p>\n<pre><code class=\"language-python\"># providers/your_provider/__init__.py\nfrom typing import Any, Dict\n\nfrom api.services.pipecat.audio_config import AudioConfig\nfrom api.services.telephony.registry import (\n    ProviderSpec,\n    ProviderUIField,\n    ProviderUIMetadata,\n    register,\n)\n\nfrom .config import YourProviderConfigurationRequest, YourProviderConfigurationResponse\nfrom .provider import YourProvider\nfrom .transport import create_transport\n\n\ndef _config_loader(value: Dict[str, Any]) -&gt; Dict[str, Any]:\n    &quot;&quot;&quot;Normalize the stored credentials dict into the constructor shape.&quot;&quot;&quot;\n    return {\n        &quot;provider&quot;: &quot;your_provider&quot;,\n        &quot;api_key&quot;: value.get(&quot;api_key&quot;),\n        &quot;api_secret&quot;: value.get(&quot;api_secret&quot;),\n        &quot;from_numbers&quot;: value.get(&quot;from_numbers&quot;, []),\n    }\n\n\n_AUDIO_CONFIG = AudioConfig(\n    transport_in_sample_rate=8000,\n    transport_out_sample_rate=8000,\n    vad_sample_rate=8000,\n    pipeline_sample_rate=8000,\n    buffer_size_seconds=5.0,\n)\n\n\n_UI_METADATA = ProviderUIMetadata(\n    display_name=&quot;Your Provider&quot;,\n    docs_url=&quot;https://docs.your-provider.com&quot;,\n    fields=[\n        ProviderUIField(name=&quot;api_key&quot;,    label=&quot;API Key&quot;,    type=&quot;text&quot;,     sensitive=True),\n        ProviderUIField(name=&quot;api_secret&quot;, label=&quot;API Secret&quot;, type=&quot;password&quot;, sensitive=True),\n        ProviderUIField(\n            name=&quot;from_numbers&quot;, label=&quot;Phone Numbers&quot;, type=&quot;string-array&quot;,\n            description=&quot;E.164-formatted phone numbers used for outbound calls&quot;,\n        ),\n    ],\n)\n\n\nSPEC = ProviderSpec(\n    name=&quot;your_provider&quot;,\n    provider_cls=YourProvider,\n    config_loader=_config_loader,\n    transport_factory=create_transport,\n    audio_config=_AUDIO_CONFIG,\n    config_request_cls=YourProviderConfigurationRequest,\n    config_response_cls=YourProviderConfigurationResponse,\n    ui_metadata=_UI_METADATA,\n    # Credential field that uniquely identifies the provider account.\n    # Used to disambiguate inbound webhooks across multiple configs of the\n    # same provider. Empty string for providers without an account-id concept.\n    account_id_credential_field=&quot;api_key&quot;,\n)\n\nregister(SPEC)\n</code></pre>\n<p><code>ProviderSpec</code> covers everything downstream code needs:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Used by</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>Stored as the discriminator on every <code>TelephonyConfiguration</code> row and as the <code>WorkflowRunMode</code> value</td>\n</tr>\n<tr>\n<td><code>provider_cls</code></td>\n<td><code>factory.get_default_telephony_provider</code>, <code>get_telephony_provider_by_id</code>, <code>get_telephony_provider_for_run</code></td>\n</tr>\n<tr>\n<td><code>config_loader</code></td>\n<td><code>factory._normalize_with_phone_numbers</code> (replaces the old if/elif chain)</td>\n</tr>\n<tr>\n<td><code>transport_factory</code></td>\n<td><code>run_pipeline_telephony</code></td>\n</tr>\n<tr>\n<td><code>audio_config</code></td>\n<td><code>create_audio_config()</code> and <code>run_pipeline_telephony</code></td>\n</tr>\n<tr>\n<td><code>config_request_cls</code> / <code>config_response_cls</code></td>\n<td><code>TelephonyConfigRequest</code> discriminated union</td>\n</tr>\n<tr>\n<td><code>ui_metadata</code></td>\n<td><code>GET /api/v1/organizations/telephony-providers/metadata</code> (drives the form UI) and the <code>_sensitive_fields</code> masking helper</td>\n</tr>\n<tr>\n<td><code>account_id_credential_field</code></td>\n<td>Inbound webhook routing across multiple configs of the same provider</td>\n</tr>\n</tbody></table>\n<h3 id=\"5-wire-the-package-into-the-registry-import-chain\">5. Wire the package into the registry import chain</h3>\n<p>Add one import line to <code>api/services/telephony/providers/__init__.py</code>:</p>\n<pre><code class=\"language-python\">from api.services.telephony.providers import (  # noqa: F401  -- side effects\n    ari,\n    cloudonix,\n    plivo,\n    telnyx,\n    twilio,\n    vobiz,\n    vonage,\n    your_provider,        # ← add this\n)\n</code></pre>\n<h3 id=\"6-add-to-the-discriminated-union\">6. Add to the discriminated union</h3>\n<p>Add one import block to <code>api/schemas/telephony_config.py</code> so the request/response classes participate in the <code>TelephonyConfigRequest</code> union and the <code>TelephonyConfigurationResponse</code> shape:</p>\n<pre><code class=\"language-python\">from api.services.telephony.providers.your_provider.config import (\n    YourProviderConfigurationRequest,\n    YourProviderConfigurationResponse,\n)\n\nTelephonyConfigRequest = Annotated[\n    Union[\n        # ...existing entries...\n        YourProviderConfigurationRequest,\n    ],\n    Field(discriminator=&quot;provider&quot;),\n]\n\n\nclass TelephonyConfigurationResponse(BaseModel):\n    # ...existing entries...\n    your_provider: Optional[YourProviderConfigurationResponse] = None\n</code></pre>\n<p>That&#39;s it for backend wiring.</p>\n<h2 id=\"frontend\">Frontend</h2>\n<p>The configuration form is <strong>metadata-driven</strong>. The UI calls <code>GET /api/v1/organizations/telephony-providers/metadata</code>, gets back the list of providers and their <code>ProviderUIField</code> definitions, and renders each form generically. <strong>No per-provider frontend code is needed</strong> — your <code>ProviderUIMetadata</code> declaration is what drives the form.</p>\n<p>If you add a new field type that the existing renderer doesn&#39;t support (e.g. a file upload), extend the renderer in <code>ui/src/app/(authenticated)/telephony-configurations/</code>. The supported <code>ProviderUIField.type</code> values today are <code>text</code>, <code>password</code>, <code>textarea</code>, <code>string-array</code>, and <code>number</code>.</p>\n<h2 id=\"audio-format-considerations\">Audio Format Considerations</h2>\n<p>Each provider declares its wire format through its <code>AudioConfig</code>. Common shapes:</p>\n<ul>\n<li><strong>Twilio / Plivo</strong>: 8 kHz μ-law, base64-encoded JSON frames</li>\n<li><strong>Vonage</strong>: 16 kHz Linear PCM as binary frames</li>\n<li><strong>Asterisk ARI</strong>: 8 kHz Linear PCM via externalMedia</li>\n</ul>\n<p>The pipeline sample rate is capped at 16 kHz to satisfy <a href=\"/studio/docs/configurations/interruption#what-is-vad\">VAD</a>, which only accepts 8000 Hz or 16000 Hz; transports handle resampling between the wire format and the pipeline&#39;s internal rate.</p>\n<h2 id=\"testing\">Testing</h2>\n<pre><code class=\"language-python\"># api/tests/telephony/test_your_provider.py\nimport pytest\nfrom api.services.telephony.providers.your_provider import YourProvider\n\n\n@pytest.mark.asyncio\nasync def test_validate_config():\n    provider = YourProvider({\n        &quot;api_key&quot;: &quot;test_key&quot;,\n        &quot;api_secret&quot;: &quot;test_secret&quot;,\n        &quot;from_numbers&quot;: [&quot;+1234567890&quot;],\n    })\n    assert provider.validate_config() is True\n</code></pre>\n<p>For end-to-end testing, save your provider through the telephony-configurations UI and trigger a test call from a workflow.</p>\n<h2 id=\"best-practices\">Best Practices</h2>\n<ol>\n<li><strong>Trust the registry</strong> — never import another provider&#39;s class directly; resolve through the factory helpers (<code>get_default_telephony_provider</code>, <code>get_telephony_provider_by_id</code>, etc.).</li>\n<li><strong>Sensitive fields</strong> — mark every credential field <code>sensitive=True</code> in <code>ProviderUIMetadata</code>. The save endpoint masks these on read and preserves the original when the client re-submits a masked value.</li>\n<li><strong>Inbound signature verification</strong> — always validate inbound webhook signatures in <code>verify_inbound_signature</code>, and fail closed. If your provider signs every webhook (most do), a <em>missing</em> signature header means the request did not come from the provider: return <code>False</code>, exactly as <code>providers/twilio/</code> and <code>providers/plivo/</code> do. Never <code>return True</code> as a placeholder — these handlers are reachable by anyone who can guess a <code>workflow_run_id</code>.</li>\n<li><strong>Transports load credentials lazily</strong> — call <code>load_credentials_for_transport</code> with the <code>telephony_configuration_id</code> from the workflow run. Don&#39;t read the org&#39;s default config from <code>transport.py</code>.</li>\n<li><strong>Logging</strong> — use <code>loguru.logger</code>.</li>\n</ol>\n<h2 id=\"reference-implementations\">Reference Implementations</h2>\n<table>\n<thead>\n<tr>\n<th>Provider</th>\n<th>Notable for</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>providers/twilio/</code></td>\n<td>Full-featured: outbound, inbound, conference transfers, status callbacks, custom strategies</td>\n</tr>\n<tr>\n<td><code>providers/plivo/</code></td>\n<td>Recently-added reference; mirrors Twilio&#39;s shape with multi-callback signatures</td>\n</tr>\n<tr>\n<td><code>providers/vonage/</code></td>\n<td>JWT auth, 16 kHz Linear PCM, NCCO responses</td>\n</tr>\n<tr>\n<td><code>providers/cloudonix/</code></td>\n<td>SIP-based, custom call strategies</td>\n</tr>\n<tr>\n<td><code>providers/telnyx/</code></td>\n<td>Call-control style: REST-driven inbound answer flow rather than markup response</td>\n</tr>\n<tr>\n<td><code>providers/ari/</code></td>\n<td>Minimal example — no <code>routes.py</code>, no inbound webhook verification, WebSocket-only</td>\n</tr>\n</tbody></table>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Use ARI as the smallest viable example when your provider doesn&#39;t expose HTTP\nwebhooks, and Twilio as the reference when it does.</p>\n</div>\n\n\n"},"integrations/mcp":{"title":"MCP Server","description":"Connect Codex, Claude, and other AI assistants to your PayInt Voice Studio workspace via the Model Context Protocol","html":"<h2 id=\"overview\">Overview</h2>\n<p>PayInt Voice Studio exposes an <a href=\"https://modelcontextprotocol.io/\">MCP (Model Context Protocol)</a> server that lets AI assistants like Codex, Claude Code, Claude Desktop, and Cursor access your workspace and documentation. Once connected, an assistant can list your agents, fetch agent definitions, and search PayInt Voice Studio docs on your behalf.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A PayInt Voice Studio API key. Generate one at <a href=\"/studio/api-keys\"><code>/api-keys</code></a> (or <code>http://localhost:3010/api-keys</code> for self-hosted). See <a href=\"/studio/docs/configurations/api-keys\">API Keys</a>.</li>\n<li>Your PayInt Voice Studio MCP endpoint:<ul>\n<li>Hosted: <code>/studio/api/v1/mcp/</code></li>\n<li>Self-hosted: <code>&lt;YOUR_BACKEND_URL&gt;/api/v1/mcp/</code></li>\n</ul>\n</li>\n</ul>\n<p>The endpoint is also shown in <strong>Platform Settings → MCP Server</strong> inside the PayInt Voice Studio UI.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>If you deployed PayInt Voice Studio to a remote server using <a href=\"/studio/docs/deployment/docker#option-2%3A-remote-server-deployment\"><code>setup_remote.sh</code></a>, your endpoint is served with a self-signed SSL certificate. Claude Code will refuse MCP connections to it unless you start the CLI with TLS verification disabled:</p>\n<pre><code class=\"language-bash\">NODE_TLS_REJECT_UNAUTHORIZED=0 claude\n</code></pre>\n<p>This is only required for self-signed certificates. If you have set up a <a href=\"/studio/docs/deployment/custom-domain\">custom domain</a> with Let&#39;s Encrypt certificates via Certbot, no extra flag is needed.</p>\n</div>\n\n\n\n<h2 id=\"claude-code\">Claude Code</h2>\n<p>Register PayInt Voice Studio as an MCP server with the Claude Code CLI:</p>\n<pre><code class=\"language-bash\">claude mcp add --transport http dograh /studio/api/v1/mcp/ \\\n  --header &quot;X-API-Key: YOUR_API_KEY&quot;\n</code></pre>\n<p>Replace <code>YOUR_API_KEY</code> with the key you generated. For self-hosted deployments, swap the URL for your backend.</p>\n<p>Verify the server is connected:</p>\n<pre><code class=\"language-bash\">claude mcp list\n</code></pre>\n<h2 id=\"codex\">Codex</h2>\n<p>Open Codex&#39;s config file (<code>~/.codex/config.toml</code>) and add a <code>dograh</code> MCP server:</p>\n<pre><code class=\"language-toml\">[mcp_servers.dograh]\nurl = &quot;/studio/api/v1/mcp/&quot;\nhttp_headers = { &quot;X-API-Key&quot; = &quot;YOUR_API_KEY&quot; }\n</code></pre>\n<p>Replace <code>YOUR_API_KEY</code> with the key you generated. For self-hosted deployments, replace the URL with your backend MCP endpoint.</p>\n<p>If you prefer to keep the API key out of <code>config.toml</code>, store it in an environment variable instead:</p>\n<pre><code class=\"language-toml\">[mcp_servers.dograh]\nurl = &quot;/studio/api/v1/mcp/&quot;\nenv_http_headers = { &quot;X-API-Key&quot; = &quot;DOGRAH_API_KEY&quot; }\n</code></pre>\n<p>Then set the API key before starting Codex:</p>\n<pre><code class=\"language-bash\">export DOGRAH_API_KEY=&quot;YOUR_API_KEY&quot;\ncodex\n</code></pre>\n<p>Verify the server is registered:</p>\n<pre><code class=\"language-bash\">codex mcp list\ncodex mcp get dograh\n</code></pre>\n<h2 id=\"claude-desktop\">Claude Desktop</h2>\n<p>Open Claude Desktop&#39;s config file (<code>claude_desktop_config.json</code>) and add the <code>dograh</code> entry under <code>mcpServers</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;dograh&quot;: {\n      &quot;url&quot;: &quot;/studio/api/v1/mcp/&quot;,\n      &quot;headers&quot;: {\n        &quot;X-API-Key&quot;: &quot;YOUR_API_KEY&quot;\n      }\n    }\n  }\n}\n</code></pre>\n<p>Restart Claude Desktop after saving. The PayInt Voice Studio tools should appear in the tool picker.</p>\n<h2 id=\"cursor-and-other-mcp-clients\">Cursor and other MCP clients</h2>\n<p>Any MCP client that supports Streamable HTTP transport can connect with the same URL and header. Paste the configuration above into your client&#39;s MCP settings file and replace <code>YOUR_API_KEY</code>.</p>\n<h2 id=\"example-prompts\">Example prompts</h2>\n<p>Once the MCP server is connected, you can drive PayInt Voice Studio from your coding agent in plain English. A few prompts to try:</p>\n<p><strong>Explore your workspace</strong></p>\n<ul>\n<li>&quot;List my agents in PayInt Voice Studio.&quot;</li>\n<li>&quot;Show me the definition of the agent called <em>Lead Qualifier</em>.&quot;</li>\n<li>&quot;Which credentials and tools are configured in my PayInt Voice Studio workspace?&quot;</li>\n<li>&quot;List the recordings from my most recent agent.&quot;</li>\n</ul>\n<p><strong>Edit an agent</strong></p>\n<ul>\n<li>&quot;In my <em>Lead Qualifier</em> agent, add a new agent node after the greeting that asks the caller for their budget, then routes to the existing qualification node.&quot;</li>\n<li>&quot;Add an end-call node to <em>Support Bot</em> that triggers when the user says they are done, with a polite goodbye prompt.&quot;</li>\n<li>&quot;Rename the <em>intro</em> node in <em>Lead Qualifier</em> to <em>greeting</em> and update any edges that reference it.&quot;</li>\n<li>&quot;Change the LLM model on all agent nodes in <em>Support Bot</em> to <code>gpt-4o-mini</code>.&quot;</li>\n</ul>\n<p><strong>Learn the platform</strong></p>\n<ul>\n<li>&quot;Search the PayInt Voice Studio docs for how to configure a TURN server.&quot;</li>\n<li>&quot;What node types does PayInt Voice Studio support, and what fields does a <code>knowledge_base</code> node take?&quot;</li>\n<li>&quot;How do I deploy PayInt Voice Studio on a custom domain with HTTPS?&quot;</li>\n</ul>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Agent edits are saved as a new <strong>draft</strong> version — your published agent keeps serving calls until you explicitly publish the draft from the PayInt Voice Studio UI.</p>\n</div>\n\n\n\n\n\n<div class=\"as-callout as-callout-note\">\n\n\n<p>The API key controls which workspace the assistant sees. Treat it like any other credential — do not commit it to source control or paste it into shared chats.</p>\n</div>\n\n\n"},"integrations/tuner":{"title":"Tuner Integration","description":"Connect PayInt Voice Studio to Tuner — the observability, simulation, and testing layer for voice","html":"<h2 id=\"overview\">Overview</h2>\n<p>The Tuner integration node automatically sends your completed call data (transcript, metadata, and call outcomes) to Tuner after each call finishes. This lets you monitor agent performance, run evaluations, and track quality trends without any custom code.</p>\n<p>Once the node is configured, you can also let Tuner <strong>call your agent</strong> with generated caller scenarios and score how it handles them — see <a href=\"#simulations\">Simulations</a> below.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A <a href=\"https://app.usetuner.ai\">Tuner account</a> with an active workspace</li>\n<li>A PayInt Voice Studio voice agent workflow</li>\n</ul>\n<h2 id=\"setup\">Setup</h2>\n<h3 id=\"1-create-an-agent-in-tuner\">1. Create an agent in Tuner</h3>\n<p>Log in to <a href=\"https://app.usetuner.ai\">Tuner</a> and create a new agent. When configuring the agent, set the <strong>Provider</strong> to <strong>PayInt Voice Studio</strong>. Enter your <strong>Agent Name</strong> and choose the <strong>Call Direction</strong> (Inbound or Outbound) for your setup. The <strong>Agent Remote ID</strong> is auto-generated after you create the agent.</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-create-agent.png\"\n  alt=\"Create a New Agent modal in Tuner — set Provider to PayInt Voice Studio\"\n/></p>\n<h3 id=\"2-gather-your-tuner-credentials\">2. Gather your Tuner credentials</h3>\n<p>You&#39;ll need three values from your Tuner account:</p>\n<table>\n<thead>\n<tr>\n<th>Credential</th>\n<th>Where to find it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Agent ID</strong></td>\n<td>Agent Settings → Agent Connection → Agent Remote ID</td>\n</tr>\n<tr>\n<td><strong>Workspace ID</strong></td>\n<td>Workspace Settings → General Settings → Workspace ID</td>\n</tr>\n<tr>\n<td><strong>API Key</strong></td>\n<td>Workspace Settings → Tuner API Key</td>\n</tr>\n</tbody></table>\n<p><strong>Agent Remote ID</strong> — open Agent Settings for your agent:</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-agent-settings.png\"\n  alt=\"Tuner Agent Settings showing Agent Remote ID\"\n/></p>\n<p><strong>Workspace ID and API Key</strong> — open Workspace Settings:</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-workspace-settings.png\"\n  alt=\"Tuner Workspace Settings showing Workspace ID and API Key\"\n/></p>\n<h3 id=\"3-add-the-tuner-node-to-your-workflow\">3. Add the Tuner node to your workflow</h3>\n<p>In your PayInt Voice Studio workflow editor, click <strong>Add node</strong> and scroll to the <strong>Integrations</strong> section. Select <strong>Tuner</strong>.</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-dograh-workflow-builder.png\"\n  alt=\"PayInt Voice Studio voice workflow builder — click Add node\"\n/></p>\n<p>Scroll to <strong>Integrations</strong> and select <strong>Tuner</strong>:</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-integrations-panel.png\"\n  alt=\"PayInt Voice Studio Integrations panel showing QA Analysis, Tuner, and Webhook\"\n/></p>\n<p>The node appears on your canvas with a <strong>Not configured</strong> badge:</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-node-not-configured.png\"\n  alt=\"Tuner node on the canvas showing Not configured\"\n/></p>\n<h3 id=\"4-configure-the-node\">4. Configure the node</h3>\n<p>Click on the Tuner node and fill in the following fields:</p>\n<ul>\n<li><strong>Tuner Agent ID</strong> — The Agent Remote ID from Tuner</li>\n<li><strong>Tuner Workspace ID</strong> — Your numeric workspace ID</li>\n<li><strong>Tuner API Key</strong> — Your workspace API key</li>\n<li><strong>Enabled</strong> — Toggle on to activate the export</li>\n</ul>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-edit-modal.png\"\n  alt=\"PayInt Voice Studio Edit Tuner modal with credential fields and Enabled toggle\"\n/></p>\n<p>Click <strong>Save</strong>, then <strong>Publish</strong> your workflow.</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-node-configured.png\"\n  alt=\"Tuner node on the canvas showing configured and enabled\"\n/></p>\n<h3 id=\"5-verify-the-connection\">5. Verify the connection</h3>\n<p>Make a test call through your agent. Once the call completes, check the <strong>Call Logs</strong> tab in your Tuner agent dashboard. The call should appear within a few moments.</p>\n<h2 id=\"simulations\">Simulations</h2>\n<p>Everything above sends <em>real</em> calls to Tuner. Simulations run the other direction: Tuner places a call <strong>into</strong> your agent over SIP, plays the part of a caller following a generated scenario, and scores the result. Your agent answers exactly as it would for a customer.</p>\n<p>PayInt Voice Studio does not speak SIP itself — Cloudonix does it on PayInt Voice Studio&#39;s behalf. So the address Tuner dials belongs to Cloudonix, and what you set up below is a route through it to one specific agent.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Simulations use the <strong>inbound</strong> path, which runs on the Cloudonix SIP domain\nPayInt Voice Studio provisions for every organization. You do not need a SIP carrier or a\nphone number — unlike outbound calling, which requires your own trunk. See\n<a href=\"/studio/docs/integrations/telephony/dograh-sip\">PayInt Voice Studio Managed SIP</a>.</p>\n</div>\n\n\n\n<h3 id=\"before-you-start\">Before you start</h3>\n<ul>\n<li>The Tuner node configured and enabled on your workflow, as set up above</li>\n<li>In Tuner, the same agent with its <strong>Call Direction</strong> set to <strong>Inbound</strong></li>\n<li>An agent that already works — test it from the agent editor first</li>\n</ul>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>An inbound simulation only attaches to its scenario when the Tuner agent&#39;s\ncall direction is <strong>Inbound</strong>. Set to Outbound, Tuner matches the returned\ncall on a different field and files it as ordinary traffic instead.</p>\n</div>\n\n\n\n<h3 id=\"1-add-an-extension-for-simulations\">1. Add an extension for simulations</h3>\n<p>Go to <strong>Telephony configurations</strong> and open <strong>PayInt Voice Studio Cloudonix SIP</strong>. Under <strong>Phone numbers</strong>, choose <strong>Add phone number</strong> and enter a short extension such as <code>9001</code>.</p>\n<p>You do not need a real phone number. That field accepts SIP URIs and plain extensions alongside PSTN numbers, and an extension is the simplest thing to point a simulation at. PayInt Voice Studio registers it with Cloudonix for you.</p>\n<h3 id=\"2-point-the-extension-at-your-agent\">2. Point the extension at your agent</h3>\n<p>Edit the extension you just added, set its <strong>Inbound workflow</strong> to the agent that should answer, and save. This is the step that tells Cloudonix where to route calls for that extension.</p>\n<h3 id=\"3-publish-the-workflow\">3. Publish the workflow</h3>\n<p>In the workflow editor, choose <strong>Save</strong>, then <strong>Publish</strong>.</p>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>Inbound calls always run the <strong>published</strong> version of a workflow, never the\ndraft. If the Tuner node is only in your draft, calls will connect and the\nconversation will happen normally — but nothing will ever reach Tuner, and\nno error appears anywhere.</p>\n</div>\n\n\n\n<h3 id=\"4-build-the-sip-uri\">4. Build the SIP URI</h3>\n<p>Expand <strong>SIP connectivity</strong> on the configuration page, choose your <strong>region</strong>, and copy the <strong>inbound hostname</strong>. Combine it with the extension from step 1:</p>\n<pre><code class=\"language-text\">sip:9001@a1b2c3d4-0000-0000-0000-9f8e7d6c5b4a.sip.cloudonix.net\n    └─┬─┘ └──────────────────────┬───────────────────────────┘\n   extension              inbound hostname\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Part</th>\n<th>Where to get it</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Extension</strong></td>\n<td>Phone numbers — the entry from step 1</td>\n<td>Must match exactly. This decides which agent answers.</td>\n</tr>\n<tr>\n<td><strong>Inbound hostname</strong></td>\n<td>SIP connectivity → Hostname</td>\n<td>Unique to your organization, and changes with the region.</td>\n</tr>\n<tr>\n<td><strong>Port</strong></td>\n<td>SIP connectivity → Ports</td>\n<td>Omit only when it is <code>5060</code>; otherwise required.</td>\n</tr>\n</tbody></table>\n<p>Set the region <strong>before</strong> copying anything — every value on the panel is region-specific, and the port changes with it:</p>\n<table>\n<thead>\n<tr>\n<th>Region</th>\n<th>Hostname ends with</th>\n<th>Port</th>\n<th>Include the port?</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Global</td>\n<td><code>.sip.cloudonix.net</code></td>\n<td><code>5060</code></td>\n<td>Optional — it is the default</td>\n</tr>\n<tr>\n<td>India</td>\n<td><code>.in.dimi.tel</code></td>\n<td><code>9060</code></td>\n<td>Required</td>\n</tr>\n<tr>\n<td>UAE</td>\n<td><code>.uae.dimi.tel</code></td>\n<td><code>9081</code></td>\n<td>Required</td>\n</tr>\n</tbody></table>\n<div class=\"as-callout as-callout-warning\">\n\n\n<p>On India or UAE, leaving the port off sends the call to port 5060, where\nnothing is listening — and the failure looks like an unreachable agent\nrather than a wrong address. Add <code>:9060</code> or <code>:9081</code> to the URI.</p>\n</div>\n\n\n\n\n\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Your inbound hostname accepts calls without a password — being unguessable is\nwhat protects it. Treat it as sensitive and share it only with systems that\nneed to call your agents.</p>\n</div>\n\n\n\n<h3 id=\"5-save-the-uri-in-tuner\">5. Save the URI in Tuner</h3>\n<p>In Tuner, open <strong>Agent Settings → SIP Settings</strong>, paste the address into <strong>SIP URI</strong>, and choose <strong>Verify &amp; Save</strong>. Tuner immediately places a short test call, so this single action checks the whole path.</p>\n<p>Leave <strong>Username</strong> and <strong>Password</strong> empty — PayInt Voice Studio&#39;s inbound endpoint does not use SIP authentication.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p><strong>Known issue in Tuner.</strong> With both auth fields blank, verification currently\nfails with &quot;endpoint did not answer&quot;. Entering any non-empty values — for\nexample <code>tuner</code> and <code>tuner</code> — works around it. PayInt Voice Studio never checks them, so\nthe values are irrelevant. Clear both fields once Tuner ships the fix.</p>\n</div>\n\n\n\n<h3 id=\"6-run-a-simulation\">6. Run a simulation</h3>\n<p>Start a run from Tuner&#39;s <strong>Simulations</strong> tab. Each scenario becomes a real call into your agent, and completed calls appear in that table with transcript, latency, and evaluation results.</p>\n<h3 id=\"how-calls-are-matched-to-scenarios\">How calls are matched to scenarios</h3>\n<p>Nothing to configure — this is automatic. Cloudonix reports the caller&#39;s SIP call id to PayInt Voice Studio, and PayInt Voice Studio includes it when it exports the finished call. Tuner uses it to attach the call to the scenario that placed it. Without it, the call would still be ingested, just filed as ordinary production traffic.</p>\n<h2 id=\"disabling-the-integration\">Disabling the integration</h2>\n<p>To temporarily stop exporting calls to Tuner, open the Tuner node configuration and toggle <strong>Enabled</strong> off. Your credentials are preserved — toggle it back on anytime to resume.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table>\n<thead>\n<tr>\n<th>Issue</th>\n<th>Solution</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Calls not appearing in Tuner</td>\n<td>Verify all three credentials are correct with no extra whitespace</td>\n</tr>\n<tr>\n<td>Node shows &quot;Not configured&quot;</td>\n<td>Open the node and fill in Agent ID, Workspace ID, and API Key</td>\n</tr>\n<tr>\n<td>Workflow not sending data</td>\n<td>Make sure the workflow is published, not just saved as a draft</td>\n</tr>\n<tr>\n<td>Wrong agent in Tuner</td>\n<td>Confirm the Tuner agent&#39;s Provider is set to <strong>PayInt Voice Studio</strong></td>\n</tr>\n</tbody></table>\n<h3 id=\"simulation-issues\">Simulation issues</h3>\n<table>\n<thead>\n<tr>\n<th>Issue</th>\n<th>Solution</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>603 Declined</code></td>\n<td>Cloudonix found the route but could not fetch call instructions from PayInt Voice Studio. On a self-hosted deployment, check that your backend is publicly reachable — see below</td>\n</tr>\n<tr>\n<td>&quot;Endpoint did not answer&quot; on Verify &amp; Save</td>\n<td>With blank auth fields this is the <a href=\"#5-save-the-uri-in-tuner\">known Tuner issue</a>. Otherwise confirm the extension exists, has an inbound workflow assigned, and that the region&#39;s port is in the URI</td>\n</tr>\n<tr>\n<td>Agent answers but nothing reaches Tuner</td>\n<td>The Tuner node is not in the <strong>published</strong> version of the workflow. Publish and try again</td>\n</tr>\n<tr>\n<td>Calls land in Call Logs instead of Simulations</td>\n<td>Confirm the Tuner agent&#39;s <strong>Call Direction</strong> is <strong>Inbound</strong></td>\n</tr>\n<tr>\n<td>A <code>422</code> logged after Verify &amp; Save</td>\n<td>Harmless. The verification call lasts about a second with no conversation, so there is no transcript for Tuner to accept</td>\n</tr>\n</tbody></table>\n<h3 id=\"self-hosted-deployments\">Self-hosted deployments</h3>\n<p>Cloudonix must reach your backend over the public internet twice per call: once for the webhook when the call arrives, and again for the audio stream. A backend on <code>localhost</code> receives neither.</p>\n<p>The address is recorded by Cloudonix when you save the inbound workflow, so whenever your public address changes you must do <strong>both</strong> of these:</p>\n<ul>\n<li>Update <code>BACKEND_API_ENDPOINT</code> and restart the backend</li>\n<li><strong>Re-save the inbound workflow on the extension</strong>, which pushes the new URL to Cloudonix</li>\n</ul>\n<p>Skipping the second step leaves Cloudonix calling the old address, and every call fails with <code>603</code>. A tunnel with a fixed hostname avoids it entirely.</p>\n<h2 id=\"learn-more\">Learn more</h2>\n<ul>\n<li><a href=\"https://usetuner.ai\">Tuner</a> — The observability, simulation, and testing layer for voice</li>\n<li><a href=\"https://docs.usetuner.ai\">Tuner documentation</a> — Complete Tuner platform docs</li>\n</ul>\n"},"integrations/paygent":{"title":"Paygent Integration","description":"Connect PayInt Voice Studio to Paygent — real-time cost tracking, multimodal usage monitoring, and billing for voice agents","html":"<h2 id=\"overview\">Overview</h2>\n<p><strong>Paygent</strong> is a specialized, usage-based billing platform designed exclusively for AI voice agents. </p>\n<p>If you are building voice agents for clients or offering them as a SaaS product, calculating margins across different AI providers (LLMs, TTS, STT, and Speech-to-Speech models) can be incredibly complex. Paygent solves this by serving as your centralized billing engine. </p>\n<h3 id=\"how-you-charge-your-customers\">How you charge your customers</h3>\n<p>Instead of building custom tracking infrastructure, you simply connect your PayInt Voice Studio workflow to Paygent. As your agents handle calls, PayInt Voice Studio passively calculates the exact multimodal token usage and audio duration. </p>\n<p>This data is securely exported to Paygent after every call, where your custom pricing margins (rate cards) are applied. This seamless flow allows you to automatically invoice your end-users for the exact infrastructure they consume — turning your AI agents into a scalable, profitable business with zero engineering overhead.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A <a href=\"https://withpaygent.com\">Paygent account</a></li>\n<li>A PayInt Voice Studio voice agent workflow</li>\n</ul>\n<h2 id=\"setup\">Setup</h2>\n<h3 id=\"1-create-an-agent-and-configure-pricing-in-paygent\">1. Create an agent and configure pricing in Paygent</h3>\n<p>Before connecting PayInt Voice Studio, you must register your agent in Paygent and define how you want to charge your customers.</p>\n<p>Log in to <a href=\"https://withpaygent.com\">Paygent</a> and click <strong>Create Agent</strong>. You will be prompted to define your agent&#39;s core details, including the <strong>Agent Name</strong> and <strong>Agent ID</strong>:</p>\n<p><img\n  src=\"/agentstudio-docs/images/paygent-create-agent-image-1.webp\"\n  alt=\"Creating a new agent in Paygent - Agent Name and ID\"\n/></p>\n<p>Next, set the <strong>Indicator Name</strong>. This is the billing event identifier you will pass from PayInt Voice Studio (e.g. <code>per-minute-call</code>) to tell Paygent which rate to apply for this agent&#39;s calls:</p>\n<p><img\n  src=\"/agentstudio-docs/images/paygent-create-agent-image-2.webp\"\n  alt=\"Setting your indicator name in Paygent\"\n/></p>\n<p>Finally, you will configure your <strong>Pricing Strategy</strong>. Paygent allows you to set custom markup rates (rate cards) for every modality. You can define exact margins for Speech-to-Text (STT) seconds, LLM tokens, Text-to-Speech (TTS) characters, and total call minutes:</p>\n<p><img\n  src=\"/agentstudio-docs/images/paygent-create-agent-image-3.webp\"\n  alt=\"Configuring custom pricing margins and rate cards in Paygent\"\n/></p>\n<p>Once your pricing is configured, confirm your setup. You will use the <strong>Agent ID</strong> and <strong>Indicator</strong> from this process to connect your PayInt Voice Studio workflow.</p>\n<h3 id=\"2-gather-your-paygent-credentials\">2. Gather your Paygent credentials</h3>\n<p>You&#39;ll need three core values from your Paygent dashboard to link your PayInt Voice Studio agent:</p>\n<table>\n<thead>\n<tr>\n<th>Credential</th>\n<th>Where to find it / Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Paygent API Key</strong></td>\n<td>Your workspace API key used to authenticate requests (<code>pg_live_...</code>)</td>\n</tr>\n<tr>\n<td><strong>Agent ID</strong></td>\n<td>The unique identifier configured for your agent in Step 1</td>\n</tr>\n<tr>\n<td><strong>Customer ID</strong></td>\n<td>Your Paygent organisation or customer ID</td>\n</tr>\n</tbody></table>\n<h3 id=\"3-add-the-paygent-node-to-your-workflow\">3. Add the Paygent node to your workflow</h3>\n<p>In your PayInt Voice Studio workflow editor, click <strong>Add node</strong> and scroll to the <strong>Integrations</strong> section. Select <strong>Paygent</strong>. The node will appear on your canvas with a <strong>Not configured</strong> badge.</p>\n<h3 id=\"4-configure-the-node\">4. Configure the node</h3>\n<p>Click on the Paygent node and fill in the following fields:</p>\n<ul>\n<li><strong>Paygent API Key</strong> — Your <code>pg_live_...</code> secret key</li>\n<li><strong>Agent ID</strong> — The unique agent identifier from Paygent</li>\n<li><strong>Customer ID</strong> — Your Paygent organisation ID</li>\n<li><strong>Indicator</strong> — The billing event name (defaults to <code>per-minute-call</code>)</li>\n<li><strong>Enabled</strong> — Toggle on to activate the export</li>\n</ul>\n<p>Click <strong>Save</strong>, then <strong>Publish</strong> your workflow.</p>\n<h3 id=\"5-verify-the-connection\">5. Verify the connection</h3>\n<p>Make a test call through your agent. Once the call completes, check your Paygent dashboard. The billing event and detailed multimodal usage breakdown should appear under your configured Agent ID within a few moments, with your configured pricing margins automatically applied.</p>\n<h2 id=\"disabling-the-integration\">Disabling the integration</h2>\n<p>To temporarily stop exporting usage data to Paygent, open the Paygent node configuration and toggle <strong>Enabled</strong> off. Your credentials are preserved — toggle it back on anytime to resume tracking.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table>\n<thead>\n<tr>\n<th>Issue</th>\n<th>Solution</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Usage data not appearing in Paygent</td>\n<td>Verify all credentials are correct with no extra whitespace</td>\n</tr>\n<tr>\n<td>Node shows &quot;Not configured&quot;</td>\n<td>Open the node and fill in API Key, Agent ID, Customer ID, and Indicator</td>\n</tr>\n<tr>\n<td>Workflow not sending data</td>\n<td>Make sure the workflow is published, not just saved as a draft</td>\n</tr>\n<tr>\n<td>Missing realtime STS tokens</td>\n<td>For OpenAI Realtime or Google Live models, verify the pipeline is running in realtime mode (<code>is_realtime=True</code>)</td>\n</tr>\n</tbody></table>\n<h2 id=\"learn-more\">Learn more</h2>\n<ul>\n<li><a href=\"https://withpaygent.com\">Paygent</a> — Usage-based billing platform for AI agents</li>\n</ul>\n"},"integrations/noveum":{"title":"Noveum Integration","description":"Connect PayInt Voice Studio to Noveum — full-call tracing, audio capture, and AI-powered evaluation for your voice agents","html":"<h2 id=\"overview\">Overview</h2>\n<p>The Noveum integration node automatically exports every completed call to <a href=\"https://noveum.ai\">Noveum</a> as a full observability trace. Each call becomes a structured trace with per-turn spans for STT, LLM, and TTS — transcripts, prompts and completions, token usage, latency breakdowns, interruptions, and (optionally) the call audio itself, ready for playback and audio-level evaluation on the Noveum platform. No custom code required.</p>\n<p>The export is <strong>post-call and bring-your-own-key</strong>: while the call is live, PayInt Voice Studio only records data locally — nothing is sent to Noveum and no latency is added to the conversation. After the call finishes, the trace and audio are uploaded in the background using the Noveum API key you configure on the node.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A <a href=\"https://noveum.ai\">Noveum account</a> with an API key</li>\n<li>A PayInt Voice Studio voice agent workflow</li>\n</ul>\n<h2 id=\"setup\">Setup</h2>\n<h3 id=\"1-get-your-noveum-credentials\">1. Get your Noveum credentials</h3>\n<p>Log in to <a href=\"https://noveum.ai\">Noveum</a> and gather two values:</p>\n<table>\n<thead>\n<tr>\n<th>Credential</th>\n<th>Where to find it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>API Key</strong></td>\n<td>Dashboard → Settings → <strong>API Keys</strong> → Create New API Key</td>\n</tr>\n<tr>\n<td><strong>Project</strong></td>\n<td>The project name your traces should land in — pick any slug (e.g. <code>my-voice-agent</code>); it is created on first export</td>\n</tr>\n</tbody></table>\n<p><img\n  src=\"/agentstudio-docs/images/noveum-api-key.png\"\n  alt=\"Noveum dashboard — creating an API key under Settings → API Keys\"\n/></p>\n<p>Treat the API key like a password — it authorizes writes into your Noveum project.</p>\n<h3 id=\"2-add-the-noveum-node-to-your-workflow\">2. Add the Noveum node to your workflow</h3>\n<p>In your PayInt Voice Studio workflow editor, click <strong>Add node</strong> and scroll to the <strong>Integrations</strong> section. Select <strong>Noveum</strong>.</p>\n<p><img\n  src=\"/agentstudio-docs/images/tuner-dograh-workflow-builder.png\"\n  alt=\"PayInt Voice Studio voice workflow builder — click Add node\"\n/></p>\n<p>Scroll to <strong>Integrations</strong> and select <strong>Noveum</strong>:</p>\n<p><img\n  src=\"/agentstudio-docs/images/noveum-integrations-panel.png\"\n  alt=\"PayInt Voice Studio Integrations panel showing the Noveum node\"\n/></p>\n<p>The node appears on your canvas with a <strong>Not configured</strong> badge. Like other integration nodes, it is standalone — it does not connect to any other node in the conversation graph, and a workflow can contain at most one Noveum node.</p>\n<h3 id=\"3-configure-the-node\">3. Configure the node</h3>\n<p>Click on the Noveum node and fill in the following fields:</p>\n<ul>\n<li><strong>Noveum API Key</strong> — Your API key from step 1</li>\n<li><strong>Noveum Project</strong> — The Noveum project the call traces are exported into</li>\n<li><strong>Noveum Environment</strong> — Environment label stamped on exported traces (defaults to <code>production</code>; use <code>staging</code>, <code>development</code>, etc. to separate environments)</li>\n<li><strong>Record audio</strong> — Capture per-segment STT/TTS audio plus the full-conversation recording for audio evaluation on Noveum (on by default)</li>\n<li><strong>Enabled</strong> — Toggle on to activate the export</li>\n</ul>\n<p><img\n  src=\"/agentstudio-docs/images/noveum-edit-modal.png\"\n  alt=\"PayInt Voice Studio Edit Noveum modal with API key, project, environment, and audio fields\"\n/></p>\n<p>Click <strong>Save</strong>, then <strong>Publish</strong> your workflow.</p>\n<p><img\n  src=\"/agentstudio-docs/images/noveum-node-configured.png\"\n  alt=\"Noveum node on the canvas showing configured and enabled\"\n/></p>\n<h3 id=\"4-verify-the-connection\">4. Verify the connection</h3>\n<p>Make a test call through your agent. Once the call completes, open your project in the Noveum dashboard — the call appears as a new trace within a few moments. Open it to see the turn-by-turn timeline with STT, LLM, and TTS spans, and play back the captured audio.</p>\n<p><img\n  src=\"/agentstudio-docs/images/noveum-trace-detail.png\"\n  alt=\"Noveum trace view showing a PayInt Voice Studio call with per-turn STT, LLM, and TTS spans\"\n/></p>\n<h2 id=\"what-gets-exported\">What gets exported</h2>\n<p>Each call produces one conversation trace containing:</p>\n<table>\n<thead>\n<tr>\n<th>Data</th>\n<th>Details</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Turn spans</strong></td>\n<td>One span per user→bot exchange, with duration, user→bot latency, and interruption (barge-in) flags</td>\n</tr>\n<tr>\n<td><strong>STT spans</strong></td>\n<td>Final transcript, interim results, confidence, language, and speech-to-final latency</td>\n</tr>\n<tr>\n<td><strong>LLM spans</strong></td>\n<td>Full message history, system prompt, completion, token usage, time-to-first-token, and tool/function calls</td>\n</tr>\n<tr>\n<td><strong>TTS spans</strong></td>\n<td>Synthesized text, voice, characters, and time-to-first-byte</td>\n</tr>\n<tr>\n<td><strong>Latency breakdown</strong></td>\n<td>Per-service TTFB (STT / LLM / TTS) and end-to-end user→bot latency per turn</td>\n</tr>\n<tr>\n<td><strong>Call metadata</strong></td>\n<td>Workflow run ID, call disposition, agent version, run mode, and the STT/LLM/TTS model names configured on the workflow</td>\n</tr>\n<tr>\n<td><strong>Audio</strong> (when <strong>Record audio</strong> is on)</td>\n<td>Per-turn user and bot audio segments plus a full-conversation stereo recording (left channel = user, right channel = bot), attached to their spans for playback and audio evaluation</td>\n</tr>\n</tbody></table>\n<p>Once the traces are in Noveum you can inspect and replay calls, build evaluation datasets from production traffic, and run automated scorers over transcripts and audio to track agent quality over time.</p>\n<h2 id=\"disabling-the-integration\">Disabling the integration</h2>\n<p>To temporarily stop exporting calls to Noveum, open the Noveum node configuration and toggle <strong>Enabled</strong> off. Your credentials are preserved — toggle it back on anytime to resume.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table>\n<thead>\n<tr>\n<th>Issue</th>\n<th>Solution</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Traces not appearing in Noveum</td>\n<td>Verify the API key and project are correct with no extra whitespace</td>\n</tr>\n<tr>\n<td>Node shows &quot;Not configured&quot;</td>\n<td>Open the node and fill in the Noveum API Key and Project — both are required when the node is enabled</td>\n</tr>\n<tr>\n<td>Workflow not sending data</td>\n<td>Make sure the workflow is published, not just saved as a draft</td>\n</tr>\n<tr>\n<td>Trace arrives but has no audio</td>\n<td>Confirm <strong>Record audio</strong> is toggled on in the node configuration</td>\n</tr>\n<tr>\n<td>Calls that never connected show no trace</td>\n<td>Expected — runs that never start a pipeline (e.g. telephony failures) produce no trace to export</td>\n</tr>\n</tbody></table>\n<h2 id=\"learn-more\">Learn more</h2>\n<ul>\n<li><a href=\"https://noveum.ai\">Noveum</a> — AI observability and evaluation platform</li>\n<li><a href=\"https://noveum.ai/docs\">Noveum documentation</a> — Complete platform docs, including the <a href=\"https://noveum.ai/docs/integration-examples/dograh/overview\">PayInt Voice Studio integration guide</a></li>\n</ul>\n"},"developer/workflow-schema":{"title":"Workflow Definition Schema","description":"Schema reference for the workflow_definition object used in the Agents API","html":"<p>The <code>workflow_definition</code> object passed to <a href=\"/studio/docs/api-reference/agents/create-from-definition\">Create from Definition</a> and <a href=\"/studio/docs/api-reference/agents/update\">Update Agent</a> defines the full conversation graph. It is the same structure the dashboard&#39;s visual workflow builder reads and writes — building an agent in the UI produces a <code>workflow_definition</code> under the hood, and anything you can configure visually can equally be expressed here as JSON.</p>\n<pre><code class=\"language-json\">{\n  &quot;nodes&quot;: [...],\n  &quot;edges&quot;: [...]\n}\n</code></pre>\n<hr>\n<h2 id=\"nodes\">Nodes</h2>\n<p>Each node represents a step in the conversation.</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;uuid-string&quot;,\n  &quot;type&quot;: &quot;agentNode&quot;,\n  &quot;position&quot;: { &quot;x&quot;: 100, &quot;y&quot;: 200 },\n  &quot;data&quot;: { ... }\n}\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td>string</td>\n<td>Unique node ID (UUID recommended)</td>\n</tr>\n<tr>\n<td><code>type</code></td>\n<td>string</td>\n<td>One of the node types below</td>\n</tr>\n<tr>\n<td><code>position</code></td>\n<td>object</td>\n<td>Visual coordinates in the workflow builder</td>\n</tr>\n<tr>\n<td><code>data</code></td>\n<td>object</td>\n<td>Node configuration — fields vary by type</td>\n</tr>\n</tbody></table>\n<h3 id=\"node-types\">Node types</h3>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>startCall</code></td>\n<td>Entry point for telephony calls</td>\n</tr>\n<tr>\n<td><code>endCall</code></td>\n<td>Terminates the call</td>\n</tr>\n<tr>\n<td><code>agentNode</code></td>\n<td>LLM-powered conversation step</td>\n</tr>\n<tr>\n<td><code>globalNode</code></td>\n<td>Global configuration applied across all agent nodes</td>\n</tr>\n<tr>\n<td><code>trigger</code></td>\n<td>Entry point for API-triggered (non-telephony) runs</td>\n</tr>\n<tr>\n<td><code>webhook</code></td>\n<td>Sends an HTTP request when reached</td>\n</tr>\n<tr>\n<td><code>qa</code></td>\n<td>Runs quality analysis on the completed call</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"node-data-fields\">Node data fields</h2>\n<h3 id=\"common-fields-all-node-types\">Common fields (all node types)</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>string</td>\n<td>required</td>\n<td>Display name for the node</td>\n</tr>\n<tr>\n<td><code>prompt</code></td>\n<td>string</td>\n<td>required*</td>\n<td>LLM system prompt. *Not required for <code>trigger</code>, <code>webhook</code>, <code>qa</code> nodes</td>\n</tr>\n<tr>\n<td><code>allow_interrupt</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Allow the caller to interrupt the agent mid-speech</td>\n</tr>\n<tr>\n<td><code>wait_for_user_response</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Pause and wait for caller input before continuing</td>\n</tr>\n<tr>\n<td><code>wait_for_user_response_timeout</code></td>\n<td>number</td>\n<td><code>null</code></td>\n<td>Seconds to wait for input before timing out</td>\n</tr>\n<tr>\n<td><code>detect_voicemail</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Detect and handle voicemail on outbound calls</td>\n</tr>\n<tr>\n<td><code>delayed_start</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Delay execution of this node</td>\n</tr>\n<tr>\n<td><code>delayed_start_duration</code></td>\n<td>number</td>\n<td><code>null</code></td>\n<td>Delay in seconds</td>\n</tr>\n<tr>\n<td><code>add_global_prompt</code></td>\n<td>boolean</td>\n<td><code>true</code></td>\n<td>Merge the <code>globalNode</code> prompt into this node&#39;s prompt</td>\n</tr>\n</tbody></table>\n<h3 id=\"agentnode-data-extraction\">agentNode — data extraction</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>extraction_enabled</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Extract structured data from the conversation</td>\n</tr>\n<tr>\n<td><code>extraction_prompt</code></td>\n<td>string</td>\n<td><code>null</code></td>\n<td>Custom prompt to guide extraction</td>\n</tr>\n<tr>\n<td><code>extraction_variables</code></td>\n<td>array</td>\n<td><code>[]</code></td>\n<td>Variables to extract (see below)</td>\n</tr>\n</tbody></table>\n<p><strong>Extraction variable schema:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;name&quot;: &quot;customer_intent&quot;,\n  &quot;type&quot;: &quot;string&quot;,\n  &quot;prompt&quot;: &quot;What did the customer want to achieve?&quot;\n}\n</code></pre>\n<p><code>type</code> is one of <code>string</code>, <code>number</code>, or <code>boolean</code>.</p>\n<h3 id=\"agentnode-tools\">agentNode — tools</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>tool_uuids</code></td>\n<td>string[]</td>\n<td>IDs of tools (HTTP API, call transfer, etc.) to attach to this node</td>\n</tr>\n<tr>\n<td><code>document_uuids</code></td>\n<td>string[]</td>\n<td>IDs of knowledge base documents available to this node</td>\n</tr>\n</tbody></table>\n<h3 id=\"trigger-node\">trigger node</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>trigger_path</code></td>\n<td>string</td>\n<td>Unique UUID that becomes the API trigger endpoint path</td>\n</tr>\n</tbody></table>\n<h3 id=\"webhook-node\">webhook node</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>enabled</code></td>\n<td>boolean</td>\n<td><code>true</code></td>\n<td>Whether this webhook fires when reached</td>\n</tr>\n<tr>\n<td><code>http_method</code></td>\n<td>string</td>\n<td>—</td>\n<td><code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, or <code>DELETE</code></td>\n</tr>\n<tr>\n<td><code>endpoint_url</code></td>\n<td>string</td>\n<td>—</td>\n<td>Target URL</td>\n</tr>\n<tr>\n<td><code>credential_uuid</code></td>\n<td>string</td>\n<td><code>null</code></td>\n<td>UUID of a stored auth credential</td>\n</tr>\n<tr>\n<td><code>custom_headers</code></td>\n<td>array</td>\n<td><code>[]</code></td>\n<td>Additional request headers <code>[{&quot;key&quot;: &quot;...&quot;, &quot;value&quot;: &quot;...&quot;}]</code></td>\n</tr>\n<tr>\n<td><code>payload_template</code></td>\n<td>object</td>\n<td><code>null</code></td>\n<td>Request body template (supports context variables)</td>\n</tr>\n</tbody></table>\n<h3 id=\"qa-node\">qa node</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>qa_enabled</code></td>\n<td>boolean</td>\n<td><code>true</code></td>\n<td>Enable QA analysis</td>\n</tr>\n<tr>\n<td><code>qa_system_prompt</code></td>\n<td>string</td>\n<td><code>null</code></td>\n<td>Custom evaluation prompt</td>\n</tr>\n<tr>\n<td><code>qa_model</code></td>\n<td>string</td>\n<td><code>null</code></td>\n<td>LLM model to use for evaluation</td>\n</tr>\n<tr>\n<td><code>qa_min_call_duration</code></td>\n<td>integer</td>\n<td><code>15</code></td>\n<td>Minimum call duration in seconds to run QA</td>\n</tr>\n<tr>\n<td><code>qa_voicemail_calls</code></td>\n<td>boolean</td>\n<td><code>false</code></td>\n<td>Include voicemail calls in QA</td>\n</tr>\n<tr>\n<td><code>qa_sample_rate</code></td>\n<td>integer</td>\n<td><code>100</code></td>\n<td>Percentage of calls to analyse (1–100)</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"edges\">Edges</h2>\n<p>Each edge connects two nodes and defines when the transition fires.</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;edge-uuid&quot;,\n  &quot;source&quot;: &quot;node-uuid-a&quot;,\n  &quot;target&quot;: &quot;node-uuid-b&quot;,\n  &quot;data&quot;: {\n    &quot;label&quot;: &quot;Customer confirms&quot;,\n    &quot;condition&quot;: &quot;The customer has confirmed their appointment&quot;,\n    &quot;transition_speech&quot;: &quot;Great, I&#39;ve got that noted.&quot;\n  }\n}\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td>string</td>\n<td>Unique edge ID</td>\n</tr>\n<tr>\n<td><code>source</code></td>\n<td>string</td>\n<td>ID of the originating node</td>\n</tr>\n<tr>\n<td><code>target</code></td>\n<td>string</td>\n<td>ID of the destination node</td>\n</tr>\n<tr>\n<td><code>data.label</code></td>\n<td>string</td>\n<td>Short label shown in the workflow builder</td>\n</tr>\n<tr>\n<td><code>data.condition</code></td>\n<td>string</td>\n<td>Natural language condition the LLM evaluates to trigger this edge</td>\n</tr>\n<tr>\n<td><code>data.transition_speech</code></td>\n<td>string</td>\n<td>Optional speech the agent says before transitioning</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"validation-rules\">Validation rules</h2>\n<ul>\n<li>All <code>source</code> and <code>target</code> IDs in edges must reference existing node IDs</li>\n<li>All nodes except <code>trigger</code>, <code>webhook</code>, and <code>qa</code> must have a non-empty <code>prompt</code></li>\n<li>Node IDs must be unique within the workflow</li>\n<li>Each workflow must have exactly one <code>startCall</code> or <code>trigger</code> node as the entry point</li>\n</ul>\n<hr>\n<h2 id=\"minimal-example\">Minimal example</h2>\n<pre><code class=\"language-json\">{\n  &quot;nodes&quot;: [\n    {\n      &quot;id&quot;: &quot;start-1&quot;,\n      &quot;type&quot;: &quot;startCall&quot;,\n      &quot;position&quot;: { &quot;x&quot;: 0, &quot;y&quot;: 0 },\n      &quot;data&quot;: {\n        &quot;name&quot;: &quot;Start&quot;,\n        &quot;prompt&quot;: &quot;You are a friendly assistant. Greet the caller and ask how you can help.&quot;\n      }\n    },\n    {\n      &quot;id&quot;: &quot;end-1&quot;,\n      &quot;type&quot;: &quot;endCall&quot;,\n      &quot;position&quot;: { &quot;x&quot;: 400, &quot;y&quot;: 0 },\n      &quot;data&quot;: {\n        &quot;name&quot;: &quot;End&quot;,\n        &quot;prompt&quot;: &quot;Thank the caller and say goodbye.&quot;\n      }\n    }\n  ],\n  &quot;edges&quot;: [\n    {\n      &quot;id&quot;: &quot;edge-1&quot;,\n      &quot;source&quot;: &quot;start-1&quot;,\n      &quot;target&quot;: &quot;end-1&quot;,\n      &quot;data&quot;: {\n        &quot;label&quot;: &quot;Done&quot;,\n        &quot;condition&quot;: &quot;The caller&#39;s question has been answered and they want to end the call&quot;\n      }\n    }\n  ]\n}\n</code></pre>\n"},"developer/call-dispositions":{"title":"Call Dispositions","description":"Configure workflow-specific business outcomes and use them consistently across PayInt Voice Studio","html":"<p>A call disposition is a stable code that describes the business outcome of a\ncompleted conversation, such as <code>qualified</code>, <code>appointment_booked</code>, or\n<code>callback_requested</code>. It is different from the call status: <code>user_hangup</code>\nexplains how a call ended, while <code>appointment_booked</code> explains what the call\nachieved.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>Call disposition extraction is disabled by default for every workflow. Until\nyou enable it, PayInt Voice Studio keeps the disposition recorded by the event that ended\nthe call.</p>\n</div>\n\n\n\n<h2 id=\"before-you-begin\">Before you begin</h2>\n<p>Before configuring a workflow, decide which outcomes matter to your business\nand write an observable criterion for each one. You also need permission to\nedit and publish the workflow.</p>\n<h2 id=\"configure-outcomes-for-a-workflow\">Configure outcomes for a workflow</h2>\n<ol>\n<li>Open the agent whose workflow you want to configure.</li>\n<li>Open <strong>Settings</strong>, then select <strong>General</strong>.</li>\n<li>Turn on <strong>Extract call disposition at the end of the call</strong>.</li>\n<li>In the <strong>Call disposition extraction</strong> dialog, review the starter outcomes.</li>\n<li>Edit or remove any outcome that does not fit this workflow.</li>\n<li>Select <strong>Add custom disposition</strong> to add your own code and description.</li>\n<li>Select <strong>Enable extraction</strong>.</li>\n<li>Select <strong>Save General Settings</strong>, then publish the agent for the change to\naffect live calls.</li>\n</ol>\n<p>The dialog starts with these suggestions. They do not become active until you\nenable extraction and save the workflow.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Selection criteria</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>qualified</code></td>\n<td>The call achieved the workflow&#39;s primary goal.</td>\n</tr>\n<tr>\n<td><code>not_interested</code></td>\n<td>The person clearly declined the offer or said they are not interested.</td>\n</tr>\n<tr>\n<td><code>wrong_number</code></td>\n<td>The call reached the wrong person or an incorrect phone number.</td>\n</tr>\n<tr>\n<td><code>voicemail_detected</code></td>\n<td>The call reached voicemail or an answering machine instead of a person.</td>\n</tr>\n<tr>\n<td><code>do_not_call</code></td>\n<td>The person explicitly asked not to be contacted again.</td>\n</tr>\n<tr>\n<td><code>callback_requested</code></td>\n<td>The person asked to be contacted again at a later time.</td>\n</tr>\n</tbody></table>\n<p>To change an enabled workflow later, select <strong>Configure options</strong>, update the\nlist, save the general settings, and publish again. Turning extraction off\nremoves the configured options from the workflow after you save and publish.</p>\n<h3 id=\"define-effective-custom-outcomes\">Define effective custom outcomes</h3>\n<p>The code is the value PayInt Voice Studio stores for the run. Keep it stable and\nmachine-readable. The description tells the classifier when it should select\nthat code.</p>\n<p>For example:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>appointment_booked</code></td>\n<td>Use when the customer agrees to a specific appointment date and time.</td>\n</tr>\n<tr>\n<td><code>follow_up_required</code></td>\n<td>Use when the customer is interested but no appointment or next step is confirmed.</td>\n</tr>\n</tbody></table>\n<p>Use mutually exclusive descriptions based on evidence in the conversation.\nAvoid vague descriptions such as &quot;the call went well,&quot; because two outcomes\nmay appear equally valid.</p>\n<p>Each workflow can have up to 50 outcomes. A code must start with a letter, may\ncontain letters, numbers, underscores, and hyphens, and can be at most 64\ncharacters. Codes are unique regardless of letter case. Each description can\nbe at most 1,000 characters, with a combined limit of 4,000 characters across\nthe workflow.</p>\n<h2 id=\"how-extraction-works\">How extraction works</h2>\n<p>At the end of an eligible call, PayInt Voice Studio analyzes the complete conversation and\nselects exactly one of the configured codes. It cannot invent a code that is\nnot in the workflow configuration.</p>\n<p>If the conversation does not support any configured outcome, contains no user\nspeech, or classification fails, PayInt Voice Studio keeps the disposition already recorded\nfor the call. A disposition explicitly set by an end-call or transfer tool is\nalso preserved rather than overwritten by final classification.</p>\n<p>This classification is separate from variable extraction on individual agent\nnodes. Node variables describe information gathered at one point in the\nworkflow; the call disposition describes the final outcome of the conversation\nas a whole.</p>\n<h2 id=\"understand-the-stored-fields\">Understand the stored fields</h2>\n<p>PayInt Voice Studio stores the termination mechanism, raw business outcome, and optional\norganization-specific code separately:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Meaning</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>gathered_context.call_status</code></td>\n<td>Observed reason the call ended; never inferred</td>\n<td><code>user_hangup</code></td>\n</tr>\n<tr>\n<td><code>gathered_context.call_disposition</code></td>\n<td>Raw outcome recorded by the workflow before organization mapping</td>\n<td><code>callback_requested</code></td>\n</tr>\n<tr>\n<td><code>gathered_context.mapped_call_disposition</code></td>\n<td>Organization-specific code after disposition mapping</td>\n<td><code>follow_up</code></td>\n</tr>\n</tbody></table>\n<p>When no custom outcome is recorded, <code>call_disposition</code> falls back to the call&#39;s\ntermination reason. When disposition mapping is disabled or no mapping matches,\n<code>mapped_call_disposition</code> keeps the raw value.</p>\n<h2 id=\"use-dispositions-in-a-webhook\">Use dispositions in a webhook</h2>\n<p>Reference the raw, mapped, and status values independently in a webhook payload\ntemplate:</p>\n<pre><code class=\"language-json\">{\n  &quot;call_status&quot;: &quot;{{gathered_context.call_status}}&quot;,\n  &quot;call_disposition&quot;: &quot;{{gathered_context.call_disposition}}&quot;,\n  &quot;mapped_call_disposition&quot;: &quot;{{gathered_context.mapped_call_disposition}}&quot;\n}\n</code></pre>\n<p>PayInt Voice Studio automatically adds a top-level <code>call_disposition</code> containing the raw\nvalue when your webhook template does not define that key. To send your\norganization-specific code instead, reference\n<code>{{gathered_context.mapped_call_disposition}}</code> explicitly. See\n<a href=\"/studio/docs/developer/webhooks\">Webhook Payloads</a> for the complete payload context.</p>\n<h2 id=\"map-outcomes-to-your-organization39s-codes\">Map outcomes to your organization&#39;s codes</h2>\n<p>Disposition mapping is optional and applies across your organization. Use it\nto normalize raw outcomes from different workflows into a shared vocabulary\nfor filtering and reporting.</p>\n<ol>\n<li>Open <strong>Platform Settings</strong>.</li>\n<li>Under <strong>Preferences</strong>, turn on <strong>Disposition mapping</strong>.</li>\n<li>Select <strong>Configure mapping</strong>.</li>\n<li>Map each raw PayInt Voice Studio or workflow disposition to your preferred organization\ncode.</li>\n<li>Select <strong>Add disposition</strong> if a custom workflow code is not already listed.</li>\n<li>Save the mapping.</li>\n</ol>\n<p>For example, you can map <code>callback_requested</code> to <code>follow_up</code>. PayInt Voice Studio preserves\n<code>call_disposition</code> as <code>callback_requested</code> and writes <code>follow_up</code> to\n<code>mapped_call_disposition</code>. Run filters and reports use the mapped value. In a\nwebhook, you can choose either the raw or mapped field explicitly. Mapping\nchanges apply to subsequent calls; they do not rewrite completed runs.</p>\n<h2 id=\"configure-dispositions-through-the-api\">Configure dispositions through the API</h2>\n<p>The workflow stores its options in\n<code>workflow_configurations.call_dispositions</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;call_dispositions&quot;: [\n    {\n      &quot;code&quot;: &quot;appointment_booked&quot;,\n      &quot;description&quot;: &quot;Use when the customer agrees to a specific appointment date and time.&quot;\n    },\n    {\n      &quot;code&quot;: &quot;callback_requested&quot;,\n      &quot;description&quot;: &quot;Use when the customer asks to be contacted again later.&quot;\n    }\n  ]\n}\n</code></pre>\n<p>The example above shows the <code>workflow_configurations</code> value, not the complete\nrequest body. When using the <a href=\"/studio/docs/api-reference/agents/update\">Update Agent API</a>,\nread the agent&#39;s current configuration first and preserve unrelated\n<code>workflow_configurations</code> fields in the update. Send an empty\n<code>call_dispositions</code> array to disable final classification.</p>\n"},"developer/webhooks":{"title":"Webhook Payloads","description":"Context variables available in webhook nodes and the data PayInt Voice Studio sends after a call","html":"<p>PayInt Voice Studio executes <strong>webhook nodes</strong> asynchronously after a workflow run completes. You configure the target URL, HTTP method, headers, and payload template directly in the workflow definition.</p>\n<hr>\n<h2 id=\"how-webhooks-work\">How webhooks work</h2>\n<ol>\n<li>A call completes (or a run finishes)</li>\n<li>PayInt Voice Studio executes any <code>webhook</code> nodes in the workflow asynchronously</li>\n<li>The payload template is rendered with the run&#39;s context and sent as a JSON <code>POST</code> (or your configured method) to your endpoint</li>\n<li>Non-200 responses are logged but do not block or retry by default (configure <code>retry_config</code> to change this)</li>\n</ol>\n<hr>\n<h2 id=\"payload-context-variables\">Payload context variables</h2>\n<p>The following variables are available in your <code>payload_template</code> using double-brace syntax (e.g. <code>{{workflow_run_id}}</code>):</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>workflow_run_id</code></td>\n<td>integer</td>\n<td>ID of the completed run</td>\n</tr>\n<tr>\n<td><code>workflow_run_name</code></td>\n<td>string</td>\n<td>Name of the run</td>\n</tr>\n<tr>\n<td><code>workflow_id</code></td>\n<td>integer</td>\n<td>ID of the workflow</td>\n</tr>\n<tr>\n<td><code>workflow_name</code></td>\n<td>string</td>\n<td>Name of the workflow</td>\n</tr>\n<tr>\n<td><code>campaign_id</code></td>\n<td>integer | null</td>\n<td>ID of the campaign this run belongs to (null for ad-hoc runs)</td>\n</tr>\n<tr>\n<td><code>call_time</code></td>\n<td>string</td>\n<td>ISO-8601 UTC timestamp of when the run was created</td>\n</tr>\n<tr>\n<td><code>initial_context</code></td>\n<td>object</td>\n<td>Context passed when the call was initiated</td>\n</tr>\n<tr>\n<td><code>gathered_context</code></td>\n<td>object</td>\n<td>Data extracted during the call by agent nodes</td>\n</tr>\n<tr>\n<td><code>gathered_context.call_status</code></td>\n<td>string</td>\n<td>Observed reason the call ended</td>\n</tr>\n<tr>\n<td><code>gathered_context.call_disposition</code></td>\n<td>string</td>\n<td>Raw final outcome before organization mapping</td>\n</tr>\n<tr>\n<td><code>gathered_context.mapped_call_disposition</code></td>\n<td>string</td>\n<td>Final outcome after organization mapping</td>\n</tr>\n<tr>\n<td><code>cost_info</code></td>\n<td>object</td>\n<td>Call cost breakdown</td>\n</tr>\n<tr>\n<td><code>cost_info.call_duration_seconds</code></td>\n<td>number</td>\n<td>Call duration in seconds</td>\n</tr>\n<tr>\n<td><code>annotations</code></td>\n<td>object</td>\n<td>QA analysis results (if a <code>qa</code> node is configured)</td>\n</tr>\n<tr>\n<td><code>recording_url</code></td>\n<td>string | null</td>\n<td>Public download URL for the call recording</td>\n</tr>\n<tr>\n<td><code>transcript_url</code></td>\n<td>string | null</td>\n<td>Public download URL for the call transcript</td>\n</tr>\n</tbody></table>\n<h3 id=\"call-disposition\">Call disposition</h3>\n<p>Use <code>{{gathered_context.call_disposition}}</code> for the raw workflow outcome or\n<code>{{gathered_context.mapped_call_disposition}}</code> for your organization&#39;s mapped\ncode. <code>{{gathered_context.call_status}}</code> remains the observed reason the call\nended. See <a href=\"/studio/docs/developer/call-dispositions\">Call Dispositions</a> to configure custom business\noutcomes for each workflow and understand the fallback behavior.</p>\n<div class=\"as-callout as-callout-note\">\n\n\n<p>PayInt Voice Studio always includes a top-level <code>call_disposition</code> field in the delivered\npayload. If your template doesn&#39;t set one, it is added automatically from the\nraw <code>gathered_context.call_disposition</code>; if your template sets it explicitly,\nyour value is kept.</p>\n</div>\n\n\n\n<h3 id=\"example-payload-template\">Example payload template</h3>\n<pre><code class=\"language-json\">{\n  &quot;run_id&quot;: &quot;{{workflow_run_id}}&quot;,\n  &quot;customer&quot;: &quot;{{initial_context.customer_name}}&quot;,\n  &quot;outcome&quot;: &quot;{{gathered_context.resolution}}&quot;,\n  &quot;call_status&quot;: &quot;{{gathered_context.call_status}}&quot;,\n  &quot;disposition&quot;: &quot;{{gathered_context.call_disposition}}&quot;,\n  &quot;mapped_disposition&quot;: &quot;{{gathered_context.mapped_call_disposition}}&quot;,\n  &quot;duration&quot;: &quot;{{cost_info.call_duration_seconds}}&quot;,\n  &quot;recording&quot;: &quot;{{recording_url}}&quot;\n}\n</code></pre>\n<hr>\n<h2 id=\"authentication\">Authentication</h2>\n<p>Webhook requests support the following authentication methods, configured via a stored credential:</p>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NONE</code></td>\n<td>No authentication</td>\n</tr>\n<tr>\n<td><code>API_KEY</code></td>\n<td>Sends the key in a custom header (e.g. <code>X-API-Key</code>)</td>\n</tr>\n<tr>\n<td><code>BEARER_TOKEN</code></td>\n<td>Sends <code>Authorization: Bearer &lt;token&gt;</code></td>\n</tr>\n<tr>\n<td><code>BASIC_AUTH</code></td>\n<td>HTTP Basic authentication (username + password)</td>\n</tr>\n<tr>\n<td><code>CUSTOM_HEADER</code></td>\n<td>Any custom header key-value pair</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"receiving-webhooks\">Receiving webhooks</h2>\n<p>Your endpoint should:</p>\n<ul>\n<li>Accept <code>POST</code> requests with <code>Content-Type: application/json</code></li>\n<li>Respond with a <code>2xx</code> status code promptly (within 30 seconds)</li>\n<li>Handle duplicate deliveries idempotently (retries may deliver the same payload more than once)</li>\n</ul>\n<h3 id=\"minimal-example-receiver-python\">Minimal example receiver (Python)</h3>\n<pre><code class=\"language-python\">from fastapi import FastAPI, Request\n\napp = FastAPI()\n\n@app.post(&quot;/webhook/dograh&quot;)\nasync def handle_dograh_webhook(request: Request):\n    payload = await request.json()\n    run_id = payload.get(&quot;run_id&quot;)\n    outcome = payload.get(&quot;outcome&quot;)\n    # process the call result...\n    return {&quot;status&quot;: &quot;ok&quot;}\n</code></pre>\n<hr>\n<h2 id=\"webhook-node-in-a-workflow-definition\">Webhook node in a workflow definition</h2>\n<p>See <a href=\"/studio/docs/developer/workflow-schema#webhook-node\">Workflow Definition Schema</a> for the full configuration reference.</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;webhook-1&quot;,\n  &quot;type&quot;: &quot;webhook&quot;,\n  &quot;position&quot;: { &quot;x&quot;: 600, &quot;y&quot;: 0 },\n  &quot;data&quot;: {\n    &quot;name&quot;: &quot;Notify CRM&quot;,\n    &quot;enabled&quot;: true,\n    &quot;http_method&quot;: &quot;POST&quot;,\n    &quot;endpoint_url&quot;: &quot;https://your-service.com/webhook/dograh&quot;,\n    &quot;payload_template&quot;: {\n      &quot;run_id&quot;: &quot;{{workflow_run_id}}&quot;,\n      &quot;customer&quot;: &quot;{{initial_context.customer_name}}&quot;,\n      &quot;outcome&quot;: &quot;{{gathered_context.resolution}}&quot;\n    }\n  }\n}\n</code></pre>\n"}};
