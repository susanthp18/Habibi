# Local tooling / agent dirs
.claude/
.agents/

# Large installers / binaries
Claude Setup.exe
*.exe

# OS / editor
.DS_Store
Thumbs.db

# Env files (also covered by subdir gitignores; belt-and-suspenders)
**/.env
**/.env.local
!**/.env.example

# Python
**/.venv/
**/__pycache__/
*.pyc

# Node / build output
**/node_modules/
**/dist/
**/.output/

# Runtime logs / machine-specific tooling config
logs.txt
*.log
.mcp.json

# Local machine-maintenance scratch (WSL/Docker disk cleanup) — not project files
_cleanup_log.txt
_diskpart_compact.txt
_docker_err.txt
_docker_out.txt
_wsl_clean.sh
.hermes/

# Patch artefacts left beside a file after `git apply`; never a project file
*.patch

# Personal working material beside the repo, never part of it
/Mockups/
/Site/
/Videos/
/index.html
/kdp-axolotl-coloring-book/
/Habibi/public/brand/
/Habibi/public/videos/

# Editor and tool state, loop/audit scratch — never project files
/.idea/
/.cursor/
/.vite/
/.loop/
/_conv_trace/
/artifacts/*
!/artifacts/retrieval_*.json

# Upstream Dograh, extracted for comparison. The engine we actually ship is
# vendored under /agentstudio/engine; this 46 MB copy is scratch.
/dograh-main/

# Working notes beside the repo, never part of it
/latency_review/
/my_view.md
/PayInt_OnePager.txt
