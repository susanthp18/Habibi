# Agent Studio pass 7 — report (2026-09-13)

Plan: `snuggly-petting-sonnet.md` (WS0–WS7). 103 commits on `autonomous-modernization`, `a973739` … `e461f8c` (plus the REFACTORING-STATE and memory commits that follow this file), one concern per commit. Nothing pushed. Dev DB migrated to **0144** (`sql/52`).

**The pass in one line:** every regulated residue item is closed with the test that would have caught it; a promise is one open commitment per account that can be renegotiated with a reason and a history on every channel; there is one error envelope, one transcript writer and one change-log verb per evidence-changing write; the studio's residue is closed by id; every ratchet is at its target (module-size, file-size, query-state and mutation baselines empty; pyright on the whole tree at 0; `noUncheckedIndexedAccess` on; type-aware lint on; the source-pin baseline fell for the first time) — and the type checker, widened to the tree, found two real defects the suite could not see.

## Decisions taken (AskUserQuestion, 2026-09-13)

| Decision | Choice |
|---|---|
| Open promises per account | **One open PTP per account**, with revise (date/amount + reason + history, cap 3) and cancel (reason); the voice/WhatsApp tool revises rather than creating a second |
| `ALLOW_ACTOR_HEADER` | stays opt-in; the ignored-header warning and the dev/console agreement test landed; `.env` untouched |
| `useMutation` in screens | all 43 moved to `api/*`, `useMutation` a restricted import under `components/` and `routes/` |
| Licensing | proprietary: `LICENSE`, `"license": "UNLICENSED"`, `THIRD_PARTY_NOTICES.txt` generated by `backend/scripts/third_party_notices.py` and pinned current |

## What landed, by workstream

### WS0 · Preconditions
Other stream's files left alone throughout (see *Deferred*). `actor_context` warns once per process when `X-Actor-User-Id` arrives with the flag off; `tests/test_actor_context.py` pins both branches. Baselines recorded before anything moved (pyright 113/36, `noUncheckedIndexedAccess` 175, `useMutation` in screens 43, check-query-state 16, dev DB 0 duplicate open promises).

### WS1 · Regulated and correctness residue — 22 of 22
One writer for transcripts at rest (`capture_events.insert_transcript_turn`; WhatsApp and manual turns are masked like voice); the DND detector reads the registry column through `contact_policy.dnd_predicate`; a second PSP settlement is parked as a `duplicate_settlement` event, not discarded; Twilio 21610 is `CarrierOptOut` and reaches the consent ledger; an inactive operator is 401; an expired pay link says so and is not marked opened; `hourly_reach` is tenant- and visibility-scoped; the three status remappers that hid `failed` are gone; `narrow_window` runs under a savepoint; the PTP tool says when the confirmation could not be sent and reports a failed CRM write; `ext.*` outside the offer is refused; G-OB6 bounds `retry_on`; the gateway retries nothing under 500 and honours `Retry-After` once, forwards `reasoning_effort`, drops `temperature` for reasoning deployments (one `_is_reasoning_deployment`); a remote JSON-RPC refusal is not a circuit failure; the Literals and the stored channel; the MinIO delete goes through the breaker; four sync calls left the event loop; one ending-reason latch, IVR `ivr_stuck`, `add_targets` as one `INSERT … SELECT unnest`; idempotency keys on callbacks and document requests and on the 360's PTP/dispute creates; the four query shapes (UNION legs with `entity_type`, a bounded ledger read, range predicates on `started_at`, `routing_rule_executions(rule_id, evaluated_at)`).

### WS2 · One open promise per account, renegotiable
Migration **0144 / `sql/52`**: `uq_promises_one_open` (partial unique on `upcoming|due_today`; the dev DB had no duplicates to resolve), `promise_revisions` (tenant column, RLS applied and enabled), `cancelled` in the status vocabulary, `revision_count`/`cancel_reason`/`cancelled_at`/`cancelled_by`, the `sql/41` duplicate indexes dropped, `followups` on the `updated_at` trigger, `promise_reminders.last_error`. `db_promises.py` is the owner: create refuses with `promise_already_open` (409 with the open id), caps the amount to outstanding with the pay link's rule, `revise_promise` writes the revision row and re-derives the reminder and the pay intent, refuses past `PTP_MAX_REVISIONS` and outside the horizon, `cancel_promise` frees the account; `patch_promise` keeps only status and `paidAmount`; transitions through `db_core.assert_transition`. Routes `POST /promises/{id}/revise|cancel` with idempotency keys and authz rows. Tools: `create_promise_to_pay` refuses with the open promise spoken back; `revise_promise_to_pay` on both channels (ptp-negotiate **1.6.0**); one eval fixture where the customer asks to delay. Frontend: `RevisePromiseSheet`, `CancelPromiseSheet`, the 360 offers "Revise it" on 409, the board shows `revisionCount` and the `cancelled` tone, the detail shows the history. `tests/test_promises_one_open.py` incl. the two-concurrent-creates `db_real` case. Parity 7,313 lines; RLS 224/224.

### WS3 · One envelope, one verb per evidence write, one transcript writer
`api_support.register_error_handlers`: a 422 that carries `type`/`loc`/`msg` only, `Retry-After` on every 429/503 (`AzureBusyError`, `CircuitOpenError.retry_after_s`, `RateLimitExceeded`), a catch-all 500 `{detail: internal_error, requestId}` that sets `X-Request-Id` itself, `pg_errors.sqlstate` with 23503/23514 mapped, `CircuitOpenError` re-raised past `except RuntimeError`, `outbound.UNAVAILABLE_REASONS` → 503 on both dial routes, no container path in a 404. Change-log verbs `agent.connector` and `agent.mcp_key` (never the key or its hash — pinned); `_VIOLATION_TRANSITIONS`. The transcript writer is WS1 #1.

### WS4 · Studio residue — 40 closed by id, 4 deferred, 1 closed by design
`raw/closures.json` names each with its sha (`e30ce97`, `3637d46`, `a91d371`, `5d5ae1a`, `6089743`, `a33efbd`, `40557ab`, `340a707`, `36349ac`). Highlights: the browser reads `Me.permissions` (Roles grid and Publish disabled with the permission named); `_known_bots` is one function for preview, publish and rollback; `publishedCard` falls back to the built-in card; G-F4 warns on a declared handoff no step offers; the end node's farewell and the typed edge's `payload_schema` have editors; the eval report carries its trials and the tab renders them; two drift pins (seeded prompt text, eval-suite kinds); render tests for the eight studio panels and the two skills screens through one `mountAt()` harness; the cached-token counter `llm_cached_input_tokens_total{deployment}`; a `db_real` test for two concurrent chain writes; the Twin hint says what the twin is.

### WS5 · Frontend structure
The studio route is **487 lines with no ref in its body**: `useStudioDraft` owns the editor (reducer, hydration, the saved baseline, every save guard), `useStudioLint`, `useFlowValidation` (the canvas loads when the Flow tab opens), `useStudioActions`, `StudioBanners`. All 43 screen mutations are `api/*` hooks with `meta.errors` and one invalidation set each; the lint gate is `no-restricted-imports`. `lib/format.ts` is the one formatter owner; `UNASSIGNED` in `api/staff`; the 35 `staleTime` restatements gone; the webhooks drawer is inspect-only; the billing tenant selector deleted; the live-call client, `navigateWorkItem`, `publishedCard` and compile `card` typed. Size baseline empty: `FlowCanvas` → `flow/canvas/{layout,chrome,useCanvasValidation,useCanvasGraph}`, `OutboundCardEditor` → `prompt-studio/outbound/*`, `VoiceCatalogGrid`, `VoicePanel` through `useVoicePreview`, `fleet/{EvalTrend,RecentChanges}`, `useLiveCallTimeline`, `api/skills.ts` + `api/tts-preview.ts`, `useSandboxSelection`. `check-query-state` 16 → 0. Accessibility: named icon buttons, `AssignedQueue` on `ui/tabs`, `FilterTable` with table roles, `th scope=col`, 3:1 button borders, hover ≠ selected, a `forced-colors` block, `text-white` → `text-text-inverse` (59), tokens in the charts; `styles.css` declares itself the design spec (Design.md never existed).

### WS6 · Backend hygiene
Module-size baseline **empty**: `outbound_pools.py`, `bank_boundary/ingest_apply.py`, `contact_ledger.py`, `voice/crm_sink_{observer,jobs}.py`, `bot_conversation.py` + `bot_turn_write.py`, `sandbox_tools.py`, `db_disputes.py` + `db_interactions.py`, `db_inbox_rag.py` + `db_whatsapp.py`, `gates_outbound.py` (each carve reaches `db.engine` at call time so the `db_tx` proxy holds). Dead: `offers_presented` and its cap, `fish_tts.EMOTION_TAGS`, `env_allows_dev_key`, `vault/persist.reveal`, `@tanstack/router-plugin`. One `db_core.writer/reader`. Ops floor: `ops/alerts.yml` (JobsDead, JobsStuck, CircuitOpen, DbPoolExhausted; promtool-checked, loaded by the dev Prometheus), `docs/ops/runbook.md`, `/ready` without the breaker dump, admission metric failures at WARNING, money-path logs carry the identifier, `_record_reminder` keeps the SID and the error apart. Supply chain: pgvector by digest, `-c requirements.txt` in the MCP install, `LICENSE` + `THIRD_PARTY_NOTICES.txt` + `test_third_party_notices_current`.

### WS7 · Types, tests, envelope
- **pyright covers the whole backend tree** (`include = ["."]`; tests, scripts, alembic out): 114 errors → 0 across 41 files, nothing suppressed. **Two were defects, not noise:** (1) `agent_core/connectors/persist.py` called `httpx.post(..., extensions=pinned.extensions)` — an argument that function does not take — so every real remote-connector dial raised `TypeError` before the socket opened, while the tests faked `httpx.post` with `**kw`; now a `Client` seam (`_post`) with a `MockTransport` test that asserts Host and SNI at the transport (`5365327`). (2) `voice/crm_sink.py`'s direct-finalisation path after a drain timeout called `self._handle_sync`, which the WS6 carve had moved — the one situation that path exists for ended with the interaction open; the new test cancels a drain that never returns (`90aac1d`). Also found: `policy://authority-matrix` imported an `export_matrix` that never existed and answered a placeholder on every read (now the matrix's live numbers); `list_tasks` read every task twice; `seed_postgres.upsert` composes its dynamic SQL with `psycopg.sql`.
- **`noUncheckedIndexedAccess` is on**: 174 sites, no `@ts-expect-error`. The numeric kernels read through `lib/arrays.at()` (throws on an index the loop already bounded); `DEFAULT_FIELD`/`DEFAULT_TEMPLATE` named instead of `[0]`; `weekDays` returns a 7-tuple; the operator table is a non-empty tuple type.
- **Type-aware lint**: `recommendedTypeCheckedOnly` with `projectService`; errors under `src/api` and `src/lib`, warnings elsewhere; `scripts/check-lint-warnings.mjs` holds the total (167) and only falls. `"a" | string` unions are `"a" | (string & {})`; the copilot stream reads its string fields as strings; the dead "Shadow tuner" strip (a `tuner_removed` stub through `useQuery`) deleted.
- **Source-pin BASELINE 106 → 102**: the follow-up sweep and the dashboard read are asserted on the statements they run (`before_cursor_execute` capture), the scheduler on `SUITE_KINDS`, the cardless fallback on a constant that no longer exists. Three more pins were retargeted after the pass's moves (`/ready` without circuits, `sandbox_tools`, `message_history`) and the mouth-turn golden gained `revise_promise_to_pay`.
- **Test residue**: `test_a_failed_alert_write_leaves_no_handoff_behind` fails the last write of `escalate_voice_interaction` and asserts zero handoff and conversation rows; `test_tenant_scoping` anchors on `'(tenant_id'`; `.github/workflows/backend-nightly.yml` runs the alembic upgrade/downgrade roundtrip nightly against a database created for the job.
- **Reports**: `raw/closures.json` is the studio backlog's closure ledger (overlaid by `merge.py`, rendered by `render_findings.py` → `AGENT_STUDIO_FINDINGS.md`); `REFACTORING-STATE.md` gained the measured table for 2026-09-13.

## Declined, deferred, not done

| Item | State | Why |
|---|---|---|
| `test_phase{4,5,6}.py` rename | declined | mixed domains per file; a rename by domain would be three splits for a cosmetic gain |
| Legacy `tts_voices` walk | kept | one `prompt_version` still carries a legacy `voiceId`; deleting the walk breaks its playback |
| BINDINGS-7 (`voice_ref`), BINDINGS-9 (locale 422), HEADER-8, VOICE-12, WS3.2 `record_provider_binding`, `/mcp/status` reachability probe | **deferred** | they live in the other stream's uncommitted files (`agent_core/providers/*`, `routers/integrations.py`, `schemas/integrations.py`, `Habibi/src/api/providers.ts`, `PublishDialog.tsx`); `raw/closures.json` names each |
| `src/lib/studio-trust.ts` type-aware rule | held at warn | same file set; the fix is `"a" | (string & {})` and the ESLint block says so |
| `queryKeys` factory | not built | one spelling per module already; nothing forced it |
| `test_specialist_hop` prefix pin, `test_outbound_completion`/`test_outbound_card_switches` source pins | left | not one-liners; the handler layer has no fixture that drives it |

## Verification

Everything below was run at the close of the pass, after the Docker engine recovered.

| Check | Result |
|---|---|
| pyright, whole backend tree | **0 errors**, 9 warnings (missing optional imports) |
| `ruff check .` | clean |
| Frontend `tsc --noEmit` (strict + `noUncheckedIndexedAccess`) | **0 errors** |
| `npm run lint` (eslint with type-aware rules + 9 gates) | exit 0; 0 errors, 167 warnings at the baseline; file-size 0, query-state 0, layering clean, 318 tokens |
| `npx vitest run` | **40 files, 364 passed** (warm; a cold run reports fork-worker timeouts — the known jsdom start cost, not failures) |
| `npm run build` | green |
| **Full backend suite, end of pass** (worker images rebuilt first, nothing edited during the run) | first run **11 failed / 4,739 passed / 79 skipped** in 21:02 — every failure a premise this pass changed (two promise-idempotency tests booked two open promises on one account, the closed-status set and the refresh wiring lacked `cancelled`/`revise_promise_to_pay`, the G15 fallback test assumed an Azure voice, a duplicate of the retargeted circuit-handler test, `DEFAULT_CALLS_LIMIT` undocumented once the carve read it through `env_int`, two snapshots); fixed in `f14d259`, the eleven files **70/70** on re-run |
| Coverage | **72%** over 55,683 statements; `--cov-fail-under` **68 → 71** |
| `migrate_from_empty.py` | **parity, 7,313 schema lines** (built in the container with `--keep`, diffed on the host) |
| `rls.py status` | 224 installed / 224 derived, 224 enabled / 224 forced |
| `fleet_parity.py` / `eval_gate_preflight.py` | 4 of 4 deployments agree / every published card satisfies its `eval.require` |
| `/metrics` | api, bot_worker, kb_worker, voice each serve the 52 queue-depth series on `:9100`; `wk_batch` declares no `METRICS_PORT` (it never did — a candidate for the next pass) |
| Browser | all 26 routes walked three times against the dev API through the dev server: every `<main>` rendered, no 5xx on any `fetch`, no duplicate-key or unhandled error attributed to any route. (The first walk's console carried 27 `confirm_identity` duplicate-key lines and three `500`s from the tab's earlier history — the API restart and the pre-pass HMR artefacts; two clean walks after it, with `console.error` and `fetch` hooked per route, reproduced neither.) |

**Note on the dev stack:** the Docker Desktop Linux engine answered `500` to every API call for ~1h45 during WS7.4–7.7 (`Wsl/Service/0x8007274c`) and recovered on its own; `wk_batch` logged `failed to resolve host 'db'` while the embedded DNS was down and came back healthy with it. Not a defect of this tree.

## The unverifiables (~60), with the command that would verify each

These are findings the audit could only settle against a running system or real traffic; none is claimed closed.

| Area | What would settle it | Command / place |
|---|---|---|
| Twilio 21610 on a real carrier | a real STOP reply writes `consent_records.sms_opted_out` | send to a handset that replied STOP; `SELECT sms_opted_out FROM consent_records WHERE customer_id=…` |
| PSP duplicate settlement | a real PSP retry with a new `provider_ref` parks one event | replay the PSP webhook twice with the sandbox account; `SELECT * FROM payment_events WHERE kind='duplicate_settlement'` |
| Reminder SID vs error | a failed then successful reminder keeps both columns | `SELECT provider_delivery_id, last_error FROM promise_reminders WHERE status='failed'` after a forced Twilio failure |
| Voice transcript masking at rest under load | no 6+-digit run in any `transcript_turns.text` | `SELECT count(*) FROM transcript_turns WHERE text ~ '\d{6,}'` on the dev DB after a simulator run |
| Cached-token counter | `llm_cached_input_tokens_total` moves on a prompt-cached turn | `curl -H "X-API-Key: …" :9100/metrics \| grep llm_cached` on the voice container |
| Alerts fire | JobsDead / JobsStuck / CircuitOpen / DbPoolExhausted reach Alertmanager | `UPDATE bot_turn_jobs SET status='dead' WHERE id=…`; Prometheus → Alerts |
| Pool exhaustion 503 | `/ready` answers 503 with `pool_exhausted` under saturation | `DB_POOL_SIZE=1 DB_MAX_OVERFLOW=0` and two concurrent long queries; `curl -i /ready` |
| Retry-After honoured by the console | a 429 pauses the client for the header's seconds | `RATE_LIMIT_PER_MINUTE=1`; watch the network tab |
| Connector SNI pinning end to end | a resolver that answers twice cannot redirect the dial | a DNS server returning a public then a private address; the test `test_the_dial_lands_on_the_checked_address` covers the code, not the resolver |
| Concurrent promise creates across processes | exactly one row from two workers | two `curl` POSTs in parallel; `SELECT count(*) FROM promises WHERE account_id=… AND status IN ('upcoming','due_today')` |
| Nightly alembic roundtrip | the workflow passes on GitHub | `.github/workflows/backend-nightly.yml`, `workflow_dispatch` |
| Coverage figure | the measured number after this pass | the second command above |
| Worker image parity | the rebuilt `worker`/`bot_worker` import the carved modules | `docker compose build worker bot_worker && docker compose logs --tail 20 worker` |
| Studio screens after WS5 | every route renders against the dev API | open each of the 24 routes; the render tests cover the wire, not the layout |
| RLS under the app role after 0144 | `promise_revisions` denies cross-tenant reads | `scripts/rls.py status` in a container; `SET app.tenant_id='other'; SELECT count(*) FROM promise_revisions` |
| The remaining ~45 from `CONFLICTS-AND-FALSE-POSITIVES.md` §"runtime questions" | as listed there | `audit-reports/CONFLICTS-AND-FALSE-POSITIVES.md`, `REFACTORING-STATE.md` §"The ten open runtime questions" |

## For the next pass
1. When the other stream commits: BINDINGS-7/9, HEADER-8, VOICE-12, `record_provider_binding`, the `/mcp/status` probe, and `studio-trust.ts`'s unions — then delete the ESLint exception block.
2. The end-of-pass verification above (full suite, coverage gate, worker rebuild).
3. `check-lint-warnings` 167 → 0 (`react-refresh/only-export-components` 46, `no-unnecessary-type-assertion` 29, `no-misused-promises` 24, `exhaustive-deps` 22, `no-floating-promises` 18).
4. Source pins: 102; the handler-layer ones need a `ToolBuildContext` fixture before they can become behaviour tests.
