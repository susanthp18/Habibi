# Agent Studio pass 5 — report (2026-09-12)

Plan: `snuggly-petting-sonnet.md` (WS0–WS8). 93 commits on `autonomous-modernization`, `5812500` … `b74f21d`, one concern per commit. Nothing pushed. Dev DB migrated to `20260912_0140`; every hardening control is on and a deployed-mode boot passes the gate.

## What landed, by workstream

### WS0 · One stream
The "honest engines" stream (76 modified + 27 untracked files) is committed in ADR-shaped commits: ADR-0004 one run-up per turn (`agent_core/turn.py`, compaction), ADR-0005 a retrieval answer names its shape, ADR-0006 identity is an assurance level, ADR-0007 a policy refusal is a schedule, ADR-0008 the borrower is told what the record says; plus the residue (usage attributed to the decision that spent it, evaluation ownership, work_runtime on one adapter, dead helpers gone, `*.patch` ignored). Migration 0136 + sql/46 mirror the sql/24 delta. The tree has been one stream since; whole-file `git add` is safe again.

### WS1 · Regulated and correctness residue — 29/30, one declined
Each its own commit with the test that would have caught it: consent no longer fabricates `Mon-Fri 10:00-19:00`; the Azure Speech region has one owner and no default; voice `decline_offer` reports a failed write; the cardless floor is stated once (`ToolGrant.for_card(None)`, ADR-0002 amended); archiving a live deployment needs `AGENT_PUBLISH`; `list_skills` is a read and the boot sync leaves a chain entry; skill import/export answer 400/409; non-bot flags never become critic findings (`guardrails.NON_BOT_FLAGS`); one recording-disclosure detector; `voicemail.max_sec` decides something; `identity.channels` is honoured where a channel is answered (`ChannelNotAuthored`, `bot_jobs.mark_dead`); `payload_schema` honoured at runtime; four catalog drifts; `HumanGate.require` floor/both retired with a G0 warning; `trafficPct` 1..100 and the `shadow` request field gone; `sync_seed` disables retired models; persona traits bounded; `actor_is_admin` reads permissions; status CHECKs on six tables, `ledger_entries.balance` dropped, `supervisor_actions` RESTRICT (0137, sql/47); two dead names; `/ready` echoes no exception text; `compare_digest`; `SecurityHeadersMiddleware` + pay-page CSP; provider-aware `SpokenTextFilter`; `dependency_call_duration_seconds` in the breaker and a breaker on the LLM gateway; the ordered route snapshot; `.env.example` completeness (67 variables documented).
**Declined**: #27 (bulk `contact_policy.evaluate` for the inbox) — a second copy of the gate's reads.

### WS2 · PII columns are encrypted — the last hardening control
`customers` is a view over `customers_pii` (sql/01 functions, sql/02 view + INSTEAD OF triggers, sql/48 migration form, migration 0138); the key travels as the `app.pii_key` startup parameter like the tenant (`pii_key.connect_option()` in db_core, alembic, conftest, rls); HMAC lookup columns; every `REFERENCES customers(id)` in sql/ names the base table; the boot gate reads extension + view + key. `HABIBI_DEPLOYED=1` boots with `inactive: []`. Key rotation is a documented script, not automation.

### WS3 · The fleet publishes as a fleet — premise corrected
Phase 3 as planned (retire member deployments) would have run outbound missions under the Door's prompt, so it was not built. What was: `rebuild_fleet_deployment` — a member publish ships a *new* deployment on every merging Door (same version, new `bundle_hash`), recorded as `agent.fleet_rebuild`, returned as `fleetRebuilds` and toasted in the Studio; `CompiledBundle.member_versions` is part of the hash; `fleet_parity.py` reports STALE when a door merges a superseded member. Any change to a hashed bundle field invalidates every live bundle until each card is recompiled — parity catches it.

### WS4 · No production function is over 300 lines
Pattern, applied eleven times: **pin behaviour first**, then a scratchpad AST splitter moves verbatim source ranges into phases over one dataclass (each phase unpacks the fields it reads and writes back the ones it stores; an early `return X` propagates through the builder).
- Pins added: `tests/snapshots/{text_turn,sandbox_turn,kb_retrieve,reader_shapes}.json` (real turn / real rehearsal / real retrieval / the four readers' key trees), beside the two pass-4 snapshots. All byte-identical across their moves.
- `voice/tools.py build_tools` 2,696 → 137 over `voice/tools_{scope,identity,verify,refusal,offers,probe,leads,position,negotiate,authority,preferences,knowledge,closing,handoff}.py`; `register_handlers` 1,021 → 121 over six modules; `bot_runtime._handle_turn` 859 → 9 over seven phases and a `Turn`; `append_sandbox_turn` 588 → 13 over six phases and a `SandboxTurn`; `kb_retrieve.retrieve` 639 → 109 over the five phases ADR-0005 names, on a `Retrieval`; the four screen readers → reads/shape phases; `validate_graph` → `GraphCheck`; `search_knowledge_base` → `KbSearch`; `ingest.load` → `Ingest`; `build_services` → three phases over the call scope; `build_authored_flow` → `FlowBuild`; `_bind_crm_session` lifted to module level.
- `tests/test_function_size.py`: ceiling 300, **baseline empty** — a function that grows past it must be split. Frontend twin `scripts/check-file-size.mjs` (routes ≤ 500, components ≤ 800, twelve baselined, only shrinks) in `npm run lint`.
- Carves: `db_evals.py` + `db_kb_snapshots.py` out of `db_inbox`; `followups_db.py` deleted (coaching → `db_coaching.py`, the writes joined `db_redaction`/`db_routing`/`db_workspace`); `db_prompt_studio/` package of seven modules. `_run_sandbox_tool_loop` (294) left whole.

### WS5 · `schemas` is a package; request fields are typed or name their validator
`schemas.py` (6,358 lines, 512 classes) → `schemas/{common,payments,agent_studio,a2a,billing,compliance,crm,evals,floor,inbox,integrations,kb,outbound,platform,routing,sandbox,telephony,voice_catalog,webhooks}.py`, each class beside the router that serves it (assignment computed from router usage; `common` has no outgoing edge; `__init__` re-exports every name; in the clean-interpreter import test). Installments, transcript turns, policy-rule draft items, webhook retry policies and headers, and routing conditions (with OR groups) are models — a bad amount or unknown operator is a 422 at the boundary. The 21 fields whose shape another owner validates are listed in `test_route_structure._OPAQUE_BY_DESIGN` with that owner, checked both ways.

### WS6 · One owner each
`agent_core.clock.utc_now` is the one UTC clock (twelve `_now()` copies are aliases, ~60 modules read it, `test_one_clock_one_environment` refuses a new copy); `env_utils.env_int` and `agent_core.numbers.clamp` own their copies; `tests/test_env_reads_have_an_owner.py` ratchets raw `os.getenv` per file (160 reads in 67 files, may only fall); `.gitattributes` `* text=auto eol=lf` + binaries, prettier back to `lf`, `bunfig.toml` deleted, the CI comment counts the suites, personal directories ignored; `npm run lint` is green on Windows. `outbound._json` kept (`default=str` differs from `db_core._jsonb`).

### WS7 · The browser parses what the API's own models describe
- **Wire contract**: `backend/scripts/gen_wire_schemas.py` rewrites the response model of every modelled route (323) as zod from `main.app.openapi()` → `Habibi/src/api/wire/generated.ts`; `api/config.ts` resolves each request path to its route template and parses the body with that schema unless the call passed its own — drift is a `WireError`, not a blank screen; objects are passthrough. `capture_wire_samples.py` records one real 200 body per GET route (143) and `wire.test.ts` parses all of them; `tests/test_wire_schemas_current.py` fails when the generated file is stale. **Found by the capture**: `GET /customers/{id}/contact-policy` had 500'd on every customer with a rule set (the model forbade `nextAllowedAt` and the policy binding) — fixed and pinned.
- `AppShell` is the pathless layout route `_app` (42 route files moved; 29 stopped mounting their own shell); the error page shows the server's `X-Request-Id`; the agent-tuning types left the fixture module (`no-seed-imports` type exceptions: none).
- **Not done**: the route reducers (`prompt-studio` 1,417 lines / 29 `useState` with autosave refs; `treatment`, `knowledge-base`, `sandbox`) and the per-tab studio-truth residue. The size ratchet holds their line and the wire contract validates their data; a reducer is a design change on the flagship screen that needs its own render tests first. `FIXTURE_IMPORTERS` stays (USE_MOCK is deliberate).

### WS8 · Types, tests, envelope
- **pyright** widened to `contact_policy.py`, `routers/`, every `db_*.py` — green (`as_utc` gained overloads; a non-datetime `expires_at` no longer reaches a comparison; the handoff session validates the builders' dicts through its model). `voice/` (52) and `db_prompt_studio` (28) remain, listed in `pyproject`.
- **Migrate from empty is real**: `alembic/baseline/` (the initial commit's sql), `alembic/replay.py` (additive ops idempotent; `apply_sql` reads a mirror as it is today and translates `customers_pii` back before 0138), 0047/0060 fixed for an empty database, migrations 0139 (updated_at triggers) and 0140 (three defaults, 75 constraint renames to the schema files' names). `scripts/migrate_from_empty.py` builds the chain from nothing and the `sql/` build side by side, turns RLS on in both, diffs normalised `pg_dump`s: **parity, 7,241 schema lines**. In CI after the sql build. **Found on the way**: the fresh `sql/` build had been broken since the PII columns landed (trigger on a view, a seed writing a dropped column, sql/48 not applicable fresh) and CI carried no PII key — all fixed.
- **Negative authz, table-driven**: all 319 registry routes, called as the roleless actor, are 403 naming the missing permission.
- **Voice evals**: all fifteen scenarios on the manifest (nine had never run), `run_voice_evals.py --validate` in CI (the real evals need Azure), `tests/test_voice_eval_suite.py` pins manifest ↔ directory.
- **Coverage gate**: `--cov-fail-under=68` in CI (measured 70%), only moves up.
- **Pins**: the source-text pin ratchet stayed at 106 (five `_handle_turn` pins read the phases through `text_turn_source()`; every snapshot test compares bytes, never `read_text`).

## Verification
- Backend: ruff clean; pyright 0 errors on the widened include; `fleet_parity.py` 4/4; `eval_gate_preflight.py` clean; `rls.status()` enforcing 222/222; `HABIBI_DEPLOYED=1` boot: `inactive: []`, gate passed; `migrate_from_empty.py` parity.
- Frontend: `tsc` 0; `npm run lint` green (incl. the two size scripts); vitest **357/357** (37 files); `npm run build` ✓; the studio, treatment, compliance, webhooks, sandbox, dashboard and customers screens verified in the browser through the wire seam.
- Full backend suite (voice container, with coverage, 33 min): **4,624 passed, 76 skipped, 40 failed**, coverage **70%** (55,555 statements). Of the 40: 15 were mine and are fixed in `2bdbf6c` (source pins that read the module a split moved out of — the hop reader, the voice-bot/tool source helpers, the connect and terminal pins; five tests still passing the retired `shadow` input; the staging boot test assuming the gate refuses; two env-template entries now that `env_int` owns their reads; and one real hit — `agent_core/treatment/ope.py` still called `scoring._clamp`, caught by the suite and fixed). The other 25 are the pre-existing set: `test_treatment_followthrough` ×18 (the dev stack lacks `TREATMENT_LABELS_ENABLED`), `test_honest_engines_w7` ×3 (the panel counts the whole dev DB), `test_the_money_path::test_falling_back_to_a_generic_template_is_never_silent` (order-dependent, passes alone), `test_contact_policy::test_a_lead_with_no_accounts_is_not_settled`, `test_outbound_studio_bindings::test_demo_call_product_fixes_are_wired` (its `confident` pin now says what ADR-0005 made it mean — fixed), `test_phase5::test_a2a_task_input_required_with_cert`. The three tree-wide pins walk with `os.walk` and prune `.venv` now (they were enumerating it over the bind mount, minutes each).

## Left in the tree, not mine
Twelve frontend files (`components/{callbacks,consent,upsell}/*`, `ui/{input,select}.tsx`) were modified by another stream while this pass was finishing — an Input/Select size-variant refactor. Untouched and uncommitted here.

## Deferred (with reasons)
- Route reducers and the per-tab studio residue (WS7) — design changes on the flagship screens; pin with render tests first.
- pyright on `voice/` and `db_prompt_studio/` — 80 findings, mostly Optional row reads; the same one-directory-at-a-time rule.
- `_run_sandbox_tool_loop` (294 lines) — under the ceiling.
- `RUN_ALEMBIC_ROUNDTRIP` stays an opt-in local test; CI's proof is the parity diff.
- Live dial through the Door; the root-owned `voice_sessions` volume — manual.
