# Agent Studio pass 6 — report (2026-09-13)

Plan: `snuggly-petting-sonnet.md` (WS0–WS7). 99 commits on `autonomous-modernization`, `92f5001` … `36f87be`, one concern per commit. Nothing pushed. Dev DB migrated to 0142.

**The pass in one line:** the repository is only what runs (6,471 → 6,353 tracked files; 1,953 → 1,835 outside `PRAXIST-main/`), the browser has one data path (`src/data/` — 12,962 lines of fixtures — and every `USE_MOCK` branch are gone), the studio's 26 lies are fixed and the handoff edge has an editor, the four flagship routes are a reducer plus components, every process exports `/metrics`, the nightlies run once, and the backend suite is green on the dev stack.

## What landed, by workstream

### WS0 · One stream
The other stream's 28 uncommitted frontend files (Input/Select size variants across the CRM sheets) committed first, as decided. `BigBound-page-reviews/` at the repo root is that stream's and stays untracked.

### WS1 · The repository is only what runs
Tracked scratch (`.loop/`, `artifacts/` except the five retrieval evidence files, `_conv_trace/`, `.idea/`, `.cursor/`, `.vite/`, a `.bak`, `Habibi/.lovable/`, `inventory.json`, the unwired `Habibi/scripts/*`, `dev-up.ps1`, the 2 MB `engines-findings-appendix.md`) deleted and ignored; the root design notes moved to `docs/design/` with every reference rewritten. Dead backend: Redis (requirement, compose service, `REDIS_URL`, `voice/config.redis_url`), `voice/spike.py`, `agent_core/telemetry.span` (a permanent no-op), `VOICE_FLOW_GRAPH` and its two test files, `FLEET_ENABLED` and its off-branch, nine `.env.example` keys nothing read (the template check now runs both directions), `pytest`/`ruff` out of the shipped images (`requirements-dev.txt`), `WORKQUEUE_WRITE`, the legacy `GET /tts-voices`; `provider_voice_sync.py` wired rather than deleted (`POST /tts-voices/catalog/sync` per bound provider; a Fish voice survives an Azure refresh). Dead frontend: `lovable-error-reporting`, `charts/use-dark-mode`, the `Design.md` references, `ui/badge` vs `ui/tag` (one kept), `package.json` name.

### WS3 · Regulated and correctness residue — 29/30, one declined
Each its own commit with the test that would have caught it. Rupees compared as `Decimal` with no slop; `_parse_promise_date` converts the instant to the tenant date; `handlerUserId` is the actor, 422 if sent; the export bundle has a visibility predicate and `no-store`; webhook event names come from the catalogue and the header is CRLF-stripped; inbox `contactableNow` and treatment consent say `policy_unavailable`/`consent_unavailable` instead of falling back; `escalate_to_human` is idempotent; mute leaves an audit row; migration 0141 (three uniquenesses, four indexes, four prefix-redundant ones dropped); one status spelling per column; transition tables on the four PATCHes; `clamp_list_limit`; `bot_jobs` backoff cap 12 and reclaim sets `run_after`; `llm_spend_daily` shared across processes; vault key salted (`scrypt`); Twilio SID masked; `PUBLIC_BASE_URL` in the hardening gate; `env_int`/`as_bool` at the six import-time reads; one `is_prod()`; one `verify_hmac`, one `twilio_client()`, the Meta breaker ignores `ValueError`; authz grants load off the loop; one Literal per column (`schemas.common`); `context.account_tail`; `money_inr.inr` direct; one consent channel map and one window parser; `kaia-v2-4` literals gone from nine sites (the fleet door is `cards[0].entryBotId`); `DisputeCreateRequest.amount > 0`; the inbox list no longer builds thread context (detail route carries it; `test_inbox_list_is_cheap`); customer insights read the fields they need and memoise the preview decision.
**Declined**: #11's `cancelled`/`canceled` half needed a wire re-capture the other stream's screens read; the Python literal and CHECK were unified, the frontend vocabulary followed.

### WS2 · The browser has one data path
`src/data/` deleted (29 seeds, 161 importers). Formatters have one owner (`lib/format.ts`, with vitest); business rules left the browser — consent `contactableNow`/`contactableReason` come from `contact_policy.evaluate` on the list, the DND window is the backend gate's refusal, routing `evaluateRules` became `POST /routing-rules/simulate` (typed `RoutingSimulateContext`), the ~300-line browser authority engine is gone. Label and tone maps live in `lib/<domain>.ts` and `api/types/<domain>.ts`; `USE_MOCK`, `mockDelay`, `MOCK_*`, `VITE_USE_MOCK`, the eslint rule and the three mock-asserting tests are gone. Wire additions: `ViolationListResponse.ruleCode/ruleLabel`, `ConsentListResponse.contactable*`. `check-file-size.mjs` measures `src/api` (≤ 800). Every screen renders against the dev API.

### WS4 · Structure
**Backend.** 488 `_db()._x` reach-throughs → `from db_core import …` (only `engine` stays behind `_db()`; `test_db_layering` pins it); `db.py` 2,906 → 2,592 with its double re-export blocks deduplicated; `ops_screens.py` → `db_floor.py`/`db_webhooks.py`/`db_provider_health.py`; `capture.py` → `capture.py`/`capture_events.py`/`capture_identity.py`; `tests/test_module_size.py` ratchet (≤ 1,500, nine baselined, may only shrink).
**Frontend.** Render pins first (`src/test/wire-fetch.ts` over `wire/samples.json`, memory router, one `*.test.tsx` per flagship route), then the moves: `studioDraftReducer`/`adoptVersion` (studio 1,430 → 1,133, still baselined), treatment 1,499 → 102 over `components/treatment/*`, knowledge-base 944 → 424 over `kbReducer`, sandbox 727 → 494 over `sandboxSessionReducer` + `useTextRehearsal`, `FlowInspector` split by tab, `McpConsole` → five panel files, `/prompt-studio` deleted (the editor lives at `/agent-studio/$botId`; `legacyShip*` gone). One chrome: `ui/stat-tile` + `records/MetricsStrip` replace fifteen strips, `ui/chip`, `ui/empty`, `ui/view-toggle`, `lib/utils.toggleIn`, `records/FiltersBar` + `FilterGroup` for the six bars, the ten `fixed inset-0` sheets are `ui/sheet` (role=dialog, focus trap, Escape), `components/shared/{SplitPanes,Waveform,EvalCockpit}`, `webhooks/EventPicker`, `hooks/use-min-width`. Homonyms renamed (five `Channel`s, `CustomerDispute`/`CustomerPromise`, two `DeliveryStatus`es, six `Filters`); `agentCard` is `AgentCard`; the copilot SSE pack parses with the generated schema; xyflow props typed. Lint gained `check-layering` (its allowlist was masking a false positive; the last `window.confirm` went; delete/rotate/revoke ask), `check-color-tokens` (318 tokens; eleven dead classes fixed) and `check-query-state` (baseline 16, falls only); `QueryState` keeps stale data through a failed refetch.
**Found on the way:** `PromptStudioShell` mounted a second `AppShell` inside the layout route's since `09e9b95` (two sidebars on every studio page); the floor emitted one flag per turn (duplicate React keys).

### WS5 · Studio truth
Ship: rollback asks and refuses unsaved edits (`rollbackLive`), `structuredChange` says "not loaded" instead of "unchanged" from `null === null`, the gateway model canary is visible, publish waits. Evals: the tenant-wide cockpit is off the card's tab. Outbound: done · skipped · pending of total. Tools/Policy: the compiler's count, G9 beside G4/G6, "required by skill <slug>", a dropped locked engine offers Restore. Connectors: the attach endpoint (+ `perm-connector-attach`, `attach_connector_to_card`) had no caller and went; a connector names what it carries (`DATA_CLASSES`, in `studio-vocabulary.json`, drift-pinned). Agent graph: `HandoffEdgeEditor` edits `carry`, `entry_node` (picked from the target's *published* graph), `bridge_line`, `refusal_line` and `memory.max_hops_per_call`; `whenDraft` gone. Flow: honest copy (no env var), a banner where nothing walks the graph, the tool list says loading/failed/empty. Voice: `und` is unknown not a mismatch (G15 and the panel), the cost row names its vendor, `VoiceDetailCard` is its own file. Plus the aria-labels, `tokens per call`, `m:ss`, the change-log retry, `role=status`, the in-flight label.

### WS6 · Observability and resilience
`observability.serve_metrics()` on `METRICS_PORT` in worker, bot_worker and voice (compose 9100; the dev Prometheus scrapes all four); the voice admission gauges register only where voice runs; `_QUEUES` names every drained table (ten; three were counted); `circuit_breaker_trips_total{dep}` and a CLOSE log line. Migration **0142** (`sql/50`): `mcp_tasks` gets lease/attempt/`run_after`/`dead` (reclaim, retry with backoff, dead-letter — `agent_core/mcp_http/tasks.py`), `nightly_runs(tenant, job, day)` with `bot_jobs.claim_daily` so the worker's four nightlies run once across replicas and restarts (verified: a restarted worker logs no second run); 0142 installs and enables the table's RLS policy (the first tenant table since 0126). `work_loop.run` is the one poll loop. `voice/reaper.reap_stale` ends calls whose worker stopped heartbeating (the dev DB carried one since 29 Aug). Breakers per webhook endpoint (an open circuit parks the delivery without charging an attempt), and on Fish, OpenRouter and Key Vault. Boundary logs: PSP webhook accept/reject with request id, the transcript export scrubs long digit runs, the Azure kill-switch path is metered as Azure's.

### WS7 · Tests, types, envelope
- **Suite green on the dev stack**: the 25 known failures read state they did not create. `attribute_outcomes(account_ids=)` and `panel.build(customer_ids=)` scope the loops; the spell tests write a borrower of their own; the A2A test fakes its card; the contact-policy test clears the sweep claim it trips on; the money-path test reads `getMessage()` (the redacting filter clears `args`); dated constants are relative. Final full run: 4,694 passed, 0 failed.
- **pyright** covers `voice/` and `db_prompt_studio/` (80 → 0): `agent_core/dicts.sub` replaces 68 un-narrowable `x.get(k) if isinstance(x.get(k), dict) else {}` sites; the third-party seams (openai, loguru, sentry, pipecat) are cast at the seam. **Tree-wide include measured 116 errors across 15 more files** (`agent_core` 47, `sandbox_runtime.py` 21, `bot_runtime.py` 18, …) — next pass, with that list.
- **Cross-language constants are generated**: `gen_wire_schemas.py` writes `api/wire/constants.json` (rollback triggers, promise/reminder/sender vocabularies, the SLA warn fraction); `test_wire_schemas_current` keeps it current; a vitest pins the `as const` lists to it.
- **Accessibility floor**: `jsx-a11y` recommended in lint, green (66 findings fixed, none baselined; one file-scoped exception for the window-splitter); skip link → `main#main-content`; `aria-sort`; `role="region"` scroll containers; `<h1>` on floor, handoff, skill; `aria-live` transcript; `aria-rowcount` on the virtualised catalogue; `ReasonedAction` → `ui/`.
- `test_compliance_detectors` compares the registry to the seeded catalogue; `"build": "tsc --noEmit && vite build"`; `.github/dependabot.yml` (pip/npm/docker/actions, weekly, grouped); `python:3.12-slim` pinned by digest.
- **Not done, and why**: `noUncheckedIndexedAccess` measured 175 sites (> the 150 the plan allowed for an honest fix; `charts/points.ts` alone is 39) — the flag waits. The source-pin `BASELINE` stays at 106: the pins whose subjects moved were retargeted, not converted. `--cov-fail-under` needs a CI run to re-measure. `queryKeys` factory not built (nothing forced it).

## Verification

| Check | Result |
|---|---|
| Backend suite, dev stack, full run with no edits during it | **4,694 passed, 77 skipped, 0 failed** (12:13) — the first run of the pass had 39 failures: the 25 known, 9 my own regressions, 5 from editing the mounted tree mid-run |
| `ruff check .` | clean |
| pyright (explicit include + `voice/` + `db_prompt_studio/` + the WS6 modules) | 0 errors |
| `migrate_from_empty.py` | parity, 7,261 schema lines (chain vs `sql/` build, RLS on in both) |
| `rls.py status` | 223 installed / 223 derived, 223 enabled / 223 forced |
| `fleet_parity.py` | 4 of 4 deployments agree with their bundle |
| `eval_gate_preflight.py` | every published card satisfies its own `eval.require` |
| `/metrics` | api (no `voice_calls_active`), worker, bot_worker, voice (`voice_calls_active` present); ten queues in each |
| Frontend `tsc` / `npm run lint` (eslint + 8 gates, jsx-a11y on) / vitest / `npm run build` | green / green / 343 passed (37 files) / green |
| Browser | every screen opened against the dev API during the pass; the studio's Policy, Tools, Agent graph, Ship tabs and the ten sheets exercised by hand |

## For the next pass
1. pyright tree-wide: 116 errors in 15 files (the list is in `c7b1101`'s measurement; `agent_core/treatment/*`, `bot_runtime.py`, `sandbox_runtime.py` carry most).
2. `noUncheckedIndexedAccess`: 175 sites; start with `charts/points.ts` (39) and `lib/prompt-studio.ts` (17).
3. The studio route (`_app.agent-studio.$botId.lazy.tsx`, 1,133 lines) is the last baselined route: the publish/discard/restore handlers and the autosave belong beside `rollbackLive` in `studio/`.
4. `check-query-state` baseline 16 → 0; `check-file-size` eight baselined files.
5. CI: re-measure `--cov-fail-under` on the green suite.
