schema_version: 1
name: ml_external_validity_pi
kind: role
version: 0.1.0
protocol_version: 1
stability: template
description: ML External Validity PI role for leakage and generalization review.
compatibility:
  praxist_core: ">=0.1.0,<1.0"
  python: ">=3.11"
dependencies: []
capabilities:
  - role.skill
  - pi.memo
role:
  role_id: external_validity
  legacy_role_id: external_validity
  display_name: ML External Validity PI
  role_kind: pi
  default_model_profile_ref: strong_reasoner
  output_schema_ref: core:pi_memo.v1
  tool_scope:
    - filesystem.read
    - memory_tools.panel_read
    - frontier_tools.panel_read
    - finding_graph_query.panel_read
  fixed_questions:
    - Which direction risks data leakage or unsupported external knowledge?
    - Which evidence would remain useful if immature feedback were removed?
    - Which process shortcut would make results hard to audit?
code: []
assets:
  - skill.md
