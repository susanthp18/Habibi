# Regression Fixtures

Place tiny deterministic fixtures here for evaluator and parser tests.

Fixtures should be small enough to commit and must not include private labels,
large datasets, or generated run artifacts.
