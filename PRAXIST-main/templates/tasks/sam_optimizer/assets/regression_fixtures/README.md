# Regression Fixtures

This directory contains fixed evaluator summaries used to test parsing,
ranking, and compatibility behavior. The fixtures are test inputs, not
scientific evidence or reusable task results. It intentionally excludes full
result directories, images, checkpoints, logs, and plots.
