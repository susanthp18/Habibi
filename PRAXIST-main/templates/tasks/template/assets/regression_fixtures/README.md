# Regression Fixtures

This directory is reserved for compact task-owned regression fixtures.

For a real task, add small deterministic inputs or expected summaries that can
run in CI without GPUs, model keys, network access, or large datasets. Do not
commit full result directories, images, checkpoints, logs, or plots.
