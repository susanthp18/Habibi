# Reference Implementations Template

This directory demonstrates where a task may keep compact reference
implementations or exemplar solutions.

For a real task:

- replace any copied placeholder examples with task-owned references;
- keep only small, curated files that help agents understand the task;
- do not store live run outputs, raw deliverables, large datasets, or private
  benchmark archives here;
- document each reference in `manifest.json`.
