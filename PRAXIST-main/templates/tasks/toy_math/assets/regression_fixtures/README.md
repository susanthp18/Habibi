# Regression Fixtures

This directory is reserved for compact toy-math regression fixtures.

For a real toy task, add small deterministic examples, expected scores, or
counterexample fixtures that can run in CI without external services. Do not
commit full run directories, logs, plots, or large generated artifacts.
