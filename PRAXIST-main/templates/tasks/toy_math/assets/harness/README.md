# Toy Math Harness

This resolve-only fixture has no real benchmark harness.

If the task is expanded into real research, add small deterministic checkers
here and expose them through `evaluations/pareto_tiered/run.py`.
