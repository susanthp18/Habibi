# Toy Math External Validation

For a real toy-math task, validate claims beyond the first finite check:

- increase the bounded search range;
- test equivalent identities under alternative parameterizations;
- separate counterexamples from proof sketches;
- downgrade broad language until a proof or much stronger evidence exists.
