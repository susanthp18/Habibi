# Rocket Booster Recovery solver

Implement the assigned classical method, preserve all hard locks, and compare a
single-factor variant against its exact parent. Use development evidence for
triage and budget complete evidence early enough to close. Check the full
Pareto signature, not only the primary rate.
