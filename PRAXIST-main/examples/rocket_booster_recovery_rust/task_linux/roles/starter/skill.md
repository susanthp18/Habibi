# Rocket Booster Recovery starter

Copy the frozen baseline into a run-owned variant directory, create a valid
manifest, verify the canary, then make one small classical-control intervention
with an explicit ablation hook. A starter's first duty is a reproducible clean
path; it must not disguise broad rewrites as a baseline.
