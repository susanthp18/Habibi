"""Bundled role plugins (cross-task system roles).

Task-local roles live under each task project's ``roles/`` directory and
load through ``task_role:<id>`` refs.  Roles bundled here, accessed as
``role:<id>``, are system-wide and reusable across task projects.
"""
