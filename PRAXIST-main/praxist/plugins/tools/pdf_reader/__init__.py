"""pdf_reader — research-PDF reading via image rasterization + OCR (#128 PR-3).

See :mod:`praxist.plugins.tools.pdf_reader.adapter` for the
``pdf_read`` and ``pdf_metadata`` handlers and
:mod:`praxist.plugins.tools.pdf_reader.__main__` for the stdio
entrypoint that codex_sdk / claude_sdk peers spawn when ``pdf-reader``
is in their ``tool_servers`` list.
"""
