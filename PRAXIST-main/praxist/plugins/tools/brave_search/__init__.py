"""brave_search — provider-agnostic web_search MCP tool (#109).

See :mod:`praxist.plugins.tools.brave_search.adapter` for the
``web_search`` handler and :mod:`praxist.plugins.tools.brave_search.__main__`
for the stdio entrypoint that codex_sdk / claude_sdk peers spawn when
``brave-search`` is in their ``tool_servers`` list.
"""
