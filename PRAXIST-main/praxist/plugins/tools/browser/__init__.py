"""browser — provider-agnostic web_read MCP tool (#128 PR-1).

See :mod:`praxist.plugins.tools.browser.adapter` for the
``web_read`` handler and :mod:`praxist.plugins.tools.browser.__main__`
for the stdio entrypoint that codex_sdk / claude_sdk peers spawn when
``browser`` is in their ``tool_servers`` list.
"""
